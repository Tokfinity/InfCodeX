# Persisted prompt-benchmark runs (FEATURE_104 v2). Local-only — never
# committed under any circumstance, including "baseline" snapshots. This
# file is the only tracked artifact under benchmark/results/. The same
# rule is also enforced at the root .gitignore.
*
!.gitignore
