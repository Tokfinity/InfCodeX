# v0.7.41 Feature Designs

> 中文标题：`v0.7.41 Feature 设计`
>
> Version: `v0.7.41`
>
> Status: Planned (not yet released)
>
> Theme: **KodaX Team Mode — Multi-Instance Auto Coordination (LLM-First Awareness + Content Hash Safety Net)** — KodaX 自创的多 session 自动协调机制。用户**零认知负担**（无需 `/team create`，无需记 team_id），KodaX 自动感知本机其他 KodaX session 状态，把状态注入 LLM system prompt 让 LLM **自决避让 / 协作 / 调度**；runtime 仅在 race condition 物理边界（content hash mismatch）兜底，不强制 lock 不强制等待。这是 KodaX 比 claude code Team Mode 在多 session 协作维度做得**更好**的差异化设计。

---

## 历史 — FEATURE_122 v0.7.41 原 slot（已 Cancelled）

> v0.7.41 原本规划 FEATURE_122 PM Thinking Framework Prompt，于 2026-05-06 经调研后取消（claudecode 4-stage workflow 是 Coordinator mode 双角色身份切换的配套 prompt；KodaX 单 Worker 模式无身份切换需求；剩余 Synthesis hint 已合并到 v0.7.36 FEATURE_114 worker-role-prompt 1-2 行微调）。
>
> v0.7.41 slot 由 FEATURE_125（KodaX Team Mode，原计划 v0.8.10）替补 —— 后者的核心价值（多 session 协作）真实存在但需要重新设计避开 claude code Team Mode 的 mode-based + 不解决冲突的缺陷。

---

## 总览

### 用户场景

KodaX 用户实际工作流中存在以下多 session 协作场景：

1. **全栈开发**：用户开 2-3 个 KodaX session 分别跑 frontend / backend / DB schema 改动
2. **跨 repo 重构**：API 改动同时影响 client repo + server repo
3. **长任务并行**：一个 session 跑 e2e 测试 + 另一个继续开发
4. **多 worktree 并行 sub-task**：在不同 git worktree 上各自开 KodaX session

### 核心问题

如果用户**未做任何隔离**（同一 cwd 多开 session），多个 KodaX session 可能同时改同一个文件，造成：
- Race condition → 后写覆盖先写（**修改丢失**）
- 文件中间状态被另一 session 读到（**partial state**）
- 没有运行时机制保证 LLM 知道兄弟 session 的存在

### claude code Team Mode 调研对照（2026-05-06）

调研 claude code 源码证实它**没有任何冲突解决机制**：

| 检查项 | 真实状态 | 源码证据 |
|---|---|---|
| 多 teammate 默认 cwd | 全部共享 leader 的 cwd | [`spawnMultiAgent.ts:337`](file:///c:/Works/claudecode/src/tools/shared/spawnMultiAgent.ts#L337) `const workingDir = cwd \|\| getCwd()` |
| Worktree 隔离 | ❌ 不自动；`worktreePath` optional | [`backends/types.ts:217`](file:///c:/Works/claudecode/src/utils/swarm/backends/types.ts#L217) |
| File lock / fcntl / flock | ❌ 完全不存在 | grep 全 swarm 目录无 lock 协议 |
| 跨进程 mutation queue | ❌ 不存在 | grep `flock` / `fcntl` 在 swarm 路径零命中 |
| Prompt 引导分区 | ❌ 没有 | [`teammatePromptAddendum.ts`](file:///c:/Works/claudecode/src/utils/swarm/teammatePromptAddendum.ts) 18 行不提冲突 |

**claude code Team Mode 在冲突维度跟"用户自己多开 session"无区别**。它增加的便利（mailbox / permission sync / task 分配）全是低价值（每天省几次切 terminal），且引入 mode 切换认知负担 + 7 条已知限制 + token 消耗 10x。

### KodaX 的差异化路径

不照搬 mode-based + 不解决冲突的 claude code 设计。KodaX 走 **LLM-First + runtime safety net** 路径：

| 维度 | claude code Team Mode | KodaX FEATURE_125 |
|---|---|---|
| 用户认知负担 | mode 概念 + `/team create` + 记 team_id | **0**（自动感知）|
| 冲突解决保证 | 无（LLM 自律）| **runtime safety net**（content hash mismatch detection）|
| 多 session 不开 mode 的体验 | 完全无协调（race condition）| 自动协调（LLM 看见 + safety net）|
| 设计哲学 | 通信网络（mailbox / SendMessage / mode 切换）| LLM 自感知 + runtime 兜底 |
| 与 KodaX 极简 + LLM-First | 不一致（mode 反向 + LLM 不知情）| ✅ 一致 |

### 设计核心判断

之前我（设计者）一度倾向于"runtime hard lock + 用户裁决 dialog 30s 升级"的强 enforcement 设计。但这违反 KodaX `CLAUDE.md` 的核心原则：

> **Leverage LLM intelligence** - Design for LLM comprehension. Use LLM for generation, review, and testing.

正确思路：**LLM 是智能 entity，给它信息让它自决**；runtime 仅做"data race 物理兜底"（content hash mismatch detection）；用户介入是 LLM 累积失败后**自己 escalate**，不是 runtime 强加的 timeout dialog。

---

## FEATURE_125: KodaX Team Mode — Multi-Instance Auto Coordination

**Status**: Planned
**Priority**: High
**Category**: Core / Architecture

### 设计：4 层架构

```
┌────────────────────────────────────────────────────────┐
│ 层 1: State Broadcast（让自己被看见）                    │
│   ~/.kodax/instances/<pid>/state.json                  │
│   - 启动时注册 + 每秒/每 tool 边界刷新                  │
│   - 内容：agent_phase / current_intent / active_files /│
│           recently_modified_files / heartbeat mtime    │
│   - 退出时清理；crash 时由其他 session 扫 mtime 清理    │
└────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│ 层 2: 注入其他 session 状态到 system prompt（让 LLM 看见）│
│   - 每次 worker LLM call 之前，runner 扫描其他 alive   │
│     session 的 state.json，拼成 system prompt block    │
│   - LLM 看到 active intent / active_files / recently  │
│     modified → LLM 自决：避让 / 协作 / 选别的文件      │
│   - 这是 KodaX 杠杆 LLM intelligence 的核心：90% 协调  │
│     由 LLM 自己处理                                     │
└────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│ 层 3: Tool-time Soft Warning（提醒，不阻断）             │
│   - LLM 调 Edit/MultiEdit/Write 时，runner 检查         │
│     active_files 重叠                                   │
│   - 重叠 → tool result 前置 warning 段：               │
│     "[Warning] Another session is editing this file..."│
│   - 不阻断 edit；LLM 看到 warning 自决                   │
│     是否 retry / undo / 继续                            │
└────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│ 层 4: Content Hash Safety Net（race condition 物理兜底）  │
│   - LLM read foo.ts → runner 记录 content hash         │
│   - LLM edit foo.ts → runner 检查：现在 hash 仍是      │
│     LLM read 时的 hash 吗？                              │
│     - 是 → 正常 apply edit                              │
│     - 否（其他 session 已改）→ tool result error：     │
│       "file changed since your read; re-read first"    │
│   - LLM 自然 re-read → 看到对方修改 → 决定如何整合      │
│   - 这是唯一的 runtime hard block；99% 场景由前 3 层    │
│     的 LLM 自决处理掉                                    │
└────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│ 用户裁决：从"常规交互"降到"LLM 累积失败 escalate"       │
│   - 取消 30s 自动弹对话框                                │
│   - LLM 多次 race + 多次 retry 失败 → LLM 自然向用户    │
│     报告："I tried to edit foo.ts but another session   │
│     keeps changing it. What would you like..."          │
│   - 用户介入是 LLM escalate，不是 runtime 强加          │
└────────────────────────────────────────────────────────┘
```

### Step 1：State Broadcast 机制

每个 KodaX session 启动时在 `~/.kodax/instances/<pid>/` 注册：

```
~/.kodax/instances/
├── 12345/
│   ├── state.json          # 实时状态，每秒刷新
│   ├── meta.json           # cwd / start time / git remote / branch
│   └── heartbeat           # mtime 用于 stale detection
└── 12389/
    └── ...
```

**`state.json` 内容**（每秒 / 每 tool 边界刷新）：

```json
{
  "version": "1",
  "updated_at": 1714780800123,
  "pid": 12345,
  "cwd": "/Users/me/work/myrepo",
  "git_branch": "main",
  "agent_phase": "running_tool",
  "current_intent": "implementing dark mode toggle in packages/ui",
  "active_files": ["packages/ui/Button.tsx"],
  "recently_modified_files": [
    {"path": "packages/ui/theme.ts", "modified_at": 1714780750}
  ]
}
```

字段语义：
- `agent_phase`: `idle` / `awaiting_llm` / `running_tool`
- `current_intent`: LLM 在做什么的简短描述（worker 自己产生 / runner 从最近 todo 推断）
- `active_files`: 当前**正在 edit/write** 的文件列表（mutation tool 调用的入参）
- `recently_modified_files`: 最近 5 分钟改过的文件 + 时间戳（指引 LLM 知道哪些文件可能 stale）

**Stale instance detection**：
- session 启动时扫 instances/，对每个目录查 heartbeat mtime
- mtime > 30s 未刷新 → 视为 dead → 清理目录
- 进程 normal exit 时主动清理；crash 时由下一个 session 自动清理

### Step 2：注入其他 session 状态到 system prompt

每次 worker 准备发 LLM call 之前，runner 调 `gatherOtherInstancesState()`：

```ts
function gatherOtherInstancesState(myPid: number): OtherSessionInfo[] {
  const dir = path.join(os.homedir(), '.kodax', 'instances');
  if (!fs.existsSync(dir)) return [];

  const result: OtherSessionInfo[] = [];
  for (const pidDir of fs.readdirSync(dir)) {
    const pid = parseInt(pidDir);
    if (pid === myPid) continue;

    const heartbeat = path.join(dir, pidDir, 'heartbeat');
    const stat = fs.statSync(heartbeat).mtime;
    if (Date.now() - stat.getTime() > 30_000) continue;  // stale

    const state = JSON.parse(fs.readFileSync(path.join(dir, pidDir, 'state.json'), 'utf8'));
    result.push(state);
  }
  return result;
}
```

如有 ≥1 个 alive 的其他 session，runner 在 worker system prompt 注入 block：

```
=== Other active KodaX sessions ===

You are not alone — the user has 2 other KodaX sessions running:

- pid 12389 @ /Users/me/work/myrepo (started 5 min ago, on branch main)
  Phase: running_tool
  Intent: "implementing dark mode toggle in packages/ui"
  Currently editing: packages/ui/Button.tsx
  Recently modified: packages/ui/theme.ts (2 min ago)

- pid 12422 @ /Users/me/work/myrepo (started 2 min ago, on branch main)
  Phase: awaiting_llm
  Intent: "refactor session expiry handling"
  Currently editing: packages/api/auth.ts

Coordination guidance:
- If your task overlaps with their active_files, consider:
  · Working on different files first (let them finish)
  · Reading their active file before editing (see their progress in real time)
  · Sending them a message via send_message (when peer-to-peer messaging
    extends across instances — currently scope-limited to within-instance)
- Their recently_modified_files may have just changed — re-read before
  relying on memory of their content
- Use your judgment. You can usually plan around concurrent work without
  conflict. Don't fight them — let them finish what they started.
```

LLM 看到这个 block 后**自己决定**：
- 选别的文件做（智能避让）
- read 对方正在改的文件看进展（智能感知）
- 调整任务顺序（智能调度）
- 通过 send_message 协调（如未来扩展跨 instance peer messaging）

**没有其他 session 时**：block 不注入（system prompt 干净，无开销）。

### Step 3：Tool-time Soft Warning（不阻断）

每次 LLM 调 Edit/MultiEdit/Write 时，runner 在 tool 执行**之前**检查重叠：

```ts
function checkActiveFileOverlap(filePath: string): SoftWarning | null {
  const others = gatherOtherInstancesState(process.pid);
  const conflicting = others.filter(o => o.active_files.includes(filePath));
  if (conflicting.length === 0) return null;

  return {
    kind: 'active-file-overlap',
    file: filePath,
    others: conflicting.map(c => ({
      pid: c.pid,
      intent: c.current_intent,
    })),
  };
}
```

有重叠 → tool result 前置 warning 段（**不阻断 tool 执行**）：

```
[Warning: Another session is editing this file]
- pid 12389 is editing packages/ui/Button.tsx
  Their intent: "implementing dark mode toggle"
Your edit may overwrite or conflict with theirs. Consider re-reading
the file first or working on a different file.

(Edit was applied. Result follows.)
```

LLM 看到 warning + edit 实际结果，自己决定：
- 直接接受（如果改的部分不冲突）
- undo + 改别的
- 继续但注意 follow-up

**关键**：不强制阻断、不弹用户对话框、不强制 wait —— **LLM 自决**。

### Step 4：Content Hash Safety Net（race condition 物理兜底）

唯一的"runtime 强制"机制 —— 防 race condition 修改丢失：

#### 4.1 Read 阶段：记录 hash

LLM 调 Read foo.ts → runner 在内部状态记录：

```ts
class ContentHashCache {
  private cache: Map<string, { hash: string; readAt: number }> = new Map();

  recordRead(filePath: string): void {
    const content = fs.readFileSync(filePath, 'utf8');
    const hash = crypto.createHash('sha256').update(content).digest('hex');
    this.cache.set(filePath, { hash, readAt: Date.now() });
  }
}
```

#### 4.2 Edit 阶段：检查 hash

LLM 调 Edit foo.ts → runner 在 tool 执行**之前**：

```ts
function checkStaleWrite(filePath: string): StaleWriteResult {
  const cached = contentHashCache.get(filePath);
  if (!cached) return { stale: false };  // 没 read 过；不检查（用户可能直接 write 新文件）

  const currentContent = fs.readFileSync(filePath, 'utf8');
  const currentHash = crypto.createHash('sha256').update(currentContent).digest('hex');

  if (currentHash !== cached.hash) {
    return {
      stale: true,
      message: `${filePath} has changed since you last read it (other session may have modified it). Re-read the file before editing.`,
    };
  }
  return { stale: false };
}
```

如果 stale → tool 不执行，返回 error：

```
[Tool Error] foo.ts has changed since you last read it (other session
may have modified it). Re-read the file before editing.
```

LLM 自然 re-read → 看到对方修改 → 决定整合 / 调整 / 放弃。

#### 4.3 边界 case

| Case | 行为 |
|---|---|
| LLM 没 read 直接 Write 新文件 | 不检查 hash（cache miss）—— 正常 write |
| LLM read 后立即 edit（无并发）| hash 一致 → 正常 apply |
| 其他 session edit 后我 edit | hash 不一致 → block + LLM re-read |
| LLM read → 我自己 edit → 再 edit | hash 检查（自己改后我自己更新 cache）|
| 文件被 user 用编辑器手动改 | hash 不一致 → block（这是好事，LLM 不该覆盖用户编辑）|

### Step 5：用户裁决（仅在 LLM 自己 escalate 时）

**取消所有 runtime 自动弹用户对话框**。LLM 多次 race condition 后自己处理：

```
LLM: Edit foo.ts → [Tool Error: file changed]
LLM: Read foo.ts → 看到对方修改
LLM: Edit foo.ts → [Tool Error: file changed again]
LLM: Read foo.ts → 看到对方修改
LLM: Edit foo.ts → [Tool Error: file changed again]
LLM 反复 3 次失败 → 自然向用户报告：
  "I tried 3 times to edit foo.ts but another session keeps modifying
   it. What would you like — wait, retry later, or skip?"
```

用户裁决是 LLM-driven escalation，不是 runtime-driven timeout。这是 KodaX `LLM-First Design` 的体现 —— LLM 知道何时该问用户，比 runtime 30s timer 更聪明。

### Step 6：与 FEATURE_131 单进程 mutation queue 的协同

| 层 | 作用范围 | 实现 |
|---|---|---|
| FEATURE_131（v0.7.36 同期）| **单进程内**同文件 race | Promise chain / Map mutex |
| FEATURE_125（本 feature） | **跨进程**同文件 race | content hash safety net |

两层正交叠加：
- 单进程内 child agent 并行改同文件 → FEATURE_131 串行化
- 跨进程多 session 改同文件 → FEATURE_125 hash 检查 + LLM 自决

### Step 7：与 worktree 的协同

- 用户用 git worktree 物理隔离（FEATURE_053/068）→ 不同 cwd → 不同绝对路径 → state.json 中 active_files 路径不重叠 → FEATURE_125 自然不触发 warning / safety net
- 用户**不**用 worktree 同 cwd 多开 → FEATURE_125 全功能生效

**两种使用方式都被 KodaX 支持**，用户自由选择。

### 影响范围

**新增**：

- `packages/agent/src/team/state-writer.ts`：state.json 写入器（启动注册 + 每秒刷新 + 退出清理）~80 行 —— **通用 agent 平台原语，落 `@kodax/agent`**（per v0.7.35.1 包归属修正：multi-instance team awareness 是任何 agent 都需要的能力，不绑定 coding）
- `packages/agent/src/team/instance-discovery.ts`：扫其他 instance + stale 检测 + 状态 collation ~80 行
- `packages/agent/src/team/system-prompt-injection.ts`：buildOtherInstancesPromptBlock helper ~50 行
- `packages/coding/src/multi-instance/content-hash-cache.ts`：read 时 record hash + edit 时 stale check ~100 行 —— **保留 coding 端**：与 coding 的 Edit / Read tool 实现紧绑，未来其他 agent（data-analysis 等）真有同需求时再抽象
- `packages/coding/src/multi-instance/active-file-warning.ts`：tool-time overlap 检测 + warning 文本生成 ~50 行 —— **保留 coding 端**：触发于 coding Edit tool 边界
- `tests/multi-instance/state-broadcast.test.ts`：unit test 覆盖 lifecycle / stale detection
- `tests/multi-instance/hash-safety-net.test.ts`：multi-process simulation 测 race condition 检测
- `tests/multi-instance/awareness-prompt.eval.ts`：FEATURE_104 prompt eval 验证 LLM 在看到其他 session 状态时正确避让 / 协作
- `docs/test-guides/FEATURE_125_v0.7.41_TEST_GUIDE.md`

**修改**：

- `packages/coding/src/agent-runtime/system-prompt-builder.ts`：在 stable 部分之后注入 other-instances block（动态部分）
- `packages/coding/src/tools/edit.ts` / `multi-edit.ts` / `write.ts`：mutation 前调 active-file-warning 检查 + content-hash check
- `packages/coding/src/tools/read.ts`（如有内部 read 工具实现）：read 时调 contentHashCache.recordRead
- `packages/coding/src/types.ts`：`KodaXToolExecutionContext.contentHashCache: ContentHashCache`

**不改动**：

- 任何工具的 LLM 端 schema（LLM 看到的工具表面不变）
- FEATURE_131 单进程 mutation queue（与本 feature 正交）
- FEATURE_115 message queue（cross-instance peer messaging 不在本 feature scope）
- guardrail / permission 层
- session lineage / repointel-protocol

**总代码量**：~530 行（含测试）

### 验收标准

1. **单 session 不退化**：`~/.kodax/instances/` 仅 1 个 alive instance 时，system prompt **不**注入 other-instances block；overhead ≤ 1ms / tool call
2. **多 session 不同文件**：active_files 不重叠 → 不互相阻塞，无 warning，正常并行
3. **多 session 同文件 LLM 看见**：另一 session 在改 foo.ts，本 session worker LLM 启动时 system prompt 含其状态；LLM **自决**（FEATURE_104 prompt eval 验证 ≥80% case 选择避让 / 协作 / re-read）
4. **Tool-time soft warning**：LLM 选择 edit 已被另一 session 标 active 的文件，tool result 前置 warning；edit **仍然被 apply**（不阻断）
5. **Content hash safety net**：LLM read foo.ts → 其他 session edit foo.ts → LLM edit foo.ts → tool result error "file changed; re-read"；LLM re-read 后能正常 edit
6. **Stale instance 自动清理**：kill 一个 KodaX session（SIGKILL），其他 session 在 heartbeat > 30s 时自动清理目录
7. **Crash 恢复**：进程被强杀 → 下一个 session 启动时清理 stale instances/
8. **Cross-platform**：Windows / Linux / macOS 都跑通（heartbeat 用 mtime touch，跨平台一致）
9. **跟 FEATURE_131 协同**：单进程内 mutation queue + 跨进程 content hash 两层正交，无 deadlock
10. **跟 worktree 协同**：用户用 worktree 隔离 → 不同绝对路径 → 不触发本 feature warning（因 active_files 路径不重叠）
11. **跨 family 验证**：4 family LLM（GLM/Kimi/Claude/GPT）看到 other-instances block 后避让 / re-read 行为 max-min spread ≤ 15pp
12. **Prompt eval 触发**：本 feature 触及 worker system prompt（其他 instance block 注入），按 FEATURE_104 必须新增 eval `tests/multi-instance/awareness-prompt.eval.ts`

### 非目标

- **不** 引入 file lock / fcntl / flock 协议（runtime hard enforcement 反 LLM-First）
- **不** 引入 mode 概念 / `/team create` / `/team join` slash command（自动感知，零认知负担）
- **不** 引入 cross-instance peer SendMessage（FEATURE_123 peer SendMessage scope 在单 instance 内；跨 instance 通信留作未来 feature 看用户实际需求）
- **不** 引入跨 instance permission sync（用户在每个 session 各自决策即可，复用 FEATURE_092）
- **不** 引入跨 instance scratchpad（FEATURE_121 scratchpad scope 在单 instance 内）
- **不** 引入 30s 自动用户裁决对话框（让 LLM accumulate failures 后自己 escalate）
- **不** 实现 line-level 精确冲突检测（v1 file-level；v2 看数据决定是否做行级精确）
- **不** 实现跨 host 协调（仅同 user 同 home 目录；多 host 用 git）
- **不** 持久化 instance state 跨进程重启（state.json 是运行时状态，重启重建）
- **不** 给用户暴露 `/instances` 等管理命令（v1 全自动；v2 看是否需要 inspect 命令）
- **不** 替代 git worktree 物理隔离（worktree 仍是用户主动选择的强隔离方式）

### 与其他 Feature 的关系

- **正交 FEATURE_131（v0.7.36 同期）**：单进程 mutation queue + 跨进程 content hash 两层 race 防护
- **协同 FEATURE_053/068（worktree）**：用户用 worktree 时本 feature 自然不触发；用户不用 worktree 时本 feature 兜底
- **协同 FEATURE_092 auto-mode**：本 feature 不参与 permission 决策（每个 session 独立 auto-mode classifier）
- **不依赖 FEATURE_125 v0.8.10 旧设计**：v0.8.10 mode-based 设计已废弃，本 feature 完全独立设计
- **铺路未来 cross-instance peer SendMessage**：如果生产数据显示用户多 session 频繁需要直接通信，可在本 feature state broadcast 基础上扩展（添加 cross-instance message queue），但 v1 不做

### 迁移策略

- 与 FEATURE_131 / FEATURE_119 同期 v0.7.36 的多 child 编排基础设施基本独立
- 本 feature ship 后默认开启（无需 feature flag）—— 单 session 不退化
- 灰度 escape hatch：`KODAX_DISABLE_MULTI_INSTANCE=1` 环境变量完全 disable（仅故障定位用，不为日常使用提供 opt-out）

### 用户体验示例

#### 场景 A：用户多开 session 改不同模块（典型场景）

```
Session 1（用户在 packages/ui 工作）：
  user: 帮我加 dark mode toggle
  worker: [system prompt 看到只有自己] 直接干 → edit Button.tsx / theme.ts

Session 2（用户在另一个 terminal 加 backend feature）：
  user: 重构 auth 模块
  worker: [system prompt 看到 session 1 在改 packages/ui]
          → 思考：我的任务在 packages/api/auth.ts，跟 ui 不冲突
          → 自决：直接干 → edit auth.ts
```

**结果**：两 session 各干各的，0 摩擦。

#### 场景 B：用户多开 session 凑巧改同文件

```
Session 1（已经在改 shared/types.ts）：
  active_files: ["shared/types.ts"]

Session 2（用户也想改 shared/types.ts）：
  user: 加一个 UserRole 类型
  worker: [system prompt 看到 session 1 在改 shared/types.ts]
          → LLM 自决（智能行为）：
            选项 A: 先读对方改后的状态再加（read shared/types.ts）
            选项 B: 等对方完成后再开始
            选项 C: 选别的相关任务先做
          → 选择 A，read 看到对方刚加的类型 → edit 加 UserRole
  runner: tool 执行前 content hash check → 自从 read 后没变化 → apply
```

**结果**：LLM 智能避让，无需 runtime 强制。

#### 场景 C：真 race condition

```
Session 1（read 后开始 edit shared/types.ts）：
  read at T0 → hash A
  edit at T1 → 期间 session 2 抢着改了

Session 2（在 T0.5 read 后 edit）：
  read at T0.5 → hash A
  edit at T0.8 → apply（hash 仍是 A）→ 文件变成 hash B

Session 1 在 T1 edit：
  runner check → 当前 hash 是 B，跟 read 时的 A 不一致 → block
  → tool result error "file changed; re-read"
  → LLM re-read at T1.1 → hash B → 看到 session 2 改的内容
  → LLM 决定整合 → 重新 edit → apply（hash 一致）
```

**结果**：物理 race 被 hash safety net 兜住，没有修改丢失。

#### 场景 D：LLM 反复 race + 自己 escalate

```
session 1 反复改 foo.ts，session 2 跟它抢
session 2 connect → edit → race → re-read → edit → race → re-read → edit → race
session 2 LLM 看到 3 次 [Tool Error: file changed]
LLM: "I noticed another session is rapidly modifying foo.ts. Should I
      wait for them, work on a different file, or coordinate via send_message?"
user: "wait for them, work on packages/ui first"
LLM: 切到 packages/ui 任务
```

**结果**：用户介入是 LLM 自然决定的，不是 runtime 30s timeout 强加的。

### 与 FEATURE_151 (v0.7.38) Todo 体系的衔接（多 session 共享 todo list）

FEATURE_151 把 KodaX 的 todo plan list 提升到 CC `TaskListV2` 同等可见性 + LLM 自主 init/update/list 能力，但**仍是单 in-memory store + 单 React state**：

- todoStore 在 `runManagedTaskViaRunnerInner` 内创建，per-managed-task 生命周期
- React `todoItems` 是 per-Ink-session 视图状态，进程内可见
- **没有跨 KodaX 实例 / 跨 session 共享**（CC V2 用 `~/.claude/tasks/<sessionId>/*.json` 持久化 + `proper-lockfile` 串行化跨进程写）

**FEATURE_125 应当处置的部分**（不属于 v0.7.41 主线，但要在 follow-up 列表里登记）：

- **跨实例 todo 可见性**（read-only）：KodaX-A 的 in-progress todo 出现在 KodaX-B 的 `<sibling-sessions>` system-prompt 注入里。这能让 LLM 知道「另一个 session 正在改 auth.ts，我应该跳过 auth.ts」。**不需要 disk-persisted todo store**——只需要 `current_intent` / `active_files` 的现有 sibling-snapshot 通道传一行 todo 状态摘要。
- **多 session 共写一份 todo list**（write-shared）：CC V2 swarm 的 `claimTask` / `blockedBy` / `owner` 模型。**FEATURE_125 显式不做**——KodaX 的协作模型是「LLM 自决避让」而不是「runtime 强制串行 lock」，把 task ownership 加到 todo 系统会引入 mode-based 双轨（自动 vs 团队）违反 minimalist 原则。

**实施依赖**：
- FEATURE_124 (v0.7.43) Memory System 落地后，todo state 可以选择性写入 memory；之后 FEATURE_125 在 sibling-snapshot 注入里读 sibling 的 memory 摘要，无需新通道
- 在 v0.7.41 范围内仅在 sibling state.json 里加一个 `current_todo_summary?: string` 字段（非 critical path，可后补）

### 参考

- KodaX `CLAUDE.md` 哲学：[Leverage LLM intelligence](../../CLAUDE.md)
- FEATURE_151 (v0.7.38) Todo 体系：[v0.7.38.md#feature_151...](v0.7.38.md#feature_151-todolist-visibility--llm-self-seeding-parity--closing-the-feature_097-coverage-gap)
- claude code Team Mode 调研（2026-05-06）：
  - [`spawnMultiAgent.ts:337`](file:///c:/Works/claudecode/src/tools/shared/spawnMultiAgent.ts#L337)
  - [`backends/types.ts:217`](file:///c:/Works/claudecode/src/utils/swarm/backends/types.ts#L217)
  - [`teammatePromptAddendum.ts`](file:///c:/Works/claudecode/src/utils/swarm/teammatePromptAddendum.ts)
  - [`COORDINATOR_MODE_ALLOWED_TOOLS`](file:///c:/Works/claudecode/src/constants/tools.ts#L107)
- FEATURE_131（v0.7.36 同期）：单进程 file mutation queue
- FEATURE_053（v0.7.30）：Managed Worktree Runtime
- FEATURE_092（v0.7.33）：auto-mode classifier
- 取消的 v0.8.10 旧设计：[v0.8.10.md](v0.8.10.md)（mode-based + 3 backend 的 claude code 风格设计已废弃）
