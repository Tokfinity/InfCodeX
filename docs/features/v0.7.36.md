# v0.7.36 Feature Designs

> 中文标题：`v0.7.36 Feature 设计`
>
> Version: `v0.7.36`
>
> Status: Planned (not yet released)
>
> Theme: **AMA Harness V2 + Message Queue Foundation + Async Child Dispatch (Pattern B) + Skill UX & prompt-overlay 错位修正** — 四件事同期 ship：(1) 把 4 角色（Scout/Planner/Generator/Evaluator）+ 3 档 harness（H0/H1/H2）简化为 **Worker + Evaluator 二角色 + 1 个 PLANNED harness**，从结构上解决 FEATURE_097 review 不可见根因；(2) 引入 **agentId-scoped Message Queue 基础设施**，吸收 FEATURE_111 soft-pause UX，作为后续 SendMessage/TaskStop/Peer-to-peer 的底座；(3) 把 `dispatch_child_task` 从同步阻塞改造成 **launch + await 异步分离**（业界研究推荐的 Pattern B），解决 Worker 派 child 后阻塞 90s 卡死的核心架构债；(4) **Skill 用户层 UX 升级（manifest 措辞硬化 / classic CLI 平等 / mid-line `/skill:foo` parser）+ `prompt-overlay` 从 user prompt 头迁回 system prompt（修 v0.7.26 错位）**——v0.7.35.1 FEATURE_144 已经把所有 6 段 capability "missing data" 漂移修完，本版本只剩 position 类 + UX 类工作。

---

## 总览

KodaX 当前 AMA harness 是一条 4 节点流水线（Scout → Planner → Generator → Evaluator），由 [`managed-protocol.ts:118-148`](../../packages/coding/src/managed-protocol.ts#L118-L148) 的 `H0_DIRECT / H1_EXECUTE_EVAL / H2_PLAN_EXECUTE_EVAL` 三档枚举驱动。FEATURE_084（v0.7.26）把它从文本协议升级到 Layer A 原语；FEATURE_097（v0.7.34）把 `execution_obligations` 接到 TodoListSurface。

但同时存在**第二条架构债**：当前 [`dispatch-child-tasks.ts:114`](../../packages/coding/src/tools/dispatch-child-tasks.ts#L114) 走 `await executeChildAgents([bundle], ctx, options)` 同步阻塞——主 Worker 派 child 后必须等 child 跑完才能继续。child 跑长任务（npm test 90s）时 Worker 完全空闲，用户插话只能 pending 等到 turn 末。**5 codebase 调研**（[multi-agent-async-orchestration-deep-dive.md](../../../tmp/multi-agent-async-orchestration-deep-dive.md)）确认这是 KodaX 当前的核心架构债。

**四个独立但同期 ship 的 Feature**：

- **FEATURE_114（AMA Harness V2）**：Worker+Evaluator 二角色重构。**Evaluator 保留为 structural gate**——5 轮调研事实证明这是 KodaX 比 Claude Code 强的护城河，绝不替换为 Claude Code 风格的 opportunistic verification nudge。
- **FEATURE_115（Message Queue Foundation）**：agentId-scoped 2 档优先级队列 + Sleep-gated mid-turn drain。是 v0.7.39 FEATURE_120（SendMessage/TaskStop）/ v0.7.44 FEATURE_123（Peer-to-peer SendMessage）的底座；本版本同时**吸收 FEATURE_111 soft-pause 的 UX 能力**（FEATURE_111 设计文档保留为历史，实现统一走 message queue）。
- **FEATURE_119（Async Child Dispatch — Pattern B）**：把 `dispatch_child_task` 拆成 `dispatch_child_task` (launch, 立即返回 task_id) + `await_child_task(task_id)` (poll, Worker 显式 await)。`KodaXToolExecutionContext` 新增 `childTaskRegistry: Map`。是 5 轮调研推荐方案——**用 ~100 行代码解决 Pattern A (Coordinator/Worker 分层) 用 ~1500 行解决的同一问题**。
- **FEATURE_143（Skill UX Upgrade + prompt-overlay 错位修正）**：v0.7.35.1 FEATURE_144 已经把 6 段 capability "missing data" 漂移修完（mcp / skills / project-agents / tool-construction / git-context / project-snapshot），本 feature 只剩 1 段 "position 类" 漂移（`prompt-overlay` 从 user prompt 头迁回 system prompt）+ Skill 用户层 UX 三件（manifest 措辞硬化为 Claude Code 风格强约束 / classic CLI 与 Ink TUI skillsPrompt 构造对齐 / mid-line `/skill:foo` parser）。触发完整 prompt eval 验证 prompt-overlay 位置迁移不退化 worker H0/H1/H2 routing 行为。

四个 feature 都触及核心 runtime 路径，一起 ship 减少 PR 风险面。

---

## FEATURE_114: AMA Harness V2 — Worker + Evaluator (Evaluator Preserved as Structural Gate)

**Status**: Planned
**Priority**: High
**Category**: Internal / Architecture

### 背景

#### 当前 H2 的 5 个结构性矛盾

1. **Plan 被劈成两半**——Scout 的 `execution_obligations` 是真实 plan（TodoStore seed 来自这），Planner 只产出 `success_criteria + constraints`。两个 role 干一件事的两半（[role-prompt.ts:627-669](../../packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts#L627-L669)）。
2. **FEATURE_107 实测证明 Planner 没创造价值**——`benchmark/datasets/h2-plan-execute-boundary/` 跑出 H2-A vs H2-B = **0pp delta**，Planner 引入的 "信息压缩" 假设被否决。
3. **Generator 在 H2 是 dispatcher 不是执行者**——Generator H2 模式下 fan-out 给写子 Agent，自己不写文件。Issue 124 显示这层 fanout 触发率偏低。
4. **Evaluator 时机错位**——当前 Evaluator 在 Generator 全部跑完后跑一次；用户要求的"每个计划节点根据需要加 evaluator"无法表达。
5. **Replan = 全量 reset**——`runner-driven.ts` 中 `revise + next_harness=H2` 触发 `todoStore.reset()` 清空全部 todo。

更关键：**这套结构和 FEATURE_097 的"计划列表可见性"诉求结构性打架**。32c5205（FEATURE_097 P0-followup）用 prompt-only 强制 Scout "EARLY emit"，但 Scout 的 prior 是 "verdict = 调研后产物"，prompt 软对抗一直压不住——review 类任务尤其严重（review 天然先调查后定论）。

#### 三轮深度调研结论

- **Phase 0**（[claude-code-architecture-study.md](../../../tmp/claude-code-architecture-study.md)）：业界 4/4 codebase 一致——单 primary agent + 按需 subagent + 实时 todo UI；KodaX 4 角色 + 3 档在工业界**找不到对应物**。
- **Phase 0.5**（[kodax-capabilities-inventory.md](../../../tmp/kodax-capabilities-inventory.md)）：KodaX 已有 Self-Construction 4 tier + Admission Contract + capability tier，远比初轮调研估计的丰富；MCP 渐进式披露是 KodaX 比 Claude Code 更优雅的设计。
- **Phase 0.6**（[claude-code-actual-usage.md](../../../tmp/claude-code-actual-usage.md)）：Claude Code 的 `'now'` priority 零生产用例；6 个 built-in agent 0 个用 frontmatter mcpServers；Explore 是 hot path（34M+ spawns/week）。
- **Phase 0.7**（[claude-code-multi-agent-deep-dive.md](../../../tmp/claude-code-multi-agent-deep-dive.md)）：Coordinator Mode 和 Team/Swarm Mode 是**两个独立子系统**；Team Mode 整体推迟到 v0.8.10。

#### KodaX Evaluator 是结构性护城河，绝不替换

Phase 0.7 §11 + Phase 0 §12 综合证明：

| 维度 | Claude Code | KodaX 当前 |
|---|---|---|
| Verification 触发 | opportunistic nudge（≥3 todo 关闭后 tool result 加提醒，LLM 可忽略）| **structurally enforced**——`resolveHandoffTarget(generator) → EVALUATOR_AGENT_NAME`，Generator 物理上无法绕过 |
| Verdict 强制 | system prompt 要求 `VERDICT:` 格式，无机制保证 | 必经 `emit_verdict` 工具，schema 强制 verdict 字段 |
| Revision loops | LLM 自决是否再调 verification | Evaluator `revise` 走确定性 routing 回 Generator/Planner |

**结论**：把 Evaluator 换成"按需 Verification subagent"= 把 KodaX 的结构保证降级成 Claude Code 的 prompt 约定。**这是回归不是进步。FEATURE_114 保留 Evaluator 为独立 role**。

### 设计

#### 顶层结构：Worker + Evaluator

```
┌──────────────────────────────────────────────────────────┐
│ Worker（单一 primary role）                               │
│                                                           │
│ 单一 system prompt（合并 Scout + Generator）              │
│   - Trivial 任务（单步/单文件/纯 lookup）：                │
│       直接答或直接干，不调 todo_update                    │
│   - 非 Trivial：                                          │
│       第一个 tool call MUST 是 todo_update                │
│       runner nudge 监控：N 个 read 后无 todo_update       │
│       → tool result 追加 reminder（不阻断、只提醒）       │
│                                                           │
│ Plan 提交后：                                              │
│   TodoStore.init(items) → TodoListSurface 立即渲染        │
│   （FEATURE_097 review 类任务可见性结构性修复）           │
│                                                           │
│ 按 plan 执行（每步循环）：                                 │
│   1. 取下一 pending step                                  │
│   2. 执行（read/write/edit/bash/dispatch_child_task）     │
│   3. 调 todo_update 标 completed                          │
│   4. step.evaluator != 'none' →                          │
│      runner 自动跑 deterministic check                    │
│      （build/test/lint）                                  │
│   5. 失败 → tool result 含 stderr → Worker 自决修补        │
│                                                           │
│ 工具集：read/grep/glob/write/edit/multi_edit/bash/        │
│         todo_update/dispatch_child_task                   │
└──────────────────────────────────────────────────────────┘
   ↓
   Worker emit_handoff
   ↓
┌──────────────────────────────────────────────────────────┐
│ Evaluator（独立 role，结构性 gate 保留）                  │
│                                                           │
│ 独立 system prompt（精简、对立验证）                      │
│ 独立 child session（read-only）                           │
│ 工具集：read/grep/glob/bash(read-only)                   │
│ emit_verdict {accept/revise/blocked}                     │
│   - accept: terminal                                      │
│   - revise: 回 Worker 修补                                │
│   - blocked: terminal (failure)                          │
└──────────────────────────────────────────────────────────┘

Replan：iterative，不 reset
  - adjust：todo_update 改当前 in_progress item
  - insert：todo_update 追加新 item
  - drop：todo_update 标 cancelled
  - reset 仅作为兜底，不自动触发
```

#### Plan-first 实现：prompt + nudge，不是 gate

业界 4/4 codebase 一致——**没有 runner-enforced plan-first gate**。Claude Code（最规范的）用 system prompt + TodoWriteTool structural nudge。

**Worker system prompt 核心条款**（伪代码）：

```
You are the Worker — KodaX's single primary agent.

For non-trivial tasks (>=2 distinct execution steps, or any task touching
>=2 files/areas/feature threads), your FIRST tool call MUST be
todo_update to commit your plan.

Trivial tasks (single typo fix, single-line edit, single-question lookup,
pure conversational answer) skip todo_update — answer or execute directly.

If you find mid-task that what looked trivial is actually multi-step,
call todo_update at that moment to retrofit the plan.

todo_update items have an optional evaluator hint:
  { content, status, evaluator: 'none' | 'build' | 'test' | 'lint' }
Mark milestone steps with the appropriate deterministic check; leave
routine steps as 'none'.
```

**Runner-side structural nudge**（[runner-driven.ts](../../packages/coding/src/task-engine/runner-driven.ts) 修改）：

```
After each tool_call, if:
  - tool count since last todo_update >= 5
  - and at least one read/grep/glob has fired
  - and no todo_update has fired this session
Append to tool_result:
  "[nudge] You have read N files without committing a plan.
   If this task is non-trivial, call todo_update now."
```

#### Per-step evaluator：仅 deterministic，不接 LLM-as-judge

`TodoItem` schema 扩展（[todo-store.ts](../../packages/coding/src/task-engine/todo-store.ts)）：

```ts
interface TodoItem {
  id: string;
  content: string;
  status: 'pending' | 'in_progress' | 'completed' | 'failed' | 'cancelled';
  evaluator?: 'build' | 'test' | 'lint' | 'eval-case:X';  // 可选
  sourceObligationIndex?: number;                          // 既有
}
```

**触发**：runner 看到 `todo_update` 把某 item 从 `in_progress` → `completed`，且该 item 有 `evaluator`，自动跑对应 deterministic check：

| evaluator 值 | 命令 | 失败处理 |
|---|---|---|
| `'build'` | `npm run build`（项目级，timeout 90s）| stderr → tool result，标 `failed`，Worker 自决修补 |
| `'test'` | `npm run test --` 或 `npx vitest run <path>` | 同上 |
| `'lint'` | `npm run lint` 或 `tsc --noEmit` | 同上 |
| `'eval-case:X'` | 调 FEATURE_104 prompt eval harness 跑指定 case | 仅 prompt 改动相关节点用 |

**为什么不做 `evaluator: 'judge'` LLM-as-judge**：业界 4/4 不做。Claude Code 的 verification subagent 是末端一次而非 per-step，且内部主要靠 build/test/lint。Per-step LLM-judge 让每个任务的 LLM call 数翻倍，违反 KodaX `CLAUDE.md` minimalist 原则。

#### 4→2 角色衰减的 4 项保护措施

Phase 0.7 调研确认 4→2 是有衰减风险的真实重构。简单删 role 文件会衰减，做对（带 4 项保护措施）净正。

1. **SCOPE COMMITMENT hard rule（FEATURE_106）port 到 Worker prompt**——当前在 Scout prompt 里，要 port 到 Worker prompt 同位
2. **Generator 的 dispatch RULE A/B/C port 到 Worker prompt**——dispatch_child_task 决策规则不丢
3. **Evaluator 仍是独立 agent invocation**——不并入 Worker turn（保持独立 reviewer 视角）
4. **新 invariant `planBeforeMutate`**——替换 FEATURE_101 第 8 项 `harnessSelectionTiming`：Worker 第一个 mutation tool 调用前必须有 `todo_update` **或** 显式 trivial 声明（observe-only severity=`warn`）

#### Replan：iterative，不 reset

| 操作 | 实现 | UI 反映 |
|---|---|---|
| **adjust** | LLM 调 `todo_update` 改当前 in_progress item description / evaluator | 内容变更，状态保留 |
| **insert** | `todo_update` 在当前 item 后插新 item（`inserted_after` 标记）| 新 item 出现，注 "added mid-task" |
| **drop** | `todo_update` 标某 pending item `cancelled` | 划线显示 |
| **branch** | `todo_update` 一次性增删多 item | 列表实时刷新 |

**reset 仅作为兜底**：Evaluator `revise` 不再自动触发 reset；Worker 显式调 `todo_reset(reason)` 工具时才清空。

#### Trivial 路径：自然形式，零额外机制

不写任何 fast path 代码。Trivial 任务的天然形式是：

- LLM 看 system prompt 判断"这是 trivial"
- 直接答 / 直接调一次 tool 结束
- 不调 `todo_update`
- runner loop 在 `finish: 'stop'` 时退出

UI 表现：TodoListSurface 在没有 todo 时**完全不渲染**（既有行为）。

### 影响范围

**新增**：

- `packages/coding/src/agents/worker-role-prompt.ts`：合并 Scout+Generator system prompt
- `packages/coding/src/task-engine/deterministic-evaluator.ts`：build/test/lint 触发器
- `packages/coding/src/task-engine/runner-nudges.ts`：plan-first nudge
- `packages/coding/src/agent-runtime/invariants/plan-before-mutate.ts`：新 invariant 替换 harnessSelectionTiming
- `tests/harness-v2-baseline.eval.ts`：FEATURE_104 prompt eval（review / 多步实现 / trivial answer 三类）
- `tests/harness-v2-runner.test.ts`：runner unit test（nudge / evaluator / replan 增删）
- `docs/test-guides/FEATURE_114_v0.7.36_TEST_GUIDE.md`：人测引导

**修改**：

- `packages/coding/src/managed-protocol.ts`：harness enum 加 `PLANNED`，保留 `H0_DIRECT/H1_EXECUTE_EVAL/H2_PLAN_EXECUTE_EVAL` 直到 v0.7.40 完全切换
- `packages/coding/src/task-engine/todo-store.ts`：`TodoItem` schema 加 `evaluator?` + `cancelled` status
- `packages/coding/src/agents/protocol-emitters.ts`：`todo_update` 接 evaluator hint
- `packages/coding/src/task-engine/runner-driven.ts`：单 loop 实现替代 H1/H2 分支（feature flag 切换）
- `packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts`：Worker 合并 prompt（保留 Scout/Planner/Generator/Evaluator 直到 v0.7.45）
- `packages/repl/src/ui/AmaTodoListSurface.tsx`：渲染 evaluator 标签 + cancelled 划线
- `docs/FEATURE_LIST.md`：新增 FEATURE_114 / FEATURE_115 行
- `CHANGELOG.md`

**不改动**：

- `packages/coding/src/guardrails/auto-mode/`（FEATURE_092 正交）
- `packages/repointel-protocol/`、`packages/session-lineage/`（不动）
- `packages/agent/src/construction/`（Self-Construction 系统不动；v0.7.35.1 已从 `packages/coding/src/construction/` 整体迁到 `@kodax/agent/construction/`，本版以后所有 construction 引用都指向 agent 包路径）
- `packages/mcp/`（MCP runtime 不动）
- Evaluator 整套（保留为 structural gate）

### 迁移策略

```
v0.7.36（本 feature）：
  - 落地 V2 runner、worker prompt、deterministic evaluator
  - feature flag 默认关闭：KODAX_HARNESS_V2=false
  - 仅 KODAX_HARNESS_V2=true 时走 V2，否则继续 H0/H1/H2
  - 跑 baseline eval 三类（review / 实现 / trivial）证明 V2 不退化

v0.7.39+（FEATURE_120 落地）：
  - 监测一周生产 telemetry，无重大回归则 feature flag 默认开启

v0.7.40+：
  - 删 H1_EXECUTE_EVAL（结构性最弱的一档先砍）
  - 保留 H0/H2 与 PLANNED 并存若干版本

v0.7.45+：
  - 删 H0/H2，统一走 PLANNED
  - 删 Scout/Planner/Generator role prompt 文件
  - 删 Layer A 中 4 角色专属协议位
```

每一步可独立 rollback；feature flag 是核心安全网。

### 验收标准

1. **V2 单 role 跑通基本三类任务**：trivial answer / 多步实现 / review/audit。三类都能 finish，trivial 不调 `todo_update`，多步任务首个 tool call 是 `todo_update`，**review 任务在前 1-2 turn emit 计划列表**（不再后置）。
2. **FEATURE_097 在 V2 下结构稳定**：跑 [`bench/datasets/`](../../benchmark/datasets/) review 类 case，TodoListSurface 在前 2 turn 内出现的比率 ≥ **95%**（V1 当前数据约 60-70%）。
3. **Per-step deterministic check 触发正确**：todo item 标 `evaluator: 'build'`，状态变 completed 时 runner 自动跑 `npm run build`；失败时 tool result 含 stderr，LLM 能在下一轮基于此修补。
4. **Evaluator 结构性 gate 保留**：Worker emit_handoff 后必经 Evaluator；Evaluator revise/accept/blocked 路径全部 work；不存在 Worker 直接 terminal 路径。
5. **Replan iterative**：mid-task 调 `todo_update` 增删 step，TodoListSurface 增量更新（不全量 reset），UI 注明 "added mid-task" / "cancelled"。
6. **`planBeforeMutate` invariant 触发**：Worker 第一个 mutation tool 调用前未 `todo_update` 也未声明 trivial → invariant emit warn 日志。
7. **Feature flag 双轨**：`KODAX_HARNESS_V2=false` 完全等价 v0.7.34/35 行为（既有 test 全绿）；`KODAX_HARNESS_V2=true` 走 V2 全新路径。
8. **Prompt eval 三类 baseline**：`tests/harness-v2-baseline.eval.ts` 在 FEATURE_104 alias 表 8 alias mean ≥ 80%（与 FEATURE_106/111 同标）。
9. **跨 family 验证**（呼应 FEATURE_109 AHE）：V2 在 GLM-Coding / Kimi-Code / Claude / GPT-4o 四家 baseline pass max-min spread ≤ **15pp**。
10. **无回归**：`npm run test` / `npm run build` 全绿；既有 H0/H1/H2 路径在 flag=false 时表现不变。

### 非目标

- **不** 引入 per-step LLM-as-judge（仅 deterministic build/test/lint）
- **不** 在 runner 层强制 plan-first gate（用 prompt + nudge）
- **不** 为 trivial 任务做 fast path 代码分支
- **不** 撤销 Evaluator 角色（保留为 structural gate，KodaX 护城河）
- **不** 引入 PauseToken / 状态机 / 新协议层
- **不** 持久化 TodoStore（v2 暂不引入，留在 FEATURE_113 v0.8.2）
- **不** 在本版本删任何旧 role prompt 文件（直到 v0.7.45）

---

## FEATURE_115: Message Queue Foundation — agentId-Scoped 2-Tier Priority Queue + Sleep-Gated Mid-Turn Drain

**Status**: Planned
**Priority**: High
**Category**: Internal / Infrastructure

### 背景

#### 当前 KodaX 通信能力的限制

- `pending-inputs.ts` 只支持用户输入 → REPL 单向队列；`MAX_PENDING_INPUTS=5`
- 无 cross-agent 路由（child agent 输出全 inline 进父 tool result）
- 无 mid-turn drain（要等 turn 自然结束）
- FEATURE_111 soft-pause 设计本质是"runner loop 加 paused boolean + tool 边界检查"——是 message queue 的**简化特例**

#### Phase 0.7 实测数据

Claude Code `messageQueueManager` 三档（now/next/later）实际：
- `'now'` priority **零生产用例**（仅 `print.ts:1860` abort 检测）
- `'next'` 是用户命令主路径
- `'later'` 是 task notification（subagent → coordinator）
- agentId-scoped drain + Sleep-gated mid-turn drain 是 correctness 必需

**KodaX 简化为 2 档**：用户级（user）+ 后台级（background），删去无生产用例的 `'now'`。

#### FEATURE_111 absorption

FEATURE_111（v0.7.43 原计划）的 soft-pause UX——单击 ESC tool 边界软暂停 + 文字注入——本质是 message queue 的具体应用：
- ESC → mark Worker `paused=true` → message queue 在 tool 边界 drain
- 文字注入 → 入 queue priority='user' → 下一轮 LLM 调用前 drain

FEATURE_115 message queue 完整实现后，FEATURE_111 设计可**完全用 queue 实现**——FEATURE_111 设计文档保留为历史，实现统一走 queue。v0.7.43 slot 腾出给 FEATURE_124（Memory alignment）。

### 设计

#### 2 档优先级 + agentId routing

```ts
type MessagePriority = 'user' | 'background';

interface QueuedMessage {
  id: string;
  priority: MessagePriority;
  agentId?: string;          // undefined = 主线程；具体值 = subagent
  mode: 'prompt' | 'task-notification' | 'system-reminder';
  content: string;
  enqueuedAt: number;
}

// agentId 路由：
//   主线程 drain agentId === undefined
//   subagent drain mode === 'task-notification' && agentId === currentAgentId
//   slash command 排除在 mid-turn drain（必须 turn 末才处理）
```

#### Sleep-gated mid-turn drain

仿 Claude Code [`query.ts:1551-1566`](file:///c:/Works/claudecode/src/query.ts#L1551) 模式：

```ts
// runner-driven.ts 修改
async function maybeDrainMidTurn(state: RunnerState): Promise<QueuedMessage[]> {
  const sleepRan = state.lastTurnToolUseBlocks.some(
    (b) => b.name === SLEEP_TOOL_NAME
  );
  // 默认只 drain user priority
  // 跑了 Sleep tool 才 drain background（包含 task-notification）
  const maxPriority = sleepRan ? 'background' : 'user';
  return queue.dequeue({
    agentId: state.currentAgentId,
    maxPriority,
  });
}

// 调用点：每个 tool 执行完、下一轮 LLM 调用之前
```

**为什么 Sleep-gated**：防止 background task notification 在 Worker 干主线工作时被处理（Sleep 是显式 yield 信号）。

#### FEATURE_111 soft-pause UX 走 queue 实现

```
单击 ESC：
  - InkREPL 设 Worker paused=true
  - spinner 文字变 "Paused (waiting for tool to finish)"

当前 tool 跑完后 runner 检查 paused：
  - 走 maybeDrainMidTurn 但跳过实际 drain
  - 等用户输入或继续

用户输入 + Enter：
  - 内容入 queue priority='user', agentId=undefined
  - paused=false
  - 下一轮 LLM 调用前 drain → 注入下一轮 user message（前置 <user-interjected> 标签）

空 Enter：
  - paused=false
  - 不入 queue
  - 下一轮 LLM 调用按原计划继续

paused 期间再按 ESC：
  - 升级为 abort（既有 double-ESC 路径）
```

完全等价 FEATURE_111 设计的 UX，实现机制走更通用的 message queue。

#### Pending-inputs migration

`pending-inputs.ts` 内部从"独立单向队列"重构为"message queue 的 user priority + agentId=undefined 视图"。对外 API（`enqueueInput()` / `dequeueInput()`）保留语义不变。`MAX_PENDING_INPUTS=5` 保留为同 priority 的入队上限。

### 影响范围

**新增**：

- `packages/agent/src/messaging/queue.ts`：2 档优先级队列 + agentId routing（~150 行）—— **通用 agent 平台原语，落 `@kodax/agent`**（per v0.7.35.1 包归属修正：messaging 是任何 agent 都需要的能力，不绑定 coding）
- `packages/agent/src/messaging/types.ts`：QueuedMessage / MessagePriority 类型
- `tests/message-queue.test.ts`：unit test 覆盖 priority / agentId routing / Sleep-gated drain

**修改**：

- `packages/coding/src/task-engine/runner-driven.ts`：tool 边界 drain 调用（~80 行新增）—— runner 在 coding 端，调用 `@kodax/agent` 的 queue 原语
- `packages/repl/src/ui/utils/pending-inputs.ts`：内部重构走 message queue（API 不变）
- `packages/repl/src/ui/InkREPL.tsx`：ESC handler 配合（FEATURE_111 UX 实现）+ 文字注入入 queue
- `packages/coding/src/agents/protocol-emitters.ts`：subagent task-notification 入 queue priority='background'

**不改动**：

- 既有 dispatch_child_task 接口（v0.7.39 FEATURE_120 才扩展）
- AbortController 路径（独立机制保留）
- permission-gate（不动权限层）

### 迁移策略

- 与 FEATURE_114 同期 ship，feature flag 共享 `KODAX_HARNESS_V2`
- pending-inputs.ts 内部重构对用户透明（API 不变）
- FEATURE_111 设计文档（v0.7.43）标记 **Absorbed into FEATURE_115**

### 验收标准

1. **2 档 priority drain 正确**：user message 优先于 background；同 priority 内 FIFO
2. **agentId routing**：主线程不消费 subagent task-notification；subagent 不消费用户输入
3. **Sleep-gated mid-turn drain**：Sleep tool 未跑时只 drain user；跑了 Sleep 才 drain background
4. **FEATURE_111 UX 等价**：单击 ESC tool 边界软暂停 + 文字注入下一轮，行为等价于 FEATURE_111 设计验收标准 1-6
5. **pending-inputs API 不变**：既有 `enqueueInput()` / `dequeueInput()` 调用点全绿
6. **subagent task-notification 流转**：subagent 完成 → 入 queue priority='background' agentId='parent' → 父 Worker 在 Sleep 后 drain 看到结果
7. **slash command 排除 mid-turn drain**：slash command 只在 turn 末（既有路径）处理
8. **无回归**：所有既有 message handling 测试绿

### 非目标

- **不** 做 3 档 priority（业界证实 `'now'` 无用）
- **不** 做 cross-agent name routing（v0.7.39 FEATURE_120 做）
- **不** 做 broadcast `to: '*'`（v0.7.44 FEATURE_123 做）
- **不** 做 file-based mailbox（v0.8.10 FEATURE_125 做）
- **不** 持久化 message queue（重启丢失，与 TodoStore 一致）
- **不** 替代 AbortController（独立路径）

---

## FEATURE_119: Async Child Dispatch — Pattern B (Launch + Await)

**Status**: Planned
**Priority**: High
**Category**: Internal / Architecture

### 背景

#### 当前 KodaX dispatch_child_task 同步阻塞问题

[`dispatch-child-tasks.ts:114`](../../packages/coding/src/tools/dispatch-child-tasks.ts#L114) 走 `await executeChildAgents([bundle], ctx, options)` ——主 Worker 派 child 后**必须等 child 跑完**才能继续。child 跑长任务（npm test 90s+）时：
- Worker 完全空闲不可用
- 用户插话只能 pending 等到 turn 末（即使 FEATURE_115 message queue 也无法 drain，因为 Worker 还没拿到 LLM call 机会）
- 不能起新 child 并行
- 不能 abort 单个 child（只能 abort 整个 Worker）

#### 5 codebase 调研结论

[multi-agent-async-orchestration-deep-dive.md](../../../tmp/multi-agent-async-orchestration-deep-dive.md) 调研明确：

| 系统 | 模式 | 主 agent 阻塞 |
|---|---|---|
| Claude Code Coordinator | Fire-and-forget + priority queue + XML notification（Pattern A，~1500 行）| **从不阻塞** |
| OpenCode TaskTool | 有 Effect Fiber 但 task.ts:131 仍 synchronous yield | **总是阻塞** |
| pi-mono agentLoop | Fire-and-forget EventStream | **从不阻塞** |
| Warp | Cancel-on-FollowUp（用户插话直接 kill 重启）| 流式期间 |
| **KodaX 当前** | `await executeChildAgents` | **总是阻塞** |
| **KodaX 推荐 Pattern B** | Launch + Await 拆分（~100 行）| **仅 await 时阻塞** |

调研推荐 **Pattern B**——用 1/15 工作量解决 Pattern A 解决的同一问题，且**符合 KodaX minimalist 哲学**（CLAUDE.md "abstract only after 3+ use cases"）。

### 设计

#### Step 1：拆分 `dispatch_child_task` → launch + await

```typescript
// dispatch_child_task — launch only（不 await）
async function* toolDispatchChildTask(input, ctx): AsyncGenerator<...> {
  // ... validation 不变 ...
  const taskId = `child-${Date.now()}-${randomSuffix()}`;

  // Fire-and-forget: 不 await
  const promise = executeChildAgents([bundle], ctx, options);
  ctx.childTaskRegistry.set(taskId, promise);

  yield { stage: 'launched', message: `Child "${taskId}" launched` };
  return `task_id:${taskId} launched. Use await_child_task("${taskId}") when result needed.`;
}

// await_child_task — 显式 await
async function toolAwaitChildTask(input, ctx): Promise<string> {
  const taskId = input.task_id;
  const promise = ctx.childTaskRegistry.get(taskId);
  if (!promise) return `[Tool Error] Unknown task_id: ${taskId}`;

  const result = await promise;
  ctx.childTaskRegistry.delete(taskId);
  return formatChildResult(result);
}
```

#### Step 2：`KodaXToolExecutionContext` 新增 `childTaskRegistry`

```typescript
// types.ts
interface KodaXToolExecutionContext {
  // ... 既有字段（abortSignal / extensionRuntime / guardrails）...
  childTaskRegistry: Map<string, Promise<KodaXChildExecutionResult>>;  // 新增
}
```

context 跨 Worker turn 持续——Promise 在多个 tool call 之间存活。

#### Step 3：Abort 传播（既有机制）

既有 `abortSignal: ctx.abortSignal` 已传给 `executeChildAgents`。父 abort 时所有 running children 在下次 `signal.throwIfAborted()` 检查停止。**无新 abort 路径**。

#### Step 4：Worker prompt 引导

新增 Worker system prompt 段落：

```
Async child dispatch protocol:
- dispatch_child_task launches a child and returns task_id IMMEDIATELY (does not block).
- After launch, you may continue using other tools, summarize progress, or
  start additional parallel children.
- Use await_child_task(task_id) when you actually need the child's result.
- Do NOT await immediately after dispatching unless you have nothing else to do.
- For multiple parallel children: dispatch all first, then await each in sequence.

Common patterns:
1. Parallel research: dispatch N read-only children → continue analyzing existing
   data → await all when ready to synthesize.
2. Long-running test/build: dispatch child for `npm test` → continue with code edits
   → await after edits done.
3. Verification: dispatch verification child → continue summarizing → await before
   reporting to user.

Synthesis discipline (when fan-out warranted):
- When you launch ≥2 parallel children for research/audit, await all, read
  their summaries, then decide implementation plan — do NOT dispatch
  implementation children based on partial information.
- You ARE the synthesizer. Don't spawn a "synthesis child" — that's your job.
```

替代当前隐式"派 child 必阻塞"行为，改为显式异步协议——类比 Claude Code coordinator system prompt 显式告诉 LLM async worker 协议。

**Synthesis hint 来源**：原 FEATURE_122 PM Thinking Framework（v0.7.41）取消后，把 Synthesis discipline 这一最有价值的 1 行从独立 feature 降级为 worker prompt 微调，合并到本 feature 的 worker dispatch protocol 段落。Continue-vs-Spawn 决策由 send_message vs dispatch_child_task 工具描述自然引导，不需要额外 prompt。

#### Step 5：与 FEATURE_115 message queue 的协同

Worker 在 await_child_task 之间的 tool call 之间，runner 走 `maybeDrainMidTurn`（FEATURE_115）—— **用户插话现在能在 child 跑中被 Worker 接收**：

```
T+0s:  Worker 调 dispatch_child_task(npm test)  → 立即返 task_id, child 在后台跑
T+1s:  Worker 调 read_file(...)                 → 期间 message queue drain check
T+5s:  用户插话 "测试要包含 X 场景" → 入 queue priority='user'
T+5s:  Worker 下一轮 LLM call 看到 user message 注入 → 处理
T+10s: Worker 决定继续 → 调 await_child_task(...)
T+90s: child 完成，await 解锁，Worker 拿结果
```

**这是 FEATURE_115 + FEATURE_119 联合产生的核心 UX 提升**：Worker 不再阻塞，用户 ESC 软暂停 + 文字注入 + 长 child 后台跑可以**同时**工作。

### 影响范围

**新增**：

- `packages/coding/src/tools/await-child-task.ts`：新工具实现（~50 行）
- `packages/coding/src/types.ts`：`KodaXToolExecutionContext.childTaskRegistry` 字段
- `tests/async-dispatch.test.ts`：Pattern B unit test（launch / await / abort 传播 / multiple parallel children）
- 工作量预估：**~100-150 行**净增

**修改**：

- `packages/coding/src/tools/dispatch-child-tasks.ts`：line 114 的 `await` 改为 fire-and-forget；返 task_id 字符串
- `packages/coding/src/tools/registry.ts`：注册 `await_child_task` 工具
- `packages/coding/src/agents/worker-role-prompt.ts`（FEATURE_114 同步）：加 async dispatch 协议段落
- `packages/coding/src/child-executor.ts`：可能需要小调整支持 fire-and-forget（保留 abort signal 传播）

**不改动**：

- `executeChildAgents` 内部逻辑（既有 Promise.allSettled + semaphore 不变）
- AbortController 路径
- guardrail / permission 层

### 验收标准

1. **launch 立即返回**：`dispatch_child_task` 在 child 跑长任务（如 90s npm test）时**立即返回** `task_id`，不阻塞 Worker
2. **await 阻塞正确**：`await_child_task(task_id)` 阻塞直到 child 完成或父 abort
3. **未知 task_id 报错**：`await_child_task("nonexistent")` 返 `[Tool Error] Unknown task_id`
4. **多 child 并行**：Worker 一次响应里 dispatch 3 个 child → 三个 task_id 立即返；后续 await 三次按完成顺序拿结果
5. **abort 传播**：父 abort 时所有 running children 停止；childTaskRegistry 清理
6. **与 FEATURE_115 协同**：90s child 跑中用户插话 → 下一个 tool call 边界 message queue drain → Worker 在下一轮 LLM call 看到用户 message
7. **Worker 自主使用 protocol**：FEATURE_104 prompt eval 验证 Worker 能正确按"launch 多个 child → 处理其他事 → await"模式工作（不是 launch 后立即 await 退化为同步）
8. **跨 family 验证**：4 family LLM（GLM/Kimi/Claude/GPT）跑通 async protocol，max-min spread ≤ 15pp
9. **无回归**：既有同步行为可通过 `dispatch_then_await(...)` helper 工具向后兼容（如有需要）；既有测试全绿

### 非目标

- **不** 引入 Coordinator/Worker 分层（Pattern A）——Pattern B 用 1/15 工作量达到同等效果
- **不** 引入 priority queue 三档（FEATURE_115 已确定 2 档）
- **不** 实现 child 运行中的中间状态推送（child 只在完成时通知；如需中间状态走 v0.7.40 FEATURE_121 Scratchpad）
- **不** 实现 child-to-child 通信（v0.7.44 FEATURE_123 Peer-to-peer SendMessage 做）
- **不** 实现 child 跑中的 SendMessage 续指令（v0.7.39 FEATURE_120 做）
- **不** 实现 task_stop kill 跑中 child（v0.7.39 FEATURE_120 做）
- **不** 持久化 childTaskRegistry（重启丢失，与 TodoStore 一致）

---

## FEATURE_143: Skill UX Upgrade + prompt-overlay 错位修正

**Status**: Planned
**Priority**: High
**Category**: Enhancement / UX + Internal / Refactor

### 需求概述

v0.7.35.1 FEATURE_144（Batch F）已经修完 6 段 capability "missing data" 漂移，AMA worker 现在能看到 mcp / skills / project-agents / tool-construction / git-context / project-snapshot。本 feature 收尾两类与 worker 视野无关、但用户实战可见的工作：

1. **`prompt-overlay` 位置错位修正**（"position 类"漂移，与 missing data 不同）：v0.7.26 FEATURE_084 把 `prompt-overlay` 从 SA path 的 system prompt 错位 stitching 到 AMA path 的 user prompt 头。worker 能拿到 routing notes 但语义错位（看起来像用户输入而非系统注入）。本 feature 把它移回 system prompt，与 SA path 对齐。
2. **Skill 用户层 UX 三件**：
   - `getSystemPromptSnippet()` manifest 措辞硬化为 Claude Code 风格强约束（"BLOCKING REQUIREMENT: invoke the relevant Skill tool BEFORE generating any other response"）
   - classic CLI REPL 在启动时调用 `skillRegistry.getSystemPromptSnippet()` 构造 `skillsPrompt`，与 Ink TUI 的 [InkREPL.tsx](../../packages/repl/src/ui/InkREPL.tsx) 行为对齐
   - `parseCommand` 增加 mid-line `/skill:foo` 检测，识别非行首 skill 引用并注入对应 SKILL.md content

### 影响范围

| 文件 | 改动 |
|---|---|
| `@kodax/coding/src/task-engine/runner-driven.ts` | 删除 `prompt-overlay` 到 user prompt 头的 stitching；改为通过 `options.context.promptOverlay` 让 capability-sections helper（FEATURE_142 Batch E 已建）注入到 system prompt |
| `@kodax/coding/src/prompts/capability-sections.ts` | 加入 `prompt-overlay` 段拼装（与 SA path 现状一致） |
| `@kodax/skills/src/skill-registry.ts` `getSystemPromptSnippet()` | manifest 文本重写为 Claude Code 风格强约束 |
| `@kodax/repl/src/interactive/repl.ts` | classic CLI 启动路径调 `skillRegistry.getSystemPromptSnippet()` 设置 `skillsPrompt` |
| `@kodax/repl/src/interactive/commands.ts` `parseCommand` | 行中 `/skill:NAME` 模式检测 + skill 内容预加载 |
| `tests/skill-ux.eval.ts` | Skill UX 三件 prompt eval |
| `tests/prompt-overlay-position-migration.eval.ts` | prompt-overlay 位置迁移前后 worker H0/H1/H2 routing 行为对比 |

### 技术方案

**`prompt-overlay` 错位修正**：

- 移除 `runner-driven.ts` stitching 逻辑（worker 不再从 user prompt 头读到 routing notes）
- worker 通过 capability-sections helper 在 system prompt 看到同样内容
- 行为变更——routing notes 从"看起来像用户输入"变成"明确的 system 注入"，可能影响 LLM 对 routing notes 的权威性判断
- 触发完整 prompt eval：8 alias × 6 task × 2 prompt variant，对比迁移前后 worker 在 H0/H1/H2 选择 / SCOPE COMMITMENT 触发 / instruction following 上的行为

**Skill manifest 措辞硬化**：参考 [Claude Code SkillTool prompt.ts:173-195](file:///c:/Works/claudecode/src/tools/SkillTool/prompt.ts#L173-L195) 的措辞——"When users reference a 'slash command' or '/<something>'... they are referring to a skill. Use this tool. **BLOCKING REQUIREMENT: invoke the relevant Skill tool BEFORE generating any other response about the task**"。具体措辞 fixture 见 [c:/Works/claudecode/src/tools/SkillTool/prompt.ts](file:///c:/Works/claudecode/src/tools/SkillTool/prompt.ts) 现状。

**classic CLI 平等**：在 `runInteractiveMode` 启动路径（与 [InkREPL.tsx](../../packages/repl/src/ui/InkREPL.tsx) 调 `getSystemPromptSnippet()` 同位置等价处）补上 skillsPrompt 构造。

**mid-line parser**：`parseCommand` 拆分为 `parseExplicitCommand`（行首 `/cmd`，已有）+ `parseInlineSkillReferences`（行中 `/skill:NAME` 引用扫描）。命中后把对应 SKILL.md content 作为 system context 注入用户消息前。

### 接口契约

- `getSystemPromptSnippet()` 返回的字符串格式不变（仍是 markdown），只是文本内容硬化
- `runInteractiveMode` 的 `skillsPrompt` 构造点为新增，不破坏现有 InkREPL 路径
- `parseCommand` 返回类型加 `inlineSkillReferences?: SkillReference[]` 字段；现有调用方默认 ignore

### 实现步骤

1. **prompt-overlay 错位修正先行**（高风险，需 prompt eval gate）
   - 加入 capability-sections helper 的 `prompt-overlay` 段拼装
   - 移除 runner-driven.ts stitching
   - 跑 `tests/prompt-overlay-position-migration.eval.ts`，mean ≥80% 通过率才进 step 2
2. **Skill manifest 措辞硬化**（中风险，需 prompt eval gate）
   - 改 `getSystemPromptSnippet()` 文本
   - 跑 `tests/skill-ux.eval.ts` 部分用例
3. **classic CLI 平等**（低风险，机械补丁）
   - 在 `runInteractiveMode` 启动加 skillsPrompt 构造
   - 加单测确认 classic CLI 启动后 `currentOptions.context.skillsPrompt` 非空
4. **mid-line `/skill:foo` parser**（中风险，新功能）
   - 加 parser + 单测
   - 加 prompt eval：行中引用触发 SKILL.md 加载

### 验收标准

- [ ] `tests/prompt-overlay-position-migration.eval.ts` 跨 8 alias mean ≥80% 通过率，**单 case 不退化** baseline 超过 -10pp
- [ ] `tests/skill-ux.eval.ts` mean ≥80% 通过率
- [ ] classic CLI 启动后 `currentOptions.context.skillsPrompt` 与 InkREPL 启动后**字符串完全一致**（消除两 surface 差异）
- [ ] 行中 `/skill:feature-list-tracker` 触发 SKILL.md 内容注入到当前 user message 前作为 system context（行首已有的 `/skill:NAME` 行为不变）
- [ ] FEATURE_LIST.md 同步更新

### 与 v0.7.35.1 FEATURE_144 的关系

| 漂移类别 | 谁负责 | 完成版本 |
|---|---|---|
| 6 段 "missing data" 漂移（mcp / skills / project-agents / tool-construction / git-context / project-snapshot） | v0.7.35.1 FEATURE_144 Batch F | v0.7.35.1 |
| 1 段 "position 类" 漂移（prompt-overlay 用户 prompt → system prompt） | v0.7.36 FEATURE_143 | v0.7.36 |
| Skill 用户层 UX 升级（manifest 硬化 / classic CLI 平等 / mid-line parser） | v0.7.36 FEATURE_143 | v0.7.36 |

两者互不阻塞，但 FEATURE_143 prompt-overlay 修正**前置依赖** FEATURE_142 Batch E 的 `capability-sections.ts` helper（v0.7.35.1 已 land）。

### 显式不做

- ❌ 把 Skill 内容形态从 user-message 拼接升级为 tool-result 注入（这是 Claude Code 模式，但 KodaX 当前用 Read tool 路径已 work；升级是大改动，留 v0.7.43 FEATURE_143-B1 配合 FEATURE_124 Memory 同期重新评估）
- ❌ 引入 token budget control（Claude Code 1% context window）——KodaX 当前 skill 数量级未触发；按 3+ rule 等用户增长再做
- ❌ 引入 Claude Code `<command-name>` tag idempotent 保护——mid-line parser 实施后没看到实际 loop 才加

---

## 与其他 Feature 的关系

```
FEATURE_114 (Worker+Evaluator) ──┐
                                 │
FEATURE_115 (Message Queue) ─────┼──► v0.7.36 三件套基础架构
                                 │
FEATURE_119 (Async Dispatch B) ──┘
        │
        ├─► v0.7.37 FEATURE_116 (Cache 主动控制)
        │       │
        │       └─► v0.7.38 FEATURE_117 (Read-Path Context Stripping，缩窄版)
        │
        ├─► v0.7.39 FEATURE_120 (SendMessage / TaskStop — child steering 在 Pattern B 之上)
        │       │                  + dispatch schema 加 model_hint? 字段
        │       │
        │       ├─► v0.7.40 FEATURE_121 (Scratchpad — 大输出解耦)
        │       │
        │       └─► v0.7.44 FEATURE_123 (Peer-to-peer SendMessage)
        │
        ├─► v0.7.41 FEATURE_122 (PM thinking framework prompt)
        │
        ├─► v0.7.43 FEATURE_124 (Memory 4-type + two-step save)
        │
        └─► v0.7.50 FEATURE_128 (Dispatch-to-Self-Construction Bridge
                                 + dynamic agent list cache)

v0.7.45-v0.7.48 (既有 102 / 105 / 108 / 109) — 设计文档需修正以匹配新架构
v0.8.5  FEATURE_128 (移至 v0.7.50)
v0.8.10 FEATURE_125 (Team / Swarm 完整系统) ← 独立大版本，依赖 FEATURE_128
v0.8.x  FEATURE_126 (PM Mode 工具集裁剪) ← 数据驱动占位

吸收 / 取代：
- FEATURE_111 (Soft-Pause Protocol)：absorbed into FEATURE_115（设计文档保留为历史）
- FEATURE_059 (Managed Task Structured Protocol V2)：已 absorbed into FEATURE_084 (v0.7.26)
- FEATURE_127 (per-agent model tier，曾占位)：撤销，schema 字段并入 FEATURE_120，routing 行为合入 FEATURE_102
```

---

## 参考

- KodaX 当前 4 角色 system prompt：[role-prompt.ts:444-819](../../packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts#L444-L819)
- KodaX 当前 harness enum：[managed-protocol.ts:118-148](../../packages/coding/src/managed-protocol.ts#L118-L148)
- KodaX 当前 runner：[runner-driven.ts](../../packages/coding/src/task-engine/runner-driven.ts)
- KodaX TodoStore：[todo-store.ts](../../packages/coding/src/task-engine/todo-store.ts)
- KodaX MCP runtime：[packages/mcp/src/](../../packages/mcp/src/)
- KodaX Self-Construction：[packages/agent/src/construction/](../../packages/agent/src/construction/)（v0.7.35.1 后；v0.7.35 之前在 `packages/coding/src/construction/`）
- FEATURE_097 P0-followup commit：`32c5205` message
- FEATURE_107 H2-A vs H2-B 0pp delta：`benchmark/datasets/h2-plan-execute-boundary/`
- 三轮深度调研：
  - Phase 0：`c:/tmp/claude-code-architecture-study.md`
  - Phase 0.5：`c:/tmp/kodax-capabilities-inventory.md`
  - Phase 0.6：`c:/tmp/claude-code-actual-usage.md`
  - Phase 0.7：`c:/tmp/claude-code-multi-agent-deep-dive.md`
- Claude Code messageQueueManager：`c:/Works/claudecode/src/utils/messageQueueManager.ts`
- Claude Code mid-turn drain：`c:/Works/claudecode/src/query.ts:1551-1566`
- Claude Code TodoWriteTool nudge：`c:/Works/claudecode/src/tools/TodoWriteTool/TodoWriteTool.ts:71-83`
- AHE 论文（FEATURE_109 引用）：[arxiv:2604.25850](https://arxiv.org/abs/2604.25850)
