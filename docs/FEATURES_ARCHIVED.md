# Archived Features

_Archive Created: 2026-05-19_

> This file holds the historical record of all features that have shipped (or were rolled into a shipped version) in KodaX.
> Each section is grouped by the release version that delivered the feature.
> Detailed design documents remain under `docs/features/v{VERSION}.md`.
> Tracker cleanup on 2026-06-13 moved stale live-roadmap rows here so `FEATURE_LIST.md` only shows active work.

---

## v0.3.1 (3 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `001` | Plan Mode | `v0.3.1` | [v0.3.1](features/v0.3.1.md) | — |
| `002` | Ask Mode Hardening | `v0.3.1` | [v0.3.1](features/v0.3.1.md) | — |
| `003` | Interactive Project Mode | `v0.3.1` | [v0.3.1](features/v0.3.1.md) | — |

## v0.3.3 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `004` | Interactive UI Refresh | `v0.3.3` | [v0.3.3](features/v0.3.3.md) | — |

## v0.4.0 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `005` | Monorepo Refactor | `v0.4.0` | [v0.4.0](features/v0.4.0.md) | — |

## v0.4.6 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `008` | Permission Model Hardening | `v0.4.6` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.0 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `009` | AI Layer Separation | `v0.5.0` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.5 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `010` | Agent Core and Skills Split | `v0.5.5` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.10 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `006` | Skills System | `v0.5.10` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.13 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `012` | TUI Autocomplete | `v0.5.13` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.14 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `011` | Smart Context Compaction | `v0.5.14` | [v0.5.0](features/v0.5.0.md) | — |

## v0.5.20 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `014` | Project Mode Enhancement | `v0.5.20` | [v0.5.20](features/v0.5.20.md) | — |

## v0.5.22 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `016` | CLI-Based OAuth Providers | `v0.5.22` | [v0.5.22](features/v0.5.22.md) | — |

## v0.5.34 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `020` | AGENTS.md Workspace Rules | `v0.5.34` | [v0.6.0](features/v0.6.0.md) | — |

## v0.5.37 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `021` | Provider-Aware Reasoning Budget | `v0.5.37` | [v0.6.0](features/v0.6.0.md) | — |

## v0.6.0 (3 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `013` | Command System 2.0 | `v0.6.0` | [v0.6.0](features/v0.6.0.md) | — |
| `015` | Project Mode 2.0 | `v0.6.0` | [v0.6.0](features/v0.6.0.md) | — |
| `017` | Pending User Inputs Queue | `v0.6.0` | [v0.6.0](features/v0.6.0.md) | — |

## v0.6.10 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `024` | Project Harness | `v0.6.10` | [v0.6.10](features/v0.6.10.md) | — |

## v0.6.15 (4 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `033` | REPL Parallel Toggle | `v0.6.15` | [v0.6.15](features/v0.6.15.md) | — |
| `036` | DeepSeek Built-in Provider | `v0.6.15` | [v0.6.15](features/v0.6.15.md) | — |
| `039` | Plan-Mode Dual-Write Allowlist | `v0.6.15` | [v0.6.15](features/v0.6.15.md) | — |
| `040` | ACP Server Support | `v0.6.15` | [v0.6.15](features/v0.6.15.md) | — |

## v0.6.20 (3 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `032` | JSON Output Mode | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) | — |
| `037` | API Token Usage Priority and Estimation Fallback | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) | — |
| `041` | Tool Output Guardrails and Context Overflow Protection | `v0.6.20` (unreleased) | [v0.6.20](features/v0.6.20.md) | — |

## v0.7.0 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `034` | Extension and Capability Runtime | `v0.7.0` | [v0.7.0](features/v0.7.0.md#feature_034-extension-and-capability-runtime) | — |

## v0.7.1 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `029` | Provider Capability Transparency and Harness Policy | `v0.7.1` | [v0.7.0](features/v0.7.0.md#feature_029-provider-capability-transparency-and-harness-policy) | — |

## v0.7.2 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `019` | Session Tree, Checkpoints, and Rewindable Task Runs | `v0.7.2` | [v0.7.0](features/v0.7.0.md#feature_019-session-tree-checkpoints-and-rewindable-task-runs) | — |

## v0.7.4 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `022` | Adaptive Task Engine and Native Multi-Agent Control Plane | `v0.7.4` | [v0.7.0](features/v0.7.0.md#feature_022-adaptive-task-engine-and-native-multi-agent-control-plane) | — |
| `025` | Adaptive Task Intelligence and Harness Router | `v0.7.4` | [v0.7.0](features/v0.7.0.md#feature_025-adaptive-task-intelligence-and-harness-router) | — |

## v0.7.10 (3 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `018` | Task-Aware Repository Intelligence Substrate | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_018-task-aware-repository-intelligence-substrate) | — |
| `027` | Adaptive Multi-Agent Mode Toggle and Team-Mode Sunset | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_027-adaptive-multi-agent-mode-toggle-and-team-mode-sunset) | — |
| `028` | First-Class Retrieval, Context, and Evidence Tooling | `v0.7.10` | [v0.7.10](features/v0.7.10.md#feature_028-first-class-retrieval-context-and-evidence-tooling) | — |

## v0.7.15 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `035` | MCP Capability Provider | `v0.7.15` | [v0.8.0](features/v0.9.5.md#feature_035-mcp-capability-provider) | — |
| `045` | Provider Stream Resilience and Graceful Recovery | `v0.7.15` (unreleased) | [v0.7.15](features/v0.7.15.md#feature_045-provider-stream-resilience-and-graceful-recovery) | — |

## v0.7.16 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `061` | Scout-First AMA Architecture Simplification | `v0.7.16` | [v0.7.16](features/v0.7.16.md#feature_061-scout-first-ama-architecture-simplification) | — |
| `062` | Managed Task Budget Simplification | `v0.7.16` | [v0.7.16](features/v0.7.16.md#feature_062-managed-task-budget-simplification) | — |

## v0.7.18 (9 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `046` | AMA Handoff Integrity and Final-Answer Convergence | `v0.7.18` | [v0.8.0](features/v0.9.5.md#feature_046-ama-handoff-integrity-and-final-answer-convergence) | — |
| `064` | Multi-Provider Cost Observatory | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_064-multi-provider-cost-observatory) | — |
| `065` | MCP Protocol Maturity | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_065-mcp-protocol-maturity) | — |
| `066` | Permission Hardening | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_066-permission-hardening) | — |
| `067` | Child Agent Execution — AMA-Native Parallel Task Dispatch | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_067-child-agent-execution--ama-native-parallel-task-dispatch) | — |
| `068` | Worktree Isolation Tool | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_068-worktree-isolation-tool) | — |
| `069` | Session Rewind & Shell Completion | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_069-session-rewind--shell-completion) | — |
| `070` | Context Engine V2 — Multi-Layer Compaction & Post-Compact Reconstruction | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_070-context-engine-v2--multi-layer-compaction--post-compact-reconstruction) | — |
| `071` | AMA Managed Task Resilience — Worker Checkpoint & Mid-Execution Recovery | `v0.7.18` | [v0.7.18](features/v0.7.18.md#feature_071-ama-managed-task-resilience--worker-checkpoint--mid-execution-recovery) | — |

## v0.7.19 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `047` | Invisible Adaptive Parallelism and Evidence-Driven Fan-Out | `v0.7.19` | [v0.7.19](features/v0.7.19.md#feature_047-invisible-adaptive-parallelism--fan-out-optimization) | 架构完成 (v0.7.16); 执行层完成 (v0.7.18 FEATURE_067); v0.7.19 补齐: winner-cancel, 调度策略, 质量度量 |
| `052` | Dual-Profile AMA Harness and Child Fan-Out Boundaries | `v0.7.19` | [v0.7.19](features/v0.7.19.md#feature_052-child-fan-out-boundary-hardening) | 架构完成 (v0.7.16); v0.7.19 补齐: SA 严格模式, 递归防护, controller 精化 |

## v0.7.20 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `074` | Subagent Permission Boundary Hardening | `v0.7.20` (unreleased) | [v0.7.20](features/v0.7.20.md#feature_074-subagent-permission-boundary-hardening) | — |
| `072` | Lineage-Native Compaction Migration | `v0.7.20` (unreleased) | [v0.7.20](features/v0.7.20.md#feature_072-lineage-native-compaction-migration) | — |

## v0.7.21 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `077` | Session-Scoped Prompt Input History | `v0.7.21` | [v0.7.21](features/v0.7.21.md#feature_077-session-scoped-prompt-input-history) | — |

## v0.7.22 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `079` | Task Engine Phase 1 — Pure Extraction | `v0.7.22` (unreleased) | [v0.7.22](features/v0.7.22.md#feature_079-task-engine-phase-1--pure-extraction) | — |

## v0.7.23 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `080` | Layer A Primitives — Agent / Handoff / Runner / Guardrail | `v0.7.23` | [v0.7.23](features/v0.7.23.md#feature_080-layer-a-primitives--agent--handoff--runner--guardrail) | — |
| `081` | Compaction Layering and Session Base/Lineage Split | `v0.7.23` | [v0.7.23](features/v0.7.23.md#feature_081-compaction-layering-and-session-baselineage-split) | — |

## v0.7.24 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `082` | Package Restructure — @kodax/core, @kodax/mcp, @kodax/tracing, @kodax/session-lineage | `v0.7.24` | [v0.7.24](features/v0.7.24.md#feature_082-package-restructure) | — |
| `083` | Unified Tracer, Span, and TracingProcessor | `v0.7.24` | [v0.7.24](features/v0.7.24.md#feature_083-unified-tracer-span-and-tracingprocessor) | — |

## v0.7.25 (4 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `051` | Host-Aware Fullscreen TUI Substrate and Transcript UX | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_051-host-aware-fullscreen-tui-substrate-and-transcript-ux) | — |
| `058` | Transcript Native Scrollback Dump | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_058-transcript-native-scrollback-dump) | — |
| `075` | Plan Approval Dialog Scroll | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_075-plan-approval-dialog-scroll) | — |
| `076` | Managed Task Round Boundary — User Conversation Preservation | `v0.7.25` | [v0.7.25](features/v0.7.25.md#feature_076-managed-task-round-boundary--user-conversation-preservation) | — |

## v0.7.26 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `084` | Task Engine Phase 2 — Rewrite Scout/Generator/Evaluator on Layer A Primitives (absorbs FEATURE_059) | `v0.7.26` | [v0.7.26](features/v0.7.26.md#feature_084-task-engine-phase-2--rewrite-scoutgeneratorevaluator-on-layer-a-primitives) | — |
| `085` | Guardrail Tri-Layer — Input / Output / Tool | `v0.7.26` | [v0.7.26](features/v0.7.26.md#feature_085-guardrail-tri-layer--input--output--tool) | — |

## v0.7.27 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `086` | KodaX Prefix Cleanup and Legacy Purge (子任务 A 放弃；子任务 B 完成；含 AMA runner-driven repo-intel 注入恢复 / Windows .exe bin quoting 修复 / ensurePremiumDaemonReady / refresh:true 分层超时 / 临时性失败不污染 failure cache / Ink TUI RepoIntel trace surface / Planner/Evaluator/H1-readonly-Generator 开放 repo-intel deep-capsule 工具) | `v0.7.27` | [v0.7.27](features/v0.7.27.md#feature_086-legacy-code-purge-子任务-a-放弃--子任务-b-完成) | — |
| `091` | Repo-Intelligence Protocol Package Extraction | `v0.7.27` | [v0.7.27](features/v0.7.27.md#feature_091-repo-intelligence-protocol-package-extraction) | — |

## v0.7.28 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `099` | Provider Catalog Refresh — DeepSeek V4 + Kimi-code Label Collapse | `v0.7.28` (unreleased) | [v0.7.28](features/v0.7.28.md#feature_099-provider-catalog-refresh--deepseek-v4--kimi-code-label-collapse) | — |
| `098` | Per-Model Context Window & Output Token Limits | `v0.7.28` (unreleased) | [v0.7.28](features/v0.7.28.md#feature_098-per-model-context-window--output-token-limits) | — |

## v0.7.30 (8 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `023` | Dual-Mode Terminal UX | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_023-dual-mode-terminal-ux) | — |
| `031` | Multimodal Artifact Inputs | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_031-multimodal-artifact-inputs) | — |
| `042` | Incremental Repository Intelligence Refresh and Java/C++ Structural Semantics | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_042-incremental-repository-intelligence-refresh-and-javac-structural-semantics) | — |
| `053` | Canonical Repo Identity and Managed Worktree Runtime | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_053-canonical-repo-identity-and-managed-worktree-runtime) | — |
| `056` | Tool Interaction Maturity and Transcript-Native Explanation Layer | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_056-tool-interaction-maturity-and-transcript-native-explanation-layer) | — |
| `054` | AMA-Project Convergence: Absorb Project Mode into Adaptive H2 | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_054-ama-project-convergence-absorb-project-mode-into-adaptive-h2) | — |
| `043` | Harness Calibration, Pivoting, Profiling, and Safe Checkpoints | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_043-harness-calibration-pivoting-profiling-and-safe-checkpoints) | — |
| `038` | Official Sandbox Extension | `v0.7.30` (unreleased) | [v0.7.30](features/v0.7.30.md#feature_038-official-sandbox-extension) | — |

## v0.7.32 (2 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `090` | Self-Construction Tier 4 — Agent Self-Modifying Role Spec | `v0.7.32` | [v0.7.32](features/v0.7.32.md#feature_090-self-construction-tier-4--agent-self-modifying-role-spec) | — |
| `107` | AMA H2 Plan-Execute Boundary Eval | `v0.7.32` | [v0.7.32](features/v0.7.32.md#feature_107-ama-h2-plan-execute-boundary-eval) | — |

## v0.7.33 (1 feature)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `092` | Auto Mode Classifier — LLM-Reviewed Permission Tier | `v0.7.33` (unreleased) | [v0.7.33](features/v0.7.33.md#feature_092-auto-mode-classifier--llm-reviewed-permission-tier-for-high-risk-tool-calls) | — |

## v0.7.34 (3 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `110` | Remove Legacy Plan Mode (Path 1) | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_110-remove-legacy-plan-mode-path-1) | — |
| `112` | Investigation-Scale-Aware Routing — Read-Scope Fix | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_112-investigation-scale-aware-routing--read-scope-fix) | — |
| `097` | AMA Runner Realtime Todo List — Claude-Aligned Visibility Surface | `v0.7.34` | [v0.7.34](features/v0.7.34.md#feature_097-ama-runner-realtime-todo-list--claude-aligned-visibility-surface) | — |

## v0.7.40 (5 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `121` | Envelope Spillover Gap-Fix — Child Task Summary 接入 Tool Result Policy (+ LLM Blob Summarizer Fallback) | `v0.7.40` | [v0.7.40](features/v0.7.40.md#feature_121-envelope-spillover-gap-fix--child-task-summary-接入-tool-result-policy) | — |
| `134` | Image / Screenshot Paste Input — REPL Vision Bridge (5 paste sources + jimp normalize + vision flag widened to 11 providers) | `v0.7.40` | [v0.7.40](features/v0.7.40.md#feature_134-image--screenshot-paste-input--repl-vision-bridge) | — |
| `159` | MessageQueue as Single Source of Truth + Idle-Yield Mode-Split — claudecode-parity observable substrate, queue→React mirror, prompt vs synthetic banner split | `v0.7.40` | [v0.7.40](features/v0.7.40.md#feature_159-messagequeue-as-single-source-of-truth--idle-yield-mode-split) | — |
| `168` | AMA Agent Tool Wiring — exclude-based wiring with registry as source of truth; recovers 17 silently-dropped tools (FEATURE_120 `send_message`/`task_stop`, FEATURE_161 4× deep-capsule pull tools, 4× web tools, +4 others); architectural Evaluator security boundary | `v0.7.40` (hotfix) | [v0.7.40](features/v0.7.40.md#feature_168--ama-agent-tool-wiringexclude-based-registry-as-source-of-truth) | — |
| `169` | Pull-Tool Prompt Adoption Hardening — Worker dispatch anti hand-feed bash + recommend pull-tool family, child agent pull-tools reverse-steering, change-review reframe (F0a / F0b / F1v2 / F3; F2 3-tier order rejected post-eval) | `v0.7.40` (hotfix) | [v0.7.40](features/v0.7.40.md#feature_169--pull-tool-prompt-adoption-hardening-worker-dispatch-objective--child-reverse-steering--change-review-reframe) | — |

## v0.7.41 (8 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `165` | Worker `emit_handoff` pending-children gate — runtime gate blocks handoff while child registry non-empty; Worker prompt explicitly steers to `task_stop`-or-wait | `v0.7.41` (hotfix) | [v0.7.41](features/v0.7.41.md#feature_165--worker-emit_handoff-pending-children-gatev0741-hotfix) | — |
| `166` | Post-handoff role label flip — REPL surface flips `[Worker]` → `[Evaluator]` immediately on `agentSwitched` event (was lagged a turn) | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_166--post-handoff-role-label-flipshipped) | — |
| `167` | Evaluator terminal-verdict 兜底 — parser fallback + retry gate + synthetic verdict when Evaluator exits text-only without calling `emit_verdict` (commit `d537c784` 2026-05-15, 3-layer probe-gated defense, 29 tests + audit DATA VALID) | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_167--evaluator-terminal-verdict-兜底shipped) | — |
| `171` | `runner-driven.ts` Modular Split — 12 submodule extraction (R1 commit `2fef1c31` 4 leaf + types.ts / R2 `f0be2d4e` 4 mid-coupling / R3 `bfb2b818` agent-chain + llm-adapter / R4 `62dc1c58` payload-builder + checkpoint-flow); 6406 → 1897 lines (-70.4%); zero behavior change; 4 reviewer APPROVE rounds; 4314/4314 tests pass each commit; ADR-026 + HLD.md §3.5.1 documented | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_171--runner-drivents-modular-splitshipped) | — |
| `125` | KodaX Team Mode — Multi-Instance Auto Coordination (LLM-First Awareness + Content Hash Safety Net): 5-layer architecture (S1 per-instance state writer / S2 sibling discovery + stale reap / S3 system-prompt formatter / S4 content-hash cache / S5 active-file warning) + full tool wiring W1-W4 (Read+Edit+Write+MultiEdit instrumented; both legacy + Ink REPL bootstrap Team Mode); S7 Layer 2 panel SHIP after audit-corrected regex (case 1 84% / case 2 60% overall primary) | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_125-kodax-team-mode--multi-instance-auto-coordination) | — |
| `164` | Mid-turn user-input injection — queued real-user messages prepended as proper non-synthetic user-bubble messages within the same round, closing the v0.7.40 idle-yield "queued prompt looks dropped" gap (shipped in same commit `0ebeb15f` as FEATURE_165 + FEATURE_166) | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_164--mid-turn-user-input-injection) | — |
| `170` | Todo V2 Migration — Per-Task CRUD + Extension Hooks: new `todo_create` tool + `todo_update` extended with patch fields + `status:'deleted'` per-item delete + `ExtensionHookMap`/`ExtensionEventMap` events (`todo:created` / `todo:updated` / `todo:deleted` / `todo:before-create` / `todo:before-complete`); `op:'init'` retained for fan-out batch (FEATURE_151 v0.7.38 contract); Layer 2 LLM-judge eval Layer A + Layer B 3.2% disagreement → DATA VALID, gate (a)+(b) MET, (c) saturation artifact (C2 baseline 96% / C3 100% mathematically unable to add +20pp) | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_170--todo-v2-migration-per-task-crud--extension-hookssshipped-2026-05-16) | — |
| `172` | REPL Render Path Optimization (Phase 1 + Phase A.0/A.1) — Phase 1 data-layer 5-commit chain (`19c6aff3` → `26d47084`): transcript-layout static/dynamic split + useMemo cache-key invariant + `React.memo` comparator; **800-item streaming-tick p95 94.18ms → 0.52ms (data layer)**. Phase 1 ship review revealed bench scope error (only measured 3-5% inner function, real ~80% in `tui/substrate/ink/`); 3 parallel Explore-agent traces + claudecode comparison → ADR-027 corrected + ADR-028 added documenting full claudecode port plan. Phase A.0 `KODAX_RENDER_TRACE` env-gated per-frame trace (`5ca91970` + `54a59caa`) + end-to-end bench scaffold (`dae85141`/`99e7f2af`) at user's real SSH viewport (148×43). Phase A.1 `ScreenBuilder` (`25bf0f52`) eliminates `setCellAt` O(N²) `cells.slice()` → **renderer p95 14.804ms → 3.095ms (-79%, 4.78× speedup)** on `mainscreen-windowed-800`. Phase A.2-E deferred pending user SSH measurement after A.1 ship. First-round TTFB compression (`e8b336ed`) lands alongside: middleware refresh:false + parallel pre-LLM + REPL-mount prewarm + multi-tier cache stack (P2/P3/P3+), ~24s → ~10-15s. | `v0.7.41` | [v0.7.41](features/v0.7.41.md#feature_172--repl-render-path-数据层修复phase-1-shipped-底层-perf-portphase-a-进行中) | — |

## v0.8.0 (5 features)

| ID | Title | Released | Design | Notes |
|---|---|---|---|---|
| `044` | Durable Compression Anchors and Artifact Recall | `v0.8.0` (unreleased) | [v0.8.0](features/v0.9.5.md#feature_044-durable-compression-anchors-and-artifact-recall) | — |
| `048` | Sectionized Prompt Assembly and Dynamic Capability Truth | `v0.8.0` (unreleased) | [v0.8.0](features/v0.9.5.md#feature_048-sectionized-prompt-assembly-and-dynamic-capability-truth) | — |
| `049` | First-Class Search, Fetch, Code Search, and Semantic Retrieval | `v0.8.0` (unreleased) | [v0.8.0](features/v0.9.5.md#feature_049-first-class-search-fetch-code-search-and-semantic-retrieval) | — |
| `050` | Prompt Contracts, Snapshots, and Regression Evaluation | `v0.8.0` (unreleased) | [v0.8.0](features/v0.9.5.md#feature_050-prompt-contracts-snapshots-and-regression-evaluation) | — |
| `055` | REPL Substrate Hardening and Summary-Only AMA UX | `v0.8.0` (unreleased) | [v0.8.0](features/v0.9.5.md#feature_055-repl-substrate-hardening-and-summary-only-ama-ux) | — |

---

## 2026-06-13 Tracker Cleanup (93 entries)

These rows were removed from `FEATURE_LIST.md` because they are released, cancelled, absorbed, or explicitly shelved. Long rationale remains in the version design docs and git history.

| ID | Title | Final status | Version / slot | Source |
|---|---|---|---|---|
| `057` | Claude-Aligned TUI Substrate Refactor | Completed / Released | `v0.7.30` | [v0.7.30](features/v0.7.30.md) |
| `059` | Managed Task Structured Protocol V2 | Absorbed | `v0.8.0` | [v0.8.0](features/v0.9.5.md) |
| `060` | Claude-Aligned Bounded-Memory Runtime and OOM Hardening | Completed / Released | `v0.7.30` | [v0.7.30](features/v0.7.30.md) |
| `063` | Extensible Hook & Automation Substrate | Cancelled | `v0.7.18` | [v0.7.18](features/v0.7.18.md) |
| `073` | Reference-Style Lineage and Island Model Removal | Cancelled | `v0.7.25` | [v0.7.25](features/v0.7.25.md) |
| `078` | Role-Aware Reasoning Profiles | Completed / Released | `v0.7.29` | [v0.7.29](features/v0.7.29.md) |
| `087` | ConstructionRuntime and Constructed-World Substrate | Completed / Released | `v0.7.28` | [v0.7.28](features/v0.7.28.md) |
| `088` | Self-Construction Tier 2 - Tool Generation | Completed / Released | `v0.7.28` | [v0.7.28](features/v0.7.28.md) |
| `089` | Self-Construction Tier 3 - Agent Generation | Completed / Released | `v0.7.31` | [v0.7.31](features/v0.7.31.md) |
| `094` | Deep Anti-Escape Hardening | Cancelled | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `095` | SSH-Friendly Cell-Level Diff Renderer for Ink Substrate | Absorbed | `v0.7.39` | [v0.7.30](features/v0.7.30.md) |
| `096` | Windows-SSH ConPTY Host Auto-Downgrade to Main-Screen Policy | Completed / Released | `v0.7.30` | [v0.7.30](features/v0.7.30.md) |
| `100` | SA Runner Frame Adoption & Capability Unification | Completed / Released | `v0.7.29` | [v0.7.29](features/v0.7.29.md) |
| `101` | Constructed Agent Admission Contract | Completed / Released | `v0.7.31` | [v0.7.31](features/v0.7.31.md) |
| `102` | Adaptive Multi-Provider Orchestration Runtime | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `103` | Scout Calibration & L5 User-Followup Escalate | Completed / Released | `v0.7.29` | [v0.7.29](features/v0.7.29.md) |
| `104` | Prompt-Eval Harness Module | Completed / Released | `v0.7.29` | [v0.7.29](features/v0.7.29.md) |
| `106` | AMA Harness Selection Calibration | Completed / Released | `v0.7.31` | [v0.7.31](features/v0.7.31.md) |
| `109` | Harness Observability Substrate RFC | Cancelled | `v0.7.48` | [v0.7.48](features/v0.7.48.md) |
| `111` | Runner-Level Soft-Pause Protocol | Absorbed | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `114` | AMA Harness V2 - Worker + Evaluator | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `115` | Message Queue Foundation | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `116` | Active Cache Control Foundation | Completed / Released | `v0.7.37` | [v0.7.37](features/v0.7.37.md) |
| `117` | Mutation Context Injection | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `119` | Async Child Dispatch - Pattern B | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `120` | Async Child Steering | Completed / Released | `v0.7.39` | [v0.7.39](features/v0.7.39.md) |
| `122` | PM Thinking Framework Prompt | Cancelled | `v0.7.41` | [v0.7.41](features/v0.7.41.md) |
| `123` | Peer-to-Peer SendMessage | Completed / Released | `v0.7.44` | [v0.7.44](features/v0.7.44.md) |
| `124` | Memory System Alignment | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `126` | PM Mode Tool Restriction | Cancelled | none; original `v0.8.x` placeholder | Original row depended on cancelled `FEATURE_122` production data |
| `127` | Per-Agent Model Tier Routing | Cancelled | none | Re-scoped into `FEATURE_120` and `FEATURE_102` |
| `128` | Dispatch-to-Self-Construction Bridge | Superseded | `v0.7.50` | Superseded by `FEATURE_191` |
| `130` | Provider Retry-After Header + Exponential Backoff | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `131` | Tool Safety Patch Pack | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `132` | LSP Native Integration | Completed / Released | `v0.7.47` | [v0.7.47](features/v0.7.47.md) |
| `137` | RPC Headless Protocol | Absorbed | `v0.8.0` | Merged into `FEATURE_030`; JSON-Line stdin/stdout bridge details moved to [v0.8.0](features/v0.9.5.md#feature_030-multi-surface-delivery) |
| `141` | Transcript Inline Diff Renderer | Completed / Released | `v0.7.37` | [v0.7.37](features/v0.7.37.md) |
| `142` | Package Boundary Cleanup | Completed / Released | `v0.7.35.1` | [v0.7.35](features/v0.7.35.md) |
| `143` | Skill UX Upgrade + Prompt Overlay Fix | Completed / Released | `v0.7.36` | [v0.7.36](features/v0.7.36.md) |
| `144` | AMA Worker Capability Context Parity | Completed / Released | `v0.7.35.1` | [v0.7.35](features/v0.7.35.md) |
| `145` | Agent Config Home Resolution Pipeline | Completed / Released | `v0.7.35.1` | [v0.7.35](features/v0.7.35.md) |
| `146` | v0.7.36 Behavioral Eval Follow-ups | Completed / Released | `v0.7.37` | [v0.7.37](features/v0.7.37.md) |
| `147` | npm Publishing Pipeline | Completed / Released | `v0.7.37` | [v0.7.37](features/v0.7.37.md) |
| `148` | Pattern B Post-Dispatch Probe | Completed / Released | `v0.7.37` | [v0.7.37](features/v0.7.37.md) |
| `149` | Queued Prompt Injection Latency & Mid-Turn UX Parity | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `151` | TodoList Visibility & LLM Self-Seeding Parity | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `152` | Bash Regex to AST Migration | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `153` | LLM-Backed Bash Prefix Extractor | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `154` | Universal `--help` Fast-Path | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `155` | Chat-While-Waiting | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `156` | Idle-Wait Visual Continuity | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `157` | Windows / macOS Case-Insensitive Workspace Match | Completed / Released | `v0.7.38` | [v0.7.38](features/v0.7.38.md) |
| `158` | auto[llm] Signal-Based Classifier | Completed / Released | `v0.7.39` | [v0.7.39](features/v0.7.39.md) |
| `173` | Session Management Public SDK | Completed / Released | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `175` | Plan-List Resilience | Completed / Released | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `184` | Sidecar Verifier Substrate | Completed / Released | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `186` | SDK Embedder Surface Closure | Completed / Released | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `187` | Stall Sidecar Middleware Unification | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `188` | dispatch_child Worktree Drop | Completed / Released | `v0.7.42` | [v0.7.42](features/v0.7.42.md) |
| `189` | Systemic Prompt Audit | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `190` | FEATURE_184 Cleanup Tail | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `191` | User-Authored Custom Agents | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `192` | User-Set Persistent Goal | Completed / Released | `v0.7.44` | [v0.7.44](features/v0.7.44.md) |
| `193` | V1 Chain Full Retirement | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `194` | Package Consolidation | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `195` | Sidecar Verifier UI Silent Accept | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `196` | Sidecar Verifier Content-Aware Gate | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `197` | Read-Only Markdown Agent Discovery | Completed / Released | `v0.7.43` | [v0.7.43](features/v0.7.43.md) |
| `198` | Provider Capability Metadata | Completed / Released | `v0.7.44` | [v0.7.44](features/v0.7.44.md) |
| `199` | Sibling-Aware Child Dispatch | Completed / Released | `v0.7.44` | [v0.7.44](features/v0.7.44.md) |
| `200` | Structural Split of Top-10 Large Source Files | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `201` | Streaming Render Upgrade | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `202` | Reasoning to Live Status Header Extraction | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `203` | Overlay Deferral Queue | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `204` | `kodax doctor` Diagnostic CLI | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `205` | Cyclic Dependencies Pull-Tool | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `206` | `/review` Slash Command | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `207` | `@` Picker Extension | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `208` | Process Hardening | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `209` | Tracing Package Activation | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `210` | Daemon / TUI Split | Shelved | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `212` | REPL Fullscreen Render Perf | Completed / Released | `v0.7.45` | [CHANGELOG.md](../CHANGELOG.md) |
| `213` | Idle-Yield Queued-Prompt UI Fix | Completed / Released | `v0.7.45` | [CHANGELOG.md](../CHANGELOG.md) |
| `214` | Inline Viewport / Codex-Style True Inline | Completed / Released | `v0.7.46` | [v0.7.46](features/v0.7.46.md) |
| `215` | Generic LLM-Judged Stop-Hook Primitive | Completed / Released | `v0.7.49` | [v0.7.49](features/v0.7.49.md) |
| `216` | Provider Credential Verification API | Completed / Released | `v0.7.45` | [v0.7.45](features/v0.7.45.md) |
| `217` | Dynamic Workflow Harness Runtime | Completed / Released | `v0.7.49` | [v0.7.49](features/v0.7.49.md) |
| `218` | KodaX Self-Knowledge Manual & Help Tool | Completed / Released | `v0.7.47` | [v0.7.47](features/v0.7.47.md) |
| `219` | Per-Project Session Storage | Completed / Released | `v0.7.46` | [v0.7.46](features/v0.7.46.md) |
| `220` | Continuous Thinking + Tool-Call Block Rendering | Completed / Released | `v0.7.47` | [v0.7.47](features/v0.7.47.md) |
| `221` | Injectable Self-Manual for SDK Consumers | Completed / Released | `v0.7.47` | [v0.7.47](features/v0.7.47.md) |
| `222` | MCP 2025-11-25 Reverse Capabilities | Completed / Released | `v0.7.48` | [v0.7.48](features/v0.7.48.md) |
| `223` | Sidecar Verifier Custom Provider Stall Fix | Completed / Released | `v0.7.49` | [v0.7.49](features/v0.7.49.md) |
| `226` | Session Custom Tag + List Filtering | Completed / Released | `v0.7.49` | [v0.7.49](features/v0.7.49.md) |

---

## Summary

- Initial archive: 99 features moved on 2026-05-19.
- Tracker cleanup: 93 stale live-roadmap entries moved on 2026-06-13.
- Current live roadmap: `docs/FEATURE_LIST.md` tracks 12 active features.

## v0.9.5 archived staging reference (displaced 2026-08-08)

The v0.9.5.md filename was repurposed for the v0.8.0/v0.8.5 vision document (F007/030/093) sliding from v0.8.5 to v0.9.5. Previous archived-staging content preserved verbatim below. F055 also remains in the new v0.9.5.md via the vision doc; F031/042/043/053 active designs live in v0.7.30.md.

# v0.9.5 Feature 设计

> **Archived (2026-04-19)**:
> This version milestone has been compressed into [v0.8.0](./v0.9.5.md).
> `FEATURE_055` (the only remaining feature here) has moved to [v0.8.0: FEATURE_055](./v0.9.5.md#feature_055-repl-substrate-hardening-and-summary-only-ama-ux).
> Features `031 / 042 / 043 / 053` were previously moved to [v0.7.30](./v0.7.30.md).
> This document is retained as a historical staging reference only and should no longer be treated as an active roadmap target.

> Version: `v0.9.5`
>
> Status: ~~Planned~~ Archived — merged into `v0.8.0`
>
> Theme: ~~Multimodal inputs, worktree-safe runtime, harness maturation, and repo-intelligence evolution~~
>
> **2026-06-18 版本后移**：本 feature 版本文件从 `v0.9.0` 统一后移到 `v0.9.5`（`v0.7.52+` 计划组 +5）。下方历史重排 / `Rescheduled From` 叙述保留原始语义；以本文件标题、`Version`、`Target Version` 为当前落点。

> **2026-07-12 clarification**: the old roadmap content remains archived here,
> but the now-empty `v0.9.0` version number has been reused for the independent
> [FEATURE_262 npm supply-chain security plan](./v0.9.0.md). None of the features
> archived or moved by this document return to `v0.9.0`.

> Migration note (2026-04-04):
> `FEATURE_031 / 042 / 043 / 053` are now planned in [v0.7.30](./v0.7.30.md).
> `FEATURE_055` is now in [v0.8.0](./v0.9.5.md#feature_055-repl-substrate-hardening-and-summary-only-ama-ux).
> At the time of this migration, `v0.9.0` no longer contained any active
> features. It was independently reactivated for `FEATURE_262` on 2026-07-12.
## 中文导读

`v0.9.0` 主要关注 multimodal artifact inputs。

它的核心意思是：KodaX 不再只接收文本，还要能把 image、screenshot、diagram 之类的 artifact 作为正式输入，接进 task engine，并参与 planning、generation、evaluation。

---

补充说明：

- `FEATURE_031` 负责把 image / screenshot / diagram 这类 artifact 真正接入 task engine
- `FEATURE_042` 继续推进 repo-intelligence 的增量刷新与 Java/C++ 结构语义
- `FEATURE_053` 负责把 canonical repo identity、worktree runtime、session resume/list 与 CLI/REPL/ACP 的 cwd 语义补齐
- `FEATURE_043` 承接 adaptive project / AMA harness 的 Phase 2 成熟化，但它消费 `FEATURE_053` 提供的 workspace isolation / checkpoint 边界，而不自己发明 worktree runtime

所以 `v0.9.0` 不再只是“多模态输入版本”，它同时也是 repo/worktree runtime 补完与 harness 第二阶段成熟化的版本。

## FEATURE_055: REPL Substrate Hardening and Summary-Only AMA UX

**Status**: Completed  
**Close-out note (2026-04-04)**: Implemented as a docs-first substrate hardening pass. The visible REPL shell remains frozen while transcript/selection helpers, input blur-paste closure, and AMA summary-only derivation were tightened without adding new multi-agent surface.
**Priority**: High
**Category**: Enhancement

### Overview

`FEATURE_055` is the follow-up feature for the remaining REPL hardening work that should no longer live under completed `FEATURE_051`.

The visible KodaX REPL shell is already at the right product tier:

- transcript-first
- one main authority
- restrained footer/task/status shell
- no visible worker topology

The remaining gap is invisible substrate maturity:

- transcript / viewport / scroll closure
- host-aware selection / copy policy
- input focus / paste / cursor behavior
- summary-only AMA runtime mapping into the existing REPL shell

### Goals

1. Keep the current REPL visible shell frozen while continuing to harden transcript, scroll, selection, and input behavior.
2. Make terminal host capability truth fully drive selection / copy / browse behavior.
3. Keep AMA Tactical / AMA Managed / child fan-out summary-only inside the existing shell.
4. Preserve `SA` / `AMA` as the only user-visible execution semantics.

### Scope

- transcript owner / viewport owner refinement
- host-aware scroll / selection / copy-on-select closure
- input focus / paste / cursor substrate hardening
- summary-only AMA status derivation for existing REPL surfaces
- feature-doc alignment for `051/055/046/047/052/054`

### Non-goals

- new transcript chrome, task cockpit, or multi-panel REPL surface
- visible worker graph, teammate panel, task workspace, or revived team mode
- new top-level user-visible modes beyond `SA` / `AMA`
- re-opening `FEATURE_051` as an active visible-shell rollout

### REPL posture

`FEATURE_055` follows the KodaX shell-freeze rule:

- `StatusBar`, footer, task bar, help, message actions, and tool rows remain visually frozen
- transcript stays the only main screen
- follow-up work should improve hand feel and stability, not add more visible UI

### AMA boundary

Multi-agent runtime maturity is allowed here only as summary-only REPL mapping:

- `AMA Tactical` and `AMA Managed` remain internal harness profiles
- child fan-out may affect `AmaWorkStrip`, `BackgroundTaskBar`, and transcript live labels
- REPL surfaces do not grow a worker graph, teammate list, or task cockpit

### Relationship to other features

- `FEATURE_051` is the completed visible-shell close-out that hands remaining substrate hardening to `055`
- `FEATURE_046`, `FEATURE_047`, and `FEATURE_052` define the AMA substrate that `055` may summarize in the REPL
- `FEATURE_054` keeps AMA/project convergence on the architecture side without reopening old project-mode UI
- `FEATURE_043` and `FEATURE_053` continue to mature the harness/runtime beneath the same frozen REPL shell

### Acceptance criteria

- [x] transcript / viewport / scroll behavior became more internally coherent without visible shell drift
- [x] host-aware selection / copy policy is capability-driven and no longer scattered across the REPL
- [x] prompt input hardening improves paste / focus / cursor behavior without changing the visible input shell
- [x] AMA tactical / managed / child-fan-out states remain summary-only in the REPL
- [x] feature docs and index stay consistent about `051` being closed out and `055` carrying the remaining hardening work

### Delivered slices

- closed the remaining docs handoff from `FEATURE_051` and aligned `FEATURE_LIST` / `v0.8.0` / `v0.9.0` / feature index guidance
- tightened transcript selection/search derivation into REPL view-model helpers instead of leaving host/capability gating scattered in `InkREPL`
- hardened prompt blur / paste transient cleanup without changing the visible input shell
- kept AMA tactical/managed and child fan-out mapped only through existing summary surfaces

---

## FEATURE_031: Multimodal Artifact Inputs

**Status**: Planned
**Priority**: High
**Category**: New

> Source-of-truth note (2026-04-04):
> The active planned version for `FEATURE_031` is now [v0.7.30](./v0.7.30.md#feature_031-multimodal-artifact-inputs).
> This section remains as historical staging context only.

### Overview

KodaX should be able to accept non-text artifacts such as images and feed them into the task engine in a provider-aware way.

This feature is not only about image upload convenience. It allows the planner, generator, and evaluator to reason over:

- screenshots
- UI mocks
- diagrams
- captured visual errors

### Goals

1. Support local image-path and clipboard-style image inputs.
2. Normalize multimodal inputs into task-engine-friendly content blocks.
3. Route multimodal work only to providers and harnesses that can support it.
4. Preserve artifact references in task state and evidence.

### Product principles

1. transcript-first artifact intake: image and diagram inputs should feel like richer task inputs, not like entering a separate media tool
2. push storage inward: artifact persistence belongs in task/evidence state, not in a new gallery-style surface
3. summary-first output: visual work may enrich transcript, evaluator, and evidence flows, but should not create a persistent design-review product shell

### Scope

- image attachment intake
- provider-aware image serialization
- artifact references in persisted task state
- evaluator access to visual artifacts

### Non-goals

- broad media editing
- video workflows
- full design-review product surface
- gallery / asset browser / persistent media library UX
- a second visual workspace that competes with transcript as the main screen

### Relationship to other features

- `029` provides provider capability truth
- `025` decides whether a task needs multimodal-capable routing
- `018` and `028` may later index or retrieve artifact-related knowledge

### REPL / UX boundary

`FEATURE_031` should map into the existing KodaX shell through transcript, evidence, and summary surfaces only. It should not be used as justification for:

- a gallery panel
- a persistent asset browser
- a dedicated design-review workspace
- a richer visual cockpit than the current transcript-first REPL

### Acceptance criteria

- [ ] KodaX can accept image artifacts from local input paths
- [ ] multimodal inputs are stored as explicit task artifacts
- [ ] provider routing respects multimodal capability constraints
- [ ] visual artifacts can participate in evaluation and evidence flows

### Restored Detailed Design Notes

Useful retained points from the earlier draft:

- support both local image-path references and clipboard-style image input
- normalize images into provider-required formats such as base64 payloads or provider-native artifact references
- add image content-block support at the AI/provider layer for multimodal-capable backends
- ensure artifact serialization remains compatible with history persistence and future compaction rules
- keep size limits, format validation, and sensitive-path boundaries explicit

#### Historical user scenarios retained

- user provides a screenshot so KodaX can inspect an error
- user provides a UI mock so KodaX can generate or critique implementation
- user provides a chart or visual artifact so KodaX can extract, explain, or summarize it

---

---

## FEATURE_042: Incremental Repository Intelligence Refresh and Java/C++ Structural Semantics

**Status**: Planned
**Priority**: High
**Category**: Enhancement

> Source-of-truth note (2026-04-04):
> The active planned version for `FEATURE_042` is now [v0.7.30](./v0.7.30.md#feature_042-incremental-repository-intelligence-refresh-and-javac-structural-semantics).
> This section remains as historical staging context only.

### Overview

`v0.8.x` establishes task-aware repo intelligence, and Go/Rust can now follow the same bundled-parser path as Python. The next practical gaps are:

- refresh still rebuilds more of the repository-intelligence index than necessary
- Java and C++ still rely on heuristic structural parsing rather than bundled parser-backed extraction

`FEATURE_042` addresses those two gaps together. The goal is a faster and more balanced repository-intelligence substrate without expanding KodaX into a graph product.

This feature is explicitly about **quality and efficiency maturation**, not a new product surface. KodaX should keep the same repo-intelligence entry points while making refresh cheaper and Java/C++ evidence more trustworthy.

### Goals

1. Add file/module-scoped invalidation so intelligence refresh can reuse unchanged work.
2. Upgrade Java/C++ from heuristic structural parsing to bundled structural parsing where practical.
3. Improve confidence stability and token efficiency for large or mixed-language repositories.
4. Keep the public retrieval surface unchanged while strengthening the underlying evidence.

### Product principles

1. Keep the external surface minimal: `repo_overview`, `module_context`, `symbol_context`, `process_context`, `impact_estimate`, and `changed_scope` remain the only first-class repo-intelligence interfaces.
2. Push complexity inward: finer-grained invalidation and parser-backed analyzers stay internal implementation details.
3. Prefer deterministic local analysis over expensive model calls.
4. Fall back safely: when incremental state is invalid, incomplete, or low-confidence, KodaX must transparently rebuild more broadly rather than serving misleading intelligence.
5. Maintain inspectable artifacts under `.agent/repo-intelligence/` instead of introducing graph databases or opaque background services.

### UI boundary

The output of `FEATURE_042` should continue to flow through existing transcript/evidence/summary surfaces. It should not be used to justify:

- a repo graph UI
- a repository workbench
- a dedicated intelligence dashboard
- a second navigation surface that competes with transcript

### Scope

- file-hash and module-hash invalidation metadata
- partial rebuild for changed files/modules/capsules
- bundled structural parsing for Java/C++
- tighter confidence calibration for Java/C++ retrieval results

### Deliverables

#### 042-A Incremental refresh engine

- extend the repo-intelligence manifest with per-file fingerprints, language, module ownership, and analyzer version metadata
- persist file-level analysis outputs so unchanged files can be reused between refreshes
- add an invalidation planner that decides among:
  - file-only reanalysis
  - module capsule rebuild
  - process capsule rebuild
  - full index rebuild
- keep fallback-to-full-rebuild as a first-class safety path when the invalidation planner is uncertain

#### 042-B Java/C++ parser-backed structural semantics

- replace the current line-regex-first extraction path for Java/C++ with bundled parser-backed structural extraction
- extract classes/interfaces/structs/enums, methods/functions, imports/includes, and qualified call names
- improve method ownership so retrieval surfaces can correctly attribute `Class.method` and `Namespace::Class::method` style symbols
- improve confidence scoring so Java/C++ repositories do not look artificially equivalent to TS/JS/Python, but also no longer collapse to low-confidence heuristic-only behavior

### Detailed design

#### Refresh pipeline

The refresh path should move from today's coarse "index is stale, rebuild almost everything" model to a staged planner:

1. Read the manifest and compare schema version, analyzer versions, and source fingerprints.
2. Compute changed files and deleted files.
3. Re-run analyzers only for changed files.
4. Rebuild module capsules only for modules that own changed files or whose dependency edges changed.
5. Rebuild process capsules only for modules whose entry symbols, dependencies, or first-hop calls changed.
6. Re-link call targets globally only for affected symbol names/modules; if the affected set is too large or ambiguous, fall back to a full re-link.
7. Persist the updated manifest and materialized indexes atomically.

#### Incremental artifacts

`FEATURE_042` should add or formalize the following internal artifacts:

- `repo-intelligence-manifest.json`
  - schema version
  - analyzer versions
  - workspace source fingerprint
  - per-file fingerprints
  - file -> module ownership
  - file -> language
- `file-analysis-index.json`
  - persisted file-level imports/symbols/calls/confidence
- existing module/symbol/process indexes remain the query-facing materialized view

#### Dirty snapshot strategy

The dirty-refresh path should stay intentionally simple:

- use a single clean git baseline as the only long-lived source of truth
- allow current dirty snapshots to be memoized for direct reuse when the same dirty state repeats
- share one resolved repo snapshot across overview, changed-scope, routing, and impact flows within a request
- treat truncated large-repo baselines conservatively: pure edits to baseline-known files may use the fast path, while deletes, renames, or baseline-external additions fall back to a full rebuild

This keeps the implementation aligned with KodaX's minimalist design philosophy: fewer cache layers, fewer hidden states, and explicit fallback to correctness when the dirty state becomes ambiguous.

#### Fallback policy

Incremental refresh is an optimization, not a correctness boundary. KodaX should rebuild more broadly when:

- the manifest is missing or schema-incompatible
- a parser upgrade changes analyzer semantics
- deleted or renamed files break module ownership assumptions
- dependency edges widen beyond the incremental planner's safe threshold
- the planner cannot prove that process capsules can be updated locally

#### Java/C++ semantic target

The target for `0.9.0` is **parser-backed structural semantics**, not full multi-language type resolution. That means:

- Java:
  - packages/imports
  - classes/interfaces/enums
  - methods/constructors
  - method-qualified calls where recoverable
- C++:
  - includes
  - namespaces/classes/structs/enums
  - out-of-line member definitions
  - qualified calls and method ownership where recoverable

Non-goal for `0.9.0`:

- full Java symbol resolution across inheritance and overload sets
- full C++ template, macro, and header/translation-unit parity

### Query-layer impact

`FEATURE_042` must not add new user-facing tools. Instead it should make existing results better:

- `module_context` should produce more stable dependency/dependent sets after small diffs
- `symbol_context` should attribute Java/C++ methods more accurately
- `process_context` should preserve unaffected capsules across small edits
- `impact_estimate` should stop over-broadening when only one file or module changed
- `changed_scope` should remain unchanged in interface, but become a stronger incremental invalidation input

### Router and task-engine impact

`025` and the task engine do not need a new public contract, but they should benefit from:

- lower latency for repo-intelligence prefetch on repeated tasks
- more stable `repoSignals.confidence` for Java/C++ repositories
- fewer unnecessary planner/investigation escalations caused by stale or overly broad refreshes

### Non-goals

- graph database or Cypher
- visual graph product surfaces
- full multi-language type-resolution parity with GitNexus
- changing the public retrieval surface away from `repo_overview / module_context / symbol_context / process_context / impact_estimate / changed_scope`
- introducing a repo-intelligence control tower or inspection cockpit

### Relationship to other features

- `018` owns the repository-intelligence substrate and persistence model
- `028` owns the retrieval surface that consumes those artifacts
- `025` benefits when routing can trust faster refreshes and more balanced cross-language confidence

### Acceptance criteria

- [ ] KodaX can refresh repo intelligence without rebuilding every unchanged module
- [ ] Java/C++ use bundled structural parsers for symbol/import/call extraction
- [ ] repo-intelligence confidence becomes more stable across TS/JS/Python/Go/Rust/Java/C++
- [ ] retrieval tools keep the same public surface while returning stronger evidence for Java/C++ repositories
- [ ] repeated repo-intelligence prefetch on small diffs is measurably cheaper than a cold rebuild
- [ ] incremental refresh has a documented and tested fallback path to full rebuild

---

## FEATURE_053: Canonical Repo Identity and Managed Worktree Runtime

**Status**: Planned
**Priority**: High
**Category**: Enhancement

> Source-of-truth note (2026-04-04):
> The active planned version for `FEATURE_053` is now [v0.7.30](./v0.7.30.md#feature_053-canonical-repo-identity-and-managed-worktree-runtime).
> This section remains as historical staging context only.

### Overview

Today KodaX still treats "git repo root", "active workspace root", and "execution cwd" as if they were the same thing.

That is acceptable in a single checkout, but it becomes a correctness bug once Git worktrees enter the picture:

- session list/resume can attach to the wrong checkout
- saved session metadata can be re-bound to the current process cwd
- ACP hosts cannot reliably restore the same workspace semantics as REPL/CLI
- future safe-checkpoint and pivot flows have no stable workspace-isolation boundary

`FEATURE_053` fixes that by introducing a first-class runtime model for:

- canonical repo identity
- current workspace/worktree root
- execution cwd
- optional managed worktree state

The purpose is not merely "support `git worktree add`". The real goal is to make KodaX work correctly across single-checkout, manually created worktree, and KodaX-managed worktree workflows without hiding unsafe assumptions in session or harness code.

### Goals

1. Separate `canonicalGitRoot`, `workspaceRoot`, and `executionCwd` across runtime and session contracts.
2. Make session save/list/resume operate across sibling worktrees of the same repo without silently resuming in the wrong checkout.
3. Support both user-created worktrees and KodaX-managed worktrees with safe keep/remove semantics.
4. Provide a stable workspace-isolation boundary that `FEATURE_043` can consume for safe checkpoint and pivot flows.

### Product principles

1. Detection before orchestration: existing user-created worktrees should already behave correctly before KodaX starts creating any new ones.
2. Separate identity from location: repo identity is not the same thing as the current checkout path or execution cwd.
3. Keep runtime ownership outside the core agent loop: CLI/REPL/ACP manage worktree lifecycle and cwd switching; the core agent consumes resolved context.
4. Fail safe on ambiguity: prefer explicit keep/remove decisions and conservative resume behavior over silent cwd drift or destructive cleanup.
5. Avoid nesting managed worktrees inside the main working tree.
6. Explain only at critical moments: worktree information should be explicit when entering, exiting, resuming, or recovering, but should not become a persistent cockpit.

### Scope

- canonical repo root resolution for main checkouts and worktrees
- runtime/session schema upgrades for repo/workspace/cwd identity
- cross-worktree session discovery, resume, and delete semantics
- summary-first worktree status/enter/exit flows for CLI, REPL, and ACP
- managed worktree lifecycle and cleanup policy
- runtime interface boundary for `FEATURE_043` workspace checkpoints

### Deliverables

#### 053-A Canonical repo and workspace identity

- resolve canonical repo identity from git common-dir semantics rather than assuming the current checkout path is the repo identity
- extend persisted session/runtime metadata with:
  - `canonicalGitRoot`
  - `workspaceRoot`
  - `executionCwd`
  - optional `worktree` descriptor
- restore saved workspace/cwd state on resume instead of recomputing everything from the current process cwd
- keep legacy `gitRoot` compatibility in `0.9.x`, but reinterpret it as the active workspace root rather than the cross-worktree repo identity

#### 053-B Existing worktree detection and attachment

- detect when the current cwd is already inside a user-created worktree
- surface current worktree status in runtime state and session metadata
- allow cross-worktree session listing/resume within the same canonical repo
- never auto-delete or take ownership of user-created worktrees

#### 053-C Managed worktree lifecycle

- add a KodaX-managed worktree flow for explicit isolation when the user or runtime asks for it
- provide user-visible surfaces such as:
  - CLI `--worktree <name>` or equivalent explicit entry
  - REPL `/worktree enter`
  - REPL `/worktree exit keep|remove`
  - REPL `/worktree status`
- carry the same workspace metadata through ACP-hosted sessions instead of collapsing everything to host cwd
- default managed worktree placement to a sibling hidden directory outside the main checkout, for example:
  - `<canonical-parent>/.kodax-worktrees/<repo-name>/<slug>`
- only `remove` KodaX-managed worktrees that were created under this feature's policy

#### 053-D Critical-moment explanation model

Worktree information should be surfaced clearly at the moments where silent ambiguity would harm user trust:

- enter: explain whether KodaX is attaching to an existing worktree or creating a managed one
- status: explain the current workspace path, whether it is managed or detected, and which branch is active
- exit: explain `keep` vs `remove` consequences, including whether files, commits, or runtime bindings will be discarded
- resume: explain whether the saved worktree still exists and what fallback will happen if it does not

This should remain a focused explanation layer, not a persistent worktree dashboard.

#### 053-E Workspace isolation boundary for `FEATURE_043`

- expose a runtime-level checkpoint target abstraction that can refer to:
  - diff/patch snapshots in the current workspace
  - an optional isolated worktree target
- keep pivot/branch orchestration in `FEATURE_043`, not here
- make it possible for future checkpoint flows to reference a safe isolated workspace without destructive reset semantics

### Detailed design

#### Data model

The minimal runtime/session shape should become:

```ts
type WorktreeState = {
  path: string
  managed: boolean
  origin: 'detected' | 'created'
  branch?: string
  name?: string
}

type WorkspaceBinding = {
  canonicalGitRoot?: string
  workspaceRoot?: string
  executionCwd?: string
  worktree?: WorktreeState
}
```

Compatibility note:

- `gitRoot` remains in `0.9.x` so older call sites do not all break at once
- new logic should treat `gitRoot` as the active workspace root
- new cross-worktree semantics should key off `canonicalGitRoot`

#### Session semantics

Session storage must stop assuming "same repo" means "same checkout path".

New behavior:

1. save the resolved `WorkspaceBinding` with every persisted session
2. list sessions by `canonicalGitRoot`, not exact `workspaceRoot`
3. resume into the saved `workspaceRoot` / `executionCwd` when still available
4. if the saved worktree path no longer exists, degrade safely:
   - explain that the original worktree is gone
   - allow resume in the current workspace only as an explicit fallback

This turns worktree support into a correctness improvement for manual workflows even before KodaX exposes managed worktree creation.

#### Runtime surface ownership

Ownership should stay layered:

- core agent/task engine
  - consumes resolved workspace context
  - does not create/remove worktrees directly
- CLI/REPL/ACP runtime
  - resolves canonical repo/worktree state
  - changes cwd/workspace binding
  - manages enter/exit/status lifecycle

This boundary keeps worktree logic from leaking into unrelated planning, evaluation, or tool code.

#### Managed-path policy

Managed worktrees should default to a stable sibling directory outside the active checkout instead of living under the main repo tree.

Why:

- avoids recursive scanning/indexing surprises
- avoids editor confusion from nested repositories inside the primary workspace
- avoids mixing source files and KodaX-managed isolation directories

Future configuration may allow override, but `0.9.0` should start with one conservative default.

#### Safety policy

The safety contract should be explicit:

- `enter` may attach to a detected worktree or create a managed one
- `exit keep` detaches the KodaX session from the worktree but leaves files on disk
- `exit remove` only removes KodaX-managed worktrees created under this feature
- no destructive reset of the user's main workspace is introduced as part of worktree cleanup
- nested worktree creation from inside another managed worktree should resolve against the canonical repo root, not chain indefinitely

### UX boundary

`FEATURE_053` should make runtime/worktree semantics clearer, but it should not become:

- a worktree cockpit
- a persistent worktree list/tree dashboard
- a session/worktree control tower
- a place where internal multi-agent fan-out becomes visible through worktree topology

The user should primarily experience:

- clear enter/exit/resume explanations
- accurate current-workspace status
- safe keep/remove semantics
- minimal but truthful runtime context

### Non-goals

- per-subagent automatic worktree fan-out
- tmux or multi-terminal orchestration
- sparse checkout or partial clone strategy
- full branch-management product UX
- destructive rollback/reset flows in the user's main workspace
- a persistent worktree cockpit or worktree-management dashboard
- exposing internal agent/worktree topology as part of normal REPL usage

### Relationship to other features

- `FEATURE_019` owns session-tree/checkpoint lineage, but not cross-worktree repo identity correctness
- `FEATURE_040` must carry the same workspace-binding semantics into ACP-hosted sessions
- `FEATURE_043` consumes this feature's worktree/checkpoint boundary for safe pivot and checkpoint flows
- `FEATURE_023` and `FEATURE_030` may later surface worktree state better in UI, but they do not own runtime correctness

### Suggested implementation phases

1. Phase 1: detect existing worktrees and persist `WorkspaceBinding` correctly across save/list/resume
2. Phase 2: add managed worktree enter/exit/status surfaces in CLI, REPL, and ACP
3. Phase 3: integrate isolated-workspace targets into `FEATURE_043` safe checkpoint and pivot flows

### Acceptance criteria

- [ ] KodaX can distinguish canonical repo identity from active workspace root and execution cwd
- [ ] sessions from sibling worktrees of the same repo can be listed and resumed without rebinding to the wrong checkout
- [ ] a user-created worktree can be detected and used without KodaX taking ownership of it
- [ ] KodaX can create, enter, exit, keep, and remove KodaX-managed worktrees through explicit runtime surfaces
- [ ] ACP-hosted sessions preserve the same workspace/worktree semantics as CLI/REPL sessions
- [ ] `FEATURE_043` has a documented workspace-isolation boundary it can consume without requiring destructive workspace reset
- [ ] enter/exit/resume flows explain managed vs detected state and keep/remove/fallback semantics clearly enough to avoid user confusion
- [ ] worktree/runtime clarity improves without introducing a persistent cockpit-style product surface

---

## FEATURE_043: Harness Calibration, Pivoting, Profiling, and Safe Checkpoints

**Status**: Planned
**Priority**: High
**Category**: Enhancement

> Source-of-truth note (2026-04-04):
> The active planned version for `FEATURE_043` is now [v0.7.30](./v0.7.30.md#feature_043-harness-calibration-pivoting-profiling-and-safe-checkpoints).
> This section remains as historical staging context only.

### Overview

`FEATURE_043` 承接 `FEATURE_022` 在 runtime 主路径已经落地之后的 Phase 2 能力。

它不是再发明一套新的 harness，而是把现有的 `Adaptive Project Harness + AMA Harness` 继续补成：

- 可校准
- 可 pivot
- 可 profiling
- 可安全 checkpoint

### Goals

1. 让 Evaluator 的质量可以被持续校准，而不只是靠人工感觉。
2. 让 harness 在发现当前方向不对时，可以显式 `pivot` 到新 branch，而不是只能沿原路径 `revise`。
3. 让系统知道“哪些 harness 组件真的带来了 lift”，避免长期只会越堆越重。
4. 让长流程执行在 workspace 层也有安全 checkpoint surface，而不只是 task artifact / continuation。

### Product principles

1. quality-control first: `043` 首先是 harness 的校准、约束与安全恢复层，不是新的 orchestration 工作台
2. summary-only user exposure: profiling、pivot、checkpoint 的状态默认只通过现有 transcript / task / status 摘要表面显露
3. consume existing boundaries: `043` 只消费 `053` 提供的 workspace/worktree isolation，不自己重新发明 runtime/workspace UX
4. constrain complexity: profiling / ablation 的目标是帮助 KodaX 保持极简且智能，而不是给系统添加新的长期可见控制面

### Scope

- evaluator false-pass / false-fail capture
- evaluator regression corpus and calibration workflow
- `pivot` / branch directive and branch-scoped artifacts
- harness profiling and ablation records
- safe patch / diff checkpoints
- checkpoint hooks built on `FEATURE_053`'s worktree/runtime boundary

### UI boundary

`FEATURE_043` 的结果可以影响现有 summary surfaces，但它不应该被实现成：

- profiling dashboard
- pivot control tower
- checkpoint workspace UI
- 新的 branch/task orchestration 面板

用户只需要在必要时知道：

- 系统是否校准过
- 是否发生了 `pivot`
- 是否存在安全 checkpoint
- 当前 harness 负担是否合理

### Deliverables

#### 043-A Evaluator calibration loop

- 记录 false-pass / false-fail case
- 建立 reviewer regression corpus
- 支持按 provider / model 维护 evaluator calibration prompt profile
- 让 QA 质量可以做 regression，而不只是看一次 run 的主观效果

#### 043-B Pivot / branch management

- 在 `accept / revise / blocked` 之外，引入 `pivot`
- 允许 Generator 在 rubric 停滞、趋势变差或实现方向明显不优时切换到新 branch
- 保留 branch-scoped artifact、feedback、scorecard 与 round history

#### 043-C Harness profiling / ablation

- 记录每次 run 中哪些 harness 组件被启用
- 记录这些组件对质量、成本、收敛速度的影响
- 支持按 provider / model / task family 形成 harness preset
- 为后续“最简单可行 harness”策略提供数据依据

#### 043-D Safe workspace checkpoints

- 默认生成非破坏性的 patch / diff snapshot
- 通过 `FEATURE_053` 暴露的 runtime/worktree boundary 接入隔离式 workspace checkpoint
- evaluator fail / pivot / continuation 时，能引用最近的 workspace checkpoint
- 不在用户主工作区执行未经明确策略允许的 destructive reset

### Non-goals

- 新增 profiling / pivot / checkpoint 控制台
- 把 branch/pivot 暴露成用户必须理解的常驻执行语义
- 用更重的可见流程去掩盖 harness 本身的复杂性
- 脱离 `FEATURE_053` 单独发明 workspace isolation 机制

### Relationship to other features

- `FEATURE_022` 负责当前 runtime 主路径和 control plane
- `FEATURE_027` 提供 `SA / AMA` 的用户级显式开关
- `FEATURE_029` 提供 provider capability truth
- `FEATURE_053` 负责 canonical repo identity、worktree runtime 与 workspace isolation；`FEATURE_043` 只消费这条边界
- `FEATURE_031` 与 `FEATURE_042` 的 artifact / intelligence 结果，都会成为 future calibration 与 profiling 的输入面

### Acceptance criteria

- [ ] Evaluator false-pass / false-fail case can be persisted and replayed as a regression corpus
- [ ] harness can emit a first-class `pivot` directive and carry branch-scoped artifacts forward
- [ ] harness profiling can explain which orchestration components were enabled and what cost / quality tradeoff they introduced
- [ ] safe patch / diff checkpoints exist for continuation / pivot / post-failure inspection
- [ ] isolated-workspace checkpoint support can be layered on top of `FEATURE_053` without forcing destructive workspace resets
- [ ] `pivot` / profiling / checkpoint state can be summarized through existing REPL/task surfaces without introducing a new control-tower UI

---

## References

- [ADR](../ADR.md)
- [HLD](../HLD.md)
- [DD](../DD.md)
- [PRD](../PRD.md)


## v0.8.15 archived cancelled-design tombstone (displaced 2026-08-08)

The v0.8.15.md filename was repurposed for FEATURE_225 sliding from v0.7.105 to v0.8.15. Previous cancelled FEATURE_125 tombstone preserved verbatim below; F125 active design lives in v0.7.41.md.

# v0.8.15 Feature Designs

> 中文标题：`v0.8.15 Feature 设计`
>
> Version: `v0.8.15`
>
> **2026-06-18 版本后移**：本 feature 版本文件从 `v0.8.10` 统一后移到 `v0.8.15`（`v0.7.52+` 计划组 +5）。下方历史重排 / `Rescheduled From` 叙述保留原始语义；以本文件标题、`Version`、`Target Version` 为当前落点。
>
> **Status: ❌ Old design Cancelled (2026-05-06) — Re-scoped to v0.7.41 with new LLM-First design**
>
> ## 关键事件序列
>
> 1. **2026-04-XX**：FEATURE_125 原计划在 v0.8.10 落 claude code 风格的 mode-based Team Mode（TeamCreate/Delete + 3 backend tmux/iterm2/in-process + file mailbox + permission sync）
> 2. **2026-05-06**：调研 claude code 源码证实 Team Mode **没有任何冲突解决机制**（默认共享 leader cwd / 无 lock / 无 worktree / 完全靠 LLM 自律），跟"用户自己多开 KodaX session"在冲突维度无区别。**取消 v0.8.10 mode-based 设计**
> 3. **2026-05-06 同日**：用户提议"KodaX 应该自感知多 session + 自动冲突避免"。设计师（我）首版给"runtime hard lock + 30s 用户裁决 dialog"被用户挑战不够极简且智能 —— 违反 KodaX `CLAUDE.md` 的 `Leverage LLM intelligence` 原则
> 4. **2026-05-06 重新设计**：4 层 LLM-First 架构（state broadcast → 注入 system prompt → soft warning → content hash safety net），LLM 看见信息**自决**协调，runtime 仅在 race condition 物理边界兜底
> 5. **决定**：FEATURE_125 复活 + 新设计落 **v0.7.41**（取代 122 PM Framework cancelled 的 slot），不在 v0.8.10
>
> ## 新设计位置
>
> 见 [v0.7.41 FEATURE_125 RFC](v0.7.41.md#feature_125-kodax-team-mode--multi-instance-auto-coordination)。
>
> ## 关键调研事实（保留作历史参考）
>
> claude code Team Mode 不解决冲突的源码证据：
>
> - [`spawnMultiAgent.ts:337`](file:///c:/Works/claudecode/src/tools/shared/spawnMultiAgent.ts#L337)：`const workingDir = cwd \|\| getCwd()` —— 默认所有 teammate 共享 leader 的 cwd
> - [`backends/types.ts:217`](file:///c:/Works/claudecode/src/utils/swarm/backends/types.ts#L217)：`worktreePath?:` optional —— **不自动 worktree 隔离**
> - grep `flock` / `fcntl` / `exclusive` / `mutation` 在 `src/utils/swarm/` 路径**零命中** —— **无 file lock / 无跨进程 mutation queue**
> - [`teammatePromptAddendum.ts`](file:///c:/Works/claudecode/src/utils/swarm/teammatePromptAddendum.ts) 18 行全文不提冲突避免 —— **完全靠 LLM 自然语言协调**
> - [`COORDINATOR_MODE_ALLOWED_TOOLS`](file:///c:/Works/claudecode/src/constants/tools.ts#L107) 仅 4 个 PM 工具 + research preview 默认关闭 + 7 条已知限制 + token 消耗 10x
>
> ## 旧 v0.8.10 设计 vs 新 v0.7.41 设计
>
> | 维度 | 旧 v0.8.10（已废弃）| 新 v0.7.41 LLM-First |
> |---|---|---|
> | 触发方式 | 用户显式 `/team create` 开 mode | **自动感知**（用户零认知负担）|
> | 冲突解决保证 | 无（mailbox / permission sync 不解决冲突）| ✅ runtime safety net（content hash mismatch）|
> | LLM 知情度 | LLM 不知情（mode prompt + tool restriction）| ✅ LLM 完全知情（system prompt block）|
> | 用户认知负担 | mode 概念 + team_id + slash command | 0（runtime 透明）|
> | 代码量 | ~600 行 | ~530 行 |
> | KodaX 哲学符合度 | mode 切换 + 把 LLM 锁住 = 反 LLM-First | ✅ Leverage LLM intelligence + 极简 |
>
> v0.8.10 slot 空出。本文件保留作设计演进历史参考。
>
> ⚠️ 历史 Theme 描述（已废弃）：~~Team / Swarm Mode — 引入跨 process / 跨 host 的 multi-Worker swarm 系统~~。

---

## 总览

v0.7.36-50 完成后，KodaX 在**单 Worker 内**已经有完整多 child 编排能力：
- Pattern B async dispatch（FEATURE_119）
- Message queue 2-tier + Sleep-gated drain（FEATURE_115）
- Worker → child / child → child / broadcast send_message（FEATURE_120 / 123）
- Scratchpad（FEATURE_121）
- PM thinking framework（FEATURE_122）
- Memory（FEATURE_124）
- subagent_type 接 Self-Construction（FEATURE_128）

但这些能力**全在单进程内 in-memory**——Worker 和所有 child 共享同一 Node.js process / 同一 LLM session。

**Team / Swarm Mode 解决跨 process / 跨 host 多 Worker 协作场景**：
- 用户开 4 个 KodaX session 分别跑前端 / 后端 / DB / DevOps，希望它们能互相通报进展
- CI agent + dev agent 协作（dev 改完代码，CI agent 自动跑全套 e2e 并通报回 dev）
- 长 audit 任务跨多天，每天 resume 时希望同 task 的子 Worker 状态被持久化

---

## FEATURE_125: Team / Swarm Mode — TeamCreate/Delete + 3 Backend (tmux/iterm2/in-process) + Mailbox + Permission File-Sync

**Status**: Planned
**Priority**: Medium
**Category**: Internal / Architecture

### 背景

#### 单 Worker 内 vs 跨 Worker 的本质差异

| 维度 | v0.7.36-50 单 Worker 多 child | v0.8.10 Team Mode 多 Worker |
|---|---|---|
| 进程 | 同 1 process | 多 process（tmux pane / iTerm2 split / 同进程 fork）|
| LLM session | 同 1 session | 各 Worker 独立 session |
| Memory queue | in-memory shared | file-based mailbox (mtime-watch) |
| Permission | 同 1 permission gate | per-Worker，file-sync 跨 Worker 共享决策 |
| Lifecycle | parent Worker 终止 → child 终止 | 各 Worker 独立 lifecycle，team 是 metadata 集合 |
| 用户感知 | 用户看 1 个 transcript | 用户切 tmux pane 看不同 Worker |

#### Phase 0.7 调研对照

Claude Code Coordinator Mode 和 Team/Swarm Mode 是**两个独立子系统**：

- Coordinator Mode：单 process 内多 child（KodaX FEATURE_122 对应）
- Team Mode：跨 process / 跨 host swarm（FEATURE_125 对应）

Claude Code Team Mode 实际工业用例不多——主要在企业内部多人协作场景；个人开发者用得很少。**KodaX 不急做** —— v0.8.10 在单 Worker 编排成熟后再上，避免过度提前投入。

### 设计

#### Step 1：Team metadata + 3 backend

```ts
// packages/coding/src/team/types.ts
export interface KodaXTeam {
  id: string;
  name: string;
  backend: 'tmux' | 'iterm2' | 'in-process';
  members: KodaXTeamMember[];
  mailboxRoot: string;  // e.g., ~/.kodax/teams/{team_id}/mailbox/
  permissionSyncRoot: string;  // e.g., ~/.kodax/teams/{team_id}/permissions/
  createdAt: number;
}

export interface KodaXTeamMember {
  workerId: string;
  agentName: string;     // optional self-constructed agent name
  cwd: string;           // 各 Worker 工作目录可不同
  pid?: number;          // tmux/iterm2 backend 时的进程 PID
  status: 'active' | 'paused' | 'terminated';
}
```

#### Step 2：3 个 backend 实现

**`tmux` backend**：
- TeamCreate 时 spawn `tmux new-session -d -s kodax-team-{id}` + 各 member spawn 独立 pane（`tmux split-window`）
- 每 pane 跑 `kodax --team-member {team_id} --worker-id {worker_id}`
- 用户用 `tmux attach -t kodax-team-{id}` 看全 team

**`iterm2` backend**（macOS only）：
- 用 iTerm2 AppleScript / Python API 创建 split window
- 各 split 跑独立 KodaX 实例
- Linux/Windows 不支持，fall back to tmux

**`in-process` backend**：
- 同 Node.js process fork worker thread；mailbox 仍走 file-based（保证模型一致 + 调试方便）
- 用于 CI / headless 场景（无 tmux 环境）

#### Step 3：File-based mailbox

```
~/.kodax/teams/{team_id}/mailbox/
  ├── {worker_id}/
  │   ├── inbox/
  │   │   ├── 0001-{from_worker_id}-{timestamp}.json
  │   │   └── ...
  │   └── outbox/
  │       └── ...
```

每条 message 是一个 JSON 文件：
```json
{
  "id": "msg-001",
  "from": "worker-frontend",
  "to": "worker-backend",
  "priority": "user|background",
  "mode": "prompt|task-notification|broadcast",
  "content": "...",
  "spilloverPaths": ["/path/to/instance-tool-results/abc.txt"],
  "timestamp": 1714780800000
}
```

Worker 在 mid-turn drain check 时除了消费 in-memory queue（FEATURE_115），还 watch `~/.kodax/teams/{team_id}/mailbox/{my_id}/inbox/` 目录 mtime；新文件 = 新 message。

**FS watch 选型**：用 chokidar（既有 KodaX 依赖），fallback to polling for SSH/network FS。

#### Step 4：Team-level scratchpad

`kodax-team-scratch://...` URI 比 FEATURE_121 的 `kodax-scratch://...` 多一层 team scope：

```
~/.kodax/teams/{team_id}/scratchpad/
  ├── {worker_id}/
  │   └── *.md
```

跨 Worker scratchpad read 走 file-based。

#### Step 5：Permission file-sync

各 Worker 独立有 permission gate（FEATURE_092 auto-mode），但用户做的 confirm 决策希望跨 Worker 共享：

```
~/.kodax/teams/{team_id}/permissions/
  ├── decisions/
  │   ├── {hash-of-tool-call}.json   # decision: allowed | denied | session-only
```

Worker 在 permission ask 之前先 hash 当前 tool call → 看 decisions/ 是否有 cached decision；有则直接走，无则 ask 用户 + write decision 文件。

**安全考虑**：decision 文件包含 hash + tool name + parameters digest（不含敏感参数全文，避免泄露 secrets）；team 是用户自己创建的，threat model 内可信。

#### Step 6：TeamCreate / TeamDelete CLI + tools

```
$ kodax team create --name "auth-rewrite" --backend tmux \
    --member frontend:./packages/repl --member backend:./packages/coding
Created team auth-rewrite (id: kodax-team-abc123) with 2 members.

$ kodax team list
- auth-rewrite (2 members, backend: tmux, attached at tmux:kodax-team-abc123)

$ kodax team delete auth-rewrite
```

Worker 内部工具：
- `team_send(to, content)` — 跨 Worker 发消息（同 send_message API 但 routing 走 mailbox）
- `team_list_members()` — 看 team 成员状态

#### Step 7：单 Worker 内 vs Team 内的 send_message 双重路由

Worker 收到 send_message 工具调用时：
- 如果 `to` 是当前 in-memory child task_id → 走 FEATURE_115 in-memory queue（既有）
- 如果 `to` 是 team member workerId → 走 file-based mailbox（FEATURE_125 新增）
- `to: '*'` 在 Team Mode 下广播给同 team 所有 member（不仅 in-memory siblings）

LLM 不需要区分两种路由——`to` 字段语义统一是 "agent identifier"。

### 影响范围

**新增**（estimated **~2000-3000 行**——这是大版本，不是小补丁）：

- `packages/coding/src/team/types.ts`：metadata 类型
- `packages/coding/src/team/runtime.ts`：team lifecycle
- `packages/coding/src/team/backends/tmux.ts`：~300 行
- `packages/coding/src/team/backends/iterm2.ts`：~250 行（macOS only）
- `packages/coding/src/team/backends/in-process.ts`：~200 行
- `packages/coding/src/team/mailbox-store.ts`：file-based mailbox + watcher（~300 行）
- `packages/coding/src/team/team-scratchpad.ts`：~150 行
- `packages/coding/src/team/permission-sync.ts`：file-based permission cache（~150 行）
- `src/cli/team-commands.ts`：CLI（~250 行）
- `packages/coding/src/tools/team-send.ts`
- `packages/coding/src/tools/team-list-members.ts`
- 大量 test：`tests/team-*.test.ts` / `tests/team-*.eval.ts`
- `docs/test-guides/FEATURE_125_v0.8.10_TEST_GUIDE.md`

**修改**：

- `packages/coding/src/tools/send-message.ts`：双重路由（in-memory vs mailbox）
- `packages/coding/src/messaging/queue.ts`：mid-turn drain 时合并 in-memory + mailbox
- `packages/coding/src/scratchpad/store.ts`：team URI 路由
- `packages/coding/src/guardrails/auto-mode/`：permission file-sync hook

**不改动**：

- 单 Worker 多 child 路径（v0.7.36-50 全部正交保留）
- Self-Construction registry / admission（FEATURE_087-101）
- LLM provider 层

### 迁移策略

```
v0.8.10：
  - Team Mode 默认 OFF（功能完整 ship 但需要 TeamCreate 显式触发）
  - 单 Worker 用户体验完全不变
  - team_send / team_list_members 工具仅在 team member Worker 中可用（非 team Worker 不注册）

v0.8.11+：
  - 监测一个月生产数据；如果 Team Mode 用户基数 < 5%，考虑是否将其降为 experimental 标记
  - 如果 ≥ 20%，考虑加 UX 完善（如 statusbar 显示当前 team / `/team` slash）

v0.8.x：
  - 永远保留 escape hatch：用户可以完全不用 Team Mode 而单 Worker 路径不受影响
```

### 验收标准

1. **TeamCreate 3 backend**：tmux / iterm2 / in-process 三 backend 都能创建 team + spawn member
2. **跨 Worker mailbox**：member A `team_send(to=B, content="...")` → member B 在下个 mid-turn drain 看到 message
3. **broadcast `to: '*'`**：跨 team 所有 member 收到
4. **scratchpad team scope**：`kodax-team-scratch://...` URI 跨 Worker 可读
5. **permission file-sync**：member A confirm 一次 tool action → member B 同 hash 的 tool action 不再 ask
6. **TeamDelete cleanup**：删除 team 时 mailbox / scratchpad / permission cache 全清理；进程优雅退出
7. **单 Worker 路径不退化**：未创建 team 的用户体验完全等价 v0.7.50；全部 v0.7.36-50 测试绿
8. **跨 host 兼容（同 NFS/SMB）**：mailbox 在共享文件系统上跨 host 工作（chokidar fallback to polling）
9. **跨 family 验证**：team mode 下 4 family LLM 都能正确使用 team_send / team_list_members
10. **无单 Worker 回归**：所有 v0.7.36-50 测试 + eval 全绿
11. **生产 telemetry**：v0.8.10 ship 后 1 月内记录 team 使用率 / 平均 member 数

### 非目标

- **不** 实现 cross-org / public team（仅本机用户的 team）
- **不** 实现 team-level shared model context（各 Worker 独立 session 是设计意图）
- **不** 实现 team 内 specialist auto-routing（用户自决用哪个 Worker；FEATURE_128 在 Worker 内已经覆盖）
- **不** 实现 message persistence 跨 team delete（删 team = 清 mailbox）
- **不** 实现实时 team UI（用户用 tmux/iterm2 切换；KodaX 不做 dashboard）
- **不** 实现 cloud team（依赖共享 FS；远程协作 FEATURE_022 范围）
- **不** 实现 team-level audit log / compliance（threat model 内单用户即可）

### 与其他 Feature 的关系

- **依赖 FEATURE_115（v0.7.36）**：message queue 内部模型作为 in-memory 路径继续生效
- **依赖 FEATURE_119（v0.7.36）**：Pattern B 在每个 Worker 内独立工作
- **依赖 FEATURE_120 / 123（v0.7.39 / 44）**：send_message 单 Worker 内完全 work；team_send 是跨 Worker 扩展
- **依赖 FEATURE_121（v0.7.40）**：scratchpad 单 Worker 内完全 work；team-scratchpad 是跨 Worker 扩展
- **依赖 FEATURE_128（v0.7.50）**：subagent_type 在 team member Worker 内仍生效
- **协同 FEATURE_092 auto-mode**：permission file-sync 复用 auto-mode classifier decision

### 参考

- Claude Code Team Mode：`c:/Works/claudecode/src/team/` 整目录（工业 reference）
- chokidar FS watch：https://github.com/paulmillr/chokidar
- tmux man page：tmux(1) `new-session` / `split-window` / `send-keys`
- iTerm2 Python API：https://iterm2.com/python-api/
- KodaX FEATURE_115/119/120/121/123/128：[features/v0.7.36.md](v0.7.36.md) / [v0.7.39.md](v0.7.39.md) / [v0.7.40.md](v0.7.40.md) / [v0.7.44.md](v0.7.44.md) / [v0.7.50.md](v0.7.50.md)
- Phase 0.7 调研 §Team Mode independent subsystem：`c:/tmp/claude-code-multi-agent-deep-dive.md`

